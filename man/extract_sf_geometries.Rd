% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sf_utils.R
\name{extract_sf_geometries}
\alias{extract_sf_geometries}
\title{Extract sf geometries from ggplot_build layer data as GeoJSON strings}
\usage{
extract_sf_geometries(df)
}
\arguments{
\item{df}{A data.frame from \code{ggplot_build()$data[[i]]} containing an sfc column}
}
\value{
Character vector of GeoJSON geometry strings, one per row
}
\description{
Extracts the sfc geometry column from a data.frame produced by
\code{ggplot_build()$data[[i]]}, normalizes CRS to WGS84 unconditionally (per D-11),
and serializes each geometry as a GeoJSON geometry string via
\code{geojsonsf::sfc_geojson()} (per D-10).
}
\details{
gg2d3's public \code{geom_sf()} renderer accepts polygon, point, and line
geometry families. Missing CRS emits
"geom_sf layer has missing CRS; coordinates will be serialized as-is".
Unsupported, empty, invalid, or missing geometries emit
"geom_sf layer skipped \%d unsupported, empty, invalid, or missing geometries"
and are skipped before rendering.
}
