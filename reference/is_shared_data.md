# Check if object is crosstalk SharedData

Check if object is crosstalk SharedData

## Usage

``` r
is_shared_data(x)
```

## Arguments

- x:

  Object to check

## Value

Logical TRUE if x is SharedData, FALSE otherwise
