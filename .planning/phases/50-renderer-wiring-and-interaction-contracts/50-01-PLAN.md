---
phase: 50-renderer-wiring-and-interaction-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/htmlwidgets/modules/geom-contracts.js
  - inst/htmlwidgets/gg2d3.yaml
  - tests/testthat/test-renderer-wiring-contracts.R
autonomous: true
requirements:
  - ARCH-02
must_haves:
  truths:
    - "Supported renderer names are represented in one internal geom contract."
    - "Every supported geom has update coverage metadata or an explicit no-update reason."
    - "Source-level tests fail when registration or update selectors drift away from the contract."
  artifacts:
    - path: "inst/htmlwidgets/modules/geom-contracts.js"
      provides: "Internal geom wiring contract"
    - path: "tests/testthat/test-renderer-wiring-contracts.R"
      provides: "Registration and update coverage source-contract tests"
  key_links:
    - from: "inst/htmlwidgets/modules/geom-registry.js"
      to: "inst/htmlwidgets/modules/geom-contracts.js"
      via: "contract names and update selectors"
      pattern: "window.gg2d3.geomContracts"
requirements_addressed:
  - ARCH-02
---

<objective>
Create the internal geom contract and make renderer registration plus zoom/update coverage testable from a single source of truth.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-renderer-wiring-and-interaction-contracts/50-CONTEXT.md
@.planning/phases/50-renderer-wiring-and-interaction-contracts/50-RESEARCH.md
@.planning/phases/50-renderer-wiring-and-interaction-contracts/50-PATTERNS.md
@.planning/phases/50-renderer-wiring-and-interaction-contracts/50-VALIDATION.md
@inst/htmlwidgets/modules/geom-registry.js
@inst/htmlwidgets/modules/geoms/
@inst/htmlwidgets/gg2d3.yaml
@tests/testthat/test-polygon-renderer.R
@tests/testthat/test-zoom-path-datum.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add internal geom contract module</name>
  <read_first>
    inst/htmlwidgets/modules/geom-registry.js
    inst/htmlwidgets/modules/geoms/point.js
    inst/htmlwidgets/modules/geoms/line.js
    inst/htmlwidgets/modules/geoms/polygon.js
    inst/htmlwidgets/modules/geoms/bar.js
    inst/htmlwidgets/modules/geoms/rect.js
    inst/htmlwidgets/modules/geoms/text.js
    inst/htmlwidgets/modules/geoms/area.js
    inst/htmlwidgets/modules/geoms/ribbon.js
    inst/htmlwidgets/modules/geoms/segment.js
    inst/htmlwidgets/modules/geoms/reference.js
    inst/htmlwidgets/modules/geoms/dotplot.js
    inst/htmlwidgets/modules/geoms/rug.js
    inst/htmlwidgets/modules/geoms/interval.js
    inst/htmlwidgets/modules/geoms/boxplot.js
    inst/htmlwidgets/modules/geoms/violin.js
    inst/htmlwidgets/modules/geoms/density.js
    inst/htmlwidgets/modules/geoms/smooth.js
    inst/htmlwidgets/modules/geoms/sf.js
    inst/htmlwidgets/gg2d3.yaml
  </read_first>
  <files>inst/htmlwidgets/modules/geom-contracts.js, inst/htmlwidgets/gg2d3.yaml</files>
  <action>
    Create `inst/htmlwidgets/modules/geom-contracts.js`. Initialize `window.gg2d3` and export `window.gg2d3.geomContracts` with:
    - `all()` returning a copy of the contract array.
    - `names()` returning every canonical geom and alias.
    - `find(name)` returning the matching contract entry by canonical name or alias.
    - `selectorsFor(surface)` returning flattened unique selectors for `events`, `brush`, or `crosstalk`.
    - `privateFields()` returning unique private fields across contracts.

    The contract array must include entries for these canonical geoms and aliases:
    - point: aliases `["point"]`
    - line: aliases `["line", "path"]`
    - polygon: aliases `["polygon"]`
    - bar: aliases `["bar", "col"]`
    - rect: aliases `["rect", "tile"]`
    - text: aliases `["text"]`
    - area: aliases `["area"]`
    - ribbon: aliases `["ribbon"]`
    - segment: aliases `["segment"]`
    - hline: aliases `["hline"]`
    - vline: aliases `["vline"]`
    - abline: aliases `["abline"]`
    - dotplot: aliases `["dotplot"]`
    - rug: aliases `["rug"]`
    - interval: aliases `["errorbar", "linerange", "pointrange"]`
    - boxplot: aliases `["boxplot"]`
    - violin: aliases `["violin"]`
    - density: aliases `["density"]`
    - smooth: aliases `["smooth"]`
    - sf: aliases `["sf"]`
    - sf_text: aliases `["sf_text"]`
    - sf_label: aliases `["sf_label"]`

    Each entry must include concrete fields named `geom`, `aliases`, `module`, `renderSelectors`, `update`, `interactions`, `privateFields`, and `publicPayload`.

    Populate `update.selectors` with existing update selectors from `geom-registry.js`. Use `update.type: "explicit-none"` with a `reason` string only for geoms that intentionally lack zoom/update coverage after checking current code. If `abline`, `sf`, `sf_text`, or `sf_label` still lack `updateGeoms()` branches, mark them explicitly rather than pretending they are covered.

    Update `inst/htmlwidgets/gg2d3.yaml` so `geom-contracts.js` loads before `tooltip.js`, `events.js`, `brush.js`, `crosstalk.js`, and `geom-registry.js`.
  </action>
  <verify>
    <automated>rg -n "window\\.gg2d3\\.geomContracts|selectorsFor|privateFields|explicit-none" inst/htmlwidgets/modules/geom-contracts.js</automated>
    <automated>rg -n "geom-contracts\\.js" inst/htmlwidgets/gg2d3.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `inst/htmlwidgets/modules/geom-contracts.js` exists.
    - `inst/htmlwidgets/modules/geom-contracts.js` contains `window.gg2d3.geomContracts`.
    - `inst/htmlwidgets/modules/geom-contracts.js` contains every alias string listed in the action.
    - `inst/htmlwidgets/modules/geom-contracts.js` contains `explicit-none` for any supported geom with no current update branch.
    - `inst/htmlwidgets/gg2d3.yaml` lists `geom-contracts.js` before `tooltip.js`.
  </acceptance_criteria>
  <done>The package has an internal contract object that represents supported geom wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Add renderer registration and update coverage contract tests</name>
  <read_first>
    tests/testthat/test-polygon-renderer.R
    tests/testthat/test-zoom-path-datum.R
    inst/htmlwidgets/modules/geom-contracts.js
    inst/htmlwidgets/modules/geom-registry.js
    inst/htmlwidgets/modules/geoms/
  </read_first>
  <files>tests/testthat/test-renderer-wiring-contracts.R</files>
  <action>
    Create `tests/testthat/test-renderer-wiring-contracts.R` with a local `read_module(path)` helper matching the existing source-test pattern.

    Add tests with these exact descriptions:
    - `geom contract lists every registered renderer alias`
    - `registered renderer aliases are covered by geom contract`
    - `geom contract update selectors match updateGeoms source`
    - `geom contract exposes private renderer fields`

    The tests must:
    - Extract alias strings from `window.gg2d3.geomRegistry.register(...)` calls in `inst/htmlwidgets/modules/geoms/*.js`.
    - Assert aliases from the contract include `point`, `line`, `path`, `polygon`, `bar`, `col`, `rect`, `tile`, `text`, `area`, `ribbon`, `segment`, `hline`, `vline`, `abline`, `dotplot`, `rug`, `errorbar`, `linerange`, `pointrange`, `boxplot`, `violin`, `density`, `smooth`, `sf`, `sf_text`, and `sf_label`.
    - Assert each non-`explicit-none` `update.selectors` selector appears literally in `inst/htmlwidgets/modules/geom-registry.js`.
    - Assert `_polygonPoints`, `_sourceIndex`, `_geom`, `_centroid`, `_sfFamily`, `_sfAnchor`, `_pointCoord`, and `_pointIndex` appear in the contract's private field coverage.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-renderer-wiring-contracts.R")'</automated>
    <automated>rg -n "geom contract lists every registered renderer alias|registered renderer aliases are covered by geom contract|geom contract update selectors match updateGeoms source|geom contract exposes private renderer fields" tests/testthat/test-renderer-wiring-contracts.R</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-renderer-wiring-contracts.R` contains all four exact test descriptions.
    - Removing any renderer alias from `geom-contracts.js` makes `geom contract lists every registered renderer alias` or `registered renderer aliases are covered by geom contract` fail.
    - Removing a non-`explicit-none` update selector from `geom-registry.js` makes `geom contract update selectors match updateGeoms source` fail.
    - The contract test file exits with status 0.
  </acceptance_criteria>
  <done>Renderer registration and update coverage are protected by contract tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer modules -> registry | Supported geom names are registered dynamically by module load order. |
| Registry -> zoom/update | `updateGeoms()` must know how to reposition visible marks during zoom/reset. |
| Contract -> tests | Tests rely on contract metadata to detect missing wiring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | T | renderer registration | mitigate | Add contract tests comparing renderer `register(...)` aliases with contract aliases. |
| T-50-02 | T | `updateGeoms()` | mitigate | Add contract tests requiring each non-excluded update selector to appear in `geom-registry.js`. |
| T-50-03 | D | module load order | mitigate | Verify `geom-contracts.js` loads before consumers in `gg2d3.yaml`. |
</threat_model>

<verification>
Run all task-level commands. Then run `Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-polygon-renderer.R"); testthat::test_file("tests/testthat/test-zoom-path-datum.R")'`.
</verification>

<success_criteria>
- Internal geom contract module exists and loads before consumers.
- Supported renderer aliases are all represented by the contract.
- Update coverage is either represented by selectors or explicitly marked as intentionally absent.
- Source-level contract tests fail on missing renderer or update coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/50-renderer-wiring-and-interaction-contracts/50-01-SUMMARY.md`.
</output>
