---
phase: 50-renderer-wiring-and-interaction-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - inst/htmlwidgets/modules/events.js
  - inst/htmlwidgets/modules/brush.js
  - inst/htmlwidgets/modules/crosstalk.js
  - tests/testthat/test-renderer-wiring-contracts.R
  - tests/testthat/test-polygon-interactivity.R
  - tests/testthat/test-sf-interactivity.R
  - tests/testthat/test-sf-annotations-interactivity.R
autonomous: true
requirements:
  - ARCH-02
must_haves:
  truths:
    - "Events, brush, and crosstalk interaction selectors are derived from or validated against the internal geom contract."
    - "Module-specific selector differences are explicit, especially crosstalk exclusions."
    - "Ordinary polygon and sf annotation interaction selectors remain covered."
  artifacts:
    - path: "tests/testthat/test-renderer-wiring-contracts.R"
      provides: "Interaction selector contract tests"
  key_links:
    - from: "inst/htmlwidgets/modules/events.js"
      to: "inst/htmlwidgets/modules/geom-contracts.js"
      via: "selectorsFor('events') or exact source-contract validation"
      pattern: "selectorsFor"
requirements_addressed:
  - ARCH-02
---

<objective>
Reduce duplication-prone interaction selector wiring while preserving the distinct responsibilities of events, brush, and crosstalk.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-renderer-wiring-and-interaction-contracts/50-CONTEXT.md
@.planning/phases/50-renderer-wiring-and-interaction-contracts/50-RESEARCH.md
@.planning/phases/50-renderer-wiring-and-interaction-contracts/50-PATTERNS.md
@.planning/phases/50-renderer-wiring-and-interaction-contracts/50-VALIDATION.md
@inst/htmlwidgets/modules/geom-contracts.js
@inst/htmlwidgets/modules/events.js
@inst/htmlwidgets/modules/brush.js
@inst/htmlwidgets/modules/crosstalk.js
@tests/testthat/test-polygon-interactivity.R
@tests/testthat/test-sf-interactivity.R
@tests/testthat/test-sf-annotations-interactivity.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire events and brush selectors to contract coverage</name>
  <read_first>
    inst/htmlwidgets/modules/geom-contracts.js
    inst/htmlwidgets/modules/events.js
    inst/htmlwidgets/modules/brush.js
    tests/testthat/test-polygon-interactivity.R
    tests/testthat/test-sf-interactivity.R
    tests/testthat/test-sf-annotations-interactivity.R
  </read_first>
  <files>inst/htmlwidgets/modules/events.js, inst/htmlwidgets/modules/brush.js, tests/testthat/test-renderer-wiring-contracts.R</files>
  <action>
    Choose the lower-risk of these two approaches after reading the current files:
    - Runtime derivation: replace the hard-coded events/brush selector arrays with `window.gg2d3.geomContracts.selectorsFor('events')` and `window.gg2d3.geomContracts.selectorsFor('brush')`, falling back to the current literal arrays only if the contract module is unavailable.
    - Test-only validation: keep the local arrays but add source-contract tests proving they exactly match `interactions.events` and `interactions.brush` in `geom-contracts.js`.

    In either approach, events and brush coverage must include these selectors:
    - `circle.geom-point`
    - `rect.geom-bar`
    - `rect.geom-rect`
    - `path.geom-line`
    - `path.geom-polygon`
    - `path.geom-area`
    - `path.geom-density`
    - `path.geom-smooth`
    - `path.geom-ribbon`
    - `path.geom-violin`
    - `.geom-sf`
    - `text.geom-text`
    - `line.geom-segment`
    - `rect.geom-boxplot-box`
    - `circle.geom-boxplot-outlier`
    - `circle.geom-dotplot`
    - `line.geom-rug`
    - `line.interval-line`
    - `line.errorbar-cap-top`
    - `line.errorbar-cap-bottom`
    - `circle.pointrange-point`

    Preserve `brush.js` sf selection behavior: sf elements may use `data-cx`/`data-cy`, while ordinary polygon paths remain on the generic `getBBox()` branch.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-renderer-wiring-contracts.R")'</automated>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-polygon-interactivity.R"); testthat::test_file("tests/testthat/test-sf-interactivity.R"); testthat::test_file("tests/testthat/test-sf-annotations-interactivity.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-renderer-wiring-contracts.R` fails if `path.geom-polygon` is removed from events or brush selector coverage.
    - `tests/testthat/test-renderer-wiring-contracts.R` fails if `.geom-sf` is removed from events or brush selector coverage.
    - `tests/testthat/test-polygon-interactivity.R` still passes the assertion that ordinary polygon brush lines do not use the sf `data-cx`/`data-cy` branch.
    - sf annotation interactivity tests pass or skip only for missing optional dependencies.
  </acceptance_criteria>
  <done>Events and brush selectors are contract-covered without changing branch semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Validate crosstalk selector differences explicitly</name>
  <read_first>
    inst/htmlwidgets/modules/geom-contracts.js
    inst/htmlwidgets/modules/crosstalk.js
    inst/htmlwidgets/modules/geoms/polygon.js
    tests/testthat/test-polygon-interactivity.R
  </read_first>
  <files>inst/htmlwidgets/modules/crosstalk.js, tests/testthat/test-renderer-wiring-contracts.R, tests/testthat/test-polygon-interactivity.R</files>
  <action>
    Connect `crosstalk.js` to the contract by runtime derivation with `selectorsFor('crosstalk')` or source-contract validation. Do not make crosstalk blindly identical to events/brush.

    Crosstalk coverage must include:
    - `circle.geom-point`
    - `rect.geom-bar`
    - `rect.geom-rect`
    - `path.geom-line`
    - `path.geom-polygon`
    - `path.geom-area`
    - `path.geom-density`
    - `path.geom-smooth`
    - `path.geom-ribbon`
    - `path.geom-violin`
    - `.geom-sf`
    - `text.geom-text`
    - `line.geom-segment`
    - `rect.geom-boxplot-box`
    - `circle.geom-boxplot-outlier`

    If dotplot, rug, or interval selectors remain excluded from crosstalk, encode those exclusions in `geom-contracts.js` with a `reason` string and assert that reason in `tests/testthat/test-renderer-wiring-contracts.R`.

    Preserve internal `_sourceIndex` use in `crosstalkKeyIndex()` for polygon representative row key binding.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-renderer-wiring-contracts.R")'</automated>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-polygon-interactivity.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-renderer-wiring-contracts.R` fails if `path.geom-polygon` or `.geom-sf` is removed from crosstalk coverage.
    - `tests/testthat/test-renderer-wiring-contracts.R` contains assertions for any intentional crosstalk exclusions.
    - `tests/testthat/test-polygon-interactivity.R` still finds `d._sourceIndex`, `Number.isFinite(sourceIndex)`, and `keyArray[rowIndex]` in `crosstalk.js`.
  </acceptance_criteria>
  <done>Crosstalk interaction coverage is explicit and still preserves private key binding internals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contract -> event/brush selectors | Missing selectors prevent public callbacks and hover/brush behavior. |
| Contract -> crosstalk selectors | Missing selectors break linked selections for supported geoms. |
| Crosstalk internals -> public data | `_sourceIndex` may be used internally but must not become public callback data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-04 | D | `events.js` / `brush.js` selector coverage | mitigate | Derive or contract-test exact module-specific selector sets. |
| T-50-05 | D | `crosstalk.js` selector coverage | mitigate | Encode crosstalk-specific inclusions and exclusions in the contract. |
| T-50-06 | I | crosstalk `_sourceIndex` | mitigate | Preserve internal use tests and leave public payload sanitization for Plan 03. |
</threat_model>

<verification>
Run all task-level commands and then `Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-regression-core.R")'`.
</verification>

<success_criteria>
- Events, brush, and crosstalk selector coverage is contract-derived or contract-tested.
- Module-specific differences are explicit and tested.
- Polygon and sf annotation interaction coverage remains present.
- Existing polygon/sf interaction tests pass or skip only for missing optional dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/50-renderer-wiring-and-interaction-contracts/50-02-SUMMARY.md`.
</output>
