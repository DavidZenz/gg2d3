---
phase: 50-renderer-wiring-and-interaction-contracts
plan: 03
type: execute
wave: 3
depends_on:
  - 50-01
  - 50-02
files_modified:
  - inst/htmlwidgets/modules/public-data.js
  - inst/htmlwidgets/gg2d3.yaml
  - inst/htmlwidgets/modules/events.js
  - inst/htmlwidgets/modules/brush.js
  - inst/htmlwidgets/modules/tooltip.js
  - tests/testthat/test-renderer-wiring-contracts.R
  - tests/testthat/test-polygon-interactivity.R
  - tests/testthat/test-sf-interactivity.R
  - tests/testthat/test-sf-annotations-interactivity.R
  - tests/testthat/test-regression-core.R
autonomous: true
requirements:
  - ARCH-02
  - ARCH-03
must_haves:
  truths:
    - "Tooltip, event, Shiny event, and brush public payload paths sanitize through one shared rule or a single contract-tested equivalent."
    - "Underscore-prefixed renderer-private fields are omitted from public payloads."
    - "Ordinary polygons and sf text/label annotations are covered by sanitizer tests."
  artifacts:
    - path: "inst/htmlwidgets/modules/public-data.js"
      provides: "Shared public datum sanitizer"
    - path: "tests/testthat/test-renderer-wiring-contracts.R"
      provides: "Sanitizer and private-field contract tests"
  key_links:
    - from: "inst/htmlwidgets/modules/events.js"
      to: "inst/htmlwidgets/modules/public-data.js"
      via: "sanitizeDatum"
      pattern: "publicData.sanitizeDatum"
requirements_addressed:
  - ARCH-02
  - ARCH-03
---

<objective>
Centralize public payload sanitization and complete Phase 50 validation across tooltip, event handlers, brush callbacks, polygons, sf marks, and sf annotations.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-renderer-wiring-and-interaction-contracts/50-CONTEXT.md
@.planning/phases/50-renderer-wiring-and-interaction-contracts/50-RESEARCH.md
@.planning/phases/50-renderer-wiring-and-interaction-contracts/50-PATTERNS.md
@.planning/phases/50-renderer-wiring-and-interaction-contracts/50-VALIDATION.md
@inst/htmlwidgets/modules/geom-contracts.js
@inst/htmlwidgets/modules/events.js
@inst/htmlwidgets/modules/brush.js
@inst/htmlwidgets/modules/tooltip.js
@inst/htmlwidgets/modules/crosstalk.js
@inst/htmlwidgets/modules/geoms/polygon.js
@inst/htmlwidgets/modules/geoms/sf.js
@tests/testthat/test-polygon-interactivity.R
@tests/testthat/test-sf-interactivity.R
@tests/testthat/test-sf-annotations-interactivity.R
@tests/testthat/test-regression-core.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared public datum sanitizer</name>
  <read_first>
    inst/htmlwidgets/modules/events.js
    inst/htmlwidgets/modules/brush.js
    inst/htmlwidgets/modules/tooltip.js
    inst/htmlwidgets/gg2d3.yaml
    tests/testthat/test-polygon-interactivity.R
    tests/testthat/test-sf-interactivity.R
    tests/testthat/test-sf-annotations-interactivity.R
  </read_first>
  <files>inst/htmlwidgets/modules/public-data.js, inst/htmlwidgets/gg2d3.yaml, inst/htmlwidgets/modules/events.js, inst/htmlwidgets/modules/brush.js, inst/htmlwidgets/modules/tooltip.js</files>
  <action>
    Create `inst/htmlwidgets/modules/public-data.js`. Initialize `window.gg2d3` and export `window.gg2d3.publicData` with:
    - `sanitizeDatum(d)`: returns `d` unchanged when `d` is null, undefined, non-object, or an array; otherwise returns a shallow object copy omitting keys where `String(key).startsWith('_')` is true.
    - `publicFieldNames(d)`: returns non-private field names for object datums and an empty array for non-object or array values.

    Update `inst/htmlwidgets/gg2d3.yaml` so `public-data.js` loads before `tooltip.js`, `events.js`, and `brush.js`.

    Update `events.js`, `brush.js`, and `tooltip.js` so public payload paths call `window.gg2d3.publicData.sanitizeDatum(d)` or a local wrapper that delegates to it. Preserve the existing local function names `sanitizeEventDatum`, `sanitizeSelectedDatum`, and `sanitizeTooltipDatum` if doing so keeps older source tests simple, but make each wrapper delegate to the shared helper.

    Preserve tooltip field filtering behavior for explicit `config.fields`: underscore-prefixed field names must still be removed.
  </action>
  <verify>
    <automated>rg -n "window\\.gg2d3\\.publicData|sanitizeDatum|publicFieldNames" inst/htmlwidgets/modules/public-data.js</automated>
    <automated>rg -n "public-data\\.js" inst/htmlwidgets/gg2d3.yaml</automated>
    <automated>rg -n "publicData\\.sanitizeDatum|sanitizeEventDatum|sanitizeSelectedDatum|sanitizeTooltipDatum" inst/htmlwidgets/modules/events.js inst/htmlwidgets/modules/brush.js inst/htmlwidgets/modules/tooltip.js</automated>
  </verify>
  <acceptance_criteria>
    - `inst/htmlwidgets/modules/public-data.js` exists and contains `window.gg2d3.publicData`.
    - `inst/htmlwidgets/modules/public-data.js` contains `sanitizeDatum` and `publicFieldNames`.
    - `inst/htmlwidgets/gg2d3.yaml` lists `public-data.js` before `tooltip.js`.
    - `events.js`, `brush.js`, and `tooltip.js` all call `publicData.sanitizeDatum` directly or through their existing sanitizer wrapper names.
    - Tooltip explicit field filtering still excludes keys starting with `_`.
  </acceptance_criteria>
  <done>Public payload sanitization has one shared underscore-stripping rule.</done>
</task>

<task type="auto">
  <name>Task 2: Add sanitizer and private-field contract coverage</name>
  <read_first>
    tests/testthat/test-renderer-wiring-contracts.R
    tests/testthat/test-polygon-interactivity.R
    tests/testthat/test-sf-interactivity.R
    tests/testthat/test-sf-annotations-interactivity.R
    inst/htmlwidgets/modules/public-data.js
    inst/htmlwidgets/modules/events.js
    inst/htmlwidgets/modules/brush.js
    inst/htmlwidgets/modules/tooltip.js
    inst/htmlwidgets/modules/geoms/polygon.js
    inst/htmlwidgets/modules/geoms/sf.js
  </read_first>
  <files>tests/testthat/test-renderer-wiring-contracts.R, tests/testthat/test-polygon-interactivity.R, tests/testthat/test-sf-interactivity.R, tests/testthat/test-sf-annotations-interactivity.R, tests/testthat/test-regression-core.R</files>
  <action>
    Extend `tests/testthat/test-renderer-wiring-contracts.R` with tests named exactly:
    - `public data sanitizer strips underscore-prefixed fields`
    - `tooltip events and brush delegate to public sanitizer`
    - `polygon and sf private fields are contract-covered`

    The tests must assert:
    - `inst/htmlwidgets/modules/public-data.js` contains `key.startsWith('_')` or `String(key).startsWith('_')`.
    - `events.js`, `brush.js`, and `tooltip.js` contain `publicData.sanitizeDatum`.
    - The contract and tests mention `_polygonPoints`, `_sourceIndex`, `_geom`, `_centroid`, `_sfFamily`, `_sfAnchor`, `_pointCoord`, and `_pointIndex`.
    - `tooltip.js` still filters `config.fields` with `!String(k).startsWith('_')`.

    Update existing polygon/sf regression tests only where wrapper implementation changed source strings. Keep their core assertions: callback sources do not literally expose `_polygonPoints`, sf annotation private fields are absent from public callback/tooltip/brush payload paths, and optional sf dependency skips remain unchanged.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-renderer-wiring-contracts.R")'</automated>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-polygon-interactivity.R"); testthat::test_file("tests/testthat/test-sf-interactivity.R"); testthat::test_file("tests/testthat/test-sf-annotations-interactivity.R")'</automated>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-regression-core.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-renderer-wiring-contracts.R` contains all three exact sanitizer test descriptions.
    - Removing `publicData.sanitizeDatum` from any of `events.js`, `brush.js`, or `tooltip.js` makes the contract test fail.
    - Removing any of `_polygonPoints`, `_sourceIndex`, `_geom`, `_centroid`, `_sfFamily`, `_sfAnchor`, `_pointCoord`, or `_pointIndex` from contract private-field coverage makes the contract test fail.
    - Polygon and sf interactivity tests pass or skip only for missing optional dependencies.
  </acceptance_criteria>
  <done>Public payload sanitization and private-field coverage are contract-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 50 validation and record skips</name>
  <read_first>
    .planning/phases/50-renderer-wiring-and-interaction-contracts/50-VALIDATION.md
    tests/testthat/test-renderer-wiring-contracts.R
    tests/testthat/test-browser-visual-smoke.R
  </read_first>
  <files>.planning/phases/50-renderer-wiring-and-interaction-contracts/50-VALIDATION.md</files>
  <action>
    Run the Phase 50 validation commands and update `50-VALIDATION.md` from `status: draft` to `status: executed`.

    Record execution notes under a new `## Phase 50 Execution Notes` section with:
    - the contract test command result;
    - the polygon/sf/source regression command result;
    - the `test-regression-core.R` command result;
    - the browser visual smoke command result or explicit skip reason.

    Do not change optional skip policy. Browser visual smoke may be recorded as skipped when `GG2D3_BROWSER_VISUAL_SMOKE=true` is not set or Chromote/browser launch is unavailable.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-renderer-wiring-contracts.R")'</automated>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-polygon-interactivity.R"); testthat::test_file("tests/testthat/test-polygon-renderer.R"); testthat::test_file("tests/testthat/test-sf-interactivity.R"); testthat::test_file("tests/testthat/test-sf-annotations-interactivity.R"); testthat::test_file("tests/testthat/test-zoom-path-datum.R")'</automated>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-regression-core.R")'</automated>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-browser-visual-smoke.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `50-VALIDATION.md` frontmatter contains `status: executed`.
    - `50-VALIDATION.md` contains `## Phase 50 Execution Notes`.
    - Contract, polygon/sf/source regression, and regression-core command results are recorded.
    - Browser visual smoke pass or skip reason is recorded.
  </acceptance_criteria>
  <done>Phase 50 validation evidence is recorded with optional dependency skips called out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer datum -> public callback payload | Private geometry metadata must be removed before user code or Shiny receives data. |
| Tooltip field selection -> visible UI | Explicit tooltip fields must not reveal underscore-prefixed private fields. |
| Internal crosstalk metadata -> key binding | `_sourceIndex` is internal-only metadata used before public sanitization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-07 | I | tooltip payloads | mitigate | Route tooltip datum and explicit field names through shared public-data sanitizer/filtering. |
| T-50-08 | I | event and Shiny payloads | mitigate | Route click/mouseover/mouseout/Shiny payloads through shared public-data sanitizer. |
| T-50-09 | I | brush callback payloads | mitigate | Route selected data through shared public-data sanitizer. |
| T-50-10 | T | final validation evidence | mitigate | Record executed source-test and optional browser smoke results in `50-VALIDATION.md`. |
</threat_model>

<verification>
Run all task-level commands. Optional browser visual smoke may skip only under the documented Phase 48/50 skip policy.
</verification>

<success_criteria>
- Public datum sanitizer is shared or single-rule delegated across tooltip, events, and brush.
- Polygon and sf private fields are covered by source-contract tests.
- Existing polygon/sf interactivity tests pass or skip only for optional dependency reasons.
- Phase 50 validation notes are recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/50-renderer-wiring-and-interaction-contracts/50-03-SUMMARY.md`.
</output>
