# Phase 42: Release Validation Gate - Pattern Map

**Mapped:** 2026-05-23  
**Files analyzed:** 4  
**Analogs found:** 4 / 4

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `.planning/phases/42-release-validation-gate/42-VALIDATION-GATE.md` | config | batch | `.planning/phases/42-release-validation-gate/42-VALIDATION.md` | exact |
| `.planning/phases/42-release-validation-gate/42-GATE-RUN.md` | test | batch + file-I/O | `.planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md` | exact |
| `.planning/phases/42-release-validation-gate/42-VERIFICATION.md` | test | batch + transform | `.planning/phases/40-package-hygiene/40-VERIFICATION.md` | exact |
| `README.Rmd` (optional maintainer doc touchpoint) | config | request-response | `README.Rmd` | exact |

## Pattern Assignments

### `.planning/phases/42-release-validation-gate/42-VALIDATION-GATE.md` (config, batch)

**Analog:** `.planning/phases/42-release-validation-gate/42-VALIDATION.md`

**Header and infrastructure pattern** (lines 1-24):

```markdown
---
phase: 42
slug: release-validation-gate
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-23
---

# Phase 42 - Validation Strategy

| **Framework** | testthat edition 3, devtools, roxygen2, R CMD build/check |
| **Quick run command** | `rtk Rscript --vanilla -e 'devtools::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-regression-core.R"); testthat::test_file("tests/testthat/test-sf-browser.R")'` |
| **Full suite command** | `rtk Rscript --vanilla -e 'devtools::document(); devtools::build_readme(); devtools::test()'` plus `rtk R CMD build` and `rtk R CMD check --as-cran` from a scratch directory |
```

**Validation matrix pattern** (lines 37-48):

```markdown
| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
| 42-01-01 | 01 | 1 | VAL-01 | T42-01 | N/A | docs/source | `rtk rg -n "Quick local gate|Full release gate|R CMD check|Expected optional skips" .planning/phases/42-release-validation-gate/42-VALIDATION-GATE.md` | yes | pending |
| 42-01-02 | 01 | 1 | VAL-02 | T42-02 | N/A | docs/source | `rtk rg -n "test-regression-core.R|test-sf-browser.R|test-sf-ir.R|test-sf-renderer.R|test-legends.R|test-date-scales.R|test-coord-flip.R" .planning/phases/42-release-validation-gate/42-VALIDATION-GATE.md` | yes | pending |
| 42-01-03 | 01 | 1 | VAL-03 | T42-03 | N/A | docs/source | `rtk rg -n "test_output/browser-sf|page-errors.log|Rcheck|README.md|man/|NAMESPACE" .planning/phases/42-release-validation-gate/42-VALIDATION-GATE.md` | yes | pending |
```

**Manual interpretation pattern** (lines 64-69):

```markdown
| Behavior | Requirement | Why Manual | Test Instructions |
| Interpret expected optional browser/spatial skips | VAL-01, VAL-02 | Local machines differ in `sf`, Chrome, and chromote availability. | Confirm `42-GATE-RUN.md` records each skip as expected or release-blocking with the exact message. |
| Inspect docs/check artifacts after full gate | VAL-03 | The package check output and docs diffs are generated by local tooling and need maintainer interpretation. | Confirm `42-GATE-RUN.md` names the exact `*.Rcheck/` directory, docs files touched, and failure/debug paths. |
```

**Supporting skip-contract analog:** `.planning/phases/40-package-hygiene/40-SKIP-AUDIT.md`

**Browser/spatial skip documentation pattern** (lines 5-17, 21-37):

```markdown
`tests/testthat/helper-browser-sf.R` centralizes live browser smoke gating in `skip_browser_sf_smoke()` with this order:

1. `testthat::skip_on_cran()`
2. `testthat::skip_if_not_installed("chromote", "0.5.1")`
3. `testthat::skip_if_not_installed("sf")`
4. `testthat::skip_if_not_installed("geojsonsf")`
5. `chromote::find_chrome()`
6. `chromote::ChromoteSession$new(width = 10, height = 10)`
```

Planner should copy this shape for an "Expected optional skips" section and keep the exact messages from lines 14-15:

```markdown
The Chrome absence message is `Chrome/Chromium not available for chromote sf smoke tests`.
The launch failure message starts with `chromote session launch unavailable:`.
```

**Coverage evidence analog:** `tests/testthat/test-regression-core.R`

**Release-surface matrix source pattern** (lines 32-59, 61-100, 102-132):

```r
test_that("HARD-03 regression matrix covers representative non-sf geoms", {
  plots <- list(
    geom_point = ggplot2::ggplot(base, ggplot2::aes(x, y)) + ggplot2::geom_point(),
    geom_line = ggplot2::ggplot(base, ggplot2::aes(x, y, group = group)) + ggplot2::geom_line(),
    geom_col = ggplot2::ggplot(base, ggplot2::aes(group, y)) + ggplot2::geom_col(),
    geom_rect = ggplot2::ggplot(rects) +
      ggplot2::geom_rect(ggplot2::aes(xmin = xmin, xmax = xmax, ymin = ymin, ymax = ymax)),
    geom_text = ggplot2::ggplot(base, ggplot2::aes(x, y, label = label)) + ggplot2::geom_text(),
    geom_segment = ggplot2::ggplot(base, ggplot2::aes(x = x, y = y, xend = x + 0.5, yend = y + 0.5)) +
      ggplot2::geom_segment(),
    geom_boxplot = ggplot2::ggplot(base, ggplot2::aes(group, y)) + ggplot2::geom_boxplot(),
    geom_smooth = ggplot2::ggplot(base, ggplot2::aes(x, y)) +
      ggplot2::geom_smooth(method = "lm", se = FALSE)
  )
})
```

```r
test_that("HARD-03 regression matrix covers sf families", {
  skip_if_not_installed("sf")
  skip_if_not_installed("geojsonsf")

  polygon_ir <- expect_regression_ir_ok(ggplot2::ggplot(polygon_sf) + ggplot2::geom_sf())
  point_ir <- expect_regression_ir_ok(ggplot2::ggplot(point_sf) + ggplot2::geom_sf())
  line_ir <- expect_regression_ir_ok(ggplot2::ggplot(line_sf) + ggplot2::geom_sf())
  mixed_ir <- expect_regression_ir_ok(ggplot2::ggplot(mixed_sf) + ggplot2::geom_sf())
})
```

```r
test_that("HARD-03 regression matrix covers facets legends dates and coord_flip", {
  expect_equal(facet_ir$facets$type, "wrap")
  expect_true(length(legend_ir$guides) >= 1L)
  expect_equal(date_ir$scales$x$transform, "date")
  expect_equal(flip_ir$coord$type, "flip")
})
```

### `.planning/phases/42-release-validation-gate/42-GATE-RUN.md` (test, batch + file-I/O)

**Analog:** `.planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md`

**Command evidence pattern** (lines 63-77):

````markdown
## Verification Evidence

Commands run after the changes:

```bash
rtk Rscript --vanilla -e 'parse("tests/testthat/test-date-scales.R"); cat("date scale test parse ok\n")'
rtk rg -n "test_output/" .gitignore
rtk rg -n "\\^test_output|Rcheck" .Rbuildignore
rtk git check-ignore test_output/browser-sf/phase40-artifact-smoke.html
rtk git check-ignore test_output/browser-sf/phase40-artifact-smoke-page-errors.log
rtk Rscript --vanilla -e 'devtools::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-date-scales.R")'
cd /private/tmp && rtk R CMD build --no-build-vignettes --no-manual /Users/davidzenz/R/gg2d3
tar -tzf /private/tmp/gg2d3_0.0.0.9000.tar.gz | rg "\\.planning|\\.claude|AGENTS\\.md|CLAUDE\\.md|test_output|Rcheck"
```
````

For Phase 42, keep the same "commands run" block, but use the planned quick/full release commands. The planner should avoid hard-coding the package tarball version in reusable instructions; derive it from `DESCRIPTION` line 3 during execution.

**Outcome evidence pattern** (lines 79-87):

```markdown
Outcomes:

- Date scale test source parsed successfully.
- `.gitignore` and `.Rbuildignore` contain the expected generated-output rules.
- `test_output/browser-sf/phase40-artifact-smoke.html` is ignored.
- `test_output/browser-sf/phase40-artifact-smoke-page-errors.log` is ignored.
- `tests/testthat/test-date-scales.R` passed with 43 assertions and 1 expected visual-test skip.
- Source package build from `/private/tmp` completed; tarball inspection returned no `.planning`, `.claude`, `AGENTS.md`, `CLAUDE.md`, `test_output`, or `Rcheck` paths.
```

**Command/outcome table analog:** `.planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md`

**Evidence table pattern** (lines 22-41):

```markdown
## Verification Evidence

### DEBT-01 evidence

| Command | Outcome |
|---------|---------|
| `rtk Rscript --vanilla -e 'read.dcf("DESCRIPTION"); cat("DESCRIPTION parse ok\n")'` | passed |
| `rtk rg -n "phase35-sf-facet-wrap.html\" = c\\(1L, 1L\\)|phase35-sf-facet-grid.html\" = c\\(1L, 0L, 0L, 1L\\)|expect_equal\\(panel_counts, expected_panel_counts\\[\\[fixture_name\\]\\]\\)" tests/testthat/test-sf-browser.R` | passed |
```

Use this compact table when recording Phase 42 gate steps that have pass/skip/fail outcomes.

**Browser failure artifact source:** `tests/testthat/helper-browser-sf.R`

**Failure artifact writer pattern** (lines 238-285):

```r
write_browser_failure_artifacts <- function(name, html_path, logs, session = NULL) {
  out_dir <- browser_sf_artifact_dir()
  prefix <- file.path(out_dir, name)
  entries <- .browser_sf_logs(logs)

  writeLines(
    vapply(console_entries, format_entry, character(1)),
    con = paste0(prefix, "-console.log")
  )
  writeLines(
    vapply(page_error_entries, format_entry, character(1)),
    con = paste0(prefix, "-page-errors.log")
  )

  jsonlite::write_json(
    list(
      html_path = normalizePath(html_path, mustWork = FALSE),
      logs = entries
    ),
    path = paste0(prefix, "-browser-log.json"),
    auto_unbox = TRUE,
    pretty = TRUE,
    null = "null"
  )
}
```

### `.planning/phases/42-release-validation-gate/42-VERIFICATION.md` (test, batch + transform)

**Analog:** `.planning/phases/40-package-hygiene/40-VERIFICATION.md`

**Frontmatter and result summary pattern** (lines 1-15):

```markdown
---
phase: 40-package-hygiene
verified: 2026-05-23T13:06:38Z
status: passed
score: 12/12 must-haves verified
overrides_applied: 0
---

# Phase 40: Package Hygiene Verification Report

**Phase Goal:** Maintainers can run tests, examples, docs, and optional validation tooling without undeclared dependencies or source-tree artifact noise.
**Verified:** 2026-05-23T13:06:38Z
**Status:** passed
```

For Phase 42, use the same frontmatter fields, but set `phase: 42-release-validation-gate` and summarize VAL-01 through VAL-03.

**Observable truths pattern** (lines 18-35):

```markdown
| # | Truth | Status | Evidence |
|---|---|---|---|
| 2 | Optional browser and spatial validation paths skip cleanly with clear messages when local tooling is unavailable. | VERIFIED | `skip_browser_sf_smoke()` includes `skip_on_cran()`, package skips, Chrome detection message, and launch failure message; non-browser sf tests retain direct `sf`/`geojsonsf` skips. |
| 3 | Generated fixtures, logs, check outputs, and smoke artifacts land in predictable ignored paths and do not pollute package sources. | VERIFIED | Browser helper writes under `test_output/browser-sf`; `.gitignore` and `.Rbuildignore` cover `test_output`, `test_*_files`, root HTML/PNG/PDF, and `*.Rcheck`; `git check-ignore` passed for smoke HTML and page-error log paths. |
```

**Required artifacts and key-link pattern** (lines 37-56):

```markdown
| Artifact | Expected | Status | Details |
| `tests/testthat/helper-browser-sf.R` | Central browser skip behavior and messages | VERIFIED | Defines `skip_browser_sf_smoke()`, browser artifact directory, and failure artifact writer. |
| `.planning/phases/40-package-hygiene/40-SKIP-AUDIT.md` | Optional dependency skip audit | VERIFIED | Documents browser skip gate, spatial skip gate, non-browser coverage, and required changes. |

| From | To | Via | Status | Details |
| `tests/testthat/test-sf-browser.R` | `tests/testthat/helper-browser-sf.R` | `skip_browser_sf_smoke()` | WIRED | Browser smoke tests call `skip_browser_sf_smoke()` at live browser entry points. |
| `tests/testthat/helper-browser-sf.R` | `.gitignore` | `test_output/browser-sf` | WIRED | Helper writes under `test_output/browser-sf`; `git check-ignore` confirms the smoke HTML/log paths are ignored. |
```

**Behavioral spot-checks pattern** (lines 64-75):

```markdown
| Behavior | Command | Result | Status |
|---|---|---|---|
| Browser skip helper and tests are wired | `rtk rg -n "skip_browser_sf_smoke|skip_on_cran|chromote session launch unavailable|Chrome/Chromium not available" tests/testthat/helper-browser-sf.R tests/testthat/test-sf-browser.R` | Found helper, CRAN skip, messages, and test calls | PASS |
| Browser smoke HTML path is ignored | `rtk git check-ignore test_output/browser-sf/phase40-artifact-smoke.html` | Returned ignored path | PASS |
| Date-scale regression test remains runnable | `rtk Rscript --vanilla -e 'devtools::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-date-scales.R")'` | 43 passed, 0 failed, 1 expected visual-test skip | PASS |
```

**Requirement coverage pattern** (lines 76-84):

```markdown
| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| HYG-02 | `40-02-PLAN.md` | Optional browser and spatial validation tooling skips cleanly with clear messages when unavailable and does not force heavyweight local dependencies in CRAN-like environments. | SATISFIED | Central skip helper has CRAN, package, Chrome, and launch gates; source checks found no Node/Playwright/Puppeteer/Selenium additions. |
| HYG-03 | `40-03-PLAN.md` | Generated browser fixtures, logs, check outputs, and local validation artifacts are written to predictable ignored paths so routine checks and local smoke runs do not pollute package sources. | SATISFIED | Artifact helper paths and ignore/build-ignore rules verified; smoke HTML/log paths pass `git check-ignore`. |
```

**Deferred non-blocker pattern:** `.planning/phases/41-release-blocking-debt-triage/41-VERIFICATION.md` (lines 50-53):

```markdown
## Deferred Non-Blockers

- Ordinary `geom_polygon()` renderer implementation remains deferred with a next step to plan it separately if parity coverage requires it.
- Rect/tile out-of-bounds renderer changes remain deferred with a next step to add a focused reproduction before changing renderer behavior.
```

Use this section for Phase 42 only if the gate records expected optional skips or Phase 41 deferred debt as non-blocking.

### `README.Rmd` (optional config, request-response)

**Analog:** `README.Rmd`

**Contributor command pattern** (lines 130-153):

````markdown
## Development (for contributors)

Vendor D3 v7 locally:

```r
dir.create("inst/htmlwidgets/lib/d3", recursive = TRUE, showWarnings = FALSE)
download.file(
  "https://d3js.org/d3.v7.min.js",
  destfile = "inst/htmlwidgets/lib/d3/d3.v7.min.js",
  mode = "wb"
)
```

Iterate:

```r
devtools::document()
devtools::load_all()
devtools::test()
```

*Note:* `README.md` is generated from `README.Rmd`. Use `devtools::build_readme()` to re-render.
````

If Phase 42 touches maintainer-facing docs, extend `README.Rmd` rather than `README.md`, keep the command list concise, and regenerate `README.md` through `devtools::build_readme()`.

## Shared Patterns

### Command Prefix

**Source:** `/Users/davidzenz/.codex/RTK.md` and project research  
**Apply to:** All shell commands recorded in `42-VALIDATION-GATE.md`, `42-GATE-RUN.md`, and `42-VERIFICATION.md`

```bash
rtk Rscript --vanilla -e 'devtools::document(); devtools::build_readme(); devtools::test()'
rtk R CMD build --no-manual /Users/davidzenz/R/gg2d3
rtk R CMD check --as-cran gg2d3_0.0.0.9000.tar.gz
```

### Testthat Package Setup

**Source:** `tests/testthat.R` lines 9-12  
**Apply to:** Any references to full package test execution

```r
library(testthat)
library(gg2d3)

test_check("gg2d3")
```

### Optional Browser Smoke Gate

**Source:** `tests/testthat/helper-browser-sf.R` lines 21-47  
**Apply to:** Browser smoke documentation and expected skip classification

```r
skip_browser_sf_smoke <- function() {
  testthat::skip_on_cran()
  testthat::skip_if_not_installed("chromote", "0.5.1")
  testthat::skip_if_not_installed("sf")
  testthat::skip_if_not_installed("geojsonsf")

  chrome <- tryCatch(chromote::find_chrome(), error = function(e) NULL)
  testthat::skip_if(
    is.null(chrome) || !nzchar(chrome),
    "Chrome/Chromium not available for chromote sf smoke tests"
  )

  launch <- tryCatch(
    {
      session <- chromote::ChromoteSession$new(width = 10, height = 10)
      on.exit(session$close(), add = TRUE)
      TRUE
    },
    error = function(e) e
  )
  testthat::skip_if(
    !isTRUE(launch),
    paste("chromote session launch unavailable:", conditionMessage(launch))
  )
}
```

### Browser Artifact Paths

**Source:** `tests/testthat/helper-browser-sf.R` lines 49-64  
**Apply to:** Failure artifact sections in `42-VALIDATION-GATE.md` and `42-GATE-RUN.md`

```r
browser_sf_artifact_dir <- function() {
  out_dir <- file.path(.test_output_dir(), "browser-sf")
  dir.create(out_dir, showWarnings = FALSE, recursive = TRUE)
  out_dir
}

save_browser_sf_widget <- function(widget, filename) {
  outpath <- file.path(browser_sf_artifact_dir(), filename)
  htmlwidgets::saveWidget(
    widget,
    file = normalizePath(outpath, mustWork = FALSE),
    selfcontained = FALSE
  )
  testthat::expect_true(file.exists(outpath))
  outpath
}
```

### Browser Error Capture

**Source:** `tests/testthat/helper-browser-sf.R` lines 217-235  
**Apply to:** Interpreting browser smoke failures

```r
assert_no_browser_errors <- function(logs) {
  entries <- .browser_sf_logs(logs)
  if (length(entries) == 0) return(invisible(TRUE))

  is_error <- vapply(entries, function(entry) {
    identical(entry$source, "Runtime.exceptionThrown") ||
      identical(entry$type, "exception") ||
      identical(entry$type, "error") ||
      identical(entry$type, "assert")
  }, logical(1))

  if (any(is_error)) {
    messages <- vapply(entries[is_error], function(entry) {
      paste(entry$source %||% "browser", entry$type %||% "error", entry$message %||% "", sep = ": ")
    }, character(1))
    testthat::fail(paste(c("Browser errors were captured:", messages), collapse = "\n"))
  }
}
```

### Deterministic Browser Smoke Assertions

**Source:** `tests/testthat/test-sf-browser.R` lines 283-340, 342-386, 388-497  
**Apply to:** Coverage matrix rows for browser smoke

```r
test_that("BRSF-01 DOM: Phase 35 sf fixtures render live geom-sf paths", {
  skip_browser_sf_smoke()
  fixtures <- .phase35_sf_fixture_set()
  expected_names <- .browser_sf_fixture_names()
  expected_counts <- .browser_sf_expected_counts()
  expect_equal(names(fixtures), expected_names)
  expect_equal(names(expected_counts), expected_names)
})
```

```r
test_that("BRSF-02 DOM: faceted sf fixtures keep panel-local path counts", {
  skip_browser_sf_smoke()
  expected_panel_counts <- list(
    "phase35-sf-facet-wrap.html" = c(1L, 1L),
    "phase35-sf-facet-grid.html" = c(1L, 0L, 0L, 1L)
  )
  expect_equal(panel_counts, expected_panel_counts[[fixture_name]])
})
```

```r
test_that("SFGEOM-04 DOM: mixed sf overlays and facets keep panel-local marks", {
  skip_browser_sf_smoke()
  expect_true(any(grepl("geom-sf-polygon", mixed_classes, fixed = TRUE)))
  expect_true(any(grepl("geom-sf-point", mixed_classes, fixed = TRUE)))
  expect_true(any(grepl("geom-sf-line", mixed_classes, fixed = TRUE)))
  expect_equal(panel_counts, c(1L, 1L, 0L))
})
```

### Browser Artifact Contract Tests

**Source:** `tests/testthat/test-sf-browser.R` lines 499-537  
**Apply to:** VAL-03 artifact evidence

```r
test_that("BRSF-03 artifact: browser sf fixtures use deterministic local output", {
  skip_browser_sf_smoke()

  fixtures <- .phase35_sf_fixture_set()
  artifact_dir <- browser_sf_artifact_dir()
  fixture_paths <- normalizePath(unlist(fixtures), winslash = "/", mustWork = TRUE)
  artifact_path <- normalizePath(artifact_dir, winslash = "/", mustWork = FALSE)

  expect_true(all(file.exists(unlist(fixtures))))
  expect_true(all(grepl("test_output", fixture_paths, fixed = TRUE)))
  expect_true(grepl("test_output/browser-sf", artifact_path, fixed = TRUE))
  expect_true(file.exists(artifact_dir))
})
```

```r
test_that("BRSF-03 artifact: browser errors write deterministic log files", {
  artifact_dir <- browser_sf_artifact_dir()
  write_browser_failure_artifacts(fixture_name, html_path, logs, session = NULL)

  expect_true(file.exists(file.path(artifact_dir, paste0(fixture_name, "-console.log"))))
  expect_true(file.exists(file.path(artifact_dir, paste0(fixture_name, "-page-errors.log"))))
  expect_error(assert_no_browser_errors(logs), "Browser errors were captured")
})
```

### Ignore And Build-Ignore Coverage

**Source:** `.gitignore` lines 10-11, 39-51 and `.Rbuildignore` lines 13-25  
**Apply to:** Artifact path documentation and package-check artifact expectations

```gitignore
test_output/
test_*_files/
*.Rcheck/
/*.png
/*.html
/*.pdf
```

```text
^AGENTS\.md$
^(.*/)?CLAUDE\.md$
^\.claude($|/)
^\.planning($|/)
^test_output$
^test_output/
^(.*/)?test_output($|/)
^test_.*_files$
^(.*/)?test_.*_files($|/)
^.*\.Rcheck$
^[^/]+\.html$
^[^/]+\.png$
^[^/]+\.pdf$
```

### Package Metadata For Gate Commands

**Source:** `DESCRIPTION` lines 1-3, 22-41  
**Apply to:** Dynamic tarball naming and dependency/skip documentation

```text
Package: gg2d3
Version: 0.0.0.9000
Suggests:
    chromote (>= 0.5.1),
    devtools,
    geojsonsf (>= 2.0.0),
    pkgload,
    rmarkdown,
    sf (>= 1.0.0),
    testthat (>= 3.0.0)
Config/testthat/edition: 3
Config/roxygen2/version: 8.0.0
```

## No Analog Found

None. All likely Phase 42 artifacts have close analogs in the current planning and test infrastructure.

## Metadata

**Analog search scope:** `.planning/phases/40-package-hygiene/`, `.planning/phases/41-release-blocking-debt-triage/`, `.planning/phases/42-release-validation-gate/`, `README.Rmd`, `DESCRIPTION`, `.gitignore`, `.Rbuildignore`, `tests/testthat/`  
**Files scanned:** 27 planning/source/test files plus project instructions  
**Pattern extraction date:** 2026-05-23
