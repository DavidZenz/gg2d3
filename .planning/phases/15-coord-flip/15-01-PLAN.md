---
phase: 15-coord-flip
plan: 01
type: execute
wave: 1
depends_on: ["14-01"]
files_modified:
  - R/as_d3_ir.R
  - inst/htmlwidgets/gg2d3.js
  - tests/testthat/test-coord-flip.R
autonomous: true

must_haves:
  truths:
    - "User sees `coord_flip` place x/y axes on correct sides after flipping"
    - "User sees `coord_flip` preserve correct orientation behavior in faceted plots"
  artifacts:
    - path: "tests/testthat/test-coord-flip.R"
      provides: "Regression tests for flipped axes and faceted orientations"
      contains: "coord_flip"
---

<objective>
Harden `coord_flip` implementation to ensure correct axis placement and faceted orientation.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate flip detection logic in R</name>
  <files>R/as_d3_ir.R</files>
  <action>Consolidate `is_flip_early` and `is_flip` logic into a single variable at the top of the function to improve code clarity and consistency.</action>
  <verify>Rscript -e "devtools::load_all(); # verify IR output is unchanged"</verify>
  <done>Redundant detection logic is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix renderPanel axis placement for flipped coords</name>
  <files>inst/htmlwidgets/gg2d3.js</files>
  <action>Update `renderPanel` in `gg2d3.js` to correctly swap d3.axis generators when `flip` is true. Ensure `xScale` (x-aesthetic) uses `axisLeft` and `yScale` (y-aesthetic) uses `axisBottom` when coordinates are flipped in faceted contexts.</action>
  <verify>Manual visual inspection of a flipped faceted plot.</verify>
  <done>Faceted flipped plots show axes on the correct sides.</done>
</task>

<task type="auto">
  <name>Task 3: Add comprehensive regression tests for coord_flip + facets</name>
  <files>tests/testthat/test-coord-flip.R</files>
  <action>Create a new test file `test-coord-flip.R` with tests for: (a) single panel flipped axes, (b) facet_wrap flipped axes, (c) facet_grid flipped axes. Verify that IR metadata (axes labels, panel ranges) remains correct.</action>
  <verify>Rscript -e "devtools::test(filter = 'coord-flip')"</verify>
  <done>All flipped coordinate scenarios are test-backed.</done>
</task>

<task type="auto">
  <name>Task 4: Add visual verification artifact</name>
  <files>tests/testthat/visual-coord-flip-check.R</files>
  <action>Create a script to generate HTML widgets for flipped faceted plots to verify visual correctness (e.g., bar chart with facet_wrap and coord_flip).</action>
  <verify>Open generated HTML and confirm axes are correctly placed.</verify>
  <done>Visual verification confirms the fix.</done>
</task>

</tasks>

<success_criteria>
- Single-panel flipped plots have x-aesthetic axis on the left and y-aesthetic axis on the bottom.
- Faceted flipped plots have x-aesthetic axis on the left (for left column) and y-aesthetic axis on the bottom (for bottom row).
- Axis titles are correctly placed (x title below bottom, y title left of side).
</success_criteria>
