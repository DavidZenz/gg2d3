---
phase: 23-advanced-stats
plan: 01
type: execute
wave: 1
depends_on: ["22-01"]
files_modified:
  - inst/htmlwidgets/modules/geom-registry.js
  - tests/testthat/test-geoms-phase5.R
autonomous: true

must_haves:
  truths:
    - "User can render geom_density with correct kernel estimation and area mapping"
    - "User can render geom_smooth with support for loess and gam methods"
  artifacts:
    - path: "tests/testthat/test-geoms-phase5.R"
      provides: "Regression tests for density and smooth geoms"
      contains: "geom_density"
---

<objective>
Harden and verify support for advanced statistical geoms (density, smooth) including transition support and various methods.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for density and smooth</name>
  <files>tests/testthat/test-geoms-phase5.R</files>
  <action>Add tests validating that: (a) geom_density produces filled area IR, (b) geom_smooth produces both ribbon and line data in IR, (c) loess and gam methods are correctly captured via b$data.</action>
  <verify>Rscript -e "devtools::test(filter = 'geoms-phase5')"</verify>
  <done>Statistical geoms are test-backed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement visual verification for advanced stats</name>
  <files>tests/testthat/visual-advanced-stats-check.R</files>
  <action>Create a script to generate HTML widgets for density plots (including stacked) and smoothed lines (loess/gam) to verify visual correctness.</action>
  <verify>Open generated HTML and confirm parity with ggplot2 static output.</verify>
  <done>Visual verification confirms high-fidelity rendering.</done>
</task>

<task type="auto">
  <name>Task 3: Verify transition support for statistical paths</name>
  <files>inst/htmlwidgets/modules/geom-registry.js</files>
  <action>Trigger zoom interactions on plots with smooth/density layers and confirm that ribbons and outlines animate smoothly without jumping to the baseline.</action>
  <verify>Manual visual inspection of animated transitions.</verify>
  <done>Statistical geoms support object-constant transitions.</done>
</task>

</tasks>

<success_criteria>
- `geom_density()` renders as a filled area with a visible outline.
- `geom_smooth(method = "loess")` renders a blue trend line with a grey confidence ribbon.
- All elements animate smoothly during zoom resets.
</success_criteria>
