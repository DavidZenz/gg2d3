---
phase: 44-ordinary-geom-polygon-support
plan: 03
type: execute
wave: 3
depends_on: ["44-02"]
files_modified:
  - inst/htmlwidgets/modules/events.js
  - inst/htmlwidgets/modules/brush.js
  - inst/htmlwidgets/modules/crosstalk.js
  - tests/testthat/test-polygon-interactivity.R
  - tests/testthat/helper-browser-polygon.R
  - tests/testthat/test-polygon-browser.R
autonomous: true
requirements: [POLY-03]
must_haves:
  truths:
    - "Tooltip, hover, custom handler, Shiny-style handler, brush, and crosstalk selector plumbing can target path.geom-polygon marks."
    - "Polygon callback and brush payloads are deterministic representative source rows."
    - "Polygon callback and brush payloads exclude renderer-private fields such as _polygonPoints."
    - "Browser DOM smoke coverage proves single-panel, grouped, faceted, styled, and interactivity-facing polygon behavior without screenshots."
  artifacts:
    - path: "tests/testthat/test-polygon-interactivity.R"
      provides: "POLY-03 selector and sanitizer source contract tests"
    - path: "tests/testthat/helper-browser-polygon.R"
      provides: "ordinary polygon browser smoke skip and artifact helpers"
    - path: "tests/testthat/test-polygon-browser.R"
      provides: "optional chromote DOM/callback assertions for polygon marks"
    - path: "inst/htmlwidgets/modules/events.js"
      provides: "tooltip, hover, custom handler, and Shiny-style selector support for path.geom-polygon"
      contains: "path.geom-polygon"
    - path: "inst/htmlwidgets/modules/brush.js"
      provides: "bounds-based brush selector support for path.geom-polygon"
      contains: "path.geom-polygon"
    - path: "inst/htmlwidgets/modules/crosstalk.js"
      provides: "linked-selection key binding selector support for path.geom-polygon"
      contains: "path.geom-polygon"
  key_links:
    - from: "inst/htmlwidgets/modules/geoms/polygon.js"
      to: "inst/htmlwidgets/modules/events.js"
      via: "path.geom-polygon selector"
      pattern: "path.geom-polygon"
    - from: "inst/htmlwidgets/modules/geoms/polygon.js"
      to: "inst/htmlwidgets/modules/brush.js"
      via: "generic path getBBox branch"
      pattern: "getBBox"
    - from: "inst/htmlwidgets/modules/events.js"
      to: "tests/testthat/test-polygon-browser.R"
      via: "click/mouseover callback payload assertions"
      pattern: "__gg2d3_polygon_click"
---

<objective>
Wire ordinary polygon marks into existing interaction hooks and prove sanitized group-level payload behavior.

Purpose: POLY-03 and D-08 through D-14 require ordinary polygons to behave like first-class interactive marks without adding a polygon-specific API or exposing renderer-private fields.
Output: selector updates, source-level sanitizer tests, ordinary polygon browser smoke helpers, and optional chromote DOM/callback coverage.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-ordinary-geom-polygon-support/44-CONTEXT.md
@.planning/phases/44-ordinary-geom-polygon-support/44-RESEARCH.md
@.planning/phases/44-ordinary-geom-polygon-support/44-VALIDATION.md
@.planning/phases/44-ordinary-geom-polygon-support/44-PATTERNS.md
@.planning/phases/44-ordinary-geom-polygon-support/44-02-SUMMARY.md
@inst/htmlwidgets/modules/geoms/polygon.js
@inst/htmlwidgets/modules/events.js
@inst/htmlwidgets/modules/brush.js
@inst/htmlwidgets/modules/crosstalk.js
@inst/htmlwidgets/modules/tooltip.js
@tests/testthat/test-sf-interactivity.R
@tests/testthat/helper-browser-sf.R
@tests/testthat/test-sf-browser.R

<interfaces>
Selector to add exactly in interaction selector arrays:
```javascript
'path.geom-polygon'
```

Payload sanitizer contracts already available:
```javascript
if (key.startsWith('_')) return;
selectedData.push(sanitizeSelectedDatum(d));
clickHandler.call(this, event, publicDatum);
window.Shiny.setInputValue(shinyId, publicDatum);
```

Browser payload field that must never appear in public callback data:
```text
_polygonPoints
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add polygon interaction selector and sanitizer source tests</name>
  <files>tests/testthat/test-polygon-interactivity.R</files>
  <read_first>
    - tests/testthat/test-sf-interactivity.R
    - inst/htmlwidgets/modules/events.js
    - inst/htmlwidgets/modules/brush.js
    - inst/htmlwidgets/modules/crosstalk.js
    - inst/htmlwidgets/modules/tooltip.js
    - inst/htmlwidgets/modules/geoms/polygon.js
    - .planning/phases/44-ordinary-geom-polygon-support/44-CONTEXT.md
    - .planning/phases/44-ordinary-geom-polygon-support/44-VALIDATION.md
  </read_first>
  <action>Create `tests/testthat/test-polygon-interactivity.R` with a `read_module()` helper matching `test-sf-interactivity.R`. Add tests that assert `path.geom-polygon` appears in `events.js`, `brush.js`, and `crosstalk.js`; `events.js` still contains `sanitizeEventDatum`, `startsWith('_')`, `publicDatum`, `clickHandler.call(this, event, publicDatum)`, and `setInputValue(shinyId, publicDatum)`; `brush.js` still contains `sanitizeSelectedDatum`, `startsWith('_')`, `selectedData.push(sanitizeSelectedDatum(d))`, `getBBox`, and does not route `path.geom-polygon` through the `.geom-sf` `data-cx`/`data-cy` branch; `tooltip.js` still strips underscore-prefixed fields; and `polygon.js` stores `_polygonPoints` only under an underscore-prefixed key. Include an assertion that the literal `_polygonPoints` does not appear in a non-sanitized callback assertion path.</action>
  <acceptance_criteria>
    - `rtk rg -n "POLY-03|path\\.geom-polygon|sanitizeEventDatum|sanitizeSelectedDatum|sanitizeTooltipDatum|_polygonPoints|startsWith\\('_'\\)" tests/testthat/test-polygon-interactivity.R` returns matches.
    - `rtk rg -n "events\\.js|brush\\.js|crosstalk\\.js|tooltip\\.js|polygon\\.js|getBBox|data-cx|data-cy" tests/testthat/test-polygon-interactivity.R` returns matches.
    - `rtk Rscript --vanilla -e 'devtools::load_all(); testthat::test_file("tests/testthat/test-polygon-interactivity.R")'` fails before selector implementation and exits 0 after selector implementation.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --vanilla -e 'devtools::load_all(); testthat::test_file("tests/testthat/test-polygon-interactivity.R")'</automated>
  </verify>
  <done>Source tests pin polygon selector coverage and sanitizer behavior across interaction modules.</done>
</task>

<task type="auto">
  <name>Task 2: Add path.geom-polygon to existing interaction selector plumbing</name>
  <files>inst/htmlwidgets/modules/events.js, inst/htmlwidgets/modules/brush.js, inst/htmlwidgets/modules/crosstalk.js, tests/testthat/test-polygon-interactivity.R</files>
  <read_first>
    - inst/htmlwidgets/modules/events.js
    - inst/htmlwidgets/modules/brush.js
    - inst/htmlwidgets/modules/crosstalk.js
    - inst/htmlwidgets/modules/tooltip.js
    - inst/htmlwidgets/modules/geoms/polygon.js
    - tests/testthat/test-polygon-interactivity.R
    - .planning/phases/44-ordinary-geom-polygon-support/44-RESEARCH.md
    - .planning/phases/44-ordinary-geom-polygon-support/44-PATTERNS.md
  </read_first>
  <action>Add the exact selector string `'path.geom-polygon'` to the existing `INTERACTIVE_SELECTORS` arrays in `inst/htmlwidgets/modules/events.js`, `inst/htmlwidgets/modules/brush.js`, and `inst/htmlwidgets/modules/crosstalk.js`. Place it next to the other path geom selectors. Do not create a new polygon-specific tooltip, hover, brush, handler, Shiny, or crosstalk API. Keep brush behavior on the existing generic `path` `getBBox()` branch; do not add `data-cx`, `data-cy`, centroid, anchor, point-in-polygon, or sf-style behavior for ordinary polygons.</action>
  <acceptance_criteria>
    - `rtk rg -n "path\\.geom-polygon" inst/htmlwidgets/modules/events.js inst/htmlwidgets/modules/brush.js inst/htmlwidgets/modules/crosstalk.js` returns one or more matches in each file.
    - `rtk rg -n "geom-polygon.*data-cx|geom-polygon.*data-cy|point-in-polygon|centroid|polygon-specific" inst/htmlwidgets/modules/events.js inst/htmlwidgets/modules/brush.js inst/htmlwidgets/modules/crosstalk.js` returns no matches.
    - `rtk Rscript --vanilla -e 'devtools::load_all(); testthat::test_file("tests/testthat/test-polygon-interactivity.R")'` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --vanilla -e 'devtools::load_all(); testthat::test_file("tests/testthat/test-polygon-interactivity.R")'</automated>
  </verify>
  <done>Ordinary polygon marks are reachable by existing selector-driven tooltip, hover, handler, brush, and crosstalk plumbing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add optional browser DOM smoke coverage for polygon rendering and payloads</name>
  <files>tests/testthat/helper-browser-polygon.R, tests/testthat/test-polygon-browser.R</files>
  <read_first>
    - tests/testthat/helper-browser-sf.R
    - tests/testthat/test-sf-browser.R
    - tests/testthat/test-polygon-ir.R
    - tests/testthat/test-polygon-renderer.R
    - tests/testthat/test-polygon-interactivity.R
    - inst/htmlwidgets/modules/geoms/polygon.js
    - inst/htmlwidgets/modules/events.js
    - inst/htmlwidgets/modules/brush.js
    - .planning/phases/44-ordinary-geom-polygon-support/44-CONTEXT.md
    - .planning/phases/44-ordinary-geom-polygon-support/44-VALIDATION.md
    - .planning/phases/44-ordinary-geom-polygon-support/44-PATTERNS.md
  </read_first>
  <action>Create `tests/testthat/helper-browser-polygon.R` with `skip_browser_polygon_smoke()`, `browser_polygon_artifact_dir()`, and `save_browser_polygon_widget()` patterned after `helper-browser-sf.R`, but require only `chromote` and Chrome/Chromium; do not skip on `sf` or `geojsonsf`. Create `tests/testthat/test-polygon-browser.R` that supports direct `testthat::test_file()` runs with `pkgload::load_all(quiet = TRUE)` and sources the helper when needed. Add small ordinary polygon fixtures for single panel, grouped polygons, faceted polygons, mapped styling, `fill = NA`, `colour = NA`, and an interactivity fixture piped through `d3_tooltip()`, `d3_hover()`, `d3_brush(on_brush = "window.__gg2d3_polygon_brush = selectedData;")`, and `d3_handlers(click = "function(event, d) { window.__gg2d3_polygon_click = d; }", mouseover = "function(event, d) { window.__gg2d3_polygon_mouseover = d; }", shiny_id = "phase44_polygon")`. Browser tests must assert live DOM `path.geom-polygon` counts, non-empty closed `d` path strings, panel-local counts for facets, clip-path ancestry, fill/stroke attributes including `"none"` for NA fill/stroke cases, and click/mouseover/brush/Shiny-style payloads that include representative public fields such as `row_id` or `group` and exclude `_polygonPoints`. Keep all assertions DOM/callback based; do not add screenshot or perceptual comparison infrastructure.</action>
  <acceptance_criteria>
    - `rtk rg -n "skip_browser_polygon_smoke|browser_polygon_artifact_dir|save_browser_polygon_widget|chromote|Chrome/Chromium" tests/testthat/helper-browser-polygon.R` returns matches.
    - `rtk rg -n "POLY-02|POLY-03|path\\.geom-polygon|__gg2d3_polygon_brush|__gg2d3_polygon_click|__gg2d3_polygon_mouseover|phase44_polygon|_polygonPoints|clip-path|fill = NA|colour = NA" tests/testthat/test-polygon-browser.R` returns matches.
    - `rtk rg -n "screenshot|pixel|visual diff|playwright|puppeteer|selenium" tests/testthat/test-polygon-browser.R tests/testthat/helper-browser-polygon.R` returns no assertion gate or new browser-stack dependency.
    - `rtk Rscript --vanilla -e 'devtools::load_all(); testthat::test_file("tests/testthat/test-polygon-browser.R")'` exits 0 or skips cleanly with explicit testthat skip messages when chromote or Chrome/Chromium is unavailable.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --vanilla -e 'devtools::load_all(); testthat::test_file("tests/testthat/test-polygon-browser.R")'</automated>
  </verify>
  <done>Optional browser smoke coverage proves rendered polygon DOM, facets, styling, selectors, and sanitized representative payloads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG mark datum -> tooltip/hover/handler callbacks | Bound polygon data crosses into user-provided JavaScript callbacks and optional Shiny-style inputs. |
| SVG mark datum -> brush callback payload | Selected polygon rows are collected from DOM elements and returned to user callback code. |
| Generated widget HTML -> chromote browser | Test fixtures execute local htmlwidgets JavaScript in a browser session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-08 | Information Disclosure | `events.js`, `tooltip.js`, `brush.js` | mitigate | Source and browser tests assert underscore-prefixed `_polygonPoints` is removed before tooltip, handler, Shiny-style, and brush payloads reach user callbacks. |
| T-44-09 | Tampering | selector arrays | mitigate | Add `path.geom-polygon` source assertions for `events.js`, `brush.js`, and `crosstalk.js` so polygon marks do not silently bypass intended interaction controls. |
| T-44-10 | Spoofing | representative group payload | mitigate | Bind deterministic representative source rows and assert callback payloads contain stable public group/row identity. |
| T-44-11 | Elevation of Privilege | callback API surface | mitigate | Reuse existing tooltip, hover, brush, handler, Shiny-style, and crosstalk APIs only; do not introduce polygon-specific callback execution paths. |
| T-44-12 | Denial of Service | browser smoke execution | accept | Use small deterministic fixtures and existing chromote skip guards; tests skip cleanly when optional browser tooling is unavailable. |
</threat_model>

<verification>
Run:

```bash
rtk Rscript --vanilla -e 'devtools::load_all(); testthat::test_file("tests/testthat/test-polygon-interactivity.R"); testthat::test_file("tests/testthat/test-polygon-browser.R")'
rtk Rscript --vanilla -e 'devtools::test()'
```

The browser command may skip when chromote or Chrome/Chromium is unavailable, but it must skip through explicit testthat skip conditions rather than failing with missing dependency, launch, or browser errors.
</verification>

<success_criteria>
- POLY-03 appears in this plan frontmatter.
- `path.geom-polygon` appears in `events.js`, `brush.js`, and `crosstalk.js`.
- Tooltip, hover, custom handler, Shiny-style handler, brush, and crosstalk selector plumbing can target ordinary polygon marks through existing APIs.
- Callback and brush payload tests prove representative public rows exclude `_polygonPoints`.
- Browser DOM smoke coverage remains optional, DOM-based, and screenshot-free.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | Users can render ordinary `geom_polygon()` layers as grouped D3 paths with representative styling, facets, and existing interactivity hooks. | Plans 44-01, 44-02, 44-03 |
| REQ | POLY-01 IR recognition and row/aesthetic preservation | Plan 44-01 |
| REQ | POLY-02 D3 closed path rendering, styling, clipping, and facets | Plan 44-02 and browser assertions in Plan 44-03 |
| REQ | POLY-03 tooltip, hover, brush, custom handlers, row identity, sanitized payloads | Plan 44-03 |
| RESEARCH | Dedicated `geoms/polygon.js`, yaml registration, grouped row-order-preserving renderer | Plan 44-02 |
| RESEARCH | Add `path.geom-polygon` to events, brush, crosstalk selectors | Plan 44-03 |
| RESEARCH | Add zoom/update path handling for polygon paths | Plan 44-02 |
| CONTEXT D-01-D-04 | One path per group, multiple groups, no explicit hole topology, preserve row order | Plans 44-01 and 44-02 |
| CONTEXT D-05-D-07 | Fill, colour/stroke, alpha, linewidth, linetype, NA fill/stroke | Plans 44-01, 44-02, 44-03 |
| CONTEXT D-08-D-11 | Group-level interactivity, sanitized representative row, path-bounds brush, no new API | Plan 44-03 |
| CONTEXT D-12-D-14 | IR/source/browser validation, representative matrix, no screenshot infrastructure | Plans 44-01, 44-02, 44-03 |
| Deferred Ideas | holes/subgroups, screenshot infrastructure, sf annotations, rect/tile edge behavior | Excluded from all tasks |
</source_audit>

<output>
After completion, create `.planning/phases/44-ordinary-geom-polygon-support/44-03-SUMMARY.md`
</output>
