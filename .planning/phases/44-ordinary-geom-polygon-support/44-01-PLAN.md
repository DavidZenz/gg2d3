---
phase: 44-ordinary-geom-polygon-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/as_d3_ir.R
  - R/validate_ir.R
  - tests/testthat/test-polygon-ir.R
autonomous: true
requirements: [POLY-01]
must_haves:
  truths:
    - "as_d3_ir() emits ordinary geom_polygon() layers with layer$geom equal to polygon."
    - "Polygon IR rows preserve ggplot2 built-data row order within each group."
    - "Polygon IR rows preserve PANEL, x, y, group, fill, colour, alpha, linewidth, and linetype values for single-panel, grouped, faceted, and NA-styling fixtures."
  artifacts:
    - path: "tests/testthat/test-polygon-ir.R"
      provides: "POLY-01 characterization tests for ordinary geom_polygon() IR extraction"
    - path: "R/as_d3_ir.R"
      provides: "GeomPolygon to polygon IR extraction contract"
      contains: "GeomPolygon= \"polygon\""
    - path: "R/validate_ir.R"
      provides: "IR validation allowlist containing polygon"
      contains: "\"polygon\""
  key_links:
    - from: "R/as_d3_ir.R"
      to: "tests/testthat/test-polygon-ir.R"
      via: "as_d3_ir() polygon fixtures"
      pattern: "GeomPolygon.*polygon"
    - from: "tests/testthat/test-polygon-ir.R"
      to: "R/validate_ir.R"
      via: "validate_ir(ir)"
      pattern: "validate_ir"
---

<objective>
Lock the ordinary `geom_polygon()` IR contract before renderer work begins.

Purpose: POLY-01 and D-01 through D-07 require row-order-preserving grouped polygon data with the styling columns the D3 renderer will consume.
Output: focused tests in `tests/testthat/test-polygon-ir.R`, with production changes only if the tests expose a real gap in existing extraction or validation.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-ordinary-geom-polygon-support/44-CONTEXT.md
@.planning/phases/44-ordinary-geom-polygon-support/44-RESEARCH.md
@.planning/phases/44-ordinary-geom-polygon-support/44-VALIDATION.md
@.planning/phases/44-ordinary-geom-polygon-support/44-PATTERNS.md
@AGENTS.md
@R/as_d3_ir.R
@R/validate_ir.R
@tests/testthat/test-ir.R

<interfaces>
Required source contracts from `R/as_d3_ir.R`:
```r
GeomPolygon= "polygon"
keep_aes <- c(
  "PANEL","x","y","xend","yend","xmin","xmax","ymin","ymax",
  "colour","fill","size","alpha","group","label",
  "stroke","shape","linewidth","linetype","lineend",
  ...
)
```

Required source contract from `R/validate_ir.R`:
```r
known_geoms <- c(..., "errorbar", "linerange", "pointrange", "polygon", "sf")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add POLY-01 polygon IR characterization tests</name>
  <files>tests/testthat/test-polygon-ir.R</files>
  <read_first>
    - tests/testthat/test-ir.R
    - tests/testthat/test-sf-renderer.R
    - R/as_d3_ir.R
    - R/validate_ir.R
    - .planning/phases/44-ordinary-geom-polygon-support/44-CONTEXT.md
    - .planning/phases/44-ordinary-geom-polygon-support/44-VALIDATION.md
    - .planning/phases/44-ordinary-geom-polygon-support/44-PATTERNS.md
  </read_first>
  <action>Create `tests/testthat/test-polygon-ir.R`. At the top, load gg2d3 for direct file runs with `if (!isNamespaceLoaded("gg2d3")) pkgload::load_all(quiet = TRUE)`. Add local fixtures for: a single non-monotone polygon; two polygon groups in one layer; a faceted polygon plot using `facet_wrap(~ panel)`; a mapped-styling plot with `fill`, `colour`, `alpha`, `linewidth`, and `linetype`; and an NA styling plot with `fill = NA` and `colour = NA`. The tests must assert `layer$geom == "polygon"`, `validate_ir(ir)` returns `TRUE`, all row lists include `PANEL`, `x`, `y`, `group`, `fill`, `colour`, `alpha`, `linewidth`, and `linetype` when supplied by ggplot2 built data, and row order inside each `group` matches the intentionally non-monotone input order per D-01 and D-04. Do not add subgroup or hole-specific tests because D-03 defers explicit topology semantics.</action>
  <acceptance_criteria>
    - `rtk rg -n "POLY-01|geom_polygon|layer\\$geom|validate_ir|facet_wrap|fill = NA|colour = NA|linewidth|linetype" tests/testthat/test-polygon-ir.R` returns matches.
    - `rtk rg -n "non.?monotone|row order|group|PANEL|alpha" tests/testthat/test-polygon-ir.R` returns matches.
    - `rtk rg -n "subgroup|hole|topology" tests/testthat/test-polygon-ir.R` returns no Phase 44 implementation assertions.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --vanilla -e 'devtools::load_all(); testthat::test_file("tests/testthat/test-polygon-ir.R")'</automated>
  </verify>
  <done>`tests/testthat/test-polygon-ir.R` proves ordinary polygon IR recognition, grouping, row order, facets, and core style-aesthetic preservation.</done>
</task>

<task type="auto">
  <name>Task 2: Repair only real IR or validation gaps exposed by the tests</name>
  <files>R/as_d3_ir.R, R/validate_ir.R, tests/testthat/test-polygon-ir.R</files>
  <read_first>
    - R/as_d3_ir.R
    - R/validate_ir.R
    - tests/testthat/test-polygon-ir.R
    - .planning/phases/44-ordinary-geom-polygon-support/44-RESEARCH.md
    - .planning/phases/44-ordinary-geom-polygon-support/44-PATTERNS.md
  </read_first>
  <action>Run the polygon IR tests. If they fail because `R/as_d3_ir.R` no longer maps `GeomPolygon` to `"polygon"`, restore that switch entry exactly. If they fail because required built-data columns are dropped, add only the missing POLY-01 columns to `keep_aes`: `PANEL`, `x`, `y`, `colour`, `fill`, `alpha`, `group`, `linewidth`, and `linetype`. If they fail because `R/validate_ir.R` does not allow `"polygon"`, add `"polygon"` to `known_geoms`. Do not sort polygon rows, do not add `subgroup`, and do not refactor unrelated geoms.</action>
  <acceptance_criteria>
    - `rtk rg -n "GeomPolygon\\s*=\\s*\"polygon\"|GeomPolygon= \"polygon\"" R/as_d3_ir.R` returns a match.
    - `rtk rg -n "\"PANEL\"|\"x\"|\"y\"|\"colour\"|\"fill\"|\"alpha\"|\"group\"|\"linewidth\"|\"linetype\"" R/as_d3_ir.R` returns matches in the `keep_aes` block.
    - `rtk rg -n "\"polygon\"" R/validate_ir.R` returns a match.
    - `rtk Rscript --vanilla -e 'devtools::load_all(); testthat::test_file("tests/testthat/test-polygon-ir.R")'` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --vanilla -e 'devtools::load_all(); testthat::test_file("tests/testthat/test-polygon-ir.R")'</automated>
  </verify>
  <done>POLY-01 is green without broad extractor refactors or topology scope expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ggplot2 built data -> gg2d3 IR | User-controlled plot data becomes JSON-serializable row payloads. |
| IR rows -> later callback payloads | Fields preserved here can become user-facing tooltip, handler, or brush data in later plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Information Disclosure | `R/as_d3_ir.R` polygon rows | mitigate | Preserve only POLY-01 public row/aesthetic fields and do not introduce renderer-private underscore fields in R-side IR. |
| T-44-02 | Tampering | polygon row ordering | mitigate | Add non-monotone row-order tests and forbid sorting in the IR path per D-04. |
| T-44-03 | Denial of Service | polygon fixtures | accept | Representative test fixtures use small in-memory data frames; large-data performance is out of Phase 44 scope. |
</threat_model>

<verification>
Run:

```bash
rtk Rscript --vanilla -e 'devtools::load_all(); testthat::test_file("tests/testthat/test-polygon-ir.R")'
```
</verification>

<success_criteria>
- POLY-01 appears in this plan frontmatter.
- `tests/testthat/test-polygon-ir.R` exists and passes.
- `GeomPolygon` is recognized as `polygon`.
- Polygon IR preserves group, row order, panel, coordinates, and core style aesthetics.
- No explicit polygon hole, subgroup, topology, screenshot, or sf annotation work is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/44-ordinary-geom-polygon-support/44-01-SUMMARY.md`
</output>
