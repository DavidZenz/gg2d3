---
phase: 54-geometry-polish-closure
plan: "01"
plan_id: "54-01"
status: ready
type: execute
wave: 1
depends_on: []
files_modified:
  - R/ir_layer_helpers.R
  - R/validate_ir.R
  - inst/htmlwidgets/modules/geoms/text.js
  - inst/htmlwidgets/modules/geom-contracts.js
  - inst/htmlwidgets/modules/geom-registry.js
  - tests/testthat/test-text-label-polish.R
  - tests/testthat/test-renderer-wiring-contracts.R
autonomous: true
requirements:
  - GEOM-01
  - GEOM-04
must_haves:
  truths:
    - "Ordinary geom_label() either renders as bounded SVG rect plus text or stops with source-backed diagnostics per D-01 through D-04."
    - "Ordinary text and label preserve small hjust, vjust, angle, and naturally-fitting family support without collision, rich text, path-following, or ggrepel behavior per D-13 through D-16."
    - "Label/text content is inserted with SVG .text(...), never HTML, so label strings cannot execute script per T-54-01."
  artifacts:
    - path: "tests/testthat/test-text-label-polish.R"
      provides: "Failing-first and regression tests for ordinary label boxes and text placement"
      contains: "GEOM-01"
    - path: "R/ir_layer_helpers.R"
      provides: "Ordinary text/label row field preservation and bounded label params"
      contains: "GeomLabel"
    - path: "inst/htmlwidgets/modules/geoms/text.js"
      provides: "Renderer-local ordinary text and label SVG output"
      contains: "geom-label"
    - path: "inst/htmlwidgets/modules/geom-contracts.js"
      provides: "Renderer selector and public payload contract coverage for text and label"
      contains: "label"
  key_links:
    - from: "R/ir_layer_helpers.R"
      to: "inst/htmlwidgets/modules/geoms/text.js"
      via: "layer geom alias and preserved row fields"
      pattern: "GeomLabel.*label|hjust|vjust|angle|family"
    - from: "inst/htmlwidgets/modules/geoms/text.js"
      to: "tests/testthat/test-text-label-polish.R"
      via: "source assertions for g.geom-label, rect.geom-label-box, text.geom-label-text, getBBox, .text(...)"
      pattern: "geom-label|geom-label-box|geom-label-text|getBBox|\\.text\\("
    - from: "inst/htmlwidgets/modules/geom-contracts.js"
      to: "tests/testthat/test-renderer-wiring-contracts.R"
      via: "Phase 53 contract parser"
      pattern: "renderSelectors|interactions|publicPayload"
---

<objective>
Ship the bounded ordinary text/label polish path selected for Phase 54: ordinary `geom_label()` boxes plus small text placement support.

Purpose: Covers GEOM-01 and GEOM-04 while respecting D-03, D-15, and D-16. This plan must not implement rich text, collision avoidance, path-following text, ggrepel behavior, or broad label-placement algorithms.
Output: Failing-first tests, R-side IR preservation, renderer-local SVG label boxes/text placement, and contract coverage.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-geometry-polish-closure/54-CONTEXT.md
@.planning/phases/54-geometry-polish-closure/54-RESEARCH.md
@.planning/phases/54-geometry-polish-closure/54-PATTERNS.md
@.planning/phases/54-geometry-polish-closure/54-VALIDATION.md
@.planning/phases/53-renderer-and-ir-contract-consolidation/53-01-SUMMARY.md
@.planning/phases/53-renderer-and-ir-contract-consolidation/53-02-SUMMARY.md
@R/ir_layer_helpers.R
@R/validate_ir.R
@inst/htmlwidgets/modules/geoms/text.js
@inst/htmlwidgets/modules/geoms/sf.js
@inst/htmlwidgets/modules/geom-contracts.js
@inst/htmlwidgets/modules/geom-registry.js
@tests/testthat/test-text-label-polish.R
@tests/testthat/test-renderer-wiring-contracts.R

<interfaces>
Existing R seams:
```r
gg2d3_ir_layer_keep_aes <- function()
gg2d3_ir_layer_params <- function(layer_obj, gcl)
gg2d3_ir_layer_geom <- function(gcl, uses_sf_coordinates = FALSE)
validate_ir <- function(ir)
```

Existing renderer seams:
```javascript
function renderText(layer, g, xScale, yScale, options)
window.gg2d3.geomRegistry.register('text', renderText);
window.gg2d3.geomRegistry.updateGeoms(container, xScale, yScale, t, options);
window.gg2d3.geomContracts.all()
```

Use the existing `sf.js` label pattern only as an analog: `g + rect + text`, `.text(...)`, `getBBox()`, and padding-driven rect sizing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 label/text characterization tests</name>
  <files>tests/testthat/test-text-label-polish.R, tests/testthat/test-renderer-wiring-contracts.R</files>
  <behavior>
    - A `geom_label()` fixture with `fill`, `colour`, `alpha`, `size`, `linewidth`, `label.padding`, `hjust`, `vjust`, `angle`, and `family` should require either `geom = "label"` with label params or an explicit D-04 diagnostic pivot.
    - Ordinary `geom_text()` and `geom_label()` should preserve simple numeric and character `hjust`/`vjust`, `angle`, and `family` only for small renderer-local placement wins.
    - Renderer source must use SVG `.text(...)` for text and label content and must not use `.html(...)`, `innerHTML`, `foreignObject`, `textPath`, collision code, or ggrepel behavior.
    - If `label` is added as a geom alias, renderer contract tests must expect `g.geom-label`, `rect.geom-label-box`, and `text.geom-label-text` selectors.
  </behavior>
  <action>
    Update `tests/testthat/test-text-label-polish.R` before implementation. Add tests named exactly `GEOM-01 ordinary labels declare bounded box IR and renderer support`, `GEOM-04 ordinary text and label preserve small placement fields`, and `GEOM-04 algorithmic label placement remains an explicit non-goal`. Use existing helpers in that file rather than broad snapshots. Update `tests/testthat/test-renderer-wiring-contracts.R` only if the new tests need a source helper for selector parsing already established in Phase 53. Run the targeted test and confirm it fails for the intended missing behavior before changing production code.
  </action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-text-label-polish.R")'</automated>
    <automated>rtk rg -n "GEOM-01 ordinary labels declare bounded box IR and renderer support|GEOM-04 ordinary text and label preserve small placement fields|GEOM-04 algorithmic label placement remains an explicit non-goal" tests/testthat/test-text-label-polish.R</automated>
  </verify>
  <expected_commit>test(54-01): characterize bounded label boxes and text placement</expected_commit>
  <done>The tests fail only because ordinary labels/text do not yet expose the selected bounded support; no implementation files are changed in this task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve bounded text and label IR fields</name>
  <files>R/ir_layer_helpers.R, R/validate_ir.R, tests/testthat/test-text-label-polish.R</files>
  <behavior>
    - `GeomLabel` maps to `"label"` per planner discretion so label support is visible to validation and renderer contracts.
    - `known_geoms` includes `"label"`.
    - Ordinary text/label rows preserve only supported small placement fields: `hjust`, `vjust`, `angle`, and `family` if present.
    - Label params include numeric padding converted from `layer_obj$geom_params$label.padding` to pixels or points converted to pixels; do not serialize grid unit internals.
    - Do not add `label.r`/rounded-corner parity unless it falls out with no extra renderer contract; basic padding is the required bounded target per D-02.
  </behavior>
  <action>
    Update `gg2d3_ir_layer_keep_aes()` to keep `hjust`, `vjust`, `angle`, and `family`. Update `gg2d3_ir_layer_geom()` so ordinary `GeomLabel` returns `"label"` while sf label routing remains unchanged. Update `gg2d3_ir_layer_params()` so `GeomLabel` returns a small `label` parameter object, including a numeric padding field derived with `grid::convertUnit(..., "pt", valueOnly = TRUE) * 96 / 72` when `label.padding` exists. Update `R/validate_ir.R` to allow `"label"`. Keep the changes in helper boundaries; do not move renderer logic into `as_d3_ir.R`.
  </action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-text-label-polish.R")'</automated>
    <automated>rtk rg -n "hjust|vjust|angle|family|GeomLabel = \"label\"|label.padding|convertUnit|known_geoms.*label|\"label\"" R/ir_layer_helpers.R R/validate_ir.R tests/testthat/test-text-label-polish.R</automated>
  </verify>
  <expected_commit>feat(54-01): preserve bounded ordinary label and placement IR fields</expected_commit>
  <done>IR tests for ordinary label/text fields pass or fail only on renderer expectations remaining for Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render ordinary labels and small placement attributes</name>
  <files>inst/htmlwidgets/modules/geoms/text.js, inst/htmlwidgets/modules/geom-contracts.js, inst/htmlwidgets/modules/geom-registry.js, tests/testthat/test-text-label-polish.R, tests/testthat/test-renderer-wiring-contracts.R</files>
  <behavior>
    - `text.js` registers the same renderer for `text` and `label`, or an equivalent contract-visible label path, without adding a new dependency or new module unless strictly necessary.
    - Text rendering supports simple numeric/character `hjust` and `vjust`, `angle`, and `family`; explicitly leave `inward`, `outward`, collision avoidance, rich text, and path-following text unsupported.
    - Label rendering emits `g.geom-label` containing `rect.geom-label-box` and `text.geom-label-text`; box fill uses `fill`, stroke uses `colour`, opacity uses `alpha`, text size uses `size`, and padding uses the numeric label param from Task 2.
    - Renderer uses `.text(...)` only for user label content and source tests prove `.html(...)`, `innerHTML`, `foreignObject`, and `textPath` are absent from the label/text path.
    - `geom-contracts.js` and `geom-registry.js` remain aligned: render, update, event, brush, crosstalk, and public payload expectations name the actual selectors.
  </behavior>
  <action>
    Implement renderer-local helpers in `text.js` for anchor, baseline, rotation, font family, label padding, and label stroke width. Prefer a branch inside `renderText()` keyed by `layer.geom === "label"` so text and label share placement helpers. For labels, append a group at the scaled x/y point, apply rotation on the group, append text at local origin, then size the background rect from `getBBox()` plus padding. Use existing `makeColorAccessors()` and helpers; do not bypass `window.gg2d3.helpers`. Extend the `updateGeoms()` text branch in `geom-registry.js` so `text.geom-text` and `g.geom-label` move consistently on update, or add a reason-bearing explicit exception in `geom-contracts.js` if a full label update branch would violate D-04. Update contract tests so selector/load-order/public payload checks pass.
  </action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-text-label-polish.R"); testthat::test_file("tests/testthat/test-renderer-wiring-contracts.R")'</automated>
    <automated>rtk rg -n "geom-label|geom-label-box|geom-label-text|getBBox|text-anchor|dominant-baseline|transform|rotate|font-family|\\.text\\(" inst/htmlwidgets/modules/geoms/text.js inst/htmlwidgets/modules/geom-contracts.js inst/htmlwidgets/modules/geom-registry.js tests/testthat/test-text-label-polish.R</automated>
    <automated>! rtk rg -n "\\.html\\(|innerHTML|foreignObject|textPath|ggrepel|collision" inst/htmlwidgets/modules/geoms/text.js</automated>
    <automated>rtk rg -n "collision|ggrepel|path-following|rich text|explicit non-goal" tests/testthat/test-text-label-polish.R</automated>
  </verify>
  <expected_commit>feat(54-01): render bounded ordinary label boxes and text placement</expected_commit>
  <done>Targeted label/text tests and renderer wiring contracts pass with bounded ordinary label boxes and small placement support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ggplot label string -> SVG text node | User-controlled label values cross from R data into browser-rendered SVG. |
| renderer-private fields -> public payload | Any new label group/private fields must not leak to callbacks or tooltips. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering / XSS | `inst/htmlwidgets/modules/geoms/text.js` | mitigate | Render label/text content only with SVG `.text(...)`; source-test absence of `.html(...)`, `innerHTML`, `foreignObject`, `textPath`, collision/path-following code. |
| T-54-02 | Information Disclosure / Integrity | Polygon topology metadata | accept-not-applicable | This plan does not touch polygon `subgroup`, hole metadata, or polygon renderer contracts. Plan 54-02 owns this threat. |
| T-54-03 | Tampering / DoS | Rect/tile transformed geometry DOM attributes | accept-not-applicable | This plan does not touch rect/tile geometry or transformed scale math. Plan 54-03 owns this threat. |
</threat_model>

<stop_rollback>
Stop before shipping if label support requires rich text, collision avoidance, path-following text, ggrepel-style layout, non-SVG HTML insertion, broad registry rewrites, or new dependencies. If Task 2 has emitted `geom = "label"` but Task 3 cannot safely render labels, revert the Task 2 commit before pivoting to D-04 diagnostics; do not leave IR claiming label support without renderer and contract support.
</stop_rollback>

<cross_plan_verification>
Final Phase 54 commands from `54-VALIDATION.md`:

```bash
rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-text-label-polish.R"); testthat::test_file("tests/testthat/test-polygon-ir.R"); testthat::test_file("tests/testthat/test-rect-tile-ir.R")'
rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-polygon-renderer.R"); testthat::test_file("tests/testthat/test-rect-tile-renderer.R"); testthat::test_file("tests/testthat/test-renderer-wiring-contracts.R")'
rtk env NOT_CRAN=true GG2D3_BROWSER_VISUAL_SMOKE=true Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-browser-visual-smoke.R")'
rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); devtools::test()'
```
</cross_plan_verification>

<success_criteria>
- Ordinary `geom_label()` bounded support is visible in IR, renderer selectors, and tests, or the plan stops and rolls back to a D-04 diagnostics pivot.
- Ordinary text and label support small `hjust`, `vjust`, `angle`, and natural `family` without algorithmic placement claims.
- T-54-01 source assertions pass and renderer contracts stay green.
</success_criteria>

<output>
After completion, create `.planning/phases/54-geometry-polish-closure/54-01-SUMMARY.md`.
</output>
