---
phase: 54-geometry-polish-closure
plan: "02"
plan_id: "54-02"
status: ready
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-polygon-ir.R
  - tests/testthat/test-polygon-renderer.R
autonomous: true
requirements:
  - GEOM-02
must_haves:
  truths:
    - "Ordinary geom_polygon() subgroup/hole behavior is fixture-proven and explicitly bounded per D-05 and D-06."
    - "gg2d3 does not imply GIS topology repair, containment inference, winding repair, invalid polygon repair, or speculative subgroup metadata per D-07 and D-08."
    - "Public/source contracts prevent private or unsupported topology metadata drift per T-54-02."
  artifacts:
    - path: "tests/testthat/test-polygon-ir.R"
      provides: "ggplot2 subgroup/rule fixture comparison and ordinary IR boundary"
      contains: "GEOM-02"
    - path: "tests/testthat/test-polygon-renderer.R"
      provides: "Source contract for ordinary polygon non-topology boundary"
      contains: "ordinary polygon renderer does not claim topology repair"
  key_links:
    - from: "tests/testthat/test-polygon-ir.R"
      to: "R/ir_layer_helpers.R"
      via: "IR row field assertions"
      pattern: "subgroup"
    - from: "tests/testthat/test-polygon-renderer.R"
      to: "inst/htmlwidgets/modules/geoms/polygon.js"
      via: "source assertions against fill-rule/topology support claims"
      pattern: "fill-rule|topology|repair"
---

<objective>
Close the ordinary polygon subgroup/hole candidate with fixture-led evidence and an explicit support boundary.

Purpose: Covers GEOM-02 while honoring D-05 through D-08. Research did not establish a tiny low-risk ordinary polygon compound-path subset that avoids implied topology semantics, so this plan strengthens the explicit non-goal contract rather than preserving speculative metadata.
Output: Focused ggplot2 comparison fixtures and source tests proving ordinary polygon topology remains outside Phase 54 support.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-geometry-polish-closure/54-CONTEXT.md
@.planning/phases/54-geometry-polish-closure/54-RESEARCH.md
@.planning/phases/54-geometry-polish-closure/54-PATTERNS.md
@.planning/phases/54-geometry-polish-closure/54-VALIDATION.md
@.planning/milestones/v1.11-phases/44-ordinary-geom-polygon-support/44-CONTEXT.md
@.planning/milestones/v1.11-phases/44-ordinary-geom-polygon-support/44-RESEARCH.md
@.planning/milestones/v1.11-phases/44-ordinary-geom-polygon-support/44-PATTERNS.md
@tests/testthat/test-polygon-ir.R
@tests/testthat/test-polygon-renderer.R
@inst/htmlwidgets/modules/geoms/polygon.js

<interfaces>
Existing polygon contract:
```r
expect_polygon_ir(plot)
expect_polygon_row_fields(rows, fields)
row_has_field(row, field)
row_values(rows, field)
```

Existing renderer boundary:
```javascript
function renderPolygon(layer, g, xScale, yScale, options)
const grouped = d3.group(indexedRows, row => val(get(row.d, "group")) ?? 1);
publicRow._polygonPoints = pts;
g.append("path").attr("class", "geom-polygon")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 subgroup and hole fixture decision</name>
  <files>tests/testthat/test-polygon-ir.R</files>
  <behavior>
    - Fixture data includes one ordinary polygon group with two explicit `subgroup` rings shaped like an outer ring and inner ring.
    - ggplot2 built data is asserted to contain `subgroup`; if `rule` is available from ggplot2 build data or layer params, assert where it lives.
    - gg2d3 ordinary polygon IR remains limited to grouped closed paths and does not preserve `subgroup` unless the same plan also implements and tests compound-path rendering.
    - Repeated vertices, reversed row order, self-crossing, and too-small polygon fixtures remain classified as ordinary input or invalid geometry, not repaired topology.
  </behavior>
  <action>
    Extend `tests/testthat/test-polygon-ir.R` with a fixture named exactly `GEOM-02 polygon subgroup hole fixtures define topology boundary`. Use local data, `ggplot2::ggplot_build(plot)$data[[1]]`, and existing IR helper assertions. The expected Phase 54 outcome is explicit non-goal: built data may contain `subgroup`, but `layer$data` must not expose `subgroup` because no bounded renderer topology support is shipped. Do not edit `R/ir_layer_helpers.R` in this plan.
  </action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-polygon-ir.R")'</automated>
    <automated>rtk rg -n "GEOM-02 polygon subgroup hole fixtures define topology boundary|subgroup|rule|hole|topology boundary" tests/testthat/test-polygon-ir.R</automated>
  </verify>
  <expected_commit>test(54-02): fixture polygon subgroup hole boundary</expected_commit>
  <done>ggplot2 subgroup/hole-style built data is fixture-proven, and ordinary gg2d3 IR does not imply unsupported topology support.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strengthen polygon renderer non-goal source contract</name>
  <files>tests/testthat/test-polygon-renderer.R</files>
  <behavior>
    - Source tests continue to prove ordinary polygon renderer groups rows by `group` and emits one closed path per group.
    - Source tests explicitly reject `subgroup`, `fill-rule`, `evenodd`, `nonzero`, topology repair, containment inference, winding repair, invalid polygon repair, and self-intersection repair in the ordinary polygon renderer.
    - Tests explain that sf `fill-rule` support is not ordinary polygon subgroup support.
  </behavior>
  <action>
    Extend `tests/testthat/test-polygon-renderer.R` with a test named exactly `GEOM-02 ordinary polygon topology remains an explicit non-goal`. Keep it as a source contract against `inst/htmlwidgets/modules/geoms/polygon.js`. Do not modify `polygon.js`; the intended outcome is stronger evidence and support signaling, not renderer topology implementation.
  </action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-polygon-ir.R"); testthat::test_file("tests/testthat/test-polygon-renderer.R")'</automated>
    <automated>rtk rg -n "GEOM-02 ordinary polygon topology remains an explicit non-goal|subgroup|fill-rule|evenodd|nonzero|containment|winding|invalid polygon|self-intersection" tests/testthat/test-polygon-renderer.R</automated>
  </verify>
  <expected_commit>test(54-02): lock ordinary polygon topology non-goal</expected_commit>
  <done>Polygon source tests prove the subgroup/hole topology boundary without speculative metadata preservation or renderer topology repair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ggplot2 polygon built data -> gg2d3 IR rows | Built `subgroup` and topology-adjacent data could imply support if copied into IR without rendering semantics. |
| renderer-private polygon data -> public payload | Private `_polygonPoints` must remain renderer-internal and not become user-facing topology metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering / XSS | Label/text renderer | accept-not-applicable | This plan does not render label/text values or touch HTML insertion paths. Plan 54-01 owns this threat. |
| T-54-02 | Information Disclosure / Integrity | `test-polygon-ir.R`, `test-polygon-renderer.R`, polygon IR boundary | mitigate | Fixture-test built `subgroup`/hole data while asserting ordinary gg2d3 IR and renderer do not claim topology support or preserve speculative subgroup metadata. |
| T-54-03 | Tampering / DoS | Rect/tile transformed geometry DOM attributes | accept-not-applicable | This plan does not touch rect/tile geometry. Plan 54-03 owns this threat. |
</threat_model>

<stop_rollback>
Stop if implementing subgroup support would require GIS topology repair, containment inference, arbitrary winding logic, invalid polygon repair, self-intersection handling, preserving subgroup metadata without rendering semantics, or changes outside the polygon fixture/rendering boundary. If any polygon production source edit is made accidentally, revert it before completing this non-goal evidence plan.
</stop_rollback>

<cross_plan_verification>
Final Phase 54 commands from `54-VALIDATION.md`:

```bash
rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-text-label-polish.R"); testthat::test_file("tests/testthat/test-polygon-ir.R"); testthat::test_file("tests/testthat/test-rect-tile-ir.R")'
rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-polygon-renderer.R"); testthat::test_file("tests/testthat/test-rect-tile-renderer.R"); testthat::test_file("tests/testthat/test-renderer-wiring-contracts.R")'
rtk env NOT_CRAN=true GG2D3_BROWSER_VISUAL_SMOKE=true Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-browser-visual-smoke.R")'
rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); devtools::test()'
```
</cross_plan_verification>

<success_criteria>
- GEOM-02 is covered by fixture-led evidence and explicit non-goal tests.
- No speculative `subgroup` IR metadata or ordinary polygon topology implementation is introduced.
- T-54-02 is mitigated by tests that distinguish built ggplot2 topology fields from gg2d3 support claims.
</success_criteria>

<output>
After completion, create `.planning/phases/54-geometry-polish-closure/54-02-SUMMARY.md`.
</output>
