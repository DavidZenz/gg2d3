---
phase: 22-polar-coords
plan: 01
type: execute
wave: 1
depends_on: ["21-01"]
files_modified:
  - R/as_d3_ir.R
  - inst/htmlwidgets/modules/scales.js
  - inst/htmlwidgets/modules/geom-registry.js
  - inst/htmlwidgets/gg2d3.js
  - tests/testthat/test-ir.R
autonomous: true

must_haves:
  truths:
    - "User sees `coord_polar` correctly mapped to D3 radial scales"
    - "User sees radial axes with correct labeling"
  artifacts:
    - path: "tests/testthat/test-polar-coords.R"
      provides: "Regression tests for polar IR structure"
      contains: "coord_polar"
---

<objective>
Implement core support for `coord_polar` to enable radial visualizations like pie and coxcomb charts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add polar coord detection in R</name>
  <files>R/as_d3_ir.R</files>
  <action>Update `as_d3_ir.R` to detect `CoordPolar`. Extract `theta`, `start`, and `direction` parameters and include them in the `ir$coord` object.</action>
  <verify>Rscript -e "devtools::load_all(); p <- ggplot(mtcars, aes(x=factor(1), fill=factor(cyl))) + geom_bar() + coord_polar(theta='y'); ir <- as_d3_ir(p); stopifnot(ir$coord$type == 'polar', ir$coord$theta == 'y')"</verify>
  <done>IR contains accurate polar coordinate metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Implement radial and angular scales in JS</name>
  <files>inst/htmlwidgets/modules/scales.js</files>
  <action>Add `createRadialScale` and `createAngularScale` helpers to `scales.js`. Provide a utility to convert polar (r, theta) to Cartesian (x, y).</action>
  <verify>Check JS console for correct coordinate conversion logic.</verify>
  <done>JS scale module supports polar-to-Cartesian transformations.</done>
</task>

<task type="auto">
  <name>Task 3: Implement polar axis and grid rendering</name>
  <files>inst/htmlwidgets/gg2d3.js</files>
  <action>Add `renderPolarAxes` helper to `gg2d3.js`. Draw circular grid lines for radial breaks and radiating lines for angular breaks. Render labels around the circumference and along the radius.</action>
  <verify>Visual check of a polar plot with visible grid and labels.</verify>
  <done>Polar plots show correct radial axes and grids.</done>
</task>

<task type="auto">
  <name>Task 4: Implement arc-based renderer for geom_bar</name>
  <files>inst/htmlwidgets/modules/geoms/bar.js</files>
  <action>Update `renderBar` (or add a specialized renderer) to use `d3.arc()` when `ir.coord.type === 'polar'`. This enables pie and coxcomb charts.</action>
  <verify>Visual check of a pie chart created with geom_bar + coord_polar.</verify>
  <done>Bar charts correctly transform to arcs in polar coordinates.</done>
</task>

<task type="auto">
  <name>Task 5: Add regression tests for polar IR</name>
  <files>tests/testthat/test-polar-coords.R</files>
  <action>Create a new test file `test-polar-coords.R` to validate the IR structure for various `coord_polar` configurations.</action>
  <verify>Rscript -e "devtools::test(filter = 'polar-coords')"</verify>
  <done>Polar coordinate support is test-backed.</done>
</task>

</tasks>

<success_criteria>
- `coord_polar(theta = "y")` produces a pie chart if used with `geom_bar(width = 1)`.
- `coord_polar(theta = "x")` produces a coxcomb plot if used with `geom_bar()`.
- Radial and angular grid lines match ggplot2 positions.
</success_criteria>
