---
phase: 30-edge-cases-and-blueprint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md
autonomous: true
requirements: [BLPR-01, BLPR-02, BLPR-03]
tags: [sf, geom_sf, blueprint, edge-cases, anti-features, impl-plan, research, documentation]

must_haves:
  truths:
    - "A reader can find, in a single doc, specific empirical findings for all three edge cases — mixed geometry types in a single layer, multiple stacked geom_sf layers, and faceted sf maps — including what the current pipeline produces in each case and what handling the build phase must add"
    - "A reader can find an anti-features list with exactly three explicitly deferred capabilities — tile basemaps, JS-side reprojection, slippy zoom — each accompanied by a one-sentence rationale that distinguishes them from merely deferred ideas"
    - "A reader (or `/gsd-plan-phase` for the v1.8+ build milestone) can find a phase-by-phase implementation plan that names specific files to create or modify, describes the concrete changes at file/line granularity (matching the depth of 29-01-SF-INTERACTIVITY-DESIGN.md), and is sequenced into discrete future phases/plans aligned with gg2d3 conventions"
  artifacts:
    - path: ".planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md"
      provides: "Combined BLPR-01/02/03 blueprint covering edge cases, anti-features, and phase-by-phase implementation plan for the v1.8+ build milestone"
      contains: "## Edge Case 1: Mixed Geometry Types, ## Edge Case 2: Multi-Layer SF Stacking, ## Edge Case 3: Faceted SF Maps, ## Anti-Features, ## Implementation Plan"
  key_links:
    - from: ".planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md § Edge Case 3 (Faceted SF)"
      to: ".planning/phases/27-r-ir-extraction-feasibility/27-02-SUMMARY.md (per-panel coord.bbox via geoIdentity().reflectY(true).fitExtent())"
      via: "explicit citation of D-03 (per-panel bbox) and D-04 (flagged unknowns) from 30-CONTEXT.md"
      pattern: "per-panel coord.bbox|d3\\.geoIdentity\\(\\).*reflectY\\(true\\).*fitExtent"
    - from: ".planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md § Implementation Plan"
      to: "inst/htmlwidgets/modules/brush.js, sf.js, zoom.js, events.js, tooltip.js, hover.js + R/as_d3_ir.R"
      via: "file/line-anchored change callouts matching 29-01-SF-INTERACTIVITY-DESIGN.md depth"
      pattern: "brush\\.js.*line|sf\\.js.*line|as_d3_ir\\.R.*line"
---

<objective>
Produce `30-01-BLUEPRINT.md` — the single combined deliverable for Phase 30 of the v1.7 Choropleth Map Research milestone. The blueprint locks edge-case findings (BLPR-01), anti-features (BLPR-02), and a file/line-specific phase-by-phase implementation plan (BLPR-03) detailed enough that `/gsd-plan-phase` for the v1.8+ build milestone (IMPL-04+) executes without performing new research.

Purpose: Hand a complete, decision-locked research artifact to a future build milestone. The blueprint replaces "what do we build for geom_sf?" with "execute these phases, in this order, touching these files at these lines."

Output: One markdown file at `.planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md`. No production R/JS code is modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-edge-cases-and-blueprint/30-CONTEXT.md
@.planning/phases/29-interactivity-design/29-01-SF-INTERACTIVITY-DESIGN.md
@.planning/phases/27-r-ir-extraction-feasibility/27-02-SUMMARY.md
@.planning/phases/27-r-ir-extraction-feasibility/IR-SCHEMA-SF.md
@.planning/phases/28-d3-renderer-prototyping/28-01-SUMMARY.md
@.planning/phases/28-d3-renderer-prototyping/28-02-SUMMARY.md
@.planning/phases/29-interactivity-design/29-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs to reference verbatim from upstream phases. -->
<!-- Executor should cite these directly in the blueprint — no codebase re-exploration. -->

From Phase 27 (`R/as_d3_ir.R` sf branch, per 27-02-SUMMARY.md):
- sf layer dispatch normalizes geometry to WGS84 before calling `get_layer_crs()`
- Adds `df[["row_id"]] <- seq_along(sf_geom_strings)` (Phase 28 follow-up)
- sf panels use `NULL` x_range/y_range; `coord.bbox` carries geographic extent
- IR fields per sf layer: `geometries[]` (GeoJSON strings), `geom_type`, `crs.epsg`
- IR fields per sf coord: `coord.type == "sf"`, `coord.bbox`

From Phase 28 (`inst/htmlwidgets/modules/geoms/sf.js`, per 28-01-SUMMARY.md):
- Registered as `'sf'` via `window.gg2d3.geomRegistry.register('sf', renderSf)`
- Uses `d3.geoIdentity().reflectY(true).fitExtent([[4,4],[w-4,h-4]], fc)` (4px padding)
- Applies `fill-rule="evenodd"` per path
- Emits `data-cx`/`data-cy` centroid attributes (provisional; Phase 29 D-02 renames to `data-centroid="x,y"`)
- Emits `data-row-id` attribute
- xScale/yScale params received but ignored — geoIdentity handles projection
- Centroids pre-computed in single pass via `d3.geoPath().centroid(feature)`

From Phase 29 (`29-01-SF-INTERACTIVITY-DESIGN.md`):
- D-01: `default_label_col` IR field on sf layer descriptor — `setdiff(names(sf_df), attr(sf_df, "sf_column"))[1]`
- D-02: `data-centroid="x,y"` (single attribute) replaces `data-cx`/`data-cy`; emit only when both finite
- D-03: Add `'path.geom-sf'` to `INTERACTIVE_SELECTORS` in `events.js` lines 23-43
- D-05: Add `'path.geom-sf'` to `INTERACTIVE_SELECTORS` in `brush.js` lines 29-49; new branch in `isElementInPixelRect()` after line 268
- D-08: Wrap sf paths in `<g class="sf-zoom-layer">` inside panel group
- D-09: `vector-effect="non-scaling-stroke"` as SVG presentation attribute (NOT CSS) on each path
- D-11: New pre-branch in `zoom.js` `zoomed()` near line 126 selecting `.sf-zoom-layer` and applying `transform.toString()`

From Phase 30 CONTEXT (`30-CONTEXT.md`):
- D-01: Empirical prototype script per edge case in `test_output/` (scripts NOT committed)
- D-02: Findings synthesized into `30-01-BLUEPRINT.md`; scripts gitignored
- D-03: Per-panel `coord.bbox` for faceted sf with `d3.geoIdentity().reflectY(true).fitExtent()` per panel — LOCKED
- D-04: Faceted-sf unknowns (panel strips, axis suppression, `coord_sf(xlim=,ylim=)` per panel, `facet_wrap(scales=)` mirroring) flagged as build-phase verification items, NOT resolved here
- D-05: Anti-features = exactly 3 items (tile basemaps, JS-side reprojection, slippy zoom); each one-sentence rationale
- D-06: Centroid-fallback brush, semantic zoom, GeomPolygon resurrection, multi-CRS-per-layer are NOT anti-features (live in CONTEXT.md `<deferred>`)
- D-07: Impl plan = phase + plan + file + concrete change with file/line specificity matching 29-01
- D-08: Single deliverable `30-01-BLUEPRINT.md`; single plan `30-01-PLAN.md`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prototype edge case 1 (mixed geometry types in a single sf layer)</name>
  <files>test_output/30-edge-mixed-geometry.R (NOT committed), test_output/30-edge-mixed-ir.json (NOT committed)</files>
  <read_first>
    - .planning/phases/30-edge-cases-and-blueprint/30-CONTEXT.md (D-01, D-02)
    - .planning/phases/27-r-ir-extraction-feasibility/IR-SCHEMA-SF.md (sf IR shape)
    - R/as_d3_ir.R (sf branch — to confirm what dispatch does today)
    - .claude/skills/ (if any SKILL.md exists)
    - CLAUDE.md (devtools::load_all() or pkgload::load_all() guidance)
  </read_first>
  <action>
    Create `test_output/30-edge-mixed-geometry.R` (do NOT `git add` it — `test_output/` is gitignored per CLAUDE.md memory). The script must:

    1. `pkgload::load_all(".")` (fallback to `devtools::load_all(".")` if pkgload missing).
    2. Build an `sf` data frame combining heterogeneous geometry types in a SINGLE layer. Two concrete fixture options (pick the one whose deps are present; if both available, prefer A):
       (A) POLYGON + POINT: Use `sf::st_sf(name=c("a","b"), geometry=sf::st_sfc(sf::st_polygon(list(rbind(c(0,0),c(1,0),c(1,1),c(0,1),c(0,0)))), sf::st_point(c(0.5,0.5)), crs=4326))`.
       (B) POLYGON + LINESTRING: Same shape, replace the POINT with `sf::st_linestring(rbind(c(0,0.5),c(1,0.5)))`.
    3. Construct `p <- ggplot2::ggplot(sf_df) + ggplot2::geom_sf()`.
    4. Wrap `ir <- as_d3_ir(p)` in `tryCatch(..., error=function(e) e)` to capture errors verbatim if the pipeline rejects the mixed input.
    5. Write `jsonlite::write_json(ir, "test_output/30-edge-mixed-ir.json", auto_unbox=TRUE, pretty=TRUE)` on success; on error, write the captured message to `test_output/30-edge-mixed-error.txt`.
    6. Print (via `message()`) a structured findings block to stdout: which geom_type `detect_dominant_geom_type()` returned, length of `ir$layers[[1]]$geometries`, value of `ir$layers[[1]]$geom_type`, and whether the non-dominant geometry is preserved, lost, or causes a crash.

    Run via `Rscript test_output/30-edge-mixed-geometry.R` and capture the stdout/stderr for use in Task 4. If the `sf` package is not installed, install via `install.packages("sf")` first — do NOT silently skip; the empirical finding is required for D-01 evidence.
  </action>
  <verify>
    <automated>test -f test_output/30-edge-mixed-geometry.R && Rscript test_output/30-edge-mixed-geometry.R 2>&1 | tee test_output/30-edge-mixed-run.log && (test -f test_output/30-edge-mixed-ir.json || test -f test_output/30-edge-mixed-error.txt)</automated>
  </verify>
  <acceptance_criteria>
    - `test_output/30-edge-mixed-geometry.R` exists (NOT staged in git)
    - `test_output/30-edge-mixed-run.log` exists with structured findings block visible (grep `geom_type` returns non-empty)
    - Either `test_output/30-edge-mixed-ir.json` OR `test_output/30-edge-mixed-error.txt` exists (one outcome path is evidence)
    - `git status --porcelain test_output/` shows files are gitignored (no entries for `test_output/*`)
    - `git status --porcelain .planning/phases/30-edge-cases-and-blueprint/` shows no NEW files yet from this task
  </acceptance_criteria>
  <done>
    Empirical evidence for Edge Case 1 captured. Findings (geom_type returned, geometries length, error or success path, observed pipeline behavior under mixed input) recorded in `test_output/30-edge-mixed-run.log` ready to be synthesized into the blueprint in Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prototype edge case 2 (multi-layer sf stacking)</name>
  <files>test_output/30-edge-multi-layer.R (NOT committed), test_output/30-edge-multi-ir.json (NOT committed)</files>
  <read_first>
    - .planning/phases/30-edge-cases-and-blueprint/30-CONTEXT.md (D-01, D-02; <specifics> for fixture suggestion)
    - .planning/phases/27-r-ir-extraction-feasibility/IR-SCHEMA-SF.md
    - .planning/phases/28-d3-renderer-prototyping/28-01-SUMMARY.md (single-layer renderer baseline)
    - R/as_d3_ir.R (sf branch — to confirm per-layer dispatch behavior)
  </read_first>
  <action>
    Create `test_output/30-edge-multi-layer.R` (NOT committed). The script must:

    1. `pkgload::load_all(".")` (fallback `devtools::load_all(".")`).
    2. Build two sf data frames sharing CRS — concrete fixture: NC counties (POLYGON fill base) + a derived state-boundary overlay (POLYGON stroke-only) created via `sf::st_union(nc)` cast back to `sf::st_sf()`. If `nc` is unavailable, fall back to two manually constructed POLYGON sf data frames with distinct attribute columns.
    3. Construct `p <- ggplot2::ggplot() + ggplot2::geom_sf(data=base_sf, aes(fill=BIR74)) + ggplot2::geom_sf(data=overlay_sf, fill=NA, color="black", size=1)`.
    4. `ir <- tryCatch(as_d3_ir(p), error=function(e) e)`.
    5. Write IR to `test_output/30-edge-multi-ir.json` (or error to `test_output/30-edge-multi-error.txt`).
    6. Print structured findings: `length(ir$layers)`, per-layer `geom_type`, per-layer `geometries` length, value of `ir$coord$bbox` (is it union of both layers or first layer only?), whether `crs.epsg` differs between layers, whether validate_ir produces warnings.

    Run via `Rscript test_output/30-edge-multi-layer.R` and capture stdout/stderr.
  </action>
  <verify>
    <automated>test -f test_output/30-edge-multi-layer.R && Rscript test_output/30-edge-multi-layer.R 2>&1 | tee test_output/30-edge-multi-run.log && (test -f test_output/30-edge-multi-ir.json || test -f test_output/30-edge-multi-error.txt)</automated>
  </verify>
  <acceptance_criteria>
    - `test_output/30-edge-multi-layer.R` exists (NOT staged in git)
    - `test_output/30-edge-multi-run.log` exists; grep `length\(ir\$layers\)` OR `n_layers` returns a hit
    - Either `30-edge-multi-ir.json` OR `30-edge-multi-error.txt` exists
    - `git status --porcelain test_output/` is empty (gitignored)
  </acceptance_criteria>
  <done>
    Empirical evidence for Edge Case 2 captured. Findings (layer count, per-layer geom_type, coord.bbox derivation, CRS consistency, sf.js renderer behavior under two sf layers in one plot) ready for Task 4 synthesis.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prototype edge case 3 (faceted sf maps)</name>
  <files>test_output/30-edge-faceted.R (NOT committed), test_output/30-edge-faceted-ir.json (NOT committed)</files>
  <read_first>
    - .planning/phases/30-edge-cases-and-blueprint/30-CONTEXT.md (D-01, D-03, D-04 — per-panel bbox decision and flagged unknowns)
    - .planning/phases/27-r-ir-extraction-feasibility/IR-SCHEMA-SF.md (sf coord.bbox shape; sf panels use NULL x_range/y_range)
    - R/as_d3_ir.R (facets panel iteration in sf branch — confirm current behavior under facets)
  </read_first>
  <action>
    Create `test_output/30-edge-faceted.R` (NOT committed). The script must:

    1. `pkgload::load_all(".")` (fallback `devtools::load_all(".")`).
    2. Load NC counties (`nc <- sf::st_read(system.file("shape/nc.shp", package="sf"), quiet=TRUE)`). Add a synthetic categorical region column (e.g., `nc$region <- ifelse(seq_len(nrow(nc)) %% 3 == 0, "A", ifelse(seq_len(nrow(nc)) %% 3 == 1, "B", "C"))`).
    3. Construct `p <- ggplot2::ggplot(nc, aes(fill=BIR74)) + ggplot2::geom_sf() + ggplot2::facet_wrap(~region)`.
    4. `ir <- tryCatch(as_d3_ir(p), error=function(e) e)`.
    5. Write IR (or error) and produce structured findings:
       - `length(ir$panels)`
       - For each panel: `panel$x_range` / `panel$y_range` (expected NULL per Phase 27 D-02)
       - Whether `ir$coord$bbox` is global (single bbox shared across panels) or per-panel
       - Whether `ir$panels[[i]]$bbox` exists (the per-panel field needed by D-03)
       - Whether facet strip metadata is present (strip labels)
       - Whether `facet_wrap(scales="free")` would have an effect (test variant if time permits)
    6. Also run the same script with `facet_wrap(~region, scales="free")` and capture differences in a SECOND log line.

    Run via `Rscript test_output/30-edge-faceted.R` and capture stdout/stderr.
  </action>
  <verify>
    <automated>test -f test_output/30-edge-faceted.R && Rscript test_output/30-edge-faceted.R 2>&1 | tee test_output/30-edge-faceted-run.log && (test -f test_output/30-edge-faceted-ir.json || test -f test_output/30-edge-faceted-error.txt)</automated>
  </verify>
  <acceptance_criteria>
    - `test_output/30-edge-faceted.R` exists (NOT staged in git)
    - `test_output/30-edge-faceted-run.log` exists; grep `panels` returns a hit
    - Either `30-edge-faceted-ir.json` OR `30-edge-faceted-error.txt` exists
    - Findings explicitly note whether per-panel `bbox` exists today or must be added by the build phase (this directly informs D-03's blueprint section)
    - `git status --porcelain test_output/` is empty (gitignored)
  </acceptance_criteria>
  <done>
    Empirical evidence for Edge Case 3 captured. Findings (panel count, per-panel range structure, current bbox derivation behavior, facet engine compatibility with sf branch) ready for Task 4 synthesis under D-03 (per-panel bbox locked) and D-04 (flagged unknowns enumerated).
  </done>
</task>

<task type="auto">
  <name>Task 4: Write 30-01-BLUEPRINT.md — Edge Cases section (BLPR-01)</name>
  <files>.planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md</files>
  <read_first>
    - test_output/30-edge-mixed-run.log (Task 1 output)
    - test_output/30-edge-multi-run.log (Task 2 output)
    - test_output/30-edge-faceted-run.log (Task 3 output)
    - .planning/phases/30-edge-cases-and-blueprint/30-CONTEXT.md (D-01, D-03, D-04)
    - .planning/phases/29-interactivity-design/29-01-SF-INTERACTIVITY-DESIGN.md (template for depth, section structure, file/line callout style)
    - .planning/phases/27-r-ir-extraction-feasibility/IR-SCHEMA-SF.md
  </read_first>
  <action>
    Create `.planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md`. Begin with YAML frontmatter modeled on `29-01-SF-INTERACTIVITY-DESIGN.md`:

    ```yaml
    ---
    phase: 30-edge-cases-and-blueprint
    plan: 01
    deliverable: SF Edge Cases and Build-Phase Blueprint (combined BLPR-01/02/03)
    status: blueprint-locked
    created: 2026-05-18
    implements: [BLPR-01, BLPR-02, BLPR-03]
    build_phase_target: IMPL-04+ (v1.8+ milestone)
    ---
    ```

    Then write the document opening (Overview section, 2-4 paragraphs) explaining: single combined doc per D-08, doc-only artifact, build lands in v1.8+. Mirror the tone of 29-01.

    Then write the BLPR-01 sections — three subsections under a top-level `## Edge Cases (BLPR-01)`:

    ### `## Edge Case 1: Mixed Geometry Types in a Single Layer`
    - State the case (POLYGON + POINT/LINESTRING in one sf data frame, one geom_sf layer)
    - Cite empirical finding from `test_output/30-edge-mixed-run.log` — what `detect_dominant_geom_type()` returns, what gets emitted in IR, whether non-dominant features are preserved or dropped
    - State recommended build-phase handling: either (a) preserve all geometries with per-feature geom_type, OR (b) document "dominant geom_type only" as a known limitation and warn at extraction. Pick ONE based on the evidence and state rationale.

    ### `## Edge Case 2: Multi-Layer SF Stacking`
    - State the case (two `geom_sf()` calls in the same plot, e.g., counties fill + state-boundary overlay)
    - Cite empirical finding from `test_output/30-edge-multi-run.log` — layer count, per-layer geom_type, how `coord.bbox` is derived (union of layers? first layer only?), CRS consistency requirement
    - State recommended build-phase handling: render order matches `ir$layers` order (back-to-front); `coord.bbox` must be union of all sf layers' bboxes (not first only); enforce CRS consistency across layers (per-layer st_transform already does this since each layer is normalized to WGS84).

    ### `## Edge Case 3: Faceted SF Maps`
    - State the case (`geom_sf() + facet_wrap(~category)`)
    - Cite empirical finding from `test_output/30-edge-faceted-run.log` — panel count, per-panel range structure, whether per-panel bbox exists today
    - State the LOCKED decision from D-03: per-panel `coord.bbox` with `d3.geoIdentity().reflectY(true).fitExtent()` invoked PER PANEL (one projection per panel, not one global projection); free-scale facets are the default behavior
    - Enumerate the D-04 flagged unknowns as a bulleted "Build-phase verification items" subsection — exactly four items:
      1. Panel strip rendering interaction with sf panels (do strips render normally on a map?)
      2. Axis suppression — sf panels typically suppress axes; does the facets engine honor that per-panel?
      3. Interaction with `coord_sf(xlim=, ylim=)` per panel
      4. Free-vs-fixed scales — currently per-panel is "free-like"; mirroring `facet_wrap(scales=)` is a follow-up

    Use literal headings (`## Edge Case 1: Mixed Geometry Types`, `## Edge Case 2: Multi-Layer SF Stacking`, `## Edge Case 3: Faceted SF Maps`) so the acceptance check is deterministic.

    Write the file complete with frontmatter + Overview + the three Edge Case sections. Anti-Features and Implementation Plan sections are added in Tasks 5 and 6 — leave clear placeholder headings `## Anti-Features (BLPR-02)` and `## Implementation Plan (BLPR-03)` at the bottom with `<!-- Task 5 fills this -->` / `<!-- Task 6 fills this -->` comments so the file is structurally complete before sequential edits.
  </action>
  <verify>
    <automated>test -f .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -c "^## Edge Case [123]:" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md | grep -qx 3 && grep -q "per-panel" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -q "d3.geoIdentity().reflectY(true).fitExtent" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -q "implements: \[BLPR-01, BLPR-02, BLPR-03\]" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md` exists
    - Frontmatter `implements: [BLPR-01, BLPR-02, BLPR-03]` present (grep)
    - Exactly THREE `## Edge Case N:` headings (grep `^## Edge Case [123]:` count == 3)
    - The string `per-panel` appears (D-03 lock evidence)
    - The string `d3.geoIdentity().reflectY(true).fitExtent` appears (D-03 lock evidence)
    - Each Edge Case section cites its empirical run log (grep `30-edge-mixed-run.log`, `30-edge-multi-run.log`, `30-edge-faceted-run.log` each return at least one hit)
    - Edge Case 3 contains a "Build-phase verification items" or equivalent bulleted list with the 4 D-04 unknowns
    - Placeholder headings `## Anti-Features (BLPR-02)` and `## Implementation Plan (BLPR-03)` present (Tasks 5/6 fill them)
  </acceptance_criteria>
  <done>
    Blueprint scaffold + BLPR-01 Edge Cases section complete. All three edge cases documented with empirical evidence references and concrete build-phase handling recommendations. Tasks 5 and 6 fill the remaining two sections.
  </done>
</task>

<task type="auto">
  <name>Task 5: Add Anti-Features section to 30-01-BLUEPRINT.md (BLPR-02)</name>
  <files>.planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md</files>
  <read_first>
    - .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md (current state from Task 4)
    - .planning/phases/30-edge-cases-and-blueprint/30-CONTEXT.md (D-05 — exactly 3 items; D-06 — what is NOT an anti-feature)
    - .planning/REQUIREMENTS.md (Out of Scope table for additional context)
  </read_first>
  <action>
    Replace the `## Anti-Features (BLPR-02)` placeholder section in `30-01-BLUEPRINT.md` with a complete section containing EXACTLY three subsections (one per anti-feature per D-05). Use this exact structure:

    Lead paragraph (2-3 sentences): Explain the distinction between anti-features (explicitly out of scope forever) and deferred ideas (not now, possibly later), and explicitly cite that the four items from CONTEXT.md `<deferred>` (centroid-fallback brush, semantic zoom, GeomPolygon resurrection, multi-CRS-per-layer) are NOT in this list per D-06.

    Then three subsections with literal headings:

    ### `### Anti-Feature 1: Tile Basemaps`
    One-sentence rationale (D-05 item 1): "Slippy-tile basemaps (OSM, Mapbox, etc.) rendered under sf layers are out of scope because gg2d3 renders ggplot output, and basemap composition is a separate authoring concern that belongs in a tool like Leaflet."

    ### `### Anti-Feature 2: JS-Side Reprojection`
    One-sentence rationale (D-05 item 2): "Any projection logic in D3 stays out of scope because Phase 27 locked R-side `sf::st_transform` to WGS84 before serialization, preserving the single-source-of-truth contract that gg2d3 renders exactly what ggplot2 computes."

    ### `### Anti-Feature 3: Slippy Zoom / Leaflet-Style Pan`
    One-sentence rationale (D-05 item 3): "Tile-loading slippy zoom is out of scope, distinct from the Phase 29 SVG-group-transform zoom, to keep gg2d3's zoom uniform across geom types and avoid forking the zoom architecture."

    Each subsection MUST be exactly the heading line plus one rationale sentence (per CONTEXT.md `<specifics>`: "short and quotable... `X is out of scope because Y` in one sentence each"). Do not add additional paragraphs, sub-bullets, or expansion — terseness is the deliverable.

    Use the Edit tool to replace the `## Anti-Features (BLPR-02)` placeholder block (including the `<!-- Task 5 fills this -->` comment) with the new content.
  </action>
  <verify>
    <automated>grep -c "^### Anti-Feature [123]:" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md | grep -qx 3 && grep -q "Tile Basemaps" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -q "JS-Side Reprojection" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -q "Slippy Zoom" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && ! grep -q "Task 5 fills this" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -q "deferred" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly THREE `### Anti-Feature N:` headings (count == 3, enforcing D-05 hard cap)
    - Each of the three charter-named items appears verbatim as a heading: `Tile Basemaps`, `JS-Side Reprojection`, `Slippy Zoom`
    - `<!-- Task 5 fills this -->` placeholder REMOVED
    - Lead paragraph references `deferred` distinction (D-06 protection — distinguishes anti-features from deferred-ideas)
    - Each Anti-Feature subsection body is one sentence (rationale) — no nested bullets, no multi-paragraph expansion
    - `grep -c "^### Anti-Feature " == 3` ensures no 4th item slipped in (would violate D-05's "exactly three")
  </acceptance_criteria>
  <done>
    BLPR-02 section complete with exactly the three charter-named anti-features per D-05, each with a one-sentence rationale, and an explicit anti-features-vs-deferred-ideas distinction per D-06.
  </done>
</task>

<task type="auto">
  <name>Task 6: Add Implementation Plan section to 30-01-BLUEPRINT.md (BLPR-03) at file/line specificity</name>
  <files>.planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md</files>
  <read_first>
    - .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md (current state from Tasks 4-5)
    - .planning/phases/30-edge-cases-and-blueprint/30-CONTEXT.md (D-07 — file/line granularity, D-08 — single doc)
    - .planning/phases/29-interactivity-design/29-01-SF-INTERACTIVITY-DESIGN.md (depth/specificity template; the Build-Phase Implementation Reference table at the end is the model)
    - .planning/REQUIREMENTS.md (IMPL-01..IMPL-05 naming conventions for build milestone)
    - inst/htmlwidgets/modules/brush.js (confirm INTERACTIVE_SELECTORS line numbers stated in 29-01 still match; if changed, update citations)
    - inst/htmlwidgets/modules/events.js (same — confirm line range)
    - inst/htmlwidgets/modules/zoom.js (same — confirm `zoomed()` location)
    - inst/htmlwidgets/modules/geoms/sf.js (confirm centroid emission line range)
    - R/as_d3_ir.R (confirm sf branch location for default_label_col addition)
  </read_first>
  <action>
    Replace the `## Implementation Plan (BLPR-03)` placeholder section in `30-01-BLUEPRINT.md` with a complete phase-by-phase build plan. Match the depth/specificity of `29-01-SF-INTERACTIVITY-DESIGN.md` — every change must name a file AND either a line number, a function name + line range, or a named code construct (e.g., `INTERACTIVE_SELECTORS array`).

    Structure the section as follows:

    Lead paragraph: State the purpose ("This plan is consumed by `/gsd-plan-phase` for the v1.8+ build milestone. Each phase below is one `/gsd-plan-phase` invocation; each plan within a phase is one PLAN.md."), the locking convention (file/line granularity per D-07), and naming alignment (use `IMPL-NN` requirement IDs from REQUIREMENTS.md where they map cleanly).

    Then enumerate the build phases. Use this sequencing aligned with existing v1.7 + REQUIREMENTS IMPL-01..IMPL-05:

    ### `### Build Phase A: SF Core Rendering Wire-Up (IMPL-03 + IMPL-04 attribute work)`
    Goal: Land the Phase 29 design-locked DOM changes in `sf.js` and the IR.
    Plans:
    - **Plan A.1 — `data-centroid` migration & path attribute additions**
      - File: `inst/htmlwidgets/modules/geoms/sf.js` lines 99–104 — replace `.attr("data-cx", ...).attr("data-cy", ...)` with single `.attr("data-centroid", function(d) { return (isFinite(d._centroid[0]) && isFinite(d._centroid[1])) ? d._centroid[0]+","+d._centroid[1] : null; })`
      - File: `inst/htmlwidgets/modules/geoms/sf.js` — add `.attr("vector-effect", "non-scaling-stroke")` to every path append (per Phase 29 D-09, presentation attribute NOT CSS)
      - File: `inst/htmlwidgets/modules/geoms/sf.js` — wrap the per-feature `<path>` append loop inside a new `<g class="sf-zoom-layer">` group (per Phase 29 D-08); insert `.append('g').attr('class','sf-zoom-layer')` in the panel-group setup before the existing geom-sf-group
    - **Plan A.2 — Add `default_label_col` to sf layer descriptor (IR)**
      - File: `R/as_d3_ir.R` sf branch (near the existing `df[["row_id"]] <- seq_along(sf_geom_strings)` insertion) — add `default_label_col = setdiff(names(sf_df), attr(sf_df, "sf_column"))[1] %||% NULL` to the layer descriptor list (per Phase 29 D-01)
      - File: `tests/testthat/test-sf-ir.R` — extend with assertion that `ir$layers[[1]]$default_label_col == "NAME"` for `nc` fixture

    ### `### Build Phase B: SF Interactivity Wire-Up (IMPL-04)`
    Goal: Land the Phase 29 design-locked registry + dispatch additions across `events.js`, `brush.js`, `tooltip.js`, `hover.js`, `zoom.js`.
    Plans:
    - **Plan B.1 — Tooltip & hover registry entry**
      - File: `inst/htmlwidgets/modules/events.js` lines 23–43 — add `'path.geom-sf'` to `INTERACTIVE_SELECTORS` array (per Phase 29 D-03)
      - File: `inst/htmlwidgets/modules/tooltip.js` — extend content resolution to honor `ir.layers[i].default_label_col` when matched selector is `path.geom-sf` (per Phase 29 D-01); NO new per-geom branch in `format()`
      - File: `inst/htmlwidgets/modules/hover.js` — confirm registry pickup (no change expected; verify dispatch fires on `path.geom-sf`)
    - **Plan B.2 — Brush selection wiring (centroid hit-test)**
      - File: `inst/htmlwidgets/modules/brush.js` lines 29–49 — add `'path.geom-sf'` to `INTERACTIVE_SELECTORS` array (per Phase 29 D-05)
      - File: `inst/htmlwidgets/modules/brush.js` after line 268 (after the `tagName === 'circle'` branch, before the generic `path` branch) — add the new sf branch reading `data-centroid` and doing point-in-rect test (verbatim code in Phase 29 D-05 section)
    - **Plan B.3 — Zoom integration (SVG group transform)**
      - File: `inst/htmlwidgets/modules/zoom.js` near line 126 (top of `zoomed()` handler, after `clearBrush()`, before existing scale-rescaling) — add the `panelGroup.select('.sf-zoom-layer')` non-empty check + `sfLayer.attr('transform', transform.toString())` (verbatim code in Phase 29 D-11 section)

    ### `### Build Phase C: SF Edge Case Handling (IMPL-05)`
    Goal: Land the BLPR-01 edge case handling — multi-layer rendering correctness and faceted per-panel bbox.
    Plans:
    - **Plan C.1 — Multi-layer sf rendering correctness**
      - File: `R/as_d3_ir.R` sf branch — confirm `coord.bbox` is computed as union across ALL sf layers (not just first); add bbox-union logic if Edge Case 2 evidence shows current behavior is single-layer-only
      - File: `inst/htmlwidgets/modules/geoms/sf.js` — verify render-order respects `ir$layers` order (back-to-front); add explicit comment documenting the contract
      - File: `tests/testthat/test-sf-ir.R` — add multi-layer sf integration test asserting layer count + bbox union
    - **Plan C.2 — Faceted sf with per-panel bbox**
      - File: `R/as_d3_ir.R` sf branch panel iteration — compute per-panel bbox from panel-subset features instead of one global bbox (per D-03); store on `ir$panels[[i]]$bbox` or extend `coord` to be panel-indexed
      - File: `inst/htmlwidgets/modules/geoms/sf.js` — update fitExtent invocation to use the per-panel bbox (per D-03)
      - File: `tests/testthat/test-sf-visual.R` — add faceted sf visual test (HTML output to `test_output/`) with human-verify checkpoint
    - **Plan C.3 — Faceted-sf flagged unknowns verification**
      - Empirically verify each of the four D-04 unknowns (panel strips on sf, axis suppression per panel, `coord_sf(xlim=,ylim=)` per panel, `facet_wrap(scales=)` mirroring) via test/inspection
      - Land findings as either fixes or documented limitations
      - File coverage emerges from findings — flag as "scope determined by Plan C.3 spike"
    - **Plan C.4 — Mixed geometry types in single layer**
      - Apply Edge Case 1 recommendation from BLPR-01 § Edge Case 1 (either preserve heterogeneity OR document limitation + warn)
      - File: `R/as_d3_ir.R` sf branch — modify `detect_dominant_geom_type()` call site per the recommendation
      - File: `tests/testthat/test-sf-ir.R` — add mixed-geometry regression test

    Then a final summary table modeled on the `Build-Phase Implementation Reference (IMPL-04)` table at the end of `29-01-SF-INTERACTIVITY-DESIGN.md` — columns: `Build Phase | Plan | File | Concrete Change | Decision/Requirement Ref`. Populate from the plans above; this gives `/gsd-plan-phase` an at-a-glance file-touch manifest.

    Use the Edit tool to replace the `## Implementation Plan (BLPR-03)` placeholder block (including `<!-- Task 6 fills this -->` comment) with the new content.
  </action>
  <verify>
    <automated>! grep -q "Task 6 fills this" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -q "^### Build Phase A" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -q "^### Build Phase B" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -q "^### Build Phase C" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -qE "brush\.js.*(line|lines) [0-9]+" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -qE "sf\.js.*(line|lines) [0-9]+" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -qE "events\.js.*(line|lines) [0-9]+" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -qE "zoom\.js.*(line|near line) [0-9]+" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -q "as_d3_ir\.R" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -q "INTERACTIVE_SELECTORS" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -q "default_label_col" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -q "data-centroid" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -q "vector-effect" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -q "sf-zoom-layer" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md</automated>
  </verify>
  <acceptance_criteria>
    - `<!-- Task 6 fills this -->` placeholder REMOVED
    - Three `### Build Phase [ABC]` headings present
    - File/line granularity present: grep returns hits for `brush.js.*line [0-9]+`, `sf.js.*line [0-9]+`, `events.js.*line [0-9]+`, `zoom.js.*(line|near line) [0-9]+` (D-07 enforcement)
    - All five integration-surface JS files mentioned: `brush.js`, `sf.js`, `events.js`, `zoom.js`, `tooltip.js` (each appears as a string)
    - R integration mentioned: `as_d3_ir.R` appears
    - All four Phase 29-locked DOM tokens cited: `data-centroid`, `vector-effect`, `sf-zoom-layer`, `INTERACTIVE_SELECTORS`
    - `default_label_col` appears (D-01 IR field cited)
    - Final summary table present with columns including `File | Concrete Change`
    - All three BLPR-01 edge cases get build plans in Phase C (multi-layer in C.1, faceted in C.2/C.3, mixed-geometry in C.4)
  </acceptance_criteria>
  <done>
    Blueprint is feature-complete: BLPR-01 (edge cases), BLPR-02 (anti-features), BLPR-03 (phase-by-phase impl plan) all present in `30-01-BLUEPRINT.md`. Implementation plan reaches file/line specificity matching 29-01 depth. Document is ready for handoff to `/gsd-plan-phase` for the v1.8+ build milestone.
  </done>
</task>

<task type="auto">
  <name>Task 7: Blueprint self-review and final acceptance check</name>
  <files>.planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md (read-only; may add a "Success Rubric" footer section)</files>
  <read_first>
    - .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md (the complete file from Tasks 4-6)
    - .planning/ROADMAP.md § "Phase 30: Edge Cases and Blueprint" (Success Criteria 1, 2, 3 — the truths must_haves derive from)
    - .planning/phases/29-interactivity-design/29-01-SF-INTERACTIVITY-DESIGN.md (Success Rubric format at the bottom — model)
  </read_first>
  <action>
    Append a `## Success Rubric` section to the end of `30-01-BLUEPRINT.md`, modeled on the rubric at the end of `29-01-SF-INTERACTIVITY-DESIGN.md`. The rubric is a self-check list that mirrors ROADMAP.md's three Phase 30 Success Criteria and the quality-gate checks from this plan. Use checkbox format. Concrete checks to include:

    - [ ] All three edge cases (mixed geometry, multi-layer stacking, faceted sf) have their own `## Edge Case N:` section with empirical-run-log citation
    - [ ] Faceted-sf section explicitly locks per-panel `coord.bbox` via `d3.geoIdentity().reflectY(true).fitExtent()` per panel (D-03)
    - [ ] Faceted-sf section enumerates the four D-04 build-phase verification items as a bulleted list
    - [ ] Anti-Features section has exactly THREE subsections — tile basemaps, JS-side reprojection, slippy zoom — each one sentence
    - [ ] Anti-Features section distinguishes anti-features from deferred ideas (D-06 protection)
    - [ ] Implementation Plan has three Build Phases (A, B, C) corresponding to core rendering, interactivity, edge cases
    - [ ] Every plan in Implementation Plan names at least one specific file AND a line number or named code construct
    - [ ] All five integration-surface JS files (brush.js, sf.js, events.js, zoom.js, tooltip.js) named with file/line callouts
    - [ ] R-side integration (as_d3_ir.R sf branch) covered for `default_label_col`, multi-layer bbox union, per-panel bbox
    - [ ] Final summary table maps each change to file + concrete change + decision/requirement ref

    After appending the rubric, walk through it self-checking the document and confirm each box can be checked. Do NOT modify the boxes to checked state — they are a reviewer artifact. Just confirm in the task `<done>` field that each check passes against the document.

    Also run the full acceptance check command listed in `<verify>` below and confirm all greps return their expected hits.
  </action>
  <verify>
    <automated>grep -q "^## Success Rubric" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md && grep -c "^- \[ \]" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md | awk '{ if ($1 >= 10) exit 0; else exit 1 }' && grep -c "^## Edge Case [123]:" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md | grep -qx 3 && grep -c "^### Anti-Feature [123]:" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md | grep -qx 3 && grep -c "^### Build Phase [ABC]" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md | grep -qx 3 && grep -q "implements: \[BLPR-01, BLPR-02, BLPR-03\]" .planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Success Rubric` section appended at end of file
    - At least 10 `- [ ]` rubric items present
    - Whole-document invariants reconfirmed by the verify command:
      - exactly 3 `## Edge Case N:` headings
      - exactly 3 `### Anti-Feature N:` headings
      - exactly 3 `### Build Phase X` headings (A, B, C)
      - frontmatter `implements: [BLPR-01, BLPR-02, BLPR-03]` intact
    - Each rubric box, when manually walked through, can be checked against the current document content (executor confirms in summary message)
  </acceptance_criteria>
  <done>
    Blueprint is feature-complete and self-verified. The three ROADMAP Phase 30 Success Criteria are demonstrably met by the document content; the rubric provides a reviewer checklist. Document is ready to commit and to be consumed by `/gsd-plan-phase` for the v1.8+ build milestone (IMPL-04+).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Phase 30 is documentation-only. No user input surface, no network calls, no auth, no PII, no executable artifact shipped to users. The R prototype scripts run locally in `test_output/` (gitignored) and load only built-in sf fixtures (`nc.shp` shipped with the sf package). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | N/A | Whole phase | accept | No new attack surface — documentation-only phase. The `30-01-BLUEPRINT.md` output is read by future planning agents and human reviewers; it contains no executable code paths. Prototype R scripts in `test_output/` use only built-in `sf` package fixtures (`nc.shp`) and the local package via `pkgload::load_all(".")` — no untrusted input, no network IO, no credential handling, no user-facing surface. Explicit confirmation: no auth (none required), no PII (NC counties data is public), no network (all data is local/bundled), no XSS/injection vectors (no string concatenation into HTML or shell). |
</threat_model>

<verification>
## Phase-Level Verification (run after all tasks complete)

All checks must pass. Each is grep-deterministic per `<deep_work_rules>`.

```bash
BLUEPRINT=.planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md

# Deliverable exists
test -f "$BLUEPRINT" || { echo "FAIL: BLUEPRINT missing"; exit 1; }

# Frontmatter requirements coverage
grep -q "implements: \[BLPR-01, BLPR-02, BLPR-03\]" "$BLUEPRINT" || { echo "FAIL: frontmatter requirements"; exit 1; }

# BLPR-01: exactly three edge case sections (Success Criterion 1)
[ "$(grep -c '^## Edge Case [123]:' "$BLUEPRINT")" = "3" ] || { echo "FAIL: edge cases != 3"; exit 1; }

# BLPR-01: per-panel bbox decision locked (D-03)
grep -q "per-panel" "$BLUEPRINT" || { echo "FAIL: per-panel"; exit 1; }
grep -q "d3.geoIdentity().reflectY(true).fitExtent" "$BLUEPRINT" || { echo "FAIL: geoIdentity"; exit 1; }

# BLPR-02: exactly three anti-features (Success Criterion 2)
[ "$(grep -c '^### Anti-Feature [123]:' "$BLUEPRINT")" = "3" ] || { echo "FAIL: anti-features != 3"; exit 1; }
grep -q "Tile Basemaps" "$BLUEPRINT" || { echo "FAIL: anti-feature tile"; exit 1; }
grep -q "JS-Side Reprojection" "$BLUEPRINT" || { echo "FAIL: anti-feature reprojection"; exit 1; }
grep -q "Slippy Zoom" "$BLUEPRINT" || { echo "FAIL: anti-feature slippy"; exit 1; }

# BLPR-03: phase-by-phase plan with file/line specificity (Success Criterion 3)
[ "$(grep -c '^### Build Phase [ABC]' "$BLUEPRINT")" = "3" ] || { echo "FAIL: build phases != 3"; exit 1; }
grep -qE 'brush\.js.*(line|lines) [0-9]+' "$BLUEPRINT" || { echo "FAIL: brush.js line"; exit 1; }
grep -qE 'sf\.js.*(line|lines) [0-9]+' "$BLUEPRINT" || { echo "FAIL: sf.js line"; exit 1; }
grep -qE 'events\.js.*(line|lines) [0-9]+' "$BLUEPRINT" || { echo "FAIL: events.js line"; exit 1; }
grep -qE 'zoom\.js.*(line|near line) [0-9]+' "$BLUEPRINT" || { echo "FAIL: zoom.js line"; exit 1; }
grep -q "as_d3_ir\.R" "$BLUEPRINT" || { echo "FAIL: as_d3_ir.R"; exit 1; }
grep -q "INTERACTIVE_SELECTORS" "$BLUEPRINT" || { echo "FAIL: INTERACTIVE_SELECTORS"; exit 1; }
grep -q "default_label_col" "$BLUEPRINT" || { echo "FAIL: default_label_col"; exit 1; }
grep -q "data-centroid" "$BLUEPRINT" || { echo "FAIL: data-centroid"; exit 1; }
grep -q "vector-effect" "$BLUEPRINT" || { echo "FAIL: vector-effect"; exit 1; }
grep -q "sf-zoom-layer" "$BLUEPRINT" || { echo "FAIL: sf-zoom-layer"; exit 1; }

# Success Rubric present
grep -q "^## Success Rubric" "$BLUEPRINT" || { echo "FAIL: success rubric"; exit 1; }

# Prototype scripts ran but were NOT committed (D-02)
git ls-files test_output/ 2>/dev/null | grep -q . && { echo "FAIL: test_output/ tracked by git"; exit 1; } || true

echo "PASS: 30-01-BLUEPRINT.md satisfies all three BLPR success criteria"
```
</verification>

<success_criteria>
Phase 30 completes when:

- [ ] `30-01-BLUEPRINT.md` exists at `.planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md`
- [ ] BLPR-01 satisfied: blueprint covers at minimum three edge cases (mixed geometry, multi-layer stacking, faceted sf) with empirical findings from prototype-script run logs and concrete build-phase handling for each
- [ ] BLPR-02 satisfied: blueprint contains exactly three anti-features (tile basemaps, JS-side reprojection, slippy zoom), each with a one-sentence rationale, distinguished from deferred ideas
- [ ] BLPR-03 satisfied: blueprint contains a phase-by-phase implementation plan with file/line specificity matching 29-01-SF-INTERACTIVITY-DESIGN.md, naming all five integration-surface JS modules + the R sf branch, sequenced into discrete Build Phases A/B/C
- [ ] All three Phase 30 ROADMAP Success Criteria pass the grep-verification block above
- [ ] Prototype scripts ran (evidence in `test_output/*-run.log`) but are NOT staged in git (D-02)
- [ ] Frontmatter `implements: [BLPR-01, BLPR-02, BLPR-03]` and `build_phase_target: IMPL-04+ (v1.8+ milestone)` present
- [ ] `## Success Rubric` section present with ≥10 reviewer checkboxes
</success_criteria>

<output>
After completion, create `.planning/phases/30-edge-cases-and-blueprint/30-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. The SUMMARY must record:

- Deliverable: `30-01-BLUEPRINT.md` created (BLPR-01/02/03 implemented)
- Tasks completed: 7 (3 prototype scripts + 3 blueprint section writes + 1 self-review)
- Files created (tracked): `.planning/phases/30-edge-cases-and-blueprint/30-01-BLUEPRINT.md`
- Files created (gitignored, evidence only): `test_output/30-edge-{mixed,multi,faceted}-*.{R,json,log,txt}`
- Key findings from each edge-case prototype (one-line each — the empirical evidence backing the blueprint's recommendations)
- Confirmation that all locked CONTEXT.md decisions D-01..D-08 are reflected in the blueprint
- Next phase: v1.7 milestone complete; v1.8+ build milestone consumes `30-01-BLUEPRINT.md` via `/gsd-plan-phase`
</output>
