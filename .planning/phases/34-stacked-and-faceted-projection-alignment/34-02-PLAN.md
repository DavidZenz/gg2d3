---
phase: 34-stacked-and-faceted-projection-alignment
plan: 02
type: execute
wave: 2
depends_on:
  - "34-01"
files_modified:
  - inst/htmlwidgets/gg2d3.js
  - inst/htmlwidgets/modules/geoms/sf.js
  - tests/testthat/test-sf-renderer.R
autonomous: true
requirements:
  - SFREND-02
  - SFREND-03
user_setup: []
must_haves:
  truths:
    - "gg2d3.js filters sf geometries with the same indexes used to filter sf data rows by PANEL"
    - "gg2d3.js passes panel sf bbox metadata to every sf renderer call in that panel"
    - "sf.js fits from shared panel bbox metadata when available"
    - "sf.js preserves Phase 33 path, centroid, row-id, and private-field behavior"
  artifacts:
    - path: "inst/htmlwidgets/gg2d3.js"
      provides: "panel sf bbox handoff and geometry/data pair filtering"
      contains: "sfBBox"
    - path: "inst/htmlwidgets/modules/geoms/sf.js"
      provides: "shared panel bbox projection fit"
      contains: "bboxToFeatureCollection"
    - path: "tests/testthat/test-sf-renderer.R"
      provides: "renderer source-contract coverage"
      contains: "sfBBox"
  key_links:
    - from: "R/as_d3_ir.R"
      to: "inst/htmlwidgets/gg2d3.js"
      via: "panelData.sf_bbox"
      pattern: "sf_bbox"
    - from: "inst/htmlwidgets/gg2d3.js"
      to: "inst/htmlwidgets/modules/geoms/sf.js"
      via: "options.sfBBox"
      pattern: "sfBBox"
---

<objective>
Pass shared panel sf projection state through the D3 renderer.

Purpose: R will provide panel-level `sf_bbox`; JavaScript must preserve sf data/geometry alignment under facets and make every sf layer in a panel fit from the same bbox.
Output: Updated panel renderer, sf geom renderer, and JS source-contract tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-stacked-and-faceted-projection-alignment/34-CONTEXT.md
@.planning/phases/34-stacked-and-faceted-projection-alignment/34-RESEARCH.md
@.planning/phases/34-stacked-and-faceted-projection-alignment/34-VALIDATION.md
@.planning/phases/34-stacked-and-faceted-projection-alignment/34-PATTERNS.md
@inst/htmlwidgets/gg2d3.js
@inst/htmlwidgets/modules/geoms/sf.js
@tests/testthat/test-sf-renderer.R
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-34-03: Renderer ignores shared panel bbox and still fits each layer independently | high | Make `sf.js` prefer `options.sfBBox` over layer FeatureCollection as the `fitExtent()` source. |
| T-34-04: Faceted sf panels bind filtered data to unfiltered geometries | high | Filter sf `data` and `geometries` as index pairs in `gg2d3.js`. |
| T-34-08: Existing single-panel sf rendering regresses when no sf_bbox is present | medium | Keep layer FeatureCollection fallback when shared bbox metadata is missing. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Filter sf geometries alongside panel data</name>
  <files>inst/htmlwidgets/gg2d3.js, tests/testthat/test-sf-renderer.R</files>
  <read_first>inst/htmlwidgets/gg2d3.js, inst/htmlwidgets/modules/geoms/sf.js, tests/testthat/test-sf-renderer.R, .planning/phases/34-stacked-and-faceted-projection-alignment/34-RESEARCH.md</read_first>
  <action>In `renderPanel()` in `inst/htmlwidgets/gg2d3.js`, replace the current faceted row-only filtering for sf layers with index-pair filtering. For layers where `layer.geom === "sf"` and `Array.isArray(layer.geometries)`, build pairs from `(layer.data || [])` and `layer.geometries` by original index, filter pairs with `pair.data.PANEL === panelNum` when `isFaceted`, and set `filteredLayer.data` and `filteredLayer.geometries` from the filtered pairs. For non-sf layers, keep existing row-only filtering behavior. The filtered sf `data.length` and `geometries.length` must remain equal. Add source-contract tests in `tests/testthat/test-sf-renderer.R` asserting `gg2d3.js` contains `layer.geom === "sf"`, `geometries`, and paired filtering logic near `PANEL` filtering.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-renderer.R")'</verify>
  <acceptance_criteria>
    - `inst/htmlwidgets/gg2d3.js` contains `layer.geom === "sf"`
    - `inst/htmlwidgets/gg2d3.js` contains `geometries`
    - `inst/htmlwidgets/gg2d3.js` filters sf rows by `PANEL`
    - `tests/testthat/test-sf-renderer.R` contains `gg2d3.js`
    - `tests/testthat/test-sf-renderer.R` contains `geometries`
  </acceptance_criteria>
  <done>Faceted sf rendering preserves data/geometry positional correspondence.</done>
</task>

<task type="auto">
  <name>Task 2: Pass panel sf bbox state into geom renderers</name>
  <files>inst/htmlwidgets/gg2d3.js, tests/testthat/test-sf-renderer.R</files>
  <read_first>inst/htmlwidgets/gg2d3.js, R/as_d3_ir.R, .planning/phases/34-stacked-and-faceted-projection-alignment/34-01-PLAN.md</read_first>
  <action>When `renderPanel()` calls `window.gg2d3.geomRegistry.render(...)`, include `panelData: panelData` and `sfBBox: panelData && panelData.sf_bbox ? panelData.sf_bbox : null` in the options object. Keep existing `colorScale`, `plotWidth`, `plotHeight`, `flip`, `coord`, and `scales` options unchanged. Add a source-contract test that asserts `gg2d3.js` passes `sfBBox` and `panelData` through the renderer options.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-renderer.R")'</verify>
  <acceptance_criteria>
    - `inst/htmlwidgets/gg2d3.js` contains `sfBBox`
    - `inst/htmlwidgets/gg2d3.js` contains `panelData`
    - `inst/htmlwidgets/gg2d3.js` still contains `plotWidth`
    - `inst/htmlwidgets/gg2d3.js` still contains `plotHeight`
    - `tests/testthat/test-sf-renderer.R` contains `sfBBox`
  </acceptance_criteria>
  <done>Every geom renderer call can access the panel's sf bbox metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Make sf.js fit from shared panel bbox when available</name>
  <files>inst/htmlwidgets/modules/geoms/sf.js, tests/testthat/test-sf-renderer.R</files>
  <read_first>inst/htmlwidgets/modules/geoms/sf.js, inst/htmlwidgets/gg2d3.js, tests/testthat/test-sf-renderer.R</read_first>
  <action>In `inst/htmlwidgets/modules/geoms/sf.js`, add a helper named `bboxToFeatureCollection` or similarly clear that accepts a numeric bbox `[xmin, ymin, xmax, ymax]` and returns a GeoJSON FeatureCollection with one rectangular Polygon covering that bbox. In `renderSf()`, after building the layer FeatureCollection `fc`, choose `fitSource = bboxToFeatureCollection(options.sfBBox)` when `options.sfBBox` exists and has length 4; otherwise use the existing layer FeatureCollection `fc`. Keep the current `d3.geoIdentity().reflectY(true).fitExtent([[padding, padding], [w - padding, h - padding]], fitSource)` pattern. Do not change class `geom-sf`, `data-row-id`, `data-cx`, `data-cy`, `fill-rule`, or private `_geom`/`_centroid` fields. Add tests that assert `sf.js` contains `bboxToFeatureCollection`, `options.sfBBox`, `fitSource`, and `reflectY(true)`.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-renderer.R")'</verify>
  <acceptance_criteria>
    - `inst/htmlwidgets/modules/geoms/sf.js` contains `bboxToFeatureCollection`
    - `inst/htmlwidgets/modules/geoms/sf.js` contains `options.sfBBox`
    - `inst/htmlwidgets/modules/geoms/sf.js` contains `fitSource`
    - `inst/htmlwidgets/modules/geoms/sf.js` contains `reflectY(true)`
    - `inst/htmlwidgets/modules/geoms/sf.js` still contains `data-row-id`
    - `inst/htmlwidgets/modules/geoms/sf.js` still contains `data-cx`
    - `inst/htmlwidgets/modules/geoms/sf.js` still contains `data-cy`
  </acceptance_criteria>
  <done>Sf layers in the same panel fit from shared panel bbox state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-renderer.R")'`
- [ ] Confirm `gg2d3.js` filters sf `geometries` with sf `data` under facets.
- [ ] Confirm `gg2d3.js` passes `sfBBox` into `geomRegistry.render()`.
- [ ] Confirm `sf.js` keeps layer FeatureCollection fallback when `options.sfBBox` is absent.
</verification>

<success_criteria>
- `gg2d3.js` passes shared panel projection state into every sf layer renderer.
- Faceted sf layer data and geometries stay parallel after filtering.
- `sf.js` uses panel bbox metadata for `fitExtent()` when available.
- Single-panel fallback behavior remains available for older/minimal IR.
</success_criteria>

<output>
After completion, create `.planning/phases/34-stacked-and-faceted-projection-alignment/34-02-SUMMARY.md`
</output>
