---
phase: 51-geometry-edge-case-classification-and-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-polygon-ir.R
  - tests/testthat/test-polygon-renderer.R
  - tests/testthat/test-polygon-browser.R
  - vignettes/d3-drawing-diagnostics.md
  - .planning/phases/51-geometry-edge-case-classification-and-polish/51-VALIDATION.md
autonomous: true
requirements:
  - GEOM-02
must_haves:
  truths:
    - "Ordinary polygons are grouped closed SVG paths that preserve ggplot2 built row order."
    - "Subgroup/hole/ring-order behavior is characterized against ggplot2 output before support claims."
    - "Unsupported topology repair, automatic hole inference, and invalid polygon fixing are documented without GIS overclaim."
  artifacts:
    - path: "tests/testthat/test-polygon-ir.R"
      provides: "Polygon topology and subgroup/hole classification fixtures"
    - path: "tests/testthat/test-polygon-renderer.R"
      provides: "Grouped closed-path renderer contract checks"
    - path: "vignettes/d3-drawing-diagnostics.md"
      provides: "Public polygon topology caveats if classification changes or clarifies the contract"
  key_links:
    - from: "tests/testthat/test-polygon-ir.R"
      to: "inst/htmlwidgets/modules/geoms/polygon.js"
      via: "grouped row-order path contract"
      pattern: "one closed SVG path per built group"
requirements_addressed:
  - GEOM-02
---

<objective>
Characterize ordinary polygon topology, holes, subgroups, and ring-order behavior without expanding gg2d3 into a topology repair engine.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-geometry-edge-case-classification-and-polish/51-CONTEXT.md
@.planning/phases/51-geometry-edge-case-classification-and-polish/51-RESEARCH.md
@.planning/phases/51-geometry-edge-case-classification-and-polish/51-PATTERNS.md
@.planning/phases/51-geometry-edge-case-classification-and-polish/51-VALIDATION.md
@tests/testthat/test-polygon-ir.R
@tests/testthat/test-polygon-renderer.R
@tests/testthat/test-polygon-interactivity.R
@tests/testthat/test-polygon-browser.R
@tests/testthat/test-zoom-path-datum.R
@R/ir_layer_helpers.R
@inst/htmlwidgets/modules/geoms/polygon.js
@inst/htmlwidgets/modules/geom-registry.js
@vignettes/d3-drawing-diagnostics.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add polygon topology classification fixtures</name>
  <read_first>
    tests/testthat/test-polygon-ir.R
    R/ir_layer_helpers.R
    inst/htmlwidgets/modules/geoms/polygon.js
  </read_first>
  <files>tests/testthat/test-polygon-ir.R</files>
  <action>
    Extend `tests/testthat/test-polygon-ir.R` with classification fixtures for:
    - multiple ordinary polygon groups that should remain independent closed paths;
    - a `subgroup`/hole-style input that proves whether ggplot2 built data retains `subgroup` and whether gg2d3 IR currently preserves it;
    - reversed ring order and repeated first/last vertex behavior;
    - fewer-than-three valid vertices or missing coordinate rows;
    - a deliberately self-crossing polygon classified as ordinary row-order input, not repaired topology.

    Assertions must compare ggplot2 built rows and `as_d3_ir()` rows. Support only behavior represented by row-order grouped paths. Do not add `subgroup` to the IR unless the renderer also gains an honest compound-path contract in this plan.
  </action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-polygon-ir.R")'</automated>
    <automated>rg -n "subgroup|hole|ring|self|three|row order|row-order" tests/testthat/test-polygon-ir.R</automated>
  </verify>
  <acceptance_criteria>
    - Polygon IR tests classify subgroup/hole behavior explicitly.
    - Supported cases preserve built row order and group membership.
    - Unsupported topology cases are tested as classification, not silently treated as supported holes.
  </acceptance_criteria>
  <done>Ordinary polygon topology behavior has IR-level classification evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Guard grouped closed-path renderer semantics</name>
  <read_first>
    tests/testthat/test-polygon-renderer.R
    inst/htmlwidgets/modules/geoms/polygon.js
    inst/htmlwidgets/modules/geom-registry.js
    tests/testthat/test-zoom-path-datum.R
  </read_first>
  <files>tests/testthat/test-polygon-renderer.R, inst/htmlwidgets/modules/geoms/polygon.js</files>
  <action>
    Extend `tests/testthat/test-polygon-renderer.R` so the renderer contract explicitly states:
    - polygons group by built `group`;
    - paths use `curveLinearClosed`;
    - no sorting, topology repair, ring containment, automatic hole inference, or `subgroup`-based compound path support is claimed unless implemented;
    - private `_polygonPoints` / `_sourceIndex` remain available for update paths but not public payloads.

    Change `polygon.js` only if a classification fixture proves a small grouped-path bug. Do not implement a GIS topology repair or automatic invalid-polygon fixer.
  </action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-polygon-renderer.R"); testthat::test_file("tests/testthat/test-zoom-path-datum.R")'</automated>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-polygon-interactivity.R")'</automated>
  </verify>
  <acceptance_criteria>
    - Renderer tests fail if row-order grouping or closed-path behavior is removed.
    - Renderer tests or validation notes explicitly exclude topology repair and automatic hole inference.
    - Polygon interactivity/private-field tests still pass or skip only for optional dependency reasons.
  </acceptance_criteria>
  <done>Ordinary polygon renderer semantics are locked without overclaiming topology support.</done>
</task>

<task type="auto">
  <name>Task 3: Update polygon caveats if classification clarifies public behavior</name>
  <read_first>
    vignettes/d3-drawing-diagnostics.md
    README.Rmd
    .planning/phases/51-geometry-edge-case-classification-and-polish/51-VALIDATION.md
  </read_first>
  <files>vignettes/d3-drawing-diagnostics.md, .planning/phases/51-geometry-edge-case-classification-and-polish/51-VALIDATION.md</files>
  <action>
    If the new tests clarify the public polygon contract, update `vignettes/d3-drawing-diagnostics.md` with a short note that ordinary `geom_polygon()` supports grouped closed paths and does not support full topology repair, automatic hole inference, invalid-polygon fixing, or GIS semantics.

    Update `51-VALIDATION.md` with the GEOM-02 evidence status. Do not edit README or roxygen unless the diagnostic caveat becomes materially inaccurate.
  </action>
  <verify>
    <automated>rg -n "geom_polygon|topology|hole|subgroup|invalid|repair" vignettes/d3-drawing-diagnostics.md .planning/phases/51-geometry-edge-case-classification-and-polish/51-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Public diagnostics do not overclaim polygon topology support.
    - `51-VALIDATION.md` records GEOM-02 pass/defer evidence.
  </acceptance_criteria>
  <done>The polygon support contract is recorded for maintainers and future milestone planning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ggplot2 polygon rows -> gg2d3 IR | Built row order and grouping determine ordinary polygon shape. |
| IR rows -> SVG path | Renderer groups rows into closed paths but does not infer topology. |
| Internal path metadata -> public payload | Private polygon point metadata must remain internal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-03 | T | polygon row order | mitigate | Add classification tests for row order, groups, rings, and invalid-shape inputs. |
| T-51-04 | R | unsupported topology claims | mitigate | Source/doc checks must state no topology repair or automatic hole inference. |
| T-51-05 | I | polygon private fields | mitigate | Keep existing sanitizer/interactivity tests green after topology classification. |
</threat_model>

<verification>
Run all task-level commands. Optional browser polygon smoke may be run with `GG2D3_BROWSER_VISUAL_SMOKE=true` but is supplementary.
</verification>

<success_criteria>
- GEOM-02 has classification evidence for groups, subgroup/hole-style input, ring order, and invalid/topology cases.
- Supported ordinary polygon behavior is locked as grouped closed SVG paths.
- Unsupported topology behavior is documented without GIS overclaim.
</success_criteria>

<output>
After completion, create `.planning/phases/51-geometry-edge-case-classification-and-polish/51-02-SUMMARY.md`.
</output>
