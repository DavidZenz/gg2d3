---
phase: 51-geometry-edge-case-classification-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-rect-tile-ir.R
  - tests/testthat/test-rect-tile-renderer.R
  - inst/htmlwidgets/modules/geoms/rect.js
  - inst/htmlwidgets/modules/geom-registry.js
  - .planning/phases/51-geometry-edge-case-classification-and-polish/51-VALIDATION.md
autonomous: true
requirements:
  - GEOM-01
must_haves:
  truths:
    - "Transformed-scale rect/tile behavior is classified against ggplot2 built data before implementation changes."
    - "Any D3-boundary rect/tile fix covers both initial render and zoom/update paths."
    - "If parity requires broad scale semantics, the limitation is recorded as an explicit non-goal with evidence."
  artifacts:
    - path: "tests/testthat/test-rect-tile-ir.R"
      provides: "Transformed rect/tile built-data and IR classification fixtures"
    - path: "tests/testthat/test-rect-tile-renderer.R"
      provides: "Rect/tile renderer/update source contracts"
  key_links:
    - from: "R/ir_scale_helpers.R"
      to: "inst/htmlwidgets/modules/geoms/rect.js"
      via: "transformed scale descriptors and transformed layer bounds"
      pattern: "ggplot2 built rows are the ground truth"
requirements_addressed:
  - GEOM-01
---

<objective>
Classify transformed-scale `geom_rect()` and `geom_tile()` behavior and make only source-local rect/tile fixes that the evidence proves.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-geometry-edge-case-classification-and-polish/51-CONTEXT.md
@.planning/phases/51-geometry-edge-case-classification-and-polish/51-RESEARCH.md
@.planning/phases/51-geometry-edge-case-classification-and-polish/51-PATTERNS.md
@.planning/phases/51-geometry-edge-case-classification-and-polish/51-VALIDATION.md
@tests/testthat/test-rect-tile-ir.R
@tests/testthat/test-rect-tile-renderer.R
@tests/testthat/test-ir.R
@tests/testthat/test-validate-ir.R
@R/as_d3_ir.R
@R/ir_scale_helpers.R
@R/ir_layer_helpers.R
@inst/htmlwidgets/modules/scales.js
@inst/htmlwidgets/modules/geoms/rect.js
@inst/htmlwidgets/modules/geom-registry.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add transformed rect/tile classification fixtures</name>
  <read_first>
    tests/testthat/test-rect-tile-ir.R
    tests/testthat/test-ir.R
    tests/testthat/test-validate-ir.R
    R/as_d3_ir.R
    R/ir_scale_helpers.R
    R/ir_layer_helpers.R
  </read_first>
  <files>tests/testthat/test-rect-tile-ir.R</files>
  <action>
    Extend `tests/testthat/test-rect-tile-ir.R` with focused transformed-scale fixtures for:
    - `geom_rect()` with positive log10 x/y bounds.
    - `geom_tile()` on sqrt x/y scales.
    - `geom_rect()` on reverse x/y scales.
    - a log-scale non-positive boundary case that proves ggplot2 censoring behavior.

    For each fixture, compare `ggplot2::ggplot_build(plot)$data[[1]]` against `as_d3_ir(plot)$layers[[1]]$data`.
    Assert that IR bounds match the ggplot2 built bounds, including `NA`/complete-row behavior, and assert expected `ir$scales$x$transform` / `ir$scales$y$transform` metadata.

    Do not change R helper code in this task unless a failing classification test proves the IR no longer mirrors ggplot2 built data.
  </action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-ir.R")'</automated>
    <automated>rg -n "log10|sqrt|reverse|ggplot_build|transform" tests/testthat/test-rect-tile-ir.R</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-rect-tile-ir.R` contains rect/tile-specific log10, sqrt, and reverse transformed-scale cases.
    - Each transformed case compares ggplot2 built bounds with gg2d3 IR bounds.
    - Non-positive log behavior is classified without relying on browser output.
  </acceptance_criteria>
  <done>GEOM-01 transformed rect/tile behavior has built-data and IR evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Classify and guard D3 rect/tile transform boundary</name>
  <read_first>
    inst/htmlwidgets/modules/scales.js
    inst/htmlwidgets/modules/geoms/rect.js
    inst/htmlwidgets/modules/geom-registry.js
    tests/testthat/test-rect-tile-renderer.R
    .planning/phases/51-geometry-edge-case-classification-and-polish/51-RESEARCH.md
  </read_first>
  <files>tests/testthat/test-rect-tile-renderer.R, inst/htmlwidgets/modules/geoms/rect.js, inst/htmlwidgets/modules/geom-registry.js</files>
  <action>
    Add source-contract tests to `tests/testthat/test-rect-tile-renderer.R` that classify the D3-boundary behavior for transformed rect/tile bounds:
    - the renderer consumes built/IR bounds directly;
    - initial render and `updateGeoms()` share equivalent rect/tile geometry logic;
    - any transform-boundary helper or explicit non-goal note is present.

    If Task 1 plus source inspection proves a small double-transform or finite-boundary mismatch in `rect.js` / `geom-registry.js`, implement the smallest local fix and update both initial render and update paths. Keep categorical band behavior and coord-flip behavior from Phase 45 intact.

    If the fix would require broad scale factory semantics, axis/tick changes, or deeper coordinate transformation emulation, do not implement it here. Instead add a clear source-test-backed classification note in `51-VALIDATION.md` and keep the renderer code unchanged.
  </action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-renderer.R")'</automated>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-ir.R"); testthat::test_file("tests/testthat/test-rect-tile-renderer.R")'</automated>
  </verify>
  <acceptance_criteria>
    - Renderer source tests mention transformed rect/tile boundary behavior.
    - If code changed, both `rect.js` and `geom-registry.js` remain covered for initial render/update parity.
    - If code did not change, `51-VALIDATION.md` records the explicit non-goal boundary and supporting evidence.
  </acceptance_criteria>
  <done>The transformed rect/tile D3 boundary is either locally fixed or explicitly classified as deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ggplot2 built data -> gg2d3 IR | Built rect/tile rows are the truth source for transformed bounds and censoring. |
| IR scale descriptors -> D3 scales | Transform metadata can affect pixel mapping and may double-apply if misunderstood. |
| Initial render -> zoom/update | Rect/tile geometry must not drift between first render and update paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | T | rect/tile IR classification | mitigate | Compare ggplot2 built data and IR rows for every transformed fixture before changes. |
| T-51-02 | T | rect/tile renderer/update | mitigate | Guard initial render and update path parity in source tests. |
| T-51-03 | R | transformed-scale non-goals | mitigate | Record explicit evidence when parity exceeds a local renderer fix. |
</threat_model>

<verification>
Run all task-level commands, then run `rtk git diff --check`.
</verification>

<success_criteria>
- GEOM-01 has log10, sqrt, and reverse rect/tile classification evidence.
- Built-data, IR, and renderer-boundary outcomes are not conflated.
- Any implementation change is source-local and covered for render/update parity.
- Deferred transformed-scale parity is explicit if no safe fix is implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/51-geometry-edge-case-classification-and-polish/51-01-SUMMARY.md`.
</output>
