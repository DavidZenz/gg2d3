---
phase: 19-custom-interactivity
plan: 01
type: execute
wave: 1
depends_on: ["18-01"]
files_modified:
  - R/gg2d3.R
  - R/d3_handlers.R
  - inst/htmlwidgets/modules/events.js
  - inst/htmlwidgets/gg2d3.js
  - tests/testthat/test-interactivity.R
autonomous: true

must_haves:
  truths:
    - "User can define custom JS event handlers for mark clicks and hovers"
    - "User can trigger Shiny input updates from plot interactions"
  artifacts:
    - path: "R/d3_handlers.R"
      provides: "R API for custom JS handlers and Shiny integration"
      contains: "d3_handlers"
---

<objective>
Implement custom interactivity handlers and Shiny input synchronization to allow extensible user logic.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add R API for custom handlers</name>
  <files>R/gg2d3.R, R/d3_handlers.R</files>
  <action>Create `d3_handlers()` function to store `click`, `mouseover`, `mouseout`, and `shiny_id` in the widget configuration.</action>
  <verify>Rscript -e "devtools::load_all(); p <- ggplot(mtcars, aes(wt, mpg)) + geom_point(); w <- gg2d3(p) |> d3_handlers(click = 'console.log(d)'); stopifnot(w$x$interactivity$handlers$click == 'console.log(d)')"</verify>
  <done>R API is functional and correctly populates IR.</done>
</task>

<task type="auto">
  <name>Task 2: Implement custom event binding in JS</name>
  <files>inst/htmlwidgets/modules/events.js</files>
  <action>Add `attachHandlers(el, config)` to `events.js`. Use `new Function('event', 'd', ...)` to execute user strings for `click`, `mouseover`, and `mouseout` events on interactive geoms.</action>
  <verify>Check visual check artifact for console logs when clicking points.</verify>
  <done>Custom JS handlers execute on mark interaction.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Shiny input synchronization</name>
  <files>inst/htmlwidgets/modules/events.js</files>
  <action>If `shiny_id` is present in config, automatically call `Shiny.setInputValue(shiny_id, d)` on mark clicks. Also notify Shiny when legend state changes if `shiny_id` is present.</action>
  <verify>Mock Shiny object in browser console and verify setInputValue call.</verify>
  <done>Plot interactions are synchronized with Shiny server state.</done>
</task>

<task type="auto">
  <name>Task 4: Wire handlers into global draw logic</name>
  <files>inst/htmlwidgets/gg2d3.js</files>
  <action>Update `draw` in `gg2d3.js` to call `window.gg2d3.events.attachHandlers()` with the configuration from the IR.</action>
  <verify>Confirm handlers are attached on initial render and resize.</verify>
  <done>Event handlers are integrated into the main rendering pipeline.</done>
</task>

<task type="auto">
  <name>Task 5: Add regression tests for custom handlers</name>
  <files>tests/testthat/test-interactivity.R</files>
  <action>Add tests validating handler registration and IR propagation for different event types.</action>
  <verify>Rscript -e "devtools::test(filter = 'interactivity')"</verify>
  <done>Custom interactivity features are test-backed.</done>
</task>

</tasks>

<success_criteria>
- Clicking a point executes custom user-defined JavaScript.
- Clicking a point or changing legend state updates a Shiny input (if configured).
- Multiple interactivity features (tooltips, hover, custom handlers) can coexist without conflict.
</success_criteria>
