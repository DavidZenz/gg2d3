---
phase: 16-transitions
plan: 01
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - R/gg2d3.R
  - inst/htmlwidgets/gg2d3.js
  - inst/htmlwidgets/modules/geom-registry.js
  - inst/htmlwidgets/modules/zoom.js
  - tests/testthat/test-interactivity.R
autonomous: true

must_haves:
  truths:
    - "User sees object-constant enter/update/exit transitions during interactive state changes"
    - "User sees marks and axes transition on a synchronized timeline when domains change"
    - "User can configure simple transition presets from R (none, fast, smooth)"
  artifacts:
    - path: "R/d3_transitions.R"
      provides: "R API for configuring transitions"
      contains: "d3_transitions"
---

<objective>
Implement animated transitions for marks and axes to provide smooth, object-constant feedback during interactions.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add R API for transition configuration</name>
  <files>R/gg2d3.R, R/d3_transitions.R</files>
  <action>Create `d3_transitions()` function to allow users to set `duration` and `easing` in the widget configuration. Update the IR to carry these settings.</action>
  <verify>Rscript -e "devtools::load_all(); p <- ggplot(mtcars, aes(wt, mpg)) + geom_point(); w <- gg2d3(p) |> d3_transitions(duration = 500); stopifnot(w$x$interactivity$transitions$duration == 500)"</verify>
  <done>R API is functional and correctly populates IR.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize geom updates in JS</name>
  <files>inst/htmlwidgets/modules/geom-registry.js, inst/htmlwidgets/modules/zoom.js</files>
  <action>Move element repositioning logic from `zoom.js` to a centralized `updateGeoms` function in `geomRegistry.js`. Update `zoom.js` to call this new helper.</action>
  <verify>Perform zoom on any plot and confirm elements still reposition correctly (no regression).</verify>
  <done>Logic is centralized without breaking existing functionality.</done>
</task>

<task type="auto">
  <name>Task 3: Implement transition support in geomRegistry</name>
  <files>inst/htmlwidgets/modules/geom-registry.js</files>
  <action>Enhance `updateGeoms` to accept an optional D3 transition object. Apply `.transition(t)` to all geom attribute updates. Ensure path-based geoms also receive smooth updates.</action>
  <verify>Trigger an interaction (like zoom reset) and confirm smooth mark animation.</verify>
  <done>Marks animate smoothly between states.</done>
</task>

<task type="auto">
  <name>Task 4: Synchronize axis transitions</name>
  <files>inst/htmlwidgets/gg2d3.js, inst/htmlwidgets/modules/zoom.js</files>
  <action>Update `updateAxes` in `zoom.js` and axis rendering in `gg2d3.js` to use transitions when updating tick positions and labels.</action>
  <verify>Confirm axes animate in sync with marks during zoom reset.</verify>
  <done>Axes and marks animate on a synchronized timeline.</done>
</task>

<task type="auto">
  <name>Task 5: Add motion preference support</name>
  <files>inst/htmlwidgets/gg2d3.js</files>
  <action>Check `window.matchMedia('(prefers-reduced-motion: reduce)').matches` and override duration to 0 if true. Also honor `duration: 0` from IR settings.</action>
  <verify>Set duration to 0 in R and confirm animations are disabled.</verify>
  <done>Accessibility and user preferences are respected.</done>
</task>

</tasks>

<success_criteria>
- Zoom reset (double-click) performs a smooth 750ms transition for both marks and axes.
- User can set custom duration via `d3_transitions(duration = X)`.
- Animations are disabled if system preference is set to reduced motion.
</success_criteria>
