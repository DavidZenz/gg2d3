---
phase: 56-pkgdown-content-and-widget-build-contract
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.Rmd
  - vignettes/d3-drawing-diagnostics.md
  - NEWS.md
  - .github/workflows/pkgdown.yaml
autonomous: true
requirements:
  - DOCS-03
  - BUILD-01
user_setup: []
must_haves:
  truths:
    - "Maintainers can distinguish source docs, generated docs, GitHub Pages output, and browser visual smoke artifacts."
    - "The GitHub Actions pkgdown route visibly reports website dependency availability before building the site."
    - "Pkgdown evidence remains separate from browser visual smoke evidence."
  artifacts:
    - path: "README.Rmd"
      provides: "Concise public pointer to pkgdown artifact taxonomy"
      contains: "generated `docs/`"
    - path: "vignettes/d3-drawing-diagnostics.md"
      provides: "Maintainer-facing artifact taxonomy and pkgdown evidence interpretation"
      contains: "GitHub Pages"
    - path: "NEWS.md"
      provides: "Development release note for pkgdown publication-surface repair"
      contains: "pkgdown"
    - path: ".github/workflows/pkgdown.yaml"
      provides: "Website dependency availability evidence before pkgdown build"
      contains: "PKGDOWN_SF_OPTIONAL_SKIP"
  key_links:
    - from: ".github/workflows/pkgdown.yaml"
      to: "DESCRIPTION"
      via: "setup-r-dependencies with needs: website"
      pattern: "needs: website"
    - from: "vignettes/d3-drawing-diagnostics.md"
      to: "test_output/browser-visual-smoke/"
      via: "artifact taxonomy distinction"
      pattern: "browser visual smoke"
---

<objective>
Document the Phase 56 artifact taxonomy and harden the existing pkgdown workflow so website dependency availability is visible before site build.

Purpose: This prevents maintainers from confusing source/generated/deploy/browser evidence and prevents the CI website dependency route from being assumed silently.
Output: Updated source docs, NEWS, and pkgdown workflow dependency evidence step.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/56-pkgdown-content-and-widget-build-contract/56-CONTEXT.md
@.planning/phases/56-pkgdown-content-and-widget-build-contract/56-RESEARCH.md
@.planning/phases/56-pkgdown-content-and-widget-build-contract/56-VALIDATION.md
@.planning/phases/56-pkgdown-content-and-widget-build-contract/56-PATTERNS.md
@AGENTS.md
@README.Rmd
@vignettes/d3-drawing-diagnostics.md
@NEWS.md
@DESCRIPTION
@_pkgdown.yml
@.github/workflows/pkgdown.yaml

<interfaces>
From `.github/workflows/pkgdown.yaml`, existing dependency/build route:
```yaml
- uses: r-lib/actions/setup-r-dependencies@v2
  with:
    extra-packages: any::pkgdown, local::.
    needs: website

- name: Build site
  run: pkgdown::build_site_github_pages(new_process = FALSE, install = FALSE)
  shell: Rscript {0}
```

From `DESCRIPTION`, website-relevant Suggests:
```text
Suggests:
    geojsonsf (>= 2.0.0),
    htmltools,
    knitr,
    pkgdown,
    rmarkdown,
    sf (>= 1.0.0),
    testthat (>= 3.0.0)
VignetteBuilder: knitr
Config/testthat/edition: 3
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pkgdown artifact taxonomy to source docs and NEWS</name>
  <files>README.Rmd, vignettes/d3-drawing-diagnostics.md, NEWS.md</files>
  <read_first>
    <file>.planning/phases/56-pkgdown-content-and-widget-build-contract/56-CONTEXT.md</file>
    <file>.planning/phases/56-pkgdown-content-and-widget-build-contract/56-RESEARCH.md</file>
    <file>README.Rmd</file>
    <file>vignettes/d3-drawing-diagnostics.md</file>
    <file>NEWS.md</file>
    <file>.planning/milestones/v1.13-phases/55-release-documentation-and-validation-gate/55-01-SUMMARY.md</file>
    <file>.planning/milestones/v1.13-phases/55-release-documentation-and-validation-gate/55-03-SUMMARY.md</file>
  </read_first>
  <action>Update source documentation per D-10 and D-11 with concrete artifact roles: source docs (`README.Rmd`, `vignettes/`, roxygen, `NEWS.md`) define intent; generated `docs/` proves local/pkgdown rendering and is committed release surface per D-01; GitHub Pages/deploy output proves publication; browser visual smoke artifacts under `test_output/browser-visual-smoke/` prove representative browser behavior. Add concise `README.Rmd` wording that points maintainers to diagnostics for the taxonomy. Add a dedicated `vignettes/d3-drawing-diagnostics.md` subsection named `Pkgdown site evidence` or equivalent that names those four artifact classes and their questions. Add a development NEWS bullet that says v1.14 repairs pkgdown publication-surface evidence without implying new rendering support. Do not add screenshot thresholds or pixel comparisons because D-06 excludes them.</action>
  <verify>
    <automated>rtk rg -n 'source docs|generated `docs/`|GitHub Pages|browser visual smoke|pkgdown' README.Rmd vignettes/d3-drawing-diagnostics.md NEWS.md</automated>
    <automated>rtk rg -n "pixel threshold|screenshot threshold|perceptual regression" README.Rmd vignettes/d3-drawing-diagnostics.md NEWS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n 'source docs|generated `docs/`|GitHub Pages|browser visual smoke' vignettes/d3-drawing-diagnostics.md` finds all four artifact classes.
    - `rtk rg -n 'generated `docs/`|pkgdown' README.Rmd` finds the public pointer.
    - `rtk rg -n "pkgdown|publication-surface|generated site" NEWS.md` finds the v1.14 development bullet.
    - `rtk rg -n "pixel threshold|screenshot threshold|perceptual regression" README.Rmd vignettes/d3-drawing-diagnostics.md NEWS.md` returns no new Phase 56 scope claims.
  </acceptance_criteria>
  <done>Maintainer-facing docs and NEWS define which artifact answers which release-readiness question, with pkgdown evidence separated from browser visual smoke.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the GitHub Actions website dependency route visibly</name>
  <files>.github/workflows/pkgdown.yaml</files>
  <read_first>
    <file>.planning/phases/56-pkgdown-content-and-widget-build-contract/56-CONTEXT.md</file>
    <file>.planning/phases/56-pkgdown-content-and-widget-build-contract/56-RESEARCH.md</file>
    <file>.github/workflows/pkgdown.yaml</file>
    <file>DESCRIPTION</file>
    <file>_pkgdown.yml</file>
  </read_first>
  <action>Preserve the existing `.github/workflows/pkgdown.yaml` dependency route per D-09: `r-lib/actions/setup-r-dependencies@v2`, `extra-packages: any::pkgdown, local::.`, and `needs: website`. Add a workflow step after setup-r-dependencies and before `Build site` named `Verify website dependencies` that runs under `shell: Rscript {0}`. The step must check `requireNamespace()` for `pkgdown`, `rmarkdown`, `knitr`, `htmlwidgets`, `sf`, and `geojsonsf`; stop if required website packages (`pkgdown`, `rmarkdown`, `knitr`, `htmlwidgets`) are missing; print package versions for available packages; and print `PKGDOWN_SF_OPTIONAL_SKIP: GitHub Actions website environment missing <package-list>` when `sf` or `geojsonsf` is unavailable. Do not add a separate `install.packages()` path because the phase decision is to harden the current `Suggests` plus `needs: website` route first.</action>
  <verify>
    <automated>rtk rg -n "Verify website dependencies|PKGDOWN_SF_OPTIONAL_SKIP|needs: website|setup-r-dependencies|build_site_github_pages" .github/workflows/pkgdown.yaml</automated>
    <automated>rtk rg -n "sf \\(>= 1\\.0\\.0\\)|geojsonsf \\(>= 2\\.0\\.0\\)|pkgdown|rmarkdown|knitr|htmlwidgets" DESCRIPTION</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/pkgdown.yaml` still contains `r-lib/actions/setup-r-dependencies@v2`, `extra-packages: any::pkgdown, local::.`, and `needs: website`.
    - `rtk rg -n "Verify website dependencies|requireNamespace|PKGDOWN_SF_OPTIONAL_SKIP" .github/workflows/pkgdown.yaml` finds the new evidence step and marker.
    - `rtk rg -n "install\\.packages" .github/workflows/pkgdown.yaml` returns no matches added for this phase.
    - `rtk rg -n "sf \\(>= 1\\.0\\.0\\)|geojsonsf \\(>= 2\\.0\\.0\\)" DESCRIPTION` confirms optional spatial dependencies remain in `Suggests`.
  </acceptance_criteria>
  <done>The pkgdown workflow reports website dependency availability before build and classifies missing optional spatial packages without replacing the existing dependency route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer -> release evidence | Maintainers interpret multiple artifact types when deciding whether docs are release-ready. |
| GitHub Actions dependency setup -> pkgdown build | Workflow dependency resolution determines whether representative site chunks render. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Repudiation | README/diagnostics/NEWS support claims | mitigate | Source docs say generated `docs/` must prove pkgdown rendering and do not claim new rendering support. |
| T-56-02 | Repudiation | optional sf dependency outcome | mitigate | Workflow emits `PKGDOWN_SF_OPTIONAL_SKIP` when CI website environment lacks `sf` or `geojsonsf`; article marker is owned by Plan 56-01. |
| T-56-03 | Repudiation | `.github/workflows/pkgdown.yaml` | mitigate | Add explicit dependency availability step before `pkgdown::build_site_github_pages(...)`, preserving `needs: website`. |
| T-56-04 | Repudiation | widget embedding evidence | transfer | Plan 56-03 verifies generated HTML widget markers and assets. |
| T-56-05 | Information Disclosure/Repudiation | artifact taxonomy docs | mitigate | Docs classify source docs, generated `docs/`, GitHub Pages output, and browser smoke artifacts by purpose and keep raw logs out of public docs. |
</threat_model>

<verification>
Run source/config checks:
```bash
rtk rg -n 'source docs|generated `docs/`|GitHub Pages|browser visual smoke|pkgdown' README.Rmd vignettes/d3-drawing-diagnostics.md NEWS.md
rtk rg -n "Verify website dependencies|PKGDOWN_SF_OPTIONAL_SKIP|needs: website|setup-r-dependencies" .github/workflows/pkgdown.yaml
```
</verification>

<success_criteria>
- Source docs distinguish all four artifact classes required by D-10 and D-11.
- The pkgdown workflow visibly reports required and optional website dependency availability before building.
- No new install machinery replaces the existing `setup-r-dependencies` plus `needs: website` route.
</success_criteria>

<output>
After completion, create `.planning/phases/56-pkgdown-content-and-widget-build-contract/56-02-SUMMARY.md`.
</output>
