---
phase: 56-pkgdown-content-and-widget-build-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - vignettes/gg2d3.Rmd
  - tests/testthat/test-pkgdown-site.R
autonomous: true
requirements:
  - DOCS-02
  - BUILD-03
user_setup: []
must_haves:
  truths:
    - "Users can find the current geom_sf polygon, point, line, and annotation support contract in the main pkgdown article source."
    - "The sf article section emits a visible classified outcome when optional spatial packages are unavailable."
    - "Maintainers have a focused test contract ready to verify source and generated pkgdown markers."
  artifacts:
    - path: "vignettes/gg2d3.Rmd"
      provides: "Canonical sf support contract and evaluated optional-dependency article chunk"
      contains: "PKGDOWN_SF_OPTIONAL_SKIP"
    - path: "tests/testthat/test-pkgdown-site.R"
      provides: "Phase 56 focused pkgdown source/generated marker tests"
      contains: "sf family maps with"
  key_links:
    - from: "vignettes/gg2d3.Rmd"
      to: "docs/articles/gg2d3.html"
      via: "pkgdown article rendering in Plan 56-03"
      pattern: "sf family maps with|PKGDOWN_SF_OPTIONAL_SKIP"
    - from: "tests/testthat/test-pkgdown-site.R"
      to: "docs/articles/gg2d3.html"
      via: "generated-file marker checks"
      pattern: "gg2d3 html-widget|d3.v7.min.js|gg2d3-modules"
---

<objective>
Make the sf pkgdown article source impossible to silently skip and create the focused Phase 56 test contract that later verifies generated site output.

Purpose: This closes the stale/missing sf support text and silent optional-dependency skip risks before generated `docs/` are rebuilt.
Output: Updated `vignettes/gg2d3.Rmd` and `tests/testthat/test-pkgdown-site.R`.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/56-pkgdown-content-and-widget-build-contract/56-CONTEXT.md
@.planning/phases/56-pkgdown-content-and-widget-build-contract/56-RESEARCH.md
@.planning/phases/56-pkgdown-content-and-widget-build-contract/56-VALIDATION.md
@.planning/phases/56-pkgdown-content-and-widget-build-contract/56-PATTERNS.md
@AGENTS.md
@CLAUDE.md
@vignettes/gg2d3.Rmd
@tests/testthat/helper-browser-visual.R
@tests/testthat/test-renderer-wiring-contracts.R

<interfaces>
From `vignettes/gg2d3.Rmd`, current sf section:
```r
### sf family maps with `geom_sf`

`geom_sf()` supports polygon-family (`POLYGON`, `MULTIPOLYGON`), point-family
(`POINT`, `MULTIPOINT`), and line-family (`LINESTRING`, `MULTILINESTRING`)
geometries.

```{r, eval = requireNamespace("sf", quietly = TRUE) && requireNamespace("geojsonsf", quietly = TRUE)}
nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)
...
```
```

From `tests/testthat/helper-browser-visual.R`, optional dependency classification pattern:
```r
browser_visual_optional_dependencies <- function(require_sf = FALSE, require_geojsonsf = FALSE) {
  missing <- character()
  if (isTRUE(require_sf) && !requireNamespace("sf", quietly = TRUE)) missing <- c(missing, "sf")
  if (isTRUE(require_geojsonsf) && !requireNamespace("geojsonsf", quietly = TRUE)) missing <- c(missing, "geojsonsf")
  if (length(missing) == 0) return(list(available = TRUE, missing = character()))
  list(available = FALSE, missing = missing)
}
```

From `tests/testthat/test-renderer-wiring-contracts.R`, file-reader pattern:
```r
read_module <- function(path) {
  installed_path <- system.file(sub("^inst/", "", path), package = "gg2d3")
  candidates <- c(path, file.path("..", "..", path), installed_path)
  resolved <- candidates[nzchar(candidates) & file.exists(candidates)][1]
  if (is.na(resolved)) stop("Cannot find module: ", path, call. = FALSE)
  paste(readLines(resolved, warn = FALSE), collapse = "\n")
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Phase 56 pkgdown marker test contract</name>
  <files>tests/testthat/test-pkgdown-site.R</files>
  <read_first>
    <file>.planning/phases/56-pkgdown-content-and-widget-build-contract/56-VALIDATION.md</file>
    <file>.planning/phases/56-pkgdown-content-and-widget-build-contract/56-PATTERNS.md</file>
    <file>tests/testthat/helper-browser-visual.R</file>
    <file>tests/testthat/test-renderer-wiring-contracts.R</file>
    <file>vignettes/gg2d3.Rmd</file>
    <file>docs/articles/gg2d3.html</file>
    <file>docs/articles/gg2d3.md</file>
  </read_first>
  <behavior>
    - Test 1: `vignettes/gg2d3.Rmd` contains `sf family maps with`, `geom_sf() supports polygon-family`, and `PKGDOWN_SF_OPTIONAL_SKIP`.
    - Test 2: helper functions are present for Plan 56-03 to enforce generated article markers: `sf family maps with`, `geom_sf() supports polygon-family`, `gg2d3 html-widget`, `d3.v7.min.js`, `gg2d3-modules`, and either `PKGDOWN_SF_OPTIONAL_SKIP` or a rendered sf widget marker.
    - Test 3: helper failure messages name the file and marker, not opaque `FALSE is not TRUE`.
  </behavior>
  <action>Create `tests/testthat/test-pkgdown-site.R` with helper functions `read_text_file(path)`, `expect_text_contains(path, markers)`, and `expect_existing_path(path)`. Include source tests that pass after Task 2 and define generated-site marker vectors/helper calls for Plan 56-03 to activate after `docs/` is rebuilt. Use exact markers from 56-VALIDATION.md: `sf family maps with`, `geom_sf() supports polygon-family`, `gg2d3 html-widget`, `d3.v7.min.js`, `gg2d3-modules`, and `PKGDOWN_SF_OPTIONAL_SKIP`. Per D-04 and D-08, do not infer widget embedding or sf classification from source code alone in final generated-site checks.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'parse("tests/testthat/test-pkgdown-site.R"); cat("pkgdown site test file parses\n")'</automated>
    <automated>rtk rg -n "sf family maps with|geom_sf\\(\\) supports polygon-family|gg2d3 html-widget|d3\\.v7\\.min\\.js|gg2d3-modules|PKGDOWN_SF_OPTIONAL_SKIP" tests/testthat/test-pkgdown-site.R</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-pkgdown-site.R` exists.
    - `rtk rg -n "read_text_file|expect_text_contains|expect_existing_path" tests/testthat/test-pkgdown-site.R` finds all three helper names.
    - `rtk rg -n "sf family maps with|geom_sf\\(\\) supports polygon-family|gg2d3 html-widget|d3\\.v7\\.min\\.js|gg2d3-modules|PKGDOWN_SF_OPTIONAL_SKIP" tests/testthat/test-pkgdown-site.R` finds every listed marker.
    - `rtk Rscript --vanilla -e 'parse("tests/testthat/test-pkgdown-site.R")'` exits 0 before Task 2.
  </acceptance_criteria>
  <done>The Phase 56 focused test file exists, names exact source/generated markers, and can be run with the validation command from 56-VALIDATION.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make sf article optional-dependency classification visible</name>
  <files>vignettes/gg2d3.Rmd, tests/testthat/test-pkgdown-site.R</files>
  <read_first>
    <file>.planning/phases/56-pkgdown-content-and-widget-build-contract/56-CONTEXT.md</file>
    <file>.planning/phases/56-pkgdown-content-and-widget-build-contract/56-RESEARCH.md</file>
    <file>vignettes/gg2d3.Rmd</file>
    <file>R/sf_utils.R</file>
    <file>tests/testthat/test-pkgdown-site.R</file>
  </read_first>
  <behavior>
    - When both `sf` and `geojsonsf` are installed, the article chunk reads the `sf` example data and returns a `gg2d3()` widget.
    - When either optional package is missing, the article output includes `PKGDOWN_SF_OPTIONAL_SKIP: sf example not rendered; missing <package-list>`.
    - The chunk is evaluated in all environments; dependency checks happen inside the chunk body, not in `eval = requireNamespace(...)`.
  </behavior>
  <action>Update the `### sf family maps with geom_sf` chunk in `vignettes/gg2d3.Rmd` per D-07 and D-08. Remove the chunk-level `eval = requireNamespace("sf", quietly = TRUE) && requireNamespace("geojsonsf", quietly = TRUE)` gate. Inside an evaluated chunk, compute `has_sf` and `has_geojsonsf`; if either is false, `cat("PKGDOWN_SF_OPTIONAL_SKIP: sf example not rendered; missing ", paste(missing, collapse = ", "), ".\\n", sep = "")`; otherwise run the existing `nc <- sf::st_read(...)` example and return `(ggplot(...) + geom_sf(...) + scale_fill_gradient(...) + labs(fill = "Area")) |> gg2d3()`. Keep the visible support-contract prose exactly present in source: `sf family maps with` and `geom_sf() supports polygon-family`.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-pkgdown-site.R")'</automated>
    <automated>rtk rg -n "PKGDOWN_SF_OPTIONAL_SKIP|has_sf|has_geojsonsf|sf example not rendered" vignettes/gg2d3.Rmd</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "PKGDOWN_SF_OPTIONAL_SKIP: sf example not rendered" vignettes/gg2d3.Rmd` finds the classified skip marker.
    - `rtk rg -n "eval = requireNamespace\\(\"sf\"" vignettes/gg2d3.Rmd` returns no matches.
    - `rtk rg -n "sf family maps with|geom_sf\\(\\) supports polygon-family" vignettes/gg2d3.Rmd` finds both support-contract markers.
    - `rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-pkgdown-site.R")'` exits 0 for source-level checks.
  </acceptance_criteria>
  <done>The sf article source always emits either a rendered widget or a visible `PKGDOWN_SF_OPTIONAL_SKIP` classification, and the focused test confirms the source markers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source docs -> generated pkgdown article | Source Rmd is evaluated by pkgdown into public HTML. |
| optional package availability -> article output | Local/CI package state controls whether the sf widget renders or the skip marker appears. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Tampering/Repudiation | `vignettes/gg2d3.Rmd` and generated article markers | mitigate | Source tests require `sf family maps with` and `geom_sf() supports polygon-family`; Plan 56-03 verifies the same markers in generated `docs/`. |
| T-56-02 | Repudiation | sf article chunk | mitigate | Replace silent chunk-level `eval` with visible `PKGDOWN_SF_OPTIONAL_SKIP` classification when `sf` or `geojsonsf` is unavailable. |
| T-56-04 | Repudiation | generated widget embedding checks | mitigate | Test contract includes generated HTML markers for `gg2d3 html-widget`, `d3.v7.min.js`, and `gg2d3-modules`; Plan 56-03 makes them green against rebuilt output. |
| T-56-03 | Repudiation | CI dependency route | transfer | Plan 56-02 owns workflow dependency evidence. |
| T-56-05 | Repudiation | artifact taxonomy | transfer | Plan 56-02 owns artifact taxonomy wording. |
</threat_model>

<verification>
Run the focused Phase 56 test contract:
```bash
rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-pkgdown-site.R")'
```
</verification>

<success_criteria>
- `vignettes/gg2d3.Rmd` contains the current sf support contract and `PKGDOWN_SF_OPTIONAL_SKIP`.
- `tests/testthat/test-pkgdown-site.R` exists and checks exact Phase 56 source/generated markers.
- No chunk-level `eval = requireNamespace("sf"...` remains in the sf article example.
</success_criteria>

<output>
After completion, create `.planning/phases/56-pkgdown-content-and-widget-build-contract/56-01-SUMMARY.md`.
</output>
