---
phase: 41-release-blocking-debt-triage
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md
  - README.Rmd
  - README.md
  - vignettes/gg2d3.Rmd
  - vignettes/d3-drawing-diagnostics.md
  - R/as_d3_ir.R
  - R/validate_ir.R
  - inst/htmlwidgets/modules/geoms/rect.js
  - tests/testthat/test-regression-core.R
autonomous: true
requirements: [DEBT-02]
user_setup: []
must_haves:
  truths:
    - "Ordinary `geom_polygon()` support signaling is not conflated with supported `geom_sf()` polygon-family rendering."
    - "If ordinary `geom_polygon()` remains unsupported, the audit records it as a deferred non-blocker with rationale and next-step guidance."
    - "Rect/tile out-of-bounds behavior is characterized, inaccurate diagnostic wording is corrected, and residual risk is classified."
  artifacts:
    - path: ".planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md"
      provides: "release-facing DEBT-02 classification"
    - path: "README.Rmd"
      provides: "source public support contract"
    - path: "vignettes/d3-drawing-diagnostics.md"
      provides: "known edge-case diagnostics"
  key_links:
    - from: "R/as_d3_ir.R"
      to: "inst/htmlwidgets/modules/geom-registry.js"
      via: "GeomPolygon maps to polygon IR but no polygon renderer is registered"
      pattern: "GeomPolygon|register"
---

<objective>
Triage stale renderer and documentation debt for Phase 41 DEBT-02.

Purpose: Maintainers need a truthful release contract for ordinary `geom_polygon()` and rect/tile out-of-bounds behavior without expanding Phase 41 into broad renderer feature work.
Output: updated support documentation and `41-DEBT-AUDIT.md` entries that classify ordinary polygon and rect/tile debt as fixed, resolved, or deferred non-blockers with next steps.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-release-blocking-debt-triage/41-CONTEXT.md
@.planning/phases/41-release-blocking-debt-triage/41-RESEARCH.md
@.planning/phases/41-release-blocking-debt-triage/41-PATTERNS.md
@.planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md
@README.Rmd
@README.md
@vignettes/gg2d3.Rmd
@vignettes/d3-drawing-diagnostics.md
@R/as_d3_ir.R
@R/validate_ir.R
@inst/htmlwidgets/modules/geom-registry.js
@inst/htmlwidgets/modules/geoms/rect.js
@tests/testthat/test-regression-core.R
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Public docs claim ordinary `geom_polygon()` support even though no renderer exists | high | Remove or qualify support-table claims and record ordinary polygon rendering as deferred unless implemented. |
| Documentation fixes accidentally weaken the shipped `geom_sf()` polygon-family contract | high | Keep all `geom_sf()` polygon-family support text intact and explicitly distinguish it from ordinary `geom_polygon()`. |
| Rect/tile edge-case docs describe stale failure modes that do not match current renderer behavior | medium | Characterize current `rect.js` behavior and replace inaccurate wording with evidence-backed diagnostics. |
| A rushed rect/tile fix creates visual regressions shortly before release validation | medium | Fix renderer behavior only when a focused reproduction proves release-blocking impact and the change is narrow. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Classify ordinary geom_polygon support signaling</name>
  <files>.planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md, README.Rmd, README.md, vignettes/gg2d3.Rmd</files>
  <read_first>
    - .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md
    - .planning/phases/41-release-blocking-debt-triage/41-CONTEXT.md
    - .planning/phases/41-release-blocking-debt-triage/41-RESEARCH.md
    - README.Rmd
    - README.md
    - vignettes/gg2d3.Rmd
    - R/as_d3_ir.R
    - R/validate_ir.R
    - inst/htmlwidgets/modules/geom-registry.js
  </read_first>
  <action>Update the `ordinary geom_polygon support signaling` entry under `## DEBT-02 Renderer and Documentation Debt` in `41-DEBT-AUDIT.md`, creating it if needed. Record evidence that `R/as_d3_ir.R` maps `GeomPolygon` to `polygon`, `R/validate_ir.R` recognizes `polygon`, and no ordinary `polygon` renderer is registered in `inst/htmlwidgets/modules/geoms`. Unless you implement an ordinary polygon renderer in this task, classify the item with `Status: Deferred non-blocker`, `Release-blocking judgment: Not blocking after documentation correction`, `Action: Correct public support signaling; do not implement ordinary polygon renderer in Phase 41`, `Rationale: v1.10 release hardening should not add new geom support; geom_sf() polygon-family rendering remains supported`, and `Next step: Plan ordinary geom_polygon renderer separately if parity coverage requires it`. Update `README.Rmd` and tracked `README.md` so the Area/Ribbon support row no longer lists ordinary `geom_polygon` as supported. Add a short note near the `geom_sf()` paragraph that says ordinary `geom_polygon()` does not currently have a D3 renderer and is separate from supported `geom_sf()` polygon-family geometries. In `vignettes/gg2d3.Rmd`, keep the unsupported `geom_polygon()` example but make the text describe the actual current behavior: no ordinary polygon marks are rendered and the browser console warns that no renderer is registered, unless this task implements a real in-panel fallback.</action>
  <verify>
    <automated>rtk rg -n "ordinary geom_polygon|geom_polygon\\(\\).*does not currently have a D3 renderer|geom_sf\\(\\).*polygon-family|Deferred non-blocker" .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md README.Rmd README.md vignettes/gg2d3.Rmd</automated>
  </verify>
  <acceptance_criteria>
    - `rtk Rscript --vanilla -e 'for (f in c("README.Rmd", "README.md")) { txt <- readLines(f); area <- grep("^\\| Area/Ribbon \\|", txt, value = TRUE); stopifnot(length(area) == 1L, !grepl("geom_polygon", area, fixed = TRUE)) }; cat("README polygon support table ok\\n")'` exits 0.
    - `rtk rg -n "ordinary geom_polygon|geom_polygon\\(\\).*does not currently have a D3 renderer|separate from supported geom_sf\\(\\) polygon-family" README.Rmd README.md` returns matches.
    - `rtk rg -n "ordinary geom_polygon support signaling.*Deferred non-blocker|Next step: Plan ordinary geom_polygon renderer separately" .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md` returns a match.
    - `rtk rg -n "unsupported geom: polygon|no renderer registered|ordinary polygon marks" vignettes/gg2d3.Rmd` returns matches.
  </acceptance_criteria>
  <done>Ordinary `geom_polygon()` is no longer advertised as supported and its deferred status is explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Characterize rect/tile out-of-bounds behavior</name>
  <files>.planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md, vignettes/d3-drawing-diagnostics.md, tests/testthat/test-regression-core.R</files>
  <read_first>
    - .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md
    - vignettes/d3-drawing-diagnostics.md
    - inst/htmlwidgets/modules/geoms/rect.js
    - tests/testthat/test-regression-core.R
    - .planning/phases/41-release-blocking-debt-triage/41-RESEARCH.md
  </read_first>
  <action>Update or create the `rect/tile out-of-bounds behavior` entry in `41-DEBT-AUDIT.md`. Record that `inst/htmlwidgets/modules/geoms/rect.js` uses `Math.abs()` for continuous rectangle width/height and renders inside the clipped panel group, so the stale diagnostic phrase `negative widths/heights` should not remain unless a focused reproduction proves it. Update `vignettes/d3-drawing-diagnostics.md` `## Rect/tile edge cases` to avoid claiming negative widths/heights. Use this exact replacement meaning: `geom_rect` and `geom_tile` are clipped at the panel boundary, but rectangles whose bounds extend beyond scale limits or interact with transformed/reversed scales remain a known edge case; the current release treats this as deferred non-blocking renderer debt unless a focused regression proves otherwise. Add a narrow regression/source guard in `tests/testthat/test-regression-core.R` only if it can be done without browser tooling; acceptable guards include checking that basic `geom_rect` IR still contains `xmin`, `xmax`, `ymin`, and `ymax`, or checking source text for `Math.abs` and panel clipping references. Do not add screenshot-diff tooling.</action>
  <verify>
    <automated>rtk rg -n "Rect/tile edge cases|clipped at the panel boundary|known edge case|Math\\.abs|rect/tile out-of-bounds behavior" vignettes/d3-drawing-diagnostics.md inst/htmlwidgets/modules/geoms/rect.js .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "negative widths/heights" vignettes/d3-drawing-diagnostics.md` returns no matches.
    - `rtk rg -n "clipped at the panel boundary|bounds extend beyond scale limits|transformed/reversed scales|deferred non-blocking renderer debt" vignettes/d3-drawing-diagnostics.md` returns matches.
    - `rtk rg -n "rect/tile out-of-bounds behavior.*(Deferred non-blocker|Fixed|Resolved)|Math\\.abs|clipped panel group|Next step:" .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md` returns matches.
    - If `tests/testthat/test-regression-core.R` is edited, `rtk Rscript --vanilla -e 'devtools::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-regression-core.R")'` exits 0 or the audit records the exact skip/blocker reason.
  </acceptance_criteria>
  <done>Rect/tile out-of-bounds behavior is characterized and stale diagnostic wording is corrected.</done>
</task>

<task type="auto">
  <name>Task 3: Verify support-contract consistency</name>
  <files>.planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md, README.Rmd, README.md, vignettes/gg2d3.Rmd, vignettes/d3-drawing-diagnostics.md</files>
  <read_first>
    - .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md
    - README.Rmd
    - README.md
    - vignettes/gg2d3.Rmd
    - vignettes/d3-drawing-diagnostics.md
    - R/as_d3_ir.R
    - R/validate_ir.R
    - inst/htmlwidgets/modules/geom-registry.js
  </read_first>
  <action>Run a final consistency pass across docs and audit. Ensure `geom_sf()` polygon-family support remains described as supported in README and vignettes. Ensure ordinary `geom_polygon()` is either absent from supported-geom tables or explicitly marked unsupported/deferred. Ensure every DEBT-02 row in `41-DEBT-AUDIT.md` has a non-pending status and a `Next step:` value. Add a `### DEBT-02 evidence` subsection under `## Verification Evidence` with the exact commands run and outcomes. Do not modify generated root `README.html`.</action>
  <verify>
    <automated>rtk rg -n "### DEBT-02 evidence|ordinary geom_polygon support signaling.*Deferred non-blocker|rect/tile out-of-bounds behavior.*(Deferred non-blocker|Fixed|Resolved)|geom_sf\\(\\).*polygon-family" .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md README.Rmd README.md vignettes/gg2d3.Rmd vignettes/d3-drawing-diagnostics.md</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "geom_sf\\(\\).*polygon-family|POLYGON|MULTIPOLYGON" README.Rmd README.md vignettes/gg2d3.Rmd` returns matches.
    - `rtk Rscript --vanilla -e 'txt <- readLines(".planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md"); debt02 <- txt[grep("## DEBT-02 Renderer and Documentation Debt", txt):grep("## Verification Evidence", txt)]; stopifnot(!any(grepl("Pending verification", debt02)), any(grepl("ordinary geom_polygon support signaling", debt02)), any(grepl("rect/tile out-of-bounds behavior", debt02)), any(grepl("Next step:", debt02))); cat("DEBT-02 audit statuses complete\\n")'` exits 0.
    - `rtk rg -n "### DEBT-02 evidence|README polygon support table ok|DEBT-02 audit statuses complete" .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md` returns matches.
  </acceptance_criteria>
  <done>DEBT-02 documentation, audit status, and support-contract language are internally consistent.</done>
</task>

</tasks>

<verification>
Run:

```bash
rtk Rscript --vanilla -e 'for (f in c("README.Rmd", "README.md")) { txt <- readLines(f); area <- grep("^\\| Area/Ribbon \\|", txt, value = TRUE); stopifnot(length(area) == 1L, !grepl("geom_polygon", area, fixed = TRUE)) }; cat("README polygon support table ok\n")'
rtk rg -n "ordinary geom_polygon|geom_polygon\\(\\).*does not currently have a D3 renderer|geom_sf\\(\\).*polygon-family" README.Rmd README.md vignettes/gg2d3.Rmd
rtk rg -n "negative widths/heights" vignettes/d3-drawing-diagnostics.md
rtk rg -n "ordinary geom_polygon support signaling.*Deferred non-blocker|rect/tile out-of-bounds behavior.*(Deferred non-blocker|Fixed|Resolved)|### DEBT-02 evidence" .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md
```
</verification>

<success_criteria>
- DEBT-02 is covered by `41-DEBT-AUDIT.md`.
- Ordinary `geom_polygon()` is not advertised as supported unless a renderer is implemented.
- Supported `geom_sf()` polygon-family docs remain intact.
- Rect/tile out-of-bounds debt is characterized and stale diagnostic wording is corrected.
- No DEBT-02 item remains `Pending verification`.
</success_criteria>
