---
phase: 32-geom-sf-ir-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/sf_utils.R
  - tests/testthat/test-sf-utils.R
autonomous: true
requirements:
  - SFIR-01
  - SFIR-02
  - SFIR-03
user_setup: []
must_haves:
  truths:
    - "sf helper code can classify and retain POLYGON and MULTIPOLYGON geometries"
    - "sf helper code warns and skips unsupported, empty, invalid, or missing geometries without losing accepted source row identity"
    - "sf helper code warns when CRS is missing and serializes coordinates as-is"
    - "sf helper code continues to normalize known CRS inputs to EPSG:4326 before serialization"
  artifacts:
    - path: "R/sf_utils.R"
      provides: "sf geometry preparation helpers for polygon-family IR"
      contains: "prepare_sf_geometry_ir"
    - path: "tests/testthat/test-sf-utils.R"
      provides: "helper tests for polygon filtering, diagnostics, and missing CRS warnings"
      contains: "prepare_sf_geometry_ir"
  key_links:
    - from: "R/sf_utils.R"
      to: "tests/testthat/test-sf-utils.R"
      via: "direct helper tests"
      pattern: "prepare_sf_geometry_ir"
---

<objective>
Harden the low-level sf helper layer so Phase 32 has a reusable, tested way to prepare polygon-family geometries for IR integration.

Purpose: `as_d3_ir()` should not own all sf edge-case logic inline. A focused helper keeps unsupported geometry filtering, CRS warnings, and diagnostics testable before the IR branch consumes them.
Output: Updated `R/sf_utils.R` helpers and expanded `tests/testthat/test-sf-utils.R` coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-geom-sf-ir-foundation/32-CONTEXT.md
@.planning/phases/32-geom-sf-ir-foundation/32-RESEARCH.md
@.planning/phases/32-geom-sf-ir-foundation/32-VALIDATION.md
@.planning/phases/32-geom-sf-ir-foundation/32-PATTERNS.md
@R/sf_utils.R
@tests/testthat/test-sf-utils.R
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Unsupported non-polygon geometry is silently serialized and later rendered as misleading map output | medium | Helper must classify unsupported rows, skip them, warn, and record diagnostics. |
| Missing CRS is silently normalized incorrectly or hidden from the user | medium | Helper must warn when CRS is missing and serialize as-is without claiming EPSG:4326. |
| Filtering changes row identity and breaks future interactivity joins | high | Helper diagnostics and filtered data must preserve source row numbers as `row_id`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add polygon-family preparation helper</name>
  <files>R/sf_utils.R</files>
  <read_first>R/sf_utils.R, .planning/phases/32-geom-sf-ir-foundation/32-CONTEXT.md, .planning/phases/32-geom-sf-ir-foundation/32-RESEARCH.md</read_first>
  <action>Add an internal helper named `prepare_sf_geometry_ir(df, supported_types = c("POLYGON", "MULTIPOLYGON"), warn = TRUE)` to `R/sf_utils.R`. The helper must: (1) dynamically detect the `sfc` column using the existing `attr(df, "sf_column")` then class-based fallback pattern; (2) assign source row ids with `seq_len(nrow(df))`; (3) inspect each geometry with `sf::st_geometry_type(..., by_geometry = TRUE)`, `sf::st_is_empty()`, and `sf::st_is_valid()`; (4) accept only non-empty, valid `POLYGON` and `MULTIPOLYGON` rows; (5) skip unsupported, empty, invalid, or missing geometries; (6) normalize accepted known-CRS geometries to WGS84 via `normalize_to_wgs84()`; (7) serialize accepted geometries with `geojsonsf::sfc_geojson()`; and (8) return a list with fields `data`, `geometries`, `geometry`, `crs`, `geom_type`, and `sf_diagnostics`. `sf_diagnostics` must include `accepted_rows`, `skipped_rows`, `skipped`, `missing_crs`, `accepted_geometry_types`, and `unsupported_geometry_types`. Preserve existing exported helpers and their current behavior unless they can safely delegate to the new helper without breaking existing tests.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); stopifnot(exists("prepare_sf_geometry_ir", envir = asNamespace("gg2d3"), inherits = FALSE))'</verify>
  <acceptance_criteria>
    - `R/sf_utils.R` contains `prepare_sf_geometry_ir <- function`
    - `R/sf_utils.R` contains `supported_types = c("POLYGON", "MULTIPOLYGON")`
    - `R/sf_utils.R` contains `sf::st_geometry_type`
    - `R/sf_utils.R` contains `sf::st_is_empty`
    - `R/sf_utils.R` contains `sf::st_is_valid`
    - `R/sf_utils.R` contains `sf_diagnostics`
    - `R/sf_utils.R` contains `accepted_rows`
    - `R/sf_utils.R` contains `skipped_rows`
  </acceptance_criteria>
  <done>`prepare_sf_geometry_ir()` exists, returns filtered sf geometry data and diagnostics, and leaves existing sf helper tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Add helper tests for filtering, CRS, and diagnostics</name>
  <files>tests/testthat/test-sf-utils.R</files>
  <read_first>tests/testthat/test-sf-utils.R, R/sf_utils.R, .planning/phases/32-geom-sf-ir-foundation/32-VALIDATION.md</read_first>
  <action>Extend `tests/testthat/test-sf-utils.R` with tests for `prepare_sf_geometry_ir()`. Add synthetic fixtures using `sf::st_sfc()` that cover: (1) a mixed geometry column with one `POLYGON` built from a closed square ring, one `MULTIPOLYGON` built from two square polygons, one unsupported `POINT`, and one unsupported `LINESTRING`; (2) empty geometry handling using `sf::st_polygon()`; (3) invalid geometry handling using a self-intersecting bowtie polygon with coordinates `(0,0) -> (1,1) -> (1,0) -> (0,1) -> (0,0)`; and (4) missing CRS warning behavior using `crs = NA`. Assert that valid polygon-family rows are retained, unsupported rows appear in `sf_diagnostics$skipped_rows`, `sf_diagnostics$unsupported_geometry_types` includes unsupported types such as `"POINT"`, `length(result$data) == length(result$geometries)`, and retained `row_id` values match original source row numbers rather than being renumbered after filtering. Use `skip_if_not_installed("sf")` and `skip_if_not_installed("geojsonsf")` as existing sf tests do.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-utils.R")'</verify>
  <acceptance_criteria>
    - `tests/testthat/test-sf-utils.R` contains `prepare_sf_geometry_ir`
    - `tests/testthat/test-sf-utils.R` contains `unsupported_geometry_types`
    - `tests/testthat/test-sf-utils.R` contains `skipped_rows`
    - `tests/testthat/test-sf-utils.R` contains `missing CRS`
    - `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-utils.R")'` exits 0 or skips only because optional spatial packages are unavailable
  </acceptance_criteria>
  <done>Helper tests prove polygon-family filtering, diagnostics, row identity, and missing CRS warning behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-utils.R")'`
- [ ] `R/sf_utils.R` contains `prepare_sf_geometry_ir`
- [ ] `tests/testthat/test-sf-utils.R` contains tests for `skipped_rows`, `unsupported_geometry_types`, and missing CRS warnings
</verification>

<success_criteria>
- `prepare_sf_geometry_ir()` prepares accepted polygon-family geometries and diagnostics.
- Unsupported, empty, invalid, or missing geometries are warned/skipped predictably.
- Missing CRS warning behavior is tested.
- Accepted row identity is preserved through `row_id`.
</success_criteria>

<output>
After completion, create `.planning/phases/32-geom-sf-ir-foundation/32-01-SUMMARY.md`
</output>
