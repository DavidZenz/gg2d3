---
phase: 21-reference-geoms
plan: 01
type: execute
wave: 1
depends_on: ["20-01"]
files_modified:
  - R/as_d3_ir.R
  - inst/htmlwidgets/modules/geom-registry.js
  - inst/htmlwidgets/gg2d3.yaml
  - tests/testthat/test-geoms-phase4.R
files_created:
  - inst/htmlwidgets/modules/geoms/reference.js
autonomous: true

must_haves:
  truths:
    - "User can render geom_hline and geom_vline with correct intercept placement"
    - "User can render geom_abline with correct slope/intercept handling"
  artifacts:
    - path: "inst/htmlwidgets/modules/geoms/reference.js"
      provides: "D3 renderers for reference lines"
      contains: "renderHline"
---

<objective>
Implement reference geoms (hline, vline, abline) to support common annotation workflows.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for reference geoms</name>
  <files>tests/testthat/test-geoms-phase4.R</files>
  <action>Add tests validating that: (a) hline/vline/abline produce the correct geom names in the IR, (b) intercept/slope values are correctly preserved in the layer data, (c) aesthetic mappings for intercepts are captured.</action>
  <verify>Rscript -e "devtools::test(filter = 'geoms-phase4')"</verify>
  <done>New tests pass or fail as expected before/after implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Fix reference aesthetic extraction in R</name>
  <files>R/as_d3_ir.R</files>
  <action>Update the `aes` list creation in `as_d3_ir.R` to include `slope`, `intercept`, `xintercept`, and `yintercept`. Ensure these columns are NOT passed through `map_discrete` even on discrete scales (they remain numeric indices).</action>
  <verify>Rscript -e "devtools::load_all(); # verify IR for geom_hline(yintercept=...)"</verify>
  <done>IR contains correct aesthetic mappings for reference geoms.</done>
</task>

<task type="auto">
  <name>Task 3: Create reference line renderers in JS</name>
  <files>inst/htmlwidgets/modules/geoms/reference.js</files>
  <action>Implement `renderHline`, `renderVline`, and `renderAbline`. Register them with the geom registry. Implement the panel-intersection logic for abline.</action>
  <verify>Visual check of a plot with all three reference line types.</verify>
  <done>JS renderers are functional and registered.</done>
</task>

<task type="auto">
  <name>Task 4: Add JS file to widget YAML dependencies</name>
  <files>inst/htmlwidgets/gg2d3.yaml</files>
  <action>Add `modules/geoms/reference.js` to the list of script dependencies.</action>
  <verify>Confirm reference lines render in a fresh browser session.</verify>
  <done>Dependency is registered.</done>
</task>

<task type="auto">
  <name>Task 5: Update updateGeoms for animations</name>
  <files>inst/htmlwidgets/modules/geom-registry.js</files>
  <action>Update `updateGeoms` to handle repositioning of `.geom-hline`, `.geom-vline`, and `.geom-abline` during transitions.</action>
  <verify>Zoom a plot with an hline and confirm it moves smoothly.</verify>
  <done>Reference geoms support smooth transitions.</done>
</task>

</tasks>

<success_criteria>
- `geom_hline(yintercept = 20)` renders a horizontal line across the panel at Y=20.
- `geom_vline(xintercept = 5)` renders a vertical line at X=5.
- `geom_abline(slope = 1, intercept = 0)` renders a diagonal line through the origin.
- All reference lines are correctly clipped to the panel area.
</success_criteria>
