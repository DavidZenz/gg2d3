---
phase: 59-release-hygiene-and-local-spatial-recovery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/pkgdown.yaml
  - .github/workflows/browser-visual-smoke.yaml
  - .planning/phases/59-release-hygiene-and-local-spatial-recovery/59-ACTIONS-ADVISORY.md
autonomous: true
requirements:
  - REL-01
user_setup: []
must_haves:
  truths:
    - "Pkgdown and browser visual workflows no longer use stale artifact-upload actions when a current Node 24-capable stable action is viable."
    - "Any remaining GitHub Actions runtime advisory is documented as upstream-known or otherwise mitigated with a tested workflow outcome."
    - "Workflow changes preserve existing artifact names, paths, retention periods, dependency setup, and deploy ordering."
  artifacts:
    - path: ".github/workflows/pkgdown.yaml"
      provides: "Pkgdown artifact upload and deploy workflow"
      contains: "actions/upload-artifact@v6"
    - path: ".github/workflows/browser-visual-smoke.yaml"
      provides: "Browser visual artifact upload workflow"
      contains: "actions/upload-artifact@v6"
    - path: ".planning/phases/59-release-hygiene-and-local-spatial-recovery/59-ACTIONS-ADVISORY.md"
      provides: "Actions runtime advisory audit and mitigation record"
      contains: "Node 20"
  key_links:
    - from: ".planning/phases/59-release-hygiene-and-local-spatial-recovery/59-ACTIONS-ADVISORY.md"
      to: ".github/workflows/pkgdown.yaml"
      via: "workflow action version audit"
      pattern: "pkgdown.yaml"
    - from: ".planning/phases/59-release-hygiene-and-local-spatial-recovery/59-ACTIONS-ADVISORY.md"
      to: ".github/workflows/browser-visual-smoke.yaml"
      via: "workflow action version audit"
      pattern: "browser-visual-smoke.yaml"
---

<objective>
Audit the GitHub Actions runtime advisory surface and update the artifact upload actions to the latest viable Node 24-capable stable version.

Purpose: This closes or explicitly mitigates `REL-01` for the two release-facing workflows without redesigning CI.
Output: updated workflow YAML where viable and a concise advisory audit record.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-CONTEXT.md
@.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-RESEARCH.md
@.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-VALIDATION.md
@.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-PATTERNS.md
@.github/workflows/pkgdown.yaml
@.github/workflows/browser-visual-smoke.yaml
@AGENTS.md

<interfaces>
Expected workflow action target:
```yaml
uses: actions/upload-artifact@v6
```

Preserve artifact settings:
```yaml
with:
  name: <existing name>
  path: <existing path>
  if-no-files-found: <existing value>
  retention-days: 14
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Actions advisory audit record</name>
  <files>.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-ACTIONS-ADVISORY.md</files>
  <read_first>
    <file>.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-CONTEXT.md</file>
    <file>.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-RESEARCH.md</file>
    <file>.github/workflows/pkgdown.yaml</file>
    <file>.github/workflows/browser-visual-smoke.yaml</file>
  </read_first>
  <action>Create `.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-ACTIONS-ADVISORY.md` with sections named `Runtime Context`, `Workflow Audit`, `Mitigation Decision`, `Verification Plan`, and `Residual Risks`. The `Runtime Context` section must include the strings `Node 20`, `Node 24`, `June 16, 2026`, and `actions/upload-artifact@v6`. The `Workflow Audit` table must include rows for `.github/workflows/pkgdown.yaml`, `.github/workflows/browser-visual-smoke.yaml`, `actions/checkout@v6`, `actions/upload-artifact@v4`, `r-lib/actions/setup-r@v2`, `r-lib/actions/setup-r-dependencies@v2`, and `JamesIves/github-pages-deploy-action`. The `Mitigation Decision` must state that `actions/upload-artifact@v4` will be upgraded to `actions/upload-artifact@v6` unless execution-time release verification finds a blocker.</action>
  <verify>
    <automated>rtk rg -n "Runtime Context|Workflow Audit|Mitigation Decision|Verification Plan|Residual Risks|Node 20|Node 24|June 16, 2026|actions/upload-artifact@v6" .planning/phases/59-release-hygiene-and-local-spatial-recovery/59-ACTIONS-ADVISORY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-ACTIONS-ADVISORY.md` exists.
    - `rtk rg -n "Node 20|Node 24|June 16, 2026|actions/upload-artifact@v6" .planning/phases/59-release-hygiene-and-local-spatial-recovery/59-ACTIONS-ADVISORY.md` exits 0.
    - `rtk rg -n "pkgdown.yaml|browser-visual-smoke.yaml|actions/checkout@v6|actions/upload-artifact@v4|r-lib/actions/setup-r@v2|r-lib/actions/setup-r-dependencies@v2|JamesIves" .planning/phases/59-release-hygiene-and-local-spatial-recovery/59-ACTIONS-ADVISORY.md` exits 0.
  </acceptance_criteria>
  <done>The advisory surface is recorded before workflow YAML changes.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade artifact upload actions while preserving workflow behavior</name>
  <files>.github/workflows/pkgdown.yaml, .github/workflows/browser-visual-smoke.yaml</files>
  <read_first>
    <file>.github/workflows/pkgdown.yaml</file>
    <file>.github/workflows/browser-visual-smoke.yaml</file>
    <file>.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-ACTIONS-ADVISORY.md</file>
  </read_first>
  <action>In `.github/workflows/pkgdown.yaml`, replace the `Upload pkgdown site artifact` step's `uses: actions/upload-artifact@v4` line with `uses: actions/upload-artifact@v6`. In `.github/workflows/browser-visual-smoke.yaml`, replace the `Upload browser visual smoke artifacts` step's `uses: actions/upload-artifact@v4` line with `uses: actions/upload-artifact@v6`. Preserve the existing step names, artifact names `pkgdown-site-${{ github.run_id }}` and `browser-visual-smoke-${{ github.run_id }}`, paths `docs/` and `test_output/browser-visual-smoke/`, `if-no-files-found` values, `retention-days: 14`, and all preceding build/test/deploy steps. If execution-time verification shows `actions/upload-artifact@v6` is not viable for GitHub-hosted `ubuntu-latest`, revert only this version change and update `59-ACTIONS-ADVISORY.md` with the exact blocker and the latest viable action version.</action>
  <verify>
    <automated>rtk rg -n "actions/upload-artifact@v6|pkgdown-site-|browser-visual-smoke-|retention-days: 14|if-no-files-found" .github/workflows/pkgdown.yaml .github/workflows/browser-visual-smoke.yaml</automated>
    <automated>! rtk rg -n "actions/upload-artifact@v4" .github/workflows/pkgdown.yaml .github/workflows/browser-visual-smoke.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "actions/upload-artifact@v6" .github/workflows/pkgdown.yaml .github/workflows/browser-visual-smoke.yaml` finds two matches.
    - `rtk rg -n "actions/upload-artifact@v4" .github/workflows/pkgdown.yaml .github/workflows/browser-visual-smoke.yaml` finds no matches unless `59-ACTIONS-ADVISORY.md` contains `v6 blocker`.
    - `.github/workflows/pkgdown.yaml` still contains `name: pkgdown-site-${{ github.run_id }}`, `path: docs/`, `if-no-files-found: error`, and `retention-days: 14`.
    - `.github/workflows/browser-visual-smoke.yaml` still contains `name: browser-visual-smoke-${{ github.run_id }}`, `path: test_output/browser-visual-smoke/`, `if-no-files-found: warn`, and `retention-days: 14`.
  </acceptance_criteria>
  <done>The artifact upload actions are updated without changing artifact behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Update the advisory record with final source-level mitigation status</name>
  <files>.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-ACTIONS-ADVISORY.md</files>
  <read_first>
    <file>.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-ACTIONS-ADVISORY.md</file>
    <file>.github/workflows/pkgdown.yaml</file>
    <file>.github/workflows/browser-visual-smoke.yaml</file>
  </read_first>
  <action>Update the `Mitigation Decision` section of `59-ACTIONS-ADVISORY.md` after the workflow edit. It must contain one of these exact outcome strings: `source outcome: artifact upload actions upgraded to actions/upload-artifact@v6` or `source outcome: v6 blocker documented`. Add a `Next Evidence` bullet that says `Run pkgdown.yaml and browser-visual-smoke.yaml after this commit and record run IDs in 59-VERIFICATION.md`.</action>
  <verify>
    <automated>rtk rg -n "source outcome: artifact upload actions upgraded to actions/upload-artifact@v6|source outcome: v6 blocker documented|Run pkgdown.yaml and browser-visual-smoke.yaml after this commit" .planning/phases/59-release-hygiene-and-local-spatial-recovery/59-ACTIONS-ADVISORY.md</automated>
  </verify>
  <acceptance_criteria>
    - `59-ACTIONS-ADVISORY.md` contains either `source outcome: artifact upload actions upgraded to actions/upload-artifact@v6` or `source outcome: v6 blocker documented`.
    - `59-ACTIONS-ADVISORY.md` contains `Run pkgdown.yaml and browser-visual-smoke.yaml after this commit and record run IDs in 59-VERIFICATION.md`.
  </acceptance_criteria>
  <done>The source-level advisory mitigation is ready for remote workflow evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub workflow source -> GitHub-hosted runner | Updated action versions must be compatible with hosted runner behavior. |
| Workflow artifact upload -> release evidence | Artifact names and paths must remain stable for existing inspection scripts. |
| Advisory warning -> release blocker decision | Remaining warnings must be classified rather than ignored. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Denial of Service | `actions/upload-artifact@v6` runner requirement | mitigate | Use GitHub-hosted `ubuntu-latest`; record blocker if runner compatibility fails. |
| T-59-02 | Repudiation | advisory classification | mitigate | Write `59-ACTIONS-ADVISORY.md` with source outcome and next remote evidence step. |
| T-59-03 | Tampering/Repudiation | artifact upload paths | mitigate | Preserve artifact names, paths, retention, and if-no-files-found values exactly. |
</threat_model>

<verification>
Run:
```bash
rtk rg -n "actions/upload-artifact@v6|pkgdown-site-|browser-visual-smoke-|retention-days: 14" .github/workflows/pkgdown.yaml .github/workflows/browser-visual-smoke.yaml
rtk rg -n "source outcome:|Node 20|Node 24|Run pkgdown.yaml and browser-visual-smoke.yaml" .planning/phases/59-release-hygiene-and-local-spatial-recovery/59-ACTIONS-ADVISORY.md
```
</verification>

<success_criteria>
- Artifact upload actions are upgraded to `actions/upload-artifact@v6`, or an exact v6 blocker is documented.
- Existing artifact names, paths, and retention settings are preserved.
- A Phase 59 advisory audit record exists and points to the remote evidence that Plan 59-03 must collect.
</success_criteria>

<output>
After completion, create `.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-01-SUMMARY.md`.
</output>
