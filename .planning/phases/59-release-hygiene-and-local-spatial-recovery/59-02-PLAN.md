---
phase: 59-release-hygiene-and-local-spatial-recovery
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/diagnose-spatial-stack.R
  - vignettes/d3-drawing-diagnostics.md
  - README.Rmd
  - README.md
autonomous: true
requirements:
  - REL-02
user_setup: []
must_haves:
  truths:
    - "Maintainers can run a local command that reports whether `sf` and `geojsonsf` are loadable."
    - "The diagnostic output distinguishes local spatial stack repair from gg2d3 package/runtime regressions."
    - "Maintainer docs explain how to interpret and repair local `classified_skip` outcomes."
  artifacts:
    - path: "tools/diagnose-spatial-stack.R"
      provides: "Local spatial stack diagnostic command"
      contains: "Spatial stack diagnostic"
    - path: "vignettes/d3-drawing-diagnostics.md"
      provides: "Maintainer repair guidance"
      contains: "Local sf/GDAL diagnostics"
    - path: "README.Rmd"
      provides: "Source README pointer to diagnostics"
      contains: "diagnose-spatial-stack"
  key_links:
    - from: "tools/diagnose-spatial-stack.R"
      to: "tests/testthat/helper-pkgdown-site.R"
      via: "shared pkgdown sf outcome helper"
      pattern: "pkgdown_site_sf_outcome"
    - from: "vignettes/d3-drawing-diagnostics.md"
      to: "tools/diagnose-spatial-stack.R"
      via: "maintainer command documentation"
      pattern: "tools/diagnose-spatial-stack.R"
---

<objective>
Add a local spatial-stack diagnostic command and maintainer guidance for repairing `sf`/GDAL load failures.

Purpose: This gives maintainers a repeatable path from local `classified_skip` to rendered sf evidence when the local spatial stack is available.
Output: `tools/diagnose-spatial-stack.R`, diagnostics documentation, and README pointer.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-CONTEXT.md
@.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-RESEARCH.md
@.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-VALIDATION.md
@.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-PATTERNS.md
@tests/testthat/helper-pkgdown-site.R
@tools/validate-pkgdown-site.R
@tools/inspect-pkgdown-publication.R
@vignettes/d3-drawing-diagnostics.md
@README.Rmd
@README.md
@AGENTS.md

<interfaces>
Diagnostic command:
```bash
rtk Rscript --vanilla tools/diagnose-spatial-stack.R
rtk Rscript --vanilla tools/diagnose-spatial-stack.R --site-root docs
```

Required output markers:
```text
Spatial stack diagnostic
sf:
geojsonsf:
pkgdown sf outcome:
recommendation:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the spatial stack diagnostic script</name>
  <files>tools/diagnose-spatial-stack.R</files>
  <read_first>
    <file>tools/validate-pkgdown-site.R</file>
    <file>tools/inspect-pkgdown-publication.R</file>
    <file>tests/testthat/helper-pkgdown-site.R</file>
    <file>DESCRIPTION</file>
  </read_first>
  <action>Create `tools/diagnose-spatial-stack.R` as an executable Rscript entrypoint. Reuse the existing `value_after()` argument pattern and project-root discovery pattern from `tools/validate-pkgdown-site.R`. Support optional `--site-root <path>` with default `docs`. Source `tests/testthat/helper-pkgdown-site.R` after locating the project root. Print exactly one section headed `Spatial stack diagnostic`. For packages `sf` and `geojsonsf`, print lines beginning `sf:` and `geojsonsf:` with values `loadable version <version>` when `requireNamespace(package, quietly = TRUE)` succeeds, or `not_loadable: <message>` when loading fails. When `sf` is loadable, print a line beginning `sf external libraries:` using `sf::sf_extSoftVersion()` collapsed as `name=value` pairs. If the site root exists, print `pkgdown sf outcome: <rendered|classified_skip|missing>` by calling `pkgdown_site_sf_outcome(root = root, site_root = site_root)`. If the site root does not exist, print `pkgdown sf outcome: site_root_unavailable`. End with one line beginning `recommendation:` that says either `spatial stack loadable; rerun pkgdown release validation for rendered sf evidence` or `repair local sf/GDAL dynamic-library stack before expecting rendered local sf evidence`.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'parse("tools/diagnose-spatial-stack.R"); cat("script parses\\n")'</automated>
    <automated>rtk Rscript --vanilla tools/diagnose-spatial-stack.R</automated>
    <automated>rtk rg -n "Spatial stack diagnostic|pkgdown_site_sf_outcome|sf external libraries|recommendation:" tools/diagnose-spatial-stack.R</automated>
  </verify>
  <acceptance_criteria>
    - `tools/diagnose-spatial-stack.R` exists.
    - `rtk Rscript --vanilla -e 'parse("tools/diagnose-spatial-stack.R")'` exits 0.
    - `rtk Rscript --vanilla tools/diagnose-spatial-stack.R` exits 0.
    - The diagnostic command output contains `Spatial stack diagnostic`, `sf:`, `geojsonsf:`, `pkgdown sf outcome:`, and `recommendation:`.
    - `rtk rg -n "pkgdown_site_sf_outcome|sf_extSoftVersion|site_root_unavailable" tools/diagnose-spatial-stack.R` exits 0.
  </acceptance_criteria>
  <done>The local diagnostic command reports spatial package loadability and pkgdown sf outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Document local sf/GDAL repair and classification semantics</name>
  <files>vignettes/d3-drawing-diagnostics.md, README.Rmd, README.md</files>
  <read_first>
    <file>vignettes/d3-drawing-diagnostics.md</file>
    <file>README.Rmd</file>
    <file>README.md</file>
    <file>tools/diagnose-spatial-stack.R</file>
    <file>.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-CONTEXT.md</file>
  </read_first>
  <action>In `vignettes/d3-drawing-diagnostics.md`, add a subsection titled `### Local sf/GDAL diagnostics` near the existing generated-site validation or optional spatial dependency discussion. The subsection must include the exact command `rtk Rscript --vanilla tools/diagnose-spatial-stack.R`, the optional command `rtk Rscript --vanilla tools/diagnose-spatial-stack.R --site-root docs`, the output markers `sf:`, `geojsonsf:`, `pkgdown sf outcome:`, and `recommendation:`, and the exact phrase `local environment repair, not a gg2d3 regression`. Explain that `classified_skip` is acceptable in quick local validation only when the diagnostic reports `sf` or `geojsonsf` as not loadable. Explain that rendered local sf evidence requires repairing or reinstalling the local spatial stack through the maintainer's package manager/R library setup, then rerunning `rtk Rscript --vanilla tools/validate-pkgdown-site.R --mode release`. In `README.Rmd`, add a short pointer containing `tools/diagnose-spatial-stack.R` and `vignettes/d3-drawing-diagnostics.md`. Regenerate `README.md` from `README.Rmd` or mirror the same pointer manually if build tooling is unavailable, preserving source-first wording.</action>
  <verify>
    <automated>rtk rg -n "Local sf/GDAL diagnostics|tools/diagnose-spatial-stack.R|local environment repair, not a gg2d3 regression|validate-pkgdown-site.R --mode release|classified_skip" vignettes/d3-drawing-diagnostics.md</automated>
    <automated>rtk rg -n "tools/diagnose-spatial-stack.R|d3-drawing-diagnostics" README.Rmd README.md</automated>
  </verify>
  <acceptance_criteria>
    - `vignettes/d3-drawing-diagnostics.md` contains `### Local sf/GDAL diagnostics`.
    - `vignettes/d3-drawing-diagnostics.md` contains `rtk Rscript --vanilla tools/diagnose-spatial-stack.R`.
    - `vignettes/d3-drawing-diagnostics.md` contains `local environment repair, not a gg2d3 regression`.
    - `vignettes/d3-drawing-diagnostics.md` contains `rtk Rscript --vanilla tools/validate-pkgdown-site.R --mode release`.
    - `README.Rmd` and `README.md` both contain `tools/diagnose-spatial-stack.R`.
  </acceptance_criteria>
  <done>Maintainers have source and README guidance for interpreting local spatial skips.</done>
</task>

<task type="auto">
  <name>Task 3: Verify diagnostic command and docs together</name>
  <files>tools/diagnose-spatial-stack.R, vignettes/d3-drawing-diagnostics.md, README.Rmd, README.md</files>
  <read_first>
    <file>tools/diagnose-spatial-stack.R</file>
    <file>vignettes/d3-drawing-diagnostics.md</file>
    <file>README.Rmd</file>
    <file>README.md</file>
  </read_first>
  <action>Run the diagnostic command once with the default site root and once with `--site-root docs`. Capture the resulting local spatial status for the plan summary. If `README.md` was manually mirrored rather than regenerated, compare the README pointer text against `README.Rmd` and keep the wording substantively identical.</action>
  <verify>
    <automated>rtk Rscript --vanilla tools/diagnose-spatial-stack.R</automated>
    <automated>rtk Rscript --vanilla tools/diagnose-spatial-stack.R --site-root docs</automated>
    <automated>rtk rg -n "Local sf/GDAL diagnostics|diagnose-spatial-stack|pkgdown sf outcome|recommendation" tools/diagnose-spatial-stack.R vignettes/d3-drawing-diagnostics.md README.Rmd README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rtk Rscript --vanilla tools/diagnose-spatial-stack.R` exits 0.
    - `rtk Rscript --vanilla tools/diagnose-spatial-stack.R --site-root docs` exits 0.
    - `rtk rg -n "diagnose-spatial-stack" vignettes/d3-drawing-diagnostics.md README.Rmd README.md` exits 0.
  </acceptance_criteria>
  <done>The diagnostic command and documentation are mutually discoverable and runnable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local R library -> package validation | `sf` may fail before gg2d3 code runs. |
| Diagnostic output -> maintainer decision | Output must clearly distinguish local repair from package regression. |
| Source docs -> generated README/docs | Maintainer guidance must not exist only in generated output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-04 | Repudiation | local spatial classification | medium | mitigate | Add diagnostic output markers and document classification semantics. |
| T-59-05 | Denial of Service | broken local `sf` load | medium | mitigate | Use `tryCatch()`/quiet load checks so diagnostics do not abort on dynamic-library errors. |
| T-59-06 | Tampering/Repudiation | stale README pointer | low | mitigate | Update both `README.Rmd` and `README.md` or record regeneration blocker. |
</threat_model>

<verification>
Run:
```bash
rtk Rscript --vanilla tools/diagnose-spatial-stack.R
rtk Rscript --vanilla tools/diagnose-spatial-stack.R --site-root docs
rtk rg -n "Local sf/GDAL diagnostics|tools/diagnose-spatial-stack.R|local environment repair, not a gg2d3 regression" vignettes/d3-drawing-diagnostics.md README.Rmd README.md
```
</verification>

<success_criteria>
- `tools/diagnose-spatial-stack.R` reports package loadability, pkgdown sf outcome, and a recommendation.
- Maintainer docs explain how to turn local `classified_skip` into rendered sf evidence after spatial stack repair.
- Docs distinguish local environment repair from gg2d3 regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/59-release-hygiene-and-local-spatial-recovery/59-02-SUMMARY.md`.
</output>
