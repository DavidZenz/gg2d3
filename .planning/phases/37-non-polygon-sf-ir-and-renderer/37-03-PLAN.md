---
phase: 37-non-polygon-sf-ir-and-renderer
plan: 03
type: execute
wave: 3
depends_on: ["37-02"]
files_modified:
  - inst/htmlwidgets/modules/brush.js
  - inst/htmlwidgets/modules/events.js
  - inst/htmlwidgets/modules/tooltip.js
  - tests/testthat/test-sf-interactivity.R
autonomous: true
requirements: [SFGEOM-03]
must_haves:
  truths:
    - "Tooltip, hover, handler, and brush selectors target point, line, and polygon sf marks through .geom-sf or explicit family selectors."
    - "Public tooltip, handler, and brush payloads strip renderer-private fields for all sf families."
    - "Brush selection uses data-cx and data-cy for sf marks before generic path bbox fallback."
    - "Multipoint child marks sharing a row_id produce deduplicated public brush/callback rows."
  artifacts:
    - path: "inst/htmlwidgets/modules/brush.js"
      provides: "sf mark selection by representative anchor plus row-id deduplication for multipoint children"
    - path: "inst/htmlwidgets/modules/events.js"
      provides: "tooltip, hover, and handler selector/sanitization support for all sf families"
    - path: "inst/htmlwidgets/modules/tooltip.js"
      provides: "tooltip payload sanitization for new sf renderer-private fields if needed"
    - path: "tests/testthat/test-sf-interactivity.R"
      provides: "source-level selector, sanitization, and deduplication assertions"
  key_links:
    - from: "tests/testthat/test-sf-interactivity.R"
      to: "inst/htmlwidgets/modules/brush.js"
      via: "data-cx/data-cy and row_id deduplication assertions"
      pattern: "dedupe"
    - from: "tests/testthat/test-sf-interactivity.R"
      to: "inst/htmlwidgets/modules/events.js"
      via: "INTERACTIVE_SELECTORS and sanitizeEventDatum assertions"
      pattern: "geom-sf-point"
---

<objective>
Extend sf interactivity selectors and source-level sanitization checks so point and line sf families participate in the same tooltip, hover, handler, and brush paths as polygons.
</objective>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Private renderer fields leak through tooltip, handler, or brush payloads | high | Extend sanitization tests to point and line sf marks and assert `_geom`, `_centroid`, `_sfFamily`, and child-coordinate internals are absent from public payloads. |
| Browser smoke fixtures load unsafe external code | medium | Reuse local non-self-contained htmlwidgets output and Phase 36 helper paths; do not add remote browser tooling or runtime dependencies. |
| Multipoint child marks duplicate public callback rows | medium | Assert brush/handler payloads are source-row-oriented and deduplicated by `row_id` where multiple child marks share one source row. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend interactivity source tests for sf point and line marks</name>
  <files>tests/testthat/test-sf-interactivity.R</files>
  <read_first>
    - tests/testthat/test-sf-interactivity.R
    - inst/htmlwidgets/modules/brush.js
    - inst/htmlwidgets/modules/events.js
    - inst/htmlwidgets/modules/tooltip.js
    - inst/htmlwidgets/modules/geoms/sf.js
    - .planning/phases/37-non-polygon-sf-ir-and-renderer/37-CONTEXT.md
  </read_first>
  <action>Add source-level tests in `tests/testthat/test-sf-interactivity.R` that assert interactivity modules target all sf families through `.geom-sf` or through selectors that include `circle.geom-sf.geom-sf-point`, `path.geom-sf.geom-sf-line`, and `path.geom-sf.geom-sf-polygon`. Extend payload sanitization tests so private fields `c("_geom", "_centroid", "_sfFamily", "_pointIndex")` or equivalent child-marker internals are absent from tooltip, handler, and brush public payloads. Add an explicit source test for a row-id deduplication helper in `brush.js`, such as `dedupeSelectedDataByRowId`, that runs before the on-brush callback receives selected rows.</action>
  <acceptance_criteria>
    - `rtk rg -n "geom-sf-point|geom-sf-line|geom-sf-polygon|circle\\.geom-sf|\\.geom-sf|_sfFamily|_pointIndex|dedupeSelectedDataByRowId|row_id" tests/testthat/test-sf-interactivity.R` returns matches.
    - `rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-interactivity.R")'` initially fails before selector/sanitization implementation and exits 0 after implementation.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-interactivity.R")'</automated>
  </verify>
  <done>Interactivity tests define the point/line sf selector, sanitization, and row-id deduplication contract.</done>
</task>

<task type="auto">
  <name>Task 2: Update interactivity selectors and payload sanitization</name>
  <files>inst/htmlwidgets/modules/brush.js, inst/htmlwidgets/modules/events.js, inst/htmlwidgets/modules/tooltip.js</files>
  <read_first>
    - inst/htmlwidgets/modules/brush.js
    - inst/htmlwidgets/modules/events.js
    - inst/htmlwidgets/modules/tooltip.js
    - inst/htmlwidgets/modules/geoms/sf.js
    - tests/testthat/test-sf-interactivity.R
    - tests/testthat/test-sf-browser.R
  </read_first>
  <action>Update sf-related interactivity selectors so tooltip, hover, custom handlers, and brush can target point circles and line paths. Prefer shared `.geom-sf` where selector scope is safe; otherwise include explicit selectors `path.geom-sf`, `circle.geom-sf`, `.geom-sf-point`, `.geom-sf-line`, and `.geom-sf-polygon`. Update any payload cleaning helper so `_geom`, `_centroid`, `_sfFamily`, `_pointIndex`, and any child coordinate arrays used only for rendering are removed before user callbacks receive data. In `brush.js`, keep the sf branch that reads `data-cx` and `data-cy` before generic path bbox logic, and make it work for both `path.geom-sf` and `circle.geom-sf` nodes. Add a helper named `dedupeSelectedDataByRowId(selectedData)` or equivalent, call it inside `collectSelectedData()` before returning, and deduplicate sanitized rows by non-null `row_id` while preserving non-sf rows without `row_id`.</action>
  <acceptance_criteria>
    - `rtk rg -n "\\.geom-sf|circle\\.geom-sf|geom-sf-point|geom-sf-line|geom-sf-polygon|data-cx|data-cy|_sfFamily|_pointIndex|dedupeSelectedDataByRowId|row_id" inst/htmlwidgets/modules/brush.js inst/htmlwidgets/modules/events.js inst/htmlwidgets/modules/tooltip.js` returns matches.
    - `rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-interactivity.R")'` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-interactivity.R")'</automated>
  </verify>
  <done>Existing interactivity modules can reach all sf families, sanitize public payloads, and deduplicate multipoint child selections by source row.</done>
</task>

</tasks>

<verification>
Run:

```bash
rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-interactivity.R")'
```

The interactivity source tests must pass. Browser smoke coverage follows in Plan 37-04.
</verification>

<success_criteria>
- SFGEOM-03 selector and payload-sanitization behavior is covered at source level.
- Point and line sf marks participate in existing tooltip, hover, handler, and brush selector paths.
- Public callback payloads remain sanitized and source-row-oriented.
- Brush logic uses sf `data-cx` and `data-cy` for both path and circle sf marks.
</success_criteria>
