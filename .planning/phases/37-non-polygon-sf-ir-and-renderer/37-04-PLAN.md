---
phase: 37-non-polygon-sf-ir-and-renderer
plan: 04
type: execute
wave: 4
depends_on:
  - 37-03
files_modified:
  - DESCRIPTION
  - tests/testthat/helper-sf-fixtures.R
  - tests/testthat/helper-browser-sf.R
  - tests/testthat/test-sf-browser.R
autonomous: true
requirements:
  - SFGEOM-01
  - SFGEOM-02
  - SFGEOM-03
  - SFGEOM-04
must_haves:
  truths:
    - "Browser smoke fixtures include point-only, line-only, polygon+point overlay, polygon+line overlay, mixed accepted/skipped rows, and faceted/empty-panel sf behavior where local dependencies allow."
    - "Live DOM smoke assertions can observe `.geom-sf` marks across path and circle families without remote network dependencies."
    - "Phase 35/36 polygon browser smoke contracts remain regression gates while point and line support is added."
    - "Direct test helper dependencies used by browser fixtures are declared in DESCRIPTION Suggests."
  artifacts:
    - path: "DESCRIPTION"
      provides: "direct Suggests entries for pkgload and rprojroot test helpers"
    - path: "tests/testthat/helper-sf-fixtures.R"
      provides: "deterministic Phase 37 point, line, overlay, mixed, facet, and empty-panel fixture builders"
    - path: "tests/testthat/helper-browser-sf.R"
      provides: "browser DOM polling for `.geom-sf` marks across path and circle families"
    - path: "tests/testthat/test-sf-browser.R"
      provides: "live browser smoke assertions for point and line DOM marks plus polygon regression"
  key_links:
    - from: "tests/testthat/test-sf-browser.R"
      to: "tests/testthat/helper-browser-sf.R"
      via: "wait_for_sf_marks() or backward-compatible wait_for_sf_paths()"
      pattern: "wait_for_sf"
    - from: "tests/testthat/test-sf-browser.R"
      to: "tests/testthat/helper-sf-fixtures.R"
      via: ".phase37_sf_fixture_set()"
      pattern: "\\.phase37_sf_fixture_set"
---

<objective>
Extend the Phase 36 browser smoke harness to prove point and line sf marks render in a real browser while retaining polygon regression coverage.

Purpose: Phase 37 needs end-to-end confidence that R IR, D3 rendering, and interactivity selectors produce visible DOM marks with stable row ids and finite anchors.
Output: Phase 37 fixture builders, generalized browser DOM helpers, point/line/mixed/facet browser smoke assertions, and direct test helper dependency declarations.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-browser-sf-smoke-harness/36-REVIEW.md
@.planning/phases/37-non-polygon-sf-ir-and-renderer/37-CONTEXT.md
@.planning/phases/37-non-polygon-sf-ir-and-renderer/37-RESEARCH.md
@.planning/phases/37-non-polygon-sf-ir-and-renderer/37-VALIDATION.md
@.planning/phases/37-non-polygon-sf-ir-and-renderer/37-03-SUMMARY.md
@DESCRIPTION
@tests/testthat/helper-sf-fixtures.R
@tests/testthat/helper-browser-sf.R
@tests/testthat/test-sf-browser.R

<interfaces>
Phase 36 helper contracts to preserve:

```r
skip_browser_sf_smoke()
browser_sf_artifact_dir()
save_browser_sf_widget(widget, filename)
with_chromote_session(function(session) ...)
browser_console_collector(session)
assert_no_browser_errors(logs)
wait_for_sf_paths(session, expected, timeout = 10)
```

Phase 37 should add a generalized mark helper while keeping `wait_for_sf_paths()` working for polygon tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 37 sf browser fixtures and direct Suggests entries</name>
  <files>DESCRIPTION, tests/testthat/helper-sf-fixtures.R</files>
  <read_first>
    - .planning/phases/36-browser-sf-smoke-harness/36-REVIEW.md
    - .planning/phases/37-non-polygon-sf-ir-and-renderer/37-CONTEXT.md
    - .planning/phases/37-non-polygon-sf-ir-and-renderer/37-RESEARCH.md
    - DESCRIPTION
    - tests/testthat/helper-sf-fixtures.R
  </read_first>
  <action>
    In `DESCRIPTION`, add direct `Suggests` entries for `pkgload` and `rprojroot` if they are absent. Keep `chromote (>= 0.5.1)` in `Suggests`, do not move browser tooling to `Imports`, and do not add Playwright, Puppeteer, Selenium, Node, or remote browser dependencies.

    In `tests/testthat/helper-sf-fixtures.R`, keep all `.phase35_*` helper names and fixture filenames intact. Add Phase 37 helpers with exact names:
    - `.phase37_make_point_sf()`
    - `.phase37_make_line_sf()`
    - `.phase37_make_polygon_point_overlay()`
    - `.phase37_make_polygon_line_overlay()`
    - `.phase37_make_mixed_sf()`
    - `.phase37_make_faceted_sf()`
    - `.phase37_sf_fixture_set()`

    `.phase37_sf_fixture_set()` must save deterministic non-self-contained widgets with exact filenames:
    - `phase37-sf-point-only.html`
    - `phase37-sf-line-only.html`
    - `phase37-sf-polygon-point-overlay.html`
    - `phase37-sf-polygon-line-overlay.html`
    - `phase37-sf-mixed-skipped.html`
    - `phase37-sf-faceted-empty.html`

    Use small in-memory `sf::st_sf()` fixtures with CRS 4326. Mixed skipped fixture must include accepted polygon, point, and line rows plus unsupported `GEOMETRYCOLLECTION` or empty geometry rows. Overlay fixtures must use coordinates inside the polygon bbox so shared projection alignment is checkable from finite DOM anchors.
  </action>
  <acceptance_criteria>
    - `rtk rg -n "pkgload|rprojroot|chromote \\(>= 0\\.5\\.1\\)" DESCRIPTION` returns matches in `Suggests`.
    - `rtk rg -n "\\.phase37_make_point_sf|\\.phase37_make_line_sf|\\.phase37_make_polygon_point_overlay|\\.phase37_make_polygon_line_overlay|\\.phase37_make_mixed_sf|\\.phase37_make_faceted_sf|\\.phase37_sf_fixture_set" tests/testthat/helper-sf-fixtures.R` returns all helper names.
    - `rtk rg -n "phase37-sf-point-only\\.html|phase37-sf-line-only\\.html|phase37-sf-polygon-point-overlay\\.html|phase37-sf-polygon-line-overlay\\.html|phase37-sf-mixed-skipped\\.html|phase37-sf-faceted-empty\\.html" tests/testthat/helper-sf-fixtures.R` returns all six fixture filenames.
    - `rtk rg -n "playwright|puppeteer|selenium|node" DESCRIPTION tests/testthat/helper-sf-fixtures.R` returns no matches except unrelated prose if already present before this task.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --vanilla -e 'source("tests/testthat/helper-sf-fixtures.R"); stopifnot(is.function(.phase37_sf_fixture_set), is.function(.phase37_make_point_sf), is.function(.phase37_make_line_sf))'</automated>
  </verify>
  <done>Phase 37 fixture builders exist, save deterministic local browser artifacts, and direct helper dependencies are declared as optional test dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Add browser DOM smoke assertions for sf point and line families</name>
  <files>tests/testthat/helper-browser-sf.R, tests/testthat/test-sf-browser.R</files>
  <read_first>
    - .planning/phases/36-browser-sf-smoke-harness/36-REVIEW.md
    - .planning/phases/37-non-polygon-sf-ir-and-renderer/37-CONTEXT.md
    - .planning/phases/37-non-polygon-sf-ir-and-renderer/37-RESEARCH.md
    - tests/testthat/helper-browser-sf.R
    - tests/testthat/helper-sf-fixtures.R
    - tests/testthat/test-sf-browser.R
    - inst/htmlwidgets/modules/geoms/sf.js
  </read_first>
  <action>
    In `tests/testthat/helper-browser-sf.R`, add a generalized helper named `wait_for_sf_marks(session, expected, timeout = 10)` that polls `document.querySelectorAll(".geom-sf")` and returns objects with `tag`, `className`, `rowId`, `dataCx`, `dataCy`, numeric `cx`, numeric `cy`, `d`, `r`, and `fill`. Keep `wait_for_sf_paths()` available for existing Phase 36 polygon tests; it can call a path-specific query or remain unchanged.

    In `tests/testthat/test-sf-browser.R`, keep existing Phase 35/36 tests and add Phase 37 browser tests with exact names:
    - `"SFGEOM-03 DOM: point and line sf fixtures render live geom-sf marks"`
    - `"SFGEOM-04 DOM: mixed sf overlays and facets keep panel-local marks"`

    The point/line DOM test must use `.phase37_sf_fixture_set()`, navigate `phase37-sf-point-only.html` and `phase37-sf-line-only.html`, call `wait_for_sf_marks()`, and assert:
    - point fixture has at least one `circle` mark with class containing `geom-sf-point`, finite numeric `cx`, finite numeric `cy`, finite numeric `r`, non-empty `rowId`, finite `data-cx`, and finite `data-cy`;
    - line fixture has at least one `path` mark with class containing `geom-sf-line`, non-empty `d`, `fill` equal to `"none"` or computed no-fill equivalent, non-empty `rowId`, finite `data-cx`, and finite `data-cy`;
    - `assert_no_browser_errors(logs)` passes.

    The mixed/facet DOM test must navigate `phase37-sf-polygon-point-overlay.html`, `phase37-sf-polygon-line-overlay.html`, `phase37-sf-mixed-skipped.html`, and `phase37-sf-faceted-empty.html`; assert polygon, point, and line family classes are present as applicable; assert skipped unsupported row ids are absent; and tighten the existing Phase 36 facet panel-count assertion by comparing `panel_counts` directly to an expected vector instead of `sort(panel_counts)`.
  </action>
  <acceptance_criteria>
    - `rtk rg -n "wait_for_sf_marks|document\\.querySelectorAll\\(\\\"\\.geom-sf\\\"\\)|className|rowId|dataCx|dataCy" tests/testthat/helper-browser-sf.R` returns matches.
    - `rtk rg -n "SFGEOM-03 DOM: point and line sf fixtures render live geom-sf marks|SFGEOM-04 DOM: mixed sf overlays and facets keep panel-local marks|geom-sf-point|geom-sf-line|geom-sf-polygon|phase37-sf-point-only|phase37-sf-line-only|phase37-sf-faceted-empty" tests/testthat/test-sf-browser.R` returns matches.
    - `rtk rg -n "sort\\(panel_counts\\)" tests/testthat/test-sf-browser.R` returns no matches.
    - `rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-browser.R")'` completes with zero failures or skips cleanly when optional browser/spatial dependencies are unavailable.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-browser.R")'</automated>
  </verify>
  <done>Browser smoke coverage proves representative point and line sf DOM marks render with row ids, finite anchors, no runtime errors, mixed overlay behavior, faceted panel identity, and polygon regression coverage intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture generation -> browser | Locally generated htmlwidgets files are opened in Chrome via chromote. |
| Browser DOM -> test process | DOM values and callback payloads are evaluated and returned to R tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-10 | Information Disclosure | browser callback payload tests | mitigate | Reuse sanitized payload assertions and include `_pointCoord`/`_pointIndex` in private field checks where browser payloads are inspected. |
| T-37-11 | Tampering | browser fixture files | mitigate | Save fixtures under deterministic local `test_output/browser-sf` paths and assert exact Phase 37 filenames. |
| T-37-12 | Elevation of Privilege | browser automation dependencies | mitigate | Keep `chromote` optional in `Suggests`; do not add remote scripts, Playwright, Puppeteer, Selenium, or browser automation runtime dependencies. |
| T-37-13 | Injection | JS snippets in browser tests | mitigate | Keep evaluated snippets static strings with no user-provided interpolation except local file URLs passed through existing helper functions. |
</threat_model>

<verification>
Run the browser smoke file and the full Phase 37 suite:

```bash
rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-browser.R")'
rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-ir.R"); testthat::test_file("tests/testthat/test-sf-renderer.R"); testthat::test_file("tests/testthat/test-sf-interactivity.R"); testthat::test_file("tests/testthat/test-sf-browser.R")'
```
</verification>

<success_criteria>
The Phase 36 browser harness now covers representative point and line sf rendering, mixed overlays, skipped-row absence, facet panel identity, and continued polygon regression behavior without new runtime browser dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/37-non-polygon-sf-ir-and-renderer/37-04-SUMMARY.md`
</output>
