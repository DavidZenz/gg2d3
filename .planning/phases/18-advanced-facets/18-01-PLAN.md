---
phase: 18-advanced-facets
plan: 01
type: execute
wave: 1
depends_on: ["17-01"]
files_modified:
  - R/as_d3_ir.R
  - inst/htmlwidgets/modules/layout.js
  - inst/htmlwidgets/gg2d3.js
  - tests/testthat/test-facets.R
autonomous: true

must_haves:
  truths:
    - "User sees nested facet_grid strips correctly rendered for multiple variables"
    - "User sees facet labels respect theme justifications and rotations"
  artifacts:
    - path: "tests/testthat/test-facets.R"
      provides: "Regression tests for nested faceting and theme parity"
      contains: "facet_grid"
---

<objective>
Implement support for nested facets (multiple variables) and improve theme parity for facet labels.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for nested facets</name>
  <files>tests/testthat/test-facets.R</files>
  <action>Add tests validating that: (a) nested facet_grid variables produce hierarchical strip metadata in the IR, (b) facet theme elements (rotation, justification) are correctly extracted.</action>
  <verify>Rscript -e "devtools::test(filter = 'facets')"</verify>
  <done>New tests pass or fail as expected before/after implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement hierarchical strip extraction in R</name>
  <files>R/as_d3_ir.R</files>
  <action>Refactor `as_d3_ir.R` to extract multiple strip levels for `facet_grid` and `facet_wrap`. Instead of a flat list, provide a structure that allows JS to know which label belongs to which variable level.</action>
  <verify>Rscript -e "devtools::load_all(); # verify IR structure for nested facets"</verify>
  <done>IR contains hierarchical strip metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Update layout engine for hierarchical headers</name>
  <files>inst/htmlwidgets/modules/layout.js</files>
  <action>Update `calculateLayout` to account for multiple strip levels. Calculate total header height/width based on nesting depth and position each level correctly.</action>
  <verify>Visual check of nested facet plot layout.</verify>
  <done>Layout engine allocates sufficient space for all header levels.</done>
</task>

<task type="auto">
  <name>Task 4: Update JS rendering for nested strips and theme parity</name>
  <files>inst/htmlwidgets/gg2d3.js</files>
  <action>Update strip rendering logic to loop through hierarchical levels. Apply theme-based rotations and justifications to the text elements.</action>
  <verify>Manual visual inspection of nested facets with theme adjustments.</verify>
  <done>Nested facets are rendered with full theme fidelity.</done>
</task>

</tasks>

<success_criteria>
- `facet_grid(am + vs ~ cyl)` renders three distinct header rows/columns.
- Facet labels respect `theme(strip.text = element_text(angle = ...))`.
- Interactive features (zoom/brush) continue to work correctly with nested facets.
</success_criteria>
