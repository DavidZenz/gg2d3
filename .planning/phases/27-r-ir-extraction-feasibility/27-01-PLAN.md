---
phase: 27-r-ir-extraction-feasibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/sf_utils.R
  - DESCRIPTION
  - tests/testthat/test-sf-utils.R
autonomous: true
requirements:
  - FEAS-01
  - FEAS-02
  - FEAS-03

must_haves:
  truths:
    - "extract_sf_geometries() returns a character vector of valid GeoJSON strings from an sfc column"
    - "normalize_to_wgs84() transforms any projected CRS to EPSG:4326"
    - "detect_dominant_geom_type() returns the geometry type string from an sfc column"
    - "get_layer_crs() returns a list with epsg and wkt fields from an sfc column"
    - "All sf_utils functions fail gracefully when sf/geojsonsf packages are missing"
    - "If ggplot_build() strips sfc column, fallback investigation paths are documented per D-04"
    - "Tests cover NC shapefile, rnaturalearth world borders, and projected CRS data per D-06"
  artifacts:
    - path: "R/sf_utils.R"
      provides: "sf geometry extraction utilities"
      exports: ["extract_sf_geometries", "normalize_to_wgs84", "detect_dominant_geom_type", "get_layer_crs"]
    - path: "tests/testthat/test-sf-utils.R"
      provides: "Unit tests for all sf_utils functions across three datasets"
      min_lines: 100
    - path: "DESCRIPTION"
      provides: "sf and geojsonsf in Suggests"
      contains: "geojsonsf"
  key_links:
    - from: "R/sf_utils.R"
      to: "sf::st_transform"
      via: "normalize_to_wgs84 function"
      pattern: "st_transform.*4326"
    - from: "R/sf_utils.R"
      to: "geojsonsf::sfc_geojson"
      via: "extract_sf_geometries function"
      pattern: "sfc_geojson"
---

<objective>
Create the sf geometry utility functions that extract, normalize, and serialize sf geometries from ggplot_build output.

Purpose: These functions form the R-side extraction pipeline that converts sfc geometry columns into GeoJSON character strings suitable for the IR. This is the core feasibility proof for the entire v1.7 milestone.

Output: R/sf_utils.R with four exported functions, DESCRIPTION updated with sf/geojsonsf in Suggests, comprehensive test suite covering three datasets (per D-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-r-ir-extraction-feasibility/27-CONTEXT.md
@.planning/phases/27-r-ir-extraction-feasibility/27-RESEARCH.md
@R/as_d3_ir.R
@DESCRIPTION

<interfaces>
<!-- Key patterns the executor needs from as_d3_ir.R -->

From R/as_d3_ir.R (line 184-224): geom dispatch switch maps GeomXxx classes to string names.
The fallback at line 215-223 lowercases the class name, so GeomSf -> "sf" automatically.

From R/as_d3_ir.R (line 227-238): keep_aes whitelist does NOT include "geometry".
to_rows() at line 241 intersects with keep_aes, so geometry column is silently dropped.

From DESCRIPTION: Suggests currently has testthat and crosstalk only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install geojsonsf and update DESCRIPTION</name>
  <files>DESCRIPTION</files>
  <read_first>
    - DESCRIPTION
    - .planning/phases/27-r-ir-extraction-feasibility/27-RESEARCH.md (Pitfall 4: geojsonsf not installed)
  </read_first>
  <action>
1. Install geojsonsf in R: `install.packages("geojsonsf")` (per D-03, research confirms sf 1.1.0 + Rcpp are present, geojsonsf 2.0.3 should install cleanly).

2. Update DESCRIPTION Suggests field from:
```
Suggests:
    testthat (>= 3.0.0),
    crosstalk
```
to:
```
Suggests:
    testthat (>= 3.0.0),
    crosstalk,
    sf (>= 1.0.0),
    geojsonsf (>= 2.0.0),
    rnaturalearth
```

Per D-03: sf and geojsonsf go in Suggests (not Imports) to avoid forcing GDAL/GEOS/PROJ on all users. rnaturalearth added for D-06 three-dataset test coverage.

3. Run `devtools::document()` to regenerate NAMESPACE if needed.
  </action>
  <verify>
    <automated>Rscript -e "requireNamespace('geojsonsf', quietly=TRUE)" && grep -q "geojsonsf" DESCRIPTION && grep -q "sf (>= 1.0.0)" DESCRIPTION</automated>
  </verify>
  <done>geojsonsf installed and sf/geojsonsf/rnaturalearth declared in DESCRIPTION Suggests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests for sf_utils functions (RED phase)</name>
  <files>tests/testthat/test-sf-utils.R</files>
  <read_first>
    - R/as_d3_ir.R (lines 1-50 for keep_aes, to_rows pattern; lines 184-224 for geom dispatch)
    - .planning/phases/27-r-ir-extraction-feasibility/27-RESEARCH.md (Pattern 1: Geometry-Aesthetic Split, all Code Examples, Pitfalls 1-4)
    - .planning/phases/27-r-ir-extraction-feasibility/27-CONTEXT.md (D-01, D-04, D-06, D-10, D-11, D-12)
  </read_first>
  <behavior>
    - extract_sf_geometries(df) where df has sfc geometry column -> returns character vector of GeoJSON strings, one per row
    - extract_sf_geometries(df) where df has no sfc column -> stops with error "Could not find sfc geometry column"
    - normalize_to_wgs84(sfc_col) where CRS is EPSG:4267 -> returns sfc with EPSG:4326
    - normalize_to_wgs84(sfc_col) where CRS is EPSG:3857 -> returns sfc with EPSG:4326
    - normalize_to_wgs84(sfc_col) where CRS is already EPSG:4326 -> returns unchanged
    - normalize_to_wgs84(non-sfc input) -> returns input unchanged
    - detect_dominant_geom_type(df) with MULTIPOLYGON geometry -> returns "MULTIPOLYGON"
    - get_layer_crs(df) -> returns list(epsg=4326, wkt="GEOGCRS...")
    - All functions stop with informative message when sf package not available
    - extract_sf_geometries stops with informative message when geojsonsf not available
    - FEAS-01 gate: ggplot_build() preserves sfc geometry column for geom_sf layers
    - rnaturalearth world borders: extract_sf_geometries handles complex multipolygons with holes/islands
    - Projected CRS (EPSG:3857): normalize_to_wgs84 transforms Web Mercator to WGS84
  </behavior>
  <action>
Create tests/testthat/test-sf-utils.R with test cases across three datasets (per D-06):

**Dataset 1 — NC shapefile (baseline, simple polygons):**
- `skip_if_not_installed("sf")` and `skip_if_not_installed("geojsonsf")` at top
- Build real ggplot_build output: `nc <- sf::st_read(system.file("shape/nc.shp", package="sf"), quiet=TRUE); b <- ggplot2::ggplot_build(ggplot2::ggplot(nc) + ggplot2::geom_sf()); df <- b$data[[1]]`
- FEAS-01 gate test (per D-04): verify `"geometry" %in% names(b$data[[1]])` is TRUE and `class(b$data[[1]]$geometry)` contains "sfc". **CRITICAL per D-04**: If this gate test fails (sfc column stripped by ggplot_build), the executor MUST NOT proceed to Task 3. Instead, investigate fallback paths:
  (a) Pre-build extraction from `p$layers[[i]]$data` before calling ggplot_build
  (b) Patching ggplot_build output by re-attaching geometry from original data
  (c) Using `stat_sf_coordinates` as alternative geometry source
  (d) Processing sf data outside ggplot entirely
  Document findings in the SUMMARY with viability assessment for each path.
- Test extract_sf_geometries returns character vector of length 100
- Test each GeoJSON string starts with `{"type":"` (valid GeoJSON structure)
- Test normalize_to_wgs84 with NC data (EPSG:4267) produces EPSG:4326
- Test normalize_to_wgs84 with already-WGS84 data returns unchanged CRS
- Test detect_dominant_geom_type returns "MULTIPOLYGON" for NC
- Test get_layer_crs returns list with epsg and wkt fields
- Test error on data.frame with no sfc column

**Dataset 2 — rnaturalearth world borders (complex multipolygons with holes/islands, per D-06):**
- Guard with `skip_if_not_installed("rnaturalearth")`
- `world <- rnaturalearth::ne_countries(scale = "medium", returnclass = "sf")` (or `scale = "small"` if medium unavailable)
- Build ggplot_build: `b_world <- ggplot2::ggplot_build(ggplot2::ggplot(world) + ggplot2::geom_sf())`
- Test extract_sf_geometries returns character vector with length > 100 (world has ~177 countries)
- Test GeoJSON strings are valid for complex multipolygons (some have holes — e.g., South Africa with Lesotho)
- Test detect_dominant_geom_type returns "MULTIPOLYGON" or "POLYGON" (mixed types possible)

**Dataset 3 — Projected CRS (EPSG:3857 Web Mercator, per D-06):**
- Transform NC to EPSG:3857: `nc_merc <- sf::st_transform(nc, 3857)`
- Build ggplot_build with projected data
- Test normalize_to_wgs84 correctly transforms EPSG:3857 -> EPSG:4326
- Test extract_sf_geometries on projected data still produces valid GeoJSON in WGS84

All tests should be written to FAIL initially (RED phase) since R/sf_utils.R does not yet exist.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/gg2d3 && Rscript -e "cat('Test file exists:', file.exists('tests/testthat/test-sf-utils.R'), '\n'); cat('Line count:', length(readLines('tests/testthat/test-sf-utils.R')), '\n')"</automated>
  </verify>
  <done>test-sf-utils.R exists with 14+ test_that blocks covering NC shapefile, rnaturalearth world borders, and projected CRS datasets. Tests fail because sf_utils.R does not exist yet (RED phase confirmed).</done>
</task>

<task type="auto">
  <name>Task 3: Implement R/sf_utils.R to pass all tests (GREEN phase)</name>
  <files>R/sf_utils.R</files>
  <read_first>
    - tests/testthat/test-sf-utils.R (the tests from Task 2 — these define the contract)
    - R/as_d3_ir.R (lines 1-50 for keep_aes, to_rows pattern)
    - .planning/phases/27-r-ir-extraction-feasibility/27-CONTEXT.md (D-01, D-10, D-11, D-12)
  </read_first>
  <action>
**IMPORTANT per D-04:** Before implementing, verify the FEAS-01 gate test from Task 2. If the gate test showed that ggplot_build() strips the sfc column, do NOT implement the standard extraction path. Instead:
1. Investigate the fallback paths listed in Task 2's D-04 protocol
2. Implement whichever fallback proves viable
3. Document findings in the SUMMARY

**Standard path (if FEAS-01 gate passes):**

Create R/sf_utils.R with these four exported functions (per D-01):

**extract_sf_geometries(df)**:
```r
#' Extract sf geometries from ggplot_build layer data as GeoJSON strings
#' @param df A data.frame from ggplot_build()$data[[i]] containing an sfc column
#' @return Character vector of GeoJSON geometry strings (one per row)
#' @export
extract_sf_geometries <- function(df) {
  if (!requireNamespace("sf", quietly = TRUE)) {
    stop("The 'sf' package is required for geom_sf support. ",
         "Install with: install.packages('sf')", call. = FALSE)
  }
  # Per D-12: detect geometry column dynamically
  geom_col_name <- attr(df, "sf_column")
  if (is.null(geom_col_name)) {
    geom_col_name <- names(df)[vapply(df, inherits, logical(1), "sfc")][1]
  }
  if (is.na(geom_col_name) || is.null(geom_col_name)) {
    stop("Could not find sfc geometry column in sf layer data", call. = FALSE)
  }
  geom_col <- df[[geom_col_name]]
  # Per D-11: unconditional CRS normalization to WGS84
  geom_col <- normalize_to_wgs84(geom_col)
  # Per D-10: use geojsonsf::sfc_geojson() for serialization
  if (!requireNamespace("geojsonsf", quietly = TRUE)) {
    stop("The 'geojsonsf' package is required for geom_sf support. ",
         "Install with: install.packages('geojsonsf')", call. = FALSE)
  }
  as.character(geojsonsf::sfc_geojson(geom_col))
}
```

**normalize_to_wgs84(geom_col)**:
```r
#' Normalize an sfc column to WGS84 (EPSG:4326)
#' @param geom_col An sfc geometry column
#' @return The sfc column transformed to EPSG:4326
#' @export
normalize_to_wgs84 <- function(geom_col) {
  if (!inherits(geom_col, "sfc")) return(geom_col)
  if (!requireNamespace("sf", quietly = TRUE)) {
    stop("The 'sf' package is required for CRS normalization. ",
         "Install with: install.packages('sf')", call. = FALSE)
  }
  current_crs <- sf::st_crs(geom_col)
  if (!is.na(current_crs) && current_crs != sf::st_crs(4326)) {
    geom_col <- sf::st_transform(geom_col, 4326)
  }
  geom_col
}
```

**detect_dominant_geom_type(df)**:
```r
#' Detect the dominant geometry type in an sf layer
#' @param df A data.frame from ggplot_build()$data[[i]] containing an sfc column
#' @return Character string like "MULTIPOLYGON", "POLYGON", "POINT", etc.
#' @export
detect_dominant_geom_type <- function(df) {
  if (!requireNamespace("sf", quietly = TRUE)) {
    stop("The 'sf' package is required for geom_sf support. ",
         "Install with: install.packages('sf')", call. = FALSE)
  }
  geom_col_name <- attr(df, "sf_column")
  if (is.null(geom_col_name)) {
    geom_col_name <- names(df)[vapply(df, inherits, logical(1), "sfc")][1]
  }
  if (is.na(geom_col_name) || is.null(geom_col_name)) {
    stop("Could not find sfc geometry column in sf layer data", call. = FALSE)
  }
  as.character(sf::st_geometry_type(df[[geom_col_name]], by_geometry = FALSE))
}
```

**get_layer_crs(df)**:
```r
#' Get CRS information from an sf layer's geometry column
#' @param df A data.frame from ggplot_build()$data[[i]] containing an sfc column
#' @return A list with epsg (integer or NA) and wkt (character string) fields
#' @export
get_layer_crs <- function(df) {
  if (!requireNamespace("sf", quietly = TRUE)) {
    stop("The 'sf' package is required for geom_sf support. ",
         "Install with: install.packages('sf')", call. = FALSE)
  }
  geom_col_name <- attr(df, "sf_column")
  if (is.null(geom_col_name)) {
    geom_col_name <- names(df)[vapply(df, inherits, logical(1), "sfc")][1]
  }
  if (is.na(geom_col_name) || is.null(geom_col_name)) {
    stop("Could not find sfc geometry column in sf layer data", call. = FALSE)
  }
  crs <- sf::st_crs(df[[geom_col_name]])
  list(
    epsg = if (!is.na(crs)) crs$epsg else NA_integer_,
    wkt  = if (!is.na(crs)) crs$wkt else NA_character_
  )
}
```

Add roxygen2 `@keywords internal` tags for functions not intended for end-user use.

Run tests to confirm GREEN: `Rscript -e "testthat::test_file('tests/testthat/test-sf-utils.R')"`
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/gg2d3 && Rscript -e "testthat::test_file('tests/testthat/test-sf-utils.R')"</automated>
  </verify>
  <done>All four sf_utils functions implemented with requireNamespace guards, tested against three datasets (NC shapefile, rnaturalearth world borders, projected CRS) with all tests passing. D-04 fallback protocol followed if FEAS-01 gate failed.</done>
</task>

</tasks>

<verification>
- `Rscript -e "testthat::test_file('tests/testthat/test-sf-utils.R')"` passes all tests
- `grep -c "extract_sf_geometries\|normalize_to_wgs84\|detect_dominant_geom_type\|get_layer_crs" R/sf_utils.R` returns 4+
- `grep "geojsonsf" DESCRIPTION` shows geojsonsf in Suggests
</verification>

<success_criteria>
- R/sf_utils.R contains four working exported functions
- DESCRIPTION has sf, geojsonsf, and rnaturalearth in Suggests
- All tests pass including FEAS-01 gate (geometry column survives ggplot_build)
- If FEAS-01 gate fails, fallback paths investigated per D-04 and findings documented
- FEAS-02 verified: GeoJSON serialization produces valid character vector
- FEAS-03 verified: CRS normalization works for EPSG:4267 and EPSG:3857
- D-06 satisfied: tests cover NC shapefile, rnaturalearth world borders, and projected CRS data
</success_criteria>

<output>
After completion, create `.planning/phases/27-r-ir-extraction-feasibility/27-01-SUMMARY.md`
</output>
