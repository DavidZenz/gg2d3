---
phase: 53-renderer-and-ir-contract-consolidation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - R/as_d3_ir.R
  - R/ir_layer_helpers.R
  - R/ir_theme_helpers.R
  - tests/testthat/test-ir-helper-boundaries.R
autonomous: true
requirements:
  - ARCH-02
must_haves:
  truths:
    - "High-risk theme extraction and geom parameter routing are isolated behind focused helpers per D-08 and D-09."
    - "Representative non-sf, sf, facet, scale, and annotation IR behavior remains unchanged per D-10, D-12, and D-13."
    - "Full as_d3_ir() modularization remains deferred to FUT-03 per D-11."
  artifacts:
    - path: "R/ir_theme_helpers.R"
      provides: "Internal theme element and theme IR helper boundary"
      contains: "gg2d3_ir_theme_element"
    - path: "R/ir_layer_helpers.R"
      provides: "Internal geom parameter routing helper"
      contains: "gg2d3_ir_layer_params"
    - path: "tests/testthat/test-ir-helper-boundaries.R"
      provides: "Curated helper-boundary characterization tests"
      contains: "theme helper preserves translated theme elements"
  key_links:
    - from: "R/as_d3_ir.R"
      to: "R/ir_theme_helpers.R"
      via: "gg2d3_ir_theme(th)"
      pattern: "gg2d3_ir_theme\\("
    - from: "R/as_d3_ir.R"
      to: "R/ir_layer_helpers.R"
      via: "gg2d3_ir_layer_params(layer_obj, gcl)"
      pattern: "gg2d3_ir_layer_params"
---

<objective>
Extract the next high-risk `as_d3_ir()` helper seams for theme extraction and geom parameter routing while preserving representative serialized IR behavior.

Purpose: Satisfies ARCH-02 by shrinking the remaining monolithic IR responsibilities without performing a full rewrite or introducing broad snapshots.
Output: Internal helper boundary code and focused characterization tests in the existing helper-boundary test file.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-renderer-and-ir-contract-consolidation/53-CONTEXT.md
@.planning/phases/53-renderer-and-ir-contract-consolidation/53-RESEARCH.md
@.planning/phases/53-renderer-and-ir-contract-consolidation/53-PATTERNS.md
@.planning/phases/53-renderer-and-ir-contract-consolidation/53-VALIDATION.md
@.planning/milestones/v1.12-phases/49-ir-helper-boundary-hardening/49-01-SUMMARY.md
@.planning/milestones/v1.12-phases/49-ir-helper-boundary-hardening/49-02-SUMMARY.md
@.planning/milestones/v1.12-phases/49-ir-helper-boundary-hardening/49-03-SUMMARY.md
@R/as_d3_ir.R
@R/ir_layer_helpers.R
@R/ir_scale_helpers.R
@R/ir_facet_helpers.R
@R/ggplot2_compat.R
@tests/testthat/test-ir-helper-boundaries.R

<interfaces>
Existing helper patterns to preserve:
```r
gg2d3_ir_map_discrete(values, scale_obj)
gg2d3_ir_apply_temporal_layer_columns(df, x_trans_name, y_trans_name)
gg2d3_ir_non_sf_layer(gname, df, aes, params, var_names)
gg2d3_ir_facets(build, scales, xscale_obj, yscale_obj, is_flip, is_sf_coord, x_trans_name, y_trans_name, theme, sf_panel_geometries)
```

Private ggplot2 API must remain quarantined:
```r
gg2d3_plot_theme(plot)
gg2d3_calc_element(element_name, theme, default = NULL)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add characterization tests for theme and geom parameter seams</name>
  <files>tests/testthat/test-ir-helper-boundaries.R</files>
  <read_first>
    - tests/testthat/test-ir-helper-boundaries.R
    - R/as_d3_ir.R
    - R/ir_layer_helpers.R
    - R/ggplot2_compat.R
    - .planning/phases/53-renderer-and-ir-contract-consolidation/53-CONTEXT.md
    - .planning/phases/53-renderer-and-ir-contract-consolidation/53-PATTERNS.md
  </read_first>
  <behavior>
    - A plot with explicit `theme(panel.background, panel.grid.major, axis.text.x, plot.margin, legend.key.size)` still produces the same IR theme fields after helper extraction.
    - `geom_rug(sides = "b")` preserves `ir$layers[[1]]$params$sides == "b"`.
    - `geom_dotplot(method = "histodot", binaxis = "x", stackdir = "center")` preserves `method`, `binaxis`, and `stackdir` in layer params.
    - Existing scale, layer, sf annotation, facet, free-scale, and sf bbox tests remain intact; do not replace them with one broad snapshot per D-12 and D-14.
  </behavior>
  <action>
    Extend `tests/testthat/test-ir-helper-boundaries.R` with tests named exactly `theme helper preserves translated theme elements` and `geom parameter helper preserves routed geom params`. Use inline ggplot fixtures and local `expect_*` assertions with `info = "theme helper ..."` and `info = "geom parameter helper ..."` labels. Do not require `{sf}` for these new tests. Run the focused test command once after adding tests; it may fail until Task 2 extracts the helpers, but failures must be about missing helper-boundary behavior and not syntax errors.
  </action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-ir-helper-boundaries.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "theme helper preserves translated theme elements|geom parameter helper preserves routed geom params" tests/testthat/test-ir-helper-boundaries.R` prints both exact test names.
    - `rtk rg -n "panel\\.background|panel\\.grid\\.major|axis\\.text\\.x|plot\\.margin|legend\\.key\\.size|geom_rug|geom_dotplot|stackdir|binaxis" tests/testthat/test-ir-helper-boundaries.R` prints the characterization surfaces.
    - The focused test command runs without parse/load errors; final pass may occur after Task 2.
  </acceptance_criteria>
  <done>Curated tests describe the exact theme and geom parameter IR behavior the helper extraction must preserve.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract theme and geom parameter helpers without IR drift</name>
  <files>R/as_d3_ir.R, R/ir_layer_helpers.R, R/ir_theme_helpers.R, tests/testthat/test-ir-helper-boundaries.R</files>
  <read_first>
    - R/as_d3_ir.R
    - R/ir_layer_helpers.R
    - R/ir_scale_helpers.R
    - R/ir_facet_helpers.R
    - R/ggplot2_compat.R
    - tests/testthat/test-ir-helper-boundaries.R
    - .planning/phases/53-renderer-and-ir-contract-consolidation/53-CONTEXT.md
    - .planning/phases/53-renderer-and-ir-contract-consolidation/53-RESEARCH.md
    - .planning/phases/53-renderer-and-ir-contract-consolidation/53-PATTERNS.md
  </read_first>
  <behavior>
    - `as_d3_ir()` remains the orchestrator: it still builds the ggplot, routes sf payloads, assembles coord/guides/facets/final IR, and calls `validate_ir(ir)`.
    - `R/ir_theme_helpers.R` contains `gg2d3_ir_theme_element(element_name, theme)` and `gg2d3_ir_theme(theme)` using `gg2d3_calc_element()` and `grid::convertUnit()` only.
    - `R/ir_layer_helpers.R` contains `gg2d3_ir_layer_params(layer_obj, gcl)` returning `layer_obj$aes_params` plus current `GeomRug` and `GeomDotplot` geom params.
    - `R/as_d3_ir.R` no longer defines nested `extract_theme_element()` and no longer contains the inline `g_params <- layer_obj$aes_params` routing block.
  </behavior>
  <action>
    Create `R/ir_theme_helpers.R` with unexported helpers `gg2d3_ir_theme_element()` and `gg2d3_ir_theme()`. Move the current nested `extract_theme_element()` body into `gg2d3_ir_theme_element()` without adding new `ggplot2:::` calls; keep all private ggplot2 access through `gg2d3_calc_element()`. Move the existing theme list assembly from `as_d3_ir()` into `gg2d3_ir_theme(th)` and preserve field names exactly: `panel`, `plot`, `grid`, `axis`, `global_text`, `legend`, and `strip`. Add `gg2d3_ir_layer_params(layer_obj, gcl)` to `R/ir_layer_helpers.R`, then replace the inline parameter routing in `as_d3_ir()` with `g_params <- gg2d3_ir_layer_params(layer_obj, gcl)`. Do not extract guides, coord metadata, final IR assembly, or sf payload routing in this phase per D-08 and D-11.
  </action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-ir-helper-boundaries.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "gg2d3_ir_theme_element <- function|gg2d3_ir_theme <- function" R/ir_theme_helpers.R` prints both helper definitions.
    - `rtk rg -n "gg2d3_ir_layer_params <- function" R/ir_layer_helpers.R` prints the helper definition.
    - `rtk rg -n "gg2d3_ir_theme\\(th\\)|gg2d3_ir_layer_params\\(layer_obj, gcl\\)" R/as_d3_ir.R` prints both call sites.
    - `rtk rg -n "extract_theme_element <- function|g_params <- layer_obj\\$aes_params" R/as_d3_ir.R` returns no matches.
    - `rtk rg -n "ggplot2:::" R/ir_theme_helpers.R R/ir_layer_helpers.R` returns no matches.
    - The automated test command exits 0, allowing existing `{sf}` skips when `{sf}` is not installed.
  </acceptance_criteria>
  <done>Theme extraction and geom parameter routing live behind focused helpers, and representative helper-boundary tests pass without changing serialized IR behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ggplot object -> serialized IR | Helper extraction must not alter user-visible IR shape, values, or supported optional-skip behavior. |
| private ggplot2 theme API -> internal helpers | Private API calls remain behind compatibility wrappers, not scattered across new helpers. |

No network, authentication, authorization, database, cryptography, or secret-management surface is added by this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Information Disclosure | Renderer public payloads | accept | This plan does not modify renderer payload paths; Plan 53-01 mitigates private renderer field leakage. |
| T-53-02 | Tampering / Integrity | IR helper contract tests | mitigate | Keep curated helper-boundary tests as the gate so helper extraction cannot silently change expected IR surfaces. |
| T-53-03 | Tampering / Integrity | `R/as_d3_ir.R`, `R/ir_theme_helpers.R`, `R/ir_layer_helpers.R` | mitigate | Extract only theme and geom parameter seams, keep `as_d3_ir()` orchestration intact, and run `test-ir-helper-boundaries.R`. |
</threat_model>

<verification>
Primary gate:
`rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-ir-helper-boundaries.R")'`

Combined Phase 53 quick gate after this plan and Plan 53-01:
`rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-renderer-wiring-contracts.R"); testthat::test_file("tests/testthat/test-ir-helper-boundaries.R")'`
</verification>

<success_criteria>
- High-risk theme and geom parameter seams are extracted without a full `as_d3_ir()` rewrite.
- Existing helper-boundary tests plus new theme/parameter tests pass.
- No new exported functions, public APIs, generated snapshots, pixel goldens, or new dependencies are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/53-renderer-and-ir-contract-consolidation/53-02-SUMMARY.md`.
</output>
