---
phase: 14-date-time-scales
plan: 01
type: execute
wave: 1
depends_on: ["13-03"]
files_modified:
  - R/as_d3_ir.R
  - inst/htmlwidgets/modules/scales.js
  - tests/testthat/test-date-scales.R
autonomous: true

must_haves:
  truths:
    - "User sees date axes respect configured `date_breaks` values"
    - "User sees date/datetime labels follow configured `date_labels` formatting"
    - "User sees datetime display behavior apply timezone semantics consistently with ggplot2"
  artifacts:
    - path: "tests/testthat/test-date-scales.R"
      provides: "Regression coverage for breaks, labels, and timezones"
      contains: "date_breaks"
---

<objective>
Implement full parity for date/datetime breaks, labels, and timezones to match ggplot2 behavior exactly.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for date_breaks and timezone-aware labels</name>
  <files>tests/testthat/test-date-scales.R</files>
  <action>Add tests validating that: (a) date_breaks correctly influences the number and position of breaks in the IR, (b) date_labels are extracted for both date and datetime scales, (c) timezone information is preserved and correctly reflects in formatted labels even if browser locale differs (mock/test approach).</action>
  <verify>Rscript -e "devtools::test(filter = 'date-scales')"</verify>
  <done>New tests pass or fail as expected before/after implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Harden timezone extraction and formatting in R</name>
  <files>R/as_d3_ir.R</files>
  <action>Improve `get_scale_info` to robustly extract timezone and formatting patterns. Ensure that for datetime scales, we extract the timezone offset or ensure the timestamps passed to D3 are "pre-shifted" or accompanied by enough metadata for D3 to render the correct local time of the target timezone.</action>
  <verify>Rscript -e "devtools::load_all(); # verify IR output for various TZs"</verify>
  <done>IR contains accurate timezone and formatting metadata for all tested scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Update scales.js for timezone-aware rendering</name>
  <files>inst/htmlwidgets/modules/scales.js</files>
  <action>Update `applyTemporalAxisFormat` to handle non-UTC timezones. If a timezone is provided that is not UTC, use a strategy to ensure D3 renders the labels according to that timezone (e.g., by adjusting the dates before formatting or using a UTC-offset strategy).</action>
  <verify>Check visual test output in different "simulated" timezones.</verify>
  <done>Date/datetime axes show correct labels for the specified timezone regardless of browser locale.</done>
</task>

<task type="auto">
  <name>Task 4: Verify against composed interactions (tooltips/zoom)</name>
  <files>tests/testthat/test-date-scales.R</files>
  <action>Ensure that tooltip date formatting also respects the new timezone/format parity. Add a test case with `d3_tooltip()` on a date axis.</action>
  <verify>Rscript -e "devtools::test(filter = 'date-scales')"</verify>
  <done>Tooltips show identical date formatting to the axis labels.</done>
</task>

</tasks>

<success_criteria>
- `date_breaks` in ggplot2 are reflected as exact tick positions in D3.
- `date_labels` patterns are correctly translated and applied.
- Datetime axes show the correct hour/day for the specified timezone, even if it differs from the system/browser time.
</success_criteria>
