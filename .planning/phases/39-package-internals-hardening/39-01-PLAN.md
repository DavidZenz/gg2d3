---
phase: 39-package-internals-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/as_d3_ir.R
  - R/sf_utils.R
  - tests/testthat/test-sf-utils.R
  - tests/testthat/test-sf-ir.R
autonomous: true
requirements: [HARD-01]
user_setup: []
must_haves:
  truths:
    - "`as_d3_ir()` delegates sf layer preparation and panel bbox attachment to named helpers outside `R/as_d3_ir.R`."
    - "Existing polygon, point, line, mixed, stacked, faceted, empty-panel, skipped-row, CRS, and diagnostic sf IR behavior remains unchanged."
    - "No non-sf IR behavior is changed by the sf helper extraction."
  artifacts:
    - path: "R/sf_utils.R"
      provides: "internal sf layer and panel bbox assembly helpers"
    - path: "R/as_d3_ir.R"
      provides: "reduced orchestration that calls sf helpers"
    - path: "tests/testthat/test-sf-utils.R"
      provides: "helper-level coverage for sf assembly helpers"
    - path: "tests/testthat/test-sf-ir.R"
      provides: "end-to-end sf IR drift protection"
  key_links:
    - from: "R/as_d3_ir.R"
      to: "R/sf_utils.R"
      via: "sf_layer_ir_payload() and attach_sf_panel_bboxes()"
      pattern: "sf_layer_ir_payload"
---

<objective>
Extract the remaining sf layer and panel bbox assembly logic from `as_d3_ir()` into focused helpers while preserving all sf and non-sf IR behavior.

Purpose: HARD-01 reduces risk in the highest-churn sf internals without widening scope into a general `as_d3_ir()` rewrite.
Output: helper functions in `R/sf_utils.R`, smaller sf orchestration in `R/as_d3_ir.R`, and characterization tests proving no IR drift.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-package-internals-hardening/39-RESEARCH.md
@.planning/phases/39-package-internals-hardening/39-PATTERNS.md
@R/as_d3_ir.R
@R/sf_utils.R
@tests/testthat/test-sf-utils.R
@tests/testthat/test-sf-ir.R
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Sf row identity and geometry vectors fall out of sync during extraction | high | Add helper-level tests that assert `row_id`, `accepted_rows`, `geometries`, and `data` lengths before replacing call sites. |
| Faceted panel `sf_bbox` values become global instead of panel-local | high | Keep and extend stacked/facet/empty-panel IR assertions after helper extraction. |
| Refactor changes non-sf behavior in `as_d3_ir()` | medium | Limit edits to the `gname == "sf"` branch and sf panel bbox attachment block; run `test-ir.R` after extraction. |
| Internal helper is accidentally exported as public API | low | Keep helper roxygen-free or `@noRd`; verify no new man page for helper names is generated. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add sf assembly characterization tests</name>
  <files>tests/testthat/test-sf-utils.R, tests/testthat/test-sf-ir.R</files>
  <read_first>
    - tests/testthat/test-sf-utils.R
    - tests/testthat/test-sf-ir.R
    - R/sf_utils.R
    - R/as_d3_ir.R
    - tests/testthat/helper-sf-fixtures.R
  </read_first>
  <action>Add tests before refactoring. In `tests/testthat/test-sf-utils.R`, add `test_that("HARD-01 prepare_sf_geometry_ir preserves row identity and diagnostics", { ... })` that constructs an sf data frame with polygon, point, line, and unsupported `GEOMETRYCOLLECTION` rows, calls `prepare_sf_geometry_ir()`, and asserts accepted rows `c(1L, 2L, 3L)`, skipped row `4L`, `sf_family == "mixed"`, `length(result$geometries) == nrow(result$data)`, and every output data row has non-empty `row_id` plus `.sf_family`. In `tests/testthat/test-sf-ir.R`, add `test_that("HARD-01 sf helper extraction keeps stacked and faceted bbox contracts", { ... })` covering stacked polygon+point and `facet_wrap(~facet, drop = FALSE)` with one empty panel; assert exact panel bbox values and `NULL` for empty-panel `sf_bbox`.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-utils.R"); testthat::test_file("tests/testthat/test-sf-ir.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "HARD-01 prepare_sf_geometry_ir|HARD-01 sf helper extraction|accepted_rows|skipped_rows|sf_family|sf_bbox" tests/testthat/test-sf-utils.R tests/testthat/test-sf-ir.R` returns matches.
    - `rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-utils.R"); testthat::test_file("tests/testthat/test-sf-ir.R")'` exits 0.
  </acceptance_criteria>
  <done>Baseline sf assembly and bbox behavior is pinned before extraction.</done>
</task>

<task type="auto">
  <name>Task 2: Extract sf layer and panel bbox helpers</name>
  <files>R/sf_utils.R, R/as_d3_ir.R, tests/testthat/test-sf-utils.R</files>
  <read_first>
    - R/sf_utils.R
    - R/as_d3_ir.R
    - tests/testthat/test-sf-utils.R
    - .planning/phases/39-package-internals-hardening/39-PATTERNS.md
  </read_first>
  <action>Add internal helpers to `R/sf_utils.R`: `sf_layer_ir_payload(df, aes, params, var_names)` must call `prepare_sf_geometry_ir(df)` and return a list with `layer`, `coord_geometry`, and `panel_geometries`. The `layer` field must have exactly the same sf layer fields currently built in `R/as_d3_ir.R`: `geom`, `geom_type`, `sf_family`, `geometries`, `data`, `aes`, `params`, `crs`, `sf_diagnostics`, and `var_names`. `panel_geometries` must be a named list keyed by character panel id, using accepted `PANEL` values when present and `"1"` otherwise. Add `attach_sf_panel_bboxes(panels, sf_panel_geometries)` that returns panels with `panel$sf_bbox <- sf_bbox_values(sf_panel_geometries[[panel_key]])`, using `panel$PANEL` or `1L` as the key. Replace the `gname == "sf"` branch in `R/as_d3_ir.R` so it calls `sf_layer_ir_payload()` and stores `payload$coord_geometry` and `payload$panel_geometries`, and replace the final panel bbox lapply with `attach_sf_panel_bboxes()`. Do not change non-sf branches.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-utils.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "sf_layer_ir_payload|attach_sf_panel_bboxes|panel_geometries|coord_geometry" R/sf_utils.R R/as_d3_ir.R tests/testthat/test-sf-utils.R` returns matches.
    - `rtk rg -n "geom_type|sf_family|sf_diagnostics|var_names" R/sf_utils.R` returns matches inside `sf_layer_ir_payload`.
    - `rtk rg -n "prepare_sf_geometry_ir\\(df\\)" R/as_d3_ir.R` returns no matches.
    - `rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-utils.R")'` exits 0.
  </acceptance_criteria>
  <done>`as_d3_ir()` delegates sf layer assembly and panel bbox attachment to focused helpers.</done>
</task>

<task type="auto">
  <name>Task 3: Verify sf extraction causes no IR drift</name>
  <files>R/as_d3_ir.R, R/sf_utils.R, tests/testthat/test-sf-ir.R, tests/testthat/test-sf-renderer.R, tests/testthat/test-ir.R</files>
  <read_first>
    - R/as_d3_ir.R
    - R/sf_utils.R
    - tests/testthat/test-sf-ir.R
    - tests/testthat/test-sf-renderer.R
    - tests/testthat/test-ir.R
  </read_first>
  <action>Run the sf and core IR regression commands after helper extraction. If regressions appear, fix only the helper extraction boundaries needed to restore previous IR behavior. Do not perform unrelated cleanup in `R/as_d3_ir.R`. If roxygen output changes unexpectedly, inspect and keep only changes that are a direct consequence of documented helper visibility.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-ir.R"); testthat::test_file("tests/testthat/test-sf-renderer.R"); testthat::test_file("tests/testthat/test-ir.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-ir.R"); testthat::test_file("tests/testthat/test-sf-renderer.R"); testthat::test_file("tests/testthat/test-ir.R")'` exits 0.
    - `rtk rg -n "sf_layer_ir_payload\\(|attach_sf_panel_bboxes\\(" R/as_d3_ir.R` returns matches.
    - `rtk rg -n "sf_layer_ir_payload|attach_sf_panel_bboxes" man` returns no matches.
  </acceptance_criteria>
  <done>Sf helper extraction is behavior-preserving across sf and core IR tests.</done>
</task>

</tasks>

<verification>
Run:

```bash
rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-utils.R")'
rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-sf-ir.R"); testthat::test_file("tests/testthat/test-sf-renderer.R"); testthat::test_file("tests/testthat/test-ir.R")'
```
</verification>

<success_criteria>
- HARD-01 is covered by helper-level and end-to-end IR tests.
- `as_d3_ir()` delegates sf layer preparation and panel bbox assembly to `R/sf_utils.R`.
- Existing sf and non-sf IR regression tests pass.
- No new public helper documentation is generated accidentally.
</success_criteria>
