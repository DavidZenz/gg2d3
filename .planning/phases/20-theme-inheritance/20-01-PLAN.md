---
phase: 20-theme-inheritance
plan: 01
type: execute
wave: 1
depends_on: ["19-01"]
files_modified:
  - R/as_d3_ir.R
  - inst/htmlwidgets/modules/theme.js
  - inst/htmlwidgets/modules/layout.js
  - inst/htmlwidgets/gg2d3.js
  - tests/testthat/test-layout.R
autonomous: true

must_haves:
  truths:
    - "User sees theme inheritance correctly applied (blank elements, recursive lookup)"
    - "User sees text elements respect complex theme properties (vjust, hjust, margin)"
    - "User sees legend box styling match theme configuration"
  artifacts:
    - path: "tests/testthat/test-theme-inheritance.R"
      provides: "Regression tests for deep theme inheritance"
      contains: "element_blank"
---

<objective>
Implement deep theme inheritance and comprehensive element styling to match ggplot2 visual fidelity.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for theme inheritance</name>
  <files>tests/testthat/test-theme-inheritance.R</files>
  <action>Add tests validating: (a) element_blank() removes elements from IR or marks them as blank, (b) specific theme overrides (e.g., axis.text.x) are correctly captured, (c) legend background styling is extracted.</action>
  <verify>Rscript -e "devtools::test(filter = 'theme-inheritance')"</verify>
  <done>New tests pass or fail as expected before/after implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Expand theme extraction in R</name>
  <files>R/as_d3_ir.R</files>
  <action>Update `extract_theme_element` to handle `unit` values and detailed `element_text` properties. Systematically capture more theme elements (legend.background, legend.key, strip.background, etc.) in `theme_ir`.</action>
  <verify>Rscript -e "devtools::load_all(); # verify IR output for complex themes"</verify>
  <done>IR contains exhaustive theme metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor theme module for deep lookups</name>
  <files>inst/htmlwidgets/modules/theme.js</files>
  <action>Enhance `createTheme` to support hierarchical lookups (e.g., if `axis.text.x` is missing, check `axis.text`, then `text`). Ensure `element_blank` (`type: "blank"`) is consistently handled.</action>
  <verify>Confirm that setting a global 'text' color affects all text elements in D3 unless overridden.</verify>
  <done>JS theme module supports ggplot2-style inheritance.</done>
</task>

<task type="auto">
  <name>Task 4: Update layout and rendering for detailed styling</name>
  <files>inst/htmlwidgets/modules/layout.js, inst/htmlwidgets/gg2d3.js</files>
  <action>Update `calculateLayout` to respect text margins and legend spacings. Update SVG rendering to apply justifications (`hjust`, `vjust`) and legend backgrounds.</action>
  <verify>Manual visual inspection of a plot with custom legend styling and rotated/justified text.</verify>
  <done>Layout and rendering reflect expanded theme metadata.</done>
</task>

</tasks>

<success_criteria>
- `theme(axis.text.x = element_blank())` correctly hides x-axis labels in D3.
- `theme(text = element_text(colour = "red"))` colors all text red except where specifically overridden.
- Legend background fill and color match the R `theme()` configuration.
</success_criteria>
