---
phase: 28-d3-renderer-prototyping
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - tests/testthat/test-sf-visual.R
autonomous: false
requirements: [REND-01, REND-02, REND-03]

must_haves:
  truths:
    - "NC counties shapefile renders as a filled choropleth in the browser via gg2d3 pipeline"
    - "MULTIPOLYGON features with holes render with transparent interiors (not filled)"
    - "Fill colors on path elements match the per-row aesthetic hex values from the IR"
  artifacts:
    - path: "tests/testthat/test-sf-visual.R"
      provides: "R script that generates visual test HTML files via gg2d3() pipeline"
      min_lines: 30
    - path: "test_output/phase28-nc-choropleth.html"
      provides: "Visual test: NC counties choropleth (REND-01)"
    - path: "test_output/phase28-world-holes.html"
      provides: "Visual test: World borders with multipolygon holes (REND-02/03)"
  key_links:
    - from: "tests/testthat/test-sf-visual.R"
      to: "inst/htmlwidgets/modules/geoms/sf.js"
      via: "gg2d3() pipeline renders IR through sf.js"
      pattern: "gg2d3\\("
    - from: "test_output/phase28-nc-choropleth.html"
      to: "inst/htmlwidgets/modules/geoms/sf.js"
      via: "htmlwidgets saveWidget embeds sf.js output"
      pattern: "geom-sf"
---

<objective>
Generate visual test HTML files that render geom_sf data through the full gg2d3 pipeline, then verify correct rendering via human inspection.

Purpose: REND-01 (polygon rendering), REND-02 (multipolygon holes), and REND-03 (aesthetic passthrough) all require visual verification -- automated tests cannot confirm shapes look correct or holes appear transparent. This plan generates the test artifacts and pauses for human review.

Output: Two HTML files in test_output/ and an R test script that generates them, plus human verification of visual correctness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-d3-renderer-prototyping/28-CONTEXT.md
@.planning/phases/28-d3-renderer-prototyping/28-RESEARCH.md
@.planning/phases/28-d3-renderer-prototyping/28-UI-SPEC.md
@.planning/phases/28-d3-renderer-prototyping/28-01-SUMMARY.md

@inst/htmlwidgets/modules/geoms/sf.js
@R/as_d3_ir.R

<interfaces>
<!-- From Plan 01 outputs -->

sf.js is registered as 'sf' in geomRegistry. It reads:
- layer.geometries[] -- array of GeoJSON strings
- layer.data[] -- array of aesthetic data rows with row_id, fill, colour, alpha
- options.plotWidth, options.plotHeight -- panel dimensions in px
- options.coord.bbox -- [xmin, ymin, xmax, ymax] geographic bounding box

It produces SVG <path> elements with:
- class="geom-sf"
- fill-rule="evenodd"
- data-cx, data-cy (centroid px coords)
- data-row-id (integer join key)
- fill, stroke, opacity from makeColorAccessors

R-side IR for sf layers now includes row_id in each data row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create visual test script generating NC and world choropleth HTML files</name>
  <files>tests/testthat/test-sf-visual.R</files>
  <read_first>
    - inst/htmlwidgets/modules/geoms/sf.js (the renderer being tested)
    - R/as_d3_ir.R (sf branch with row_id, lines 320-345)
    - R/gg2d3.R (gg2d3() function entry point)
    - .planning/phases/28-d3-renderer-prototyping/28-UI-SPEC.md (visual test copy strings and panel labels)
    - .planning/phases/28-d3-renderer-prototyping/28-RESEARCH.md (visual test pattern in Code Examples section)
  </read_first>
  <action>
Create `tests/testthat/test-sf-visual.R` as a testthat test file that generates visual test HTML output. The file produces two HTML files in `test_output/` for manual inspection.

Structure:

```r
# Visual tests for geom_sf D3 rendering (Phase 28)
# These tests generate HTML output files for manual visual verification.
# Run: testthat::test_file("tests/testthat/test-sf-visual.R")

test_that("REND-01: NC counties render as filled choropleth via gg2d3 pipeline", {
  skip_if_not_installed("sf")
  skip_if_not_installed("geojsonsf")

  dir.create("test_output", showWarnings = FALSE)

  nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)
  p <- ggplot2::ggplot(nc, ggplot2::aes(fill = BIR74)) + ggplot2::geom_sf()

  w <- gg2d3(p)
  expect_s3_class(w, "htmlwidget")

  outpath <- "test_output/phase28-nc-choropleth.html"
  htmlwidgets::saveWidget(w, file = normalizePath(outpath, mustWork = FALSE),
                          selfcontained = TRUE)
  expect_true(file.exists(outpath))

  # Verify the IR contains expected sf layer structure
  ir <- as_d3_ir(p)
  sf_layer <- ir$layers[[1]]
  expect_equal(sf_layer$geom, "sf")
  expect_true(length(sf_layer$geometries) > 0)
  expect_true("row_id" %in% names(sf_layer$data[[1]]))
})

test_that("REND-02/03: World borders render with multipolygon holes and correct aesthetics", {
  skip_if_not_installed("sf")
  skip_if_not_installed("geojsonsf")
  skip_if_not_installed("rnaturalearth")
  skip_if_not_installed("rnaturalearthdata")

  dir.create("test_output", showWarnings = FALSE)

  world <- rnaturalearth::ne_countries(scale = "medium", returnclass = "sf")
  p <- ggplot2::ggplot(world) +
    ggplot2::geom_sf(ggplot2::aes(fill = pop_est))

  w <- gg2d3(p)
  expect_s3_class(w, "htmlwidget")

  outpath <- "test_output/phase28-world-holes.html"
  htmlwidgets::saveWidget(w, file = normalizePath(outpath, mustWork = FALSE),
                          selfcontained = TRUE)
  expect_true(file.exists(outpath))

  # Verify IR structure
  ir <- as_d3_ir(p)
  sf_layer <- ir$layers[[1]]
  expect_equal(sf_layer$geom, "sf")
  # World data has multipolygons -- verify geom_type
  expect_true(sf_layer$geom_type %in% c("MULTIPOLYGON", "POLYGON"))
  # Verify fill aesthetic data exists
  expect_true("fill" %in% names(sf_layer$data[[1]]))
})
```

Key points:
- Uses `skip_if_not_installed()` guards for all optional packages
- Generates self-contained HTML files in `test_output/` (gitignored per CLAUDE.md)
- Verifies basic IR structure as automated assertions
- Visual verification of shapes, holes, and colors is manual (per checkpoint below)
- NC counties test covers REND-01
- World borders test covers REND-02 (holes) and REND-03 (fill aesthetic passthrough)

Run the test to generate the HTML files:
```
Rscript -e 'pkgload::load_all(); testthat::test_file("tests/testthat/test-sf-visual.R")'
```
  </action>
  <verify>
    <automated>Rscript -e 'pkgload::load_all(); testthat::test_file("tests/testthat/test-sf-visual.R")'</automated>
  </verify>
  <acceptance_criteria>
    - tests/testthat/test-sf-visual.R exists with at least 2 test_that blocks
    - tests/testthat/test-sf-visual.R contains `skip_if_not_installed("sf")`
    - tests/testthat/test-sf-visual.R contains `skip_if_not_installed("rnaturalearth")`
    - tests/testthat/test-sf-visual.R contains `htmlwidgets::saveWidget`
    - tests/testthat/test-sf-visual.R contains `gg2d3(p)` calls
    - test_output/phase28-nc-choropleth.html exists after running
    - test_output/phase28-world-holes.html exists after running
    - `Rscript -e 'pkgload::load_all(); testthat::test_file("tests/testthat/test-sf-visual.R")'` exits 0
  </acceptance_criteria>
  <done>Both visual test HTML files exist in test_output/ and were generated through the full gg2d3 pipeline (R IR -> JSON -> sf.js renderer); automated IR structure assertions pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of choropleth rendering</name>
  <what-built>
Complete geom_sf D3 rendering pipeline: R IR extraction with row_id -> JSON -> sf.js renderer using d3.geoPath + geoIdentity().reflectY(true).fitExtent(). Two test HTML files generated.
  </what-built>
  <how-to-verify>
**REND-01: NC Counties Choropleth**
1. Open `test_output/phase28-nc-choropleth.html` in a browser
2. Compare with ggplot2 reference: run `ggplot2::ggsave("test_output/nc-ref.png", ggplot2::ggplot(sf::st_read(system.file("shape/nc.shp", package="sf"), quiet=TRUE), ggplot2::aes(fill=BIR74)) + ggplot2::geom_sf())` in R
3. Verify: 100 NC county shapes are visible, filled with gradient colors, shapes match ggplot2 reference layout

**REND-02: Multipolygon Holes**
1. Open `test_output/phase28-world-holes.html` in a browser
2. Look at countries with interior water bodies: Canada (Great Lakes area), Finland (lake districts), Indonesia (island groups)
3. Verify: Interior rings (holes) render as transparent (background showing through), NOT filled with the country's color

**REND-03: Fill/Stroke Aesthetics**
1. In either HTML file, open browser devtools (F12 -> Elements)
2. Click on any `<path class="geom-sf">` element
3. Verify: `fill` attribute contains a hex color value (e.g., `#4393c3`), `stroke` attribute contains a color, `fill-rule` is `evenodd`
4. Verify: Different regions have different fill colors (not all the same)
5. Verify: `data-cx` and `data-cy` attributes contain numeric values
6. Verify: `data-row-id` attribute contains an integer
  </how-to-verify>
  <resume-signal>Type "approved" if all three REND checks pass, or describe specific issues</resume-signal>
</task>

</tasks>

<verification>
After both tasks complete:

1. `Rscript -e 'pkgload::load_all(); testthat::test_file("tests/testthat/test-sf-visual.R")'` -- visual test script passes
2. `Rscript -e 'devtools::test()'` -- full test suite green
3. `test_output/phase28-nc-choropleth.html` exists and opens in browser
4. `test_output/phase28-world-holes.html` exists and opens in browser
5. Human confirms visual correctness of all three REND requirements
</verification>

<success_criteria>
- Visual test HTML files generated through full gg2d3 pipeline
- NC counties render as filled choropleth matching ggplot2 shapes (REND-01)
- Multipolygon holes render transparent, not filled (REND-02)
- Fill/stroke hex values from IR appear correctly on path elements (REND-03)
- Human approves visual verification checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/28-d3-renderer-prototyping/28-02-SUMMARY.md`
</output>
