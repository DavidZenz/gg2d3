---
phase: 48-browser-visual-smoke-coverage
plan: 03
type: execute
wave: 3
depends_on:
  - 48-02
files_modified:
  - vignettes/d3-drawing-diagnostics.md
  - tests/testthat/helper-browser-visual.R
  - tests/testthat/test-browser-visual-smoke.R
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
must_haves:
  truths:
    - "Maintainers can find the exact quick and full browser visual smoke commands in maintainer-facing diagnostics."
    - "Documentation names the generated report and artifact directory without publishing local artifact contents."
    - "Final source checks confirm no generated outputs, browser stack additions, pixel diffs, or committed goldens were introduced."
  artifacts:
    - path: "vignettes/d3-drawing-diagnostics.md"
      provides: "Maintainer-facing browser visual smoke command, artifact contract, coverage summary, and skip semantics"
    - path: "tests/testthat/helper-browser-visual.R"
      provides: "Source-checked helper contract from Plan 01"
    - path: "tests/testthat/test-browser-visual-smoke.R"
      provides: "Source-checked fixture matrix and opt-in runner from Plan 02"
  key_links:
    - from: "vignettes/d3-drawing-diagnostics.md"
      to: "tests/testthat/test-browser-visual-smoke.R"
      via: "documented full command"
      pattern: "GG2D3_BROWSER_VISUAL_SMOKE=true.*test-browser-visual-smoke\\.R"
    - from: "vignettes/d3-drawing-diagnostics.md"
      to: "test_output/browser-visual-smoke/"
      via: "artifact path description"
      pattern: "test_output/browser-visual-smoke"
---

<objective>
Document the Phase 48 maintainer workflow and run final source hygiene checks.

Purpose: The visual smoke command must be discoverable, the artifact contract must be explicit, and final checks must prove the implementation did not introduce unignored outputs or new browser tooling. This implements D-02, D-03, D-07, D-08, D-09, D-10, and D-11.

Output: Updated `vignettes/d3-drawing-diagnostics.md` plus verification evidence.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-browser-visual-smoke-coverage/48-CONTEXT.md
@.planning/phases/48-browser-visual-smoke-coverage/48-RESEARCH.md
@.planning/phases/48-browser-visual-smoke-coverage/48-PATTERNS.md
@.planning/phases/48-browser-visual-smoke-coverage/48-VALIDATION.md
@vignettes/d3-drawing-diagnostics.md
@tests/testthat/helper-browser-visual.R
@tests/testthat/test-browser-visual-smoke.R
@.gitignore
@.Rbuildignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document command, artifacts, coverage, and skips</name>
  <read_first>
    vignettes/d3-drawing-diagnostics.md
    .planning/phases/48-browser-visual-smoke-coverage/48-CONTEXT.md
    .planning/phases/48-browser-visual-smoke-coverage/48-VALIDATION.md
    tests/testthat/test-browser-visual-smoke.R
  </read_first>
  <files>vignettes/d3-drawing-diagnostics.md</files>
  <action>
    Update `vignettes/d3-drawing-diagnostics.md` with a maintainer-facing section titled `## Browser visual smoke artifacts` near the existing browser validation discussion.

    Include these exact commands:
    - Quick skip-friendly command: `Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-browser-visual-smoke.R")'`
    - Full artifact command: `NOT_CRAN=true GG2D3_BROWSER_VISUAL_SMOKE=true Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-browser-visual-smoke.R")'`

    Document the exact artifact directory `test_output/browser-visual-smoke/` and the generated files `index.html`, `index.json`, `<fixture>.html`, `<fixture>.png`, `<fixture>-dom-summary.json`, and `<fixture>-browser-log.json`.

    State that coverage includes Cartesian geoms, facets, interactivity-facing marks, ordinary `geom_polygon()`, sf geometry marks, and `geom_sf_text()` / `geom_sf_label()` annotations. State that the run may skip explicitly when `GG2D3_BROWSER_VISUAL_SMOKE` is not true, when Chrome/Chromium or chromote launch is unavailable, or for sf-dependent rows when `sf` or `geojsonsf` is unavailable.

    Keep the documentation source-first and do not paste local log contents, screenshots, absolute local paths, or generated report rows.
  </action>
  <verify>
    <automated>rg -n "Browser visual smoke artifacts|GG2D3_BROWSER_VISUAL_SMOKE|test_output/browser-visual-smoke|index\\.html|index\\.json|geom_sf_text\\(\\)|geom_sf_label\\(\\)|Chrome/Chromium|geojsonsf" vignettes/d3-drawing-diagnostics.md</automated>
  </verify>
  <acceptance_criteria>
    - `vignettes/d3-drawing-diagnostics.md` contains both exact commands.
    - The docs list the exact artifact directory and all required generated artifact filename patterns.
    - The docs describe all required fixture coverage categories.
    - The docs describe opt-in, browser/chromote, and sf/geojsonsf skip semantics.
    - The docs do not include absolute local artifact paths or pasted browser log contents.
  </acceptance_criteria>
  <done>Maintainer-facing diagnostics explain how to run and inspect browser visual smoke artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Run final source and artifact hygiene checks</name>
  <read_first>
    tests/testthat/helper-browser-visual.R
    tests/testthat/test-browser-visual-smoke.R
    vignettes/d3-drawing-diagnostics.md
    .gitignore
    .Rbuildignore
    DESCRIPTION
  </read_first>
  <files>tests/testthat/helper-browser-visual.R, tests/testthat/test-browser-visual-smoke.R, vignettes/d3-drawing-diagnostics.md</files>
  <action>
    Run the final verification commands below. If a source check fails, fix the implicated Phase 48 source file while preserving D-01 through D-11 and the threat mitigations.

    Required source checks:
    - Confirm `DESCRIPTION` does not gain Playwright, Puppeteer, Selenium, webshot2, or Node browser tooling.
    - Confirm helper/test/docs reference `test_output/browser-visual-smoke/` and no generated artifact root outside `test_output/`.
    - Confirm `helper-browser-visual.R` contains `selfcontained = FALSE`, `screenshot(`, `jsonlite::write_json`, `htmltools::htmlEscape`, and sanitized fixture ID logic.
    - Confirm `test-browser-visual-smoke.R` contains all fixture IDs from Plan 02 and uses `GG2D3_BROWSER_VISUAL_SMOKE`.
    - Confirm `.gitignore` and `.Rbuildignore` already ignore/build-ignore `test_output/`; do not edit those files unless verification proves the boundary is absent.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-browser-visual-smoke.R")'</automated>
    <automated>NOT_CRAN=true GG2D3_BROWSER_VISUAL_SMOKE=true Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-browser-visual-smoke.R")'</automated>
    <automated>rg -n "browser-visual-smoke|GG2D3_BROWSER_VISUAL_SMOKE|selfcontained = FALSE|screenshot\\(|jsonlite::write_json|htmltools::htmlEscape|cartesian-point-line-text|ordinary-polygon|sf-annotations-text-label" tests/testthat/helper-browser-visual.R tests/testthat/test-browser-visual-smoke.R vignettes/d3-drawing-diagnostics.md</automated>
    <automated>Rscript --vanilla -e 'files <- c("DESCRIPTION", "tests/testthat/helper-browser-visual.R", "tests/testthat/test-browser-visual-smoke.R", "vignettes/d3-drawing-diagnostics.md"); pattern <- "Playwright|Puppeteer|Selenium|webshot2|pixel diff|golden|goldens"; hits <- unlist(lapply(files[file.exists(files)], function(f) grep(pattern, readLines(f, warn=FALSE), value=TRUE))); if (length(hits)) stop(paste(hits, collapse="\n"))'</automated>
    <automated>rg -n "^test_output/|^test_output$|test_output" .gitignore .Rbuildignore</automated>
  </verify>
  <acceptance_criteria>
    - Quick command skips cleanly without opt-in.
    - Full command either writes artifacts under `test_output/browser-visual-smoke/` or skips with explicit browser dependency messages.
    - Source checks find no new forbidden browser stack, pixel-diff, or committed-golden implementation.
    - `git status --short` does not show generated files under `test_output/browser-visual-smoke/`.
    - VIS-01, VIS-02, and VIS-03 are all evidenced by source, docs, and command behavior.
  </acceptance_criteria>
  <done>Phase 48 source/docs pass the final hygiene checks and generated artifacts remain ignored local outputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local artifact evidence -> public docs | Documentation references artifact classes and commands without publishing local generated contents. |
| Test source -> package metadata | Final checks prevent accidental new browser dependencies or artifact paths. |
| Generated output -> git/source package | Ignore/build-ignore boundaries keep local screenshots and logs out of commits and source builds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | I | Diagnostics docs and generated outputs | mitigate | Document only `test_output/browser-visual-smoke/` and filename patterns; verify generated outputs are ignored and not added to git. |
| T-48-02 | T/E | DOM summary evidence | mitigate | Final source checks require static helper-owned DOM summary scripts and sanitized fixture filenames. |
| T-48-03 | T/D | Dependency footprint | mitigate | Final source checks block Playwright, Puppeteer, Selenium, webshot2, pixel diffs, and committed goldens; keep chromote/htmlwidgets/testthat stack. |
</threat_model>

<verification>
Run every task-level automated command. In the summary, classify any full-command browser/spatial skip as expected only when the output names the unavailable dependency or browser condition.
</verification>

<success_criteria>
- Maintainer docs contain exact quick and full commands.
- Docs identify `test_output/browser-visual-smoke/` and all generated artifact types.
- Final source checks confirm the implementation remains opt-in, skip-friendly, and confined to ignored local artifacts.
- No generated browser visual artifact is committed.
</success_criteria>

<output>
After completion, create `.planning/phases/48-browser-visual-smoke-coverage/48-03-SUMMARY.md`.
</output>
