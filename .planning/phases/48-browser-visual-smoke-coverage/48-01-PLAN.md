---
phase: 48-browser-visual-smoke-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/helper-browser-visual.R
autonomous: true
requirements:
  - VIS-01
  - VIS-03
must_haves:
  truths:
    - "Maintainers have one shared browser visual smoke helper that writes all generated artifacts under test_output/browser-visual-smoke/."
    - "Browser visual helper code can identify opt-in, Chrome/chromote, sf, and geojsonsf availability with explicit messages."
    - "Each fixture can produce an HTML file, PNG screenshot, DOM summary JSON, browser log JSON, and local index row."
  artifacts:
    - path: "tests/testthat/helper-browser-visual.R"
      provides: "Shared artifact directory, dependency checks, screenshot, DOM summary, browser-log, and index helpers"
      exports:
        - "browser_visual_artifact_dir"
        - "skip_browser_visual_smoke"
        - "browser_visual_optional_dependencies"
        - "save_browser_visual_widget"
        - "capture_browser_visual_fixture"
        - "write_browser_visual_index"
  key_links:
    - from: "tests/testthat/helper-browser-visual.R"
      to: "test_output/browser-visual-smoke/"
      via: "browser_visual_artifact_dir()"
      pattern: "file\\.path\\(.test_output_dir\\(\\), \"browser-visual-smoke\"\\)"
    - from: "tests/testthat/helper-browser-visual.R"
      to: "chromote"
      via: "skip_browser_visual_smoke() and with_chromote_session()"
      pattern: "chromote::(find_chrome|ChromoteSession)"
    - from: "tests/testthat/helper-browser-visual.R"
      to: "jsonlite"
      via: "DOM summary, browser log, and index JSON writers"
      pattern: "jsonlite::write_json"
---

<objective>
Create the shared Phase 48 browser visual smoke helper contract.

Purpose: Plans 02 and 03 need a single deterministic helper for artifact paths, optional-dependency reporting, browser screenshots, DOM summaries, browser logs, and local index generation. This implements D-01, D-02, D-03, D-08, D-09, D-10, and D-11.

Output: `tests/testthat/helper-browser-visual.R`.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-browser-visual-smoke-coverage/48-CONTEXT.md
@.planning/phases/48-browser-visual-smoke-coverage/48-RESEARCH.md
@.planning/phases/48-browser-visual-smoke-coverage/48-PATTERNS.md
@.planning/phases/48-browser-visual-smoke-coverage/48-VALIDATION.md
@AGENTS.md
@tests/testthat/helper-browser-sf.R
@tests/testthat/helper-browser-polygon.R
@tests/testthat/helper-sf-fixtures.R
@.gitignore
@.Rbuildignore

<interfaces>
From `tests/testthat/helper-sf-fixtures.R`:
```r
.test_output_dir <- function() {
  pkg_root <- tryCatch(
    rprojroot::find_package_root_file(),
    error = function(e) normalizePath(file.path(getwd(), "../../.."), mustWork = FALSE)
  )
  file.path(pkg_root, "test_output")
}
```

From `tests/testthat/helper-browser-sf.R` and `tests/testthat/helper-browser-polygon.R`:
```r
with_chromote_session <- function(code, width = 900, height = 700) { ... }
eval_js_value <- function(session, script) { ... }
browser_console_collector <- function(session) { ... }
assert_no_browser_errors <- function(logs) { ... }
```

Reuse these names where already loaded, or define compatible generic versions in `helper-browser-visual.R` only when the function does not already exist. Do not modify existing sf or polygon helper behavior in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add artifact root and dependency gates</name>
  <read_first>
    .planning/phases/48-browser-visual-smoke-coverage/48-CONTEXT.md
    .planning/phases/48-browser-visual-smoke-coverage/48-PATTERNS.md
    tests/testthat/helper-browser-sf.R
    tests/testthat/helper-browser-polygon.R
    tests/testthat/helper-sf-fixtures.R
    .gitignore
    .Rbuildignore
  </read_first>
  <files>tests/testthat/helper-browser-visual.R</files>
  <action>
    Create `tests/testthat/helper-browser-visual.R`. At the top, load gg2d3 with `pkgload::load_all(quiet = TRUE)` when the namespace is absent, source `helper-sf-fixtures.R` when `.test_output_dir()` is missing, and define `%||%` only when absent.

    Implement these concrete helpers:
    - `browser_visual_artifact_dir()` returns `file.path(.test_output_dir(), "browser-visual-smoke")`, creates it recursively, and never writes outside `test_output/` per D-03 and T-48-01.
    - `browser_visual_fixture_id(id)` sanitizes fixture IDs with `gsub("[^A-Za-z0-9_-]+", "-", id)`, trims leading/trailing `-`, and errors if the result is empty per T-48-02.
    - `browser_visual_paths(id)` returns a named list with `html`, `png`, `dom_summary`, and `browser_log` paths using the sanitized ID and exact suffixes `.html`, `.png`, `-dom-summary.json`, and `-browser-log.json`.
    - `browser_visual_file_url(path)` returns a `file://` URL using `normalizePath(path, winslash = "/", mustWork = TRUE)`.
    - `browser_visual_require_opt_in()` calls `testthat::skip_if_not(identical(Sys.getenv("GG2D3_BROWSER_VISUAL_SMOKE"), "true"), "Set GG2D3_BROWSER_VISUAL_SMOKE=true to generate browser visual smoke artifacts")`.
    - `skip_browser_visual_smoke()` calls `browser_visual_require_opt_in()`, `testthat::skip_on_cran()`, `testthat::skip_if_not_installed("chromote", "0.5.1")`, verifies `chromote::find_chrome()`, and probes `chromote::ChromoteSession$new(width = 10, height = 10)` with an explicit `chromote session launch unavailable:` skip message.
    - `browser_visual_optional_dependencies(require_sf = FALSE, require_geojsonsf = FALSE)` returns `list(available = TRUE/FALSE, missing = character(), message = character(1))` using `requireNamespace()` for per-fixture rows. It must mention `sf` and `geojsonsf` by name when missing and must not call `testthat::skip()` for row-level optional dependency checks.

    Reuse existing chromote/htmlwidgets/testthat/jsonlite/rprojroot/pkgload stack only. Do not introduce Playwright, Puppeteer, Selenium, webshot2, pixel diffs, committed goldens, or a new artifact root.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); source("tests/testthat/helper-browser-visual.R"); stopifnot(grepl("test_output/browser-visual-smoke$", normalizePath(browser_visual_artifact_dir(), winslash="/", mustWork=FALSE))); stopifnot(identical(browser_visual_fixture_id("sf text/label"), "sf-text-label")); s <- browser_visual_optional_dependencies(require_sf=FALSE, require_geojsonsf=FALSE); stopifnot(isTRUE(s$available))'</automated>
    <automated>rg -n "GG2D3_BROWSER_VISUAL_SMOKE|browser-visual-smoke|Chrome/Chromium|chromote session launch unavailable|requireNamespace\\(\"sf\"|requireNamespace\\(\"geojsonsf\"" tests/testthat/helper-browser-visual.R</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/helper-browser-visual.R` exists.
    - `browser_visual_artifact_dir()` contains `file.path(.test_output_dir(), "browser-visual-smoke")`.
    - `browser_visual_require_opt_in()` contains `GG2D3_BROWSER_VISUAL_SMOKE`.
    - `skip_browser_visual_smoke()` contains explicit Chrome/Chromium and chromote launch skip messages.
    - `browser_visual_optional_dependencies()` returns row-level status without globally skipping sf rows.
  </acceptance_criteria>
  <done>Helper bootstrap, artifact path, sanitization, opt-in gate, browser gate, and per-row optional dependency status helpers are sourceable and verifiable.</done>
</task>

<task type="auto">
  <name>Task 2: Add screenshot, DOM summary, log, and index helpers</name>
  <read_first>
    tests/testthat/helper-browser-visual.R
    tests/testthat/helper-browser-sf.R
    tests/testthat/helper-browser-polygon.R
    tests/testthat/test-sf-browser.R
    tests/testthat/test-polygon-browser.R
  </read_first>
  <files>tests/testthat/helper-browser-visual.R</files>
  <action>
    Extend `helper-browser-visual.R` with evidence-capture and report helpers implementing D-01, D-02, D-10, D-11, and T-48-02:
    - `save_browser_visual_widget(widget, id)` saves `gg2d3`/htmlwidget output to `browser_visual_paths(id)$html` with `htmlwidgets::saveWidget(widget, file = normalizePath(..., mustWork = FALSE), selfcontained = FALSE)`, then asserts the HTML file exists.
    - `browser_visual_dom_summary_script(extra_selector_counts = character())` returns one static JavaScript string. It must not interpolate arbitrary fixture-supplied JavaScript. The summary must include at least `title`, `svgCount`, `panelCount`, `markCounts` for selectors `circle.geom-point`, `path.geom-line`, `rect.geom-bar`, `rect.geom-rect`, `text.geom-text`, `path.geom-polygon`, `.geom-sf`, `text.geom-sf.geom-sf-text`, and `g.geom-sf.geom-sf-label`, plus `bodyTextLength`.
    - `browser_visual_console_collector(session)`, `browser_visual_logs(logs)`, and `assert_no_browser_visual_errors(logs)` should mirror existing console/error collection semantics and treat `Runtime.exceptionThrown`, console `error`, `exception`, and `assert` entries as failures.
    - `write_browser_visual_artifacts(id, html_path, logs, session = NULL, dom_summary = NULL, status = "passed", error = NULL, skip_reason = NULL)` copies HTML to the sanitized destination if needed, writes browser logs to `-browser-log.json`, writes DOM summary to `-dom-summary.json` when present, and attempts `session$screenshot(filename = paths$png, selector = "html", delay = 0.5)` when a session is available. Screenshot failure should be recorded in the browser-log JSON without masking the original test error.
    - `capture_browser_visual_fixture(session, fixture)` takes a fixture list containing `id`, `category`, `widget`, and optional `expected` named selector counts. It saves HTML, opens `file://` via `session$go_to(..., delay = 1)`, polls until `document.querySelectorAll("svg").length > 0`, captures DOM summary, validates expected selector counts when supplied, asserts no browser errors, writes artifacts, and returns a status row with `id`, `category`, `status`, `html`, `screenshot`, `dom_summary`, `browser_log`, `skip_reason`, and `error`.
    - `write_browser_visual_index(rows)` writes `index.json` and `index.html` in `browser_visual_artifact_dir()`. The HTML should be simple local report markup with escaped fixture IDs/statuses/paths and links to each artifact. Use `htmltools::htmlEscape()` since `htmltools` is already declared in `DESCRIPTION`; do not embed local log contents in the report.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); source("tests/testthat/helper-browser-visual.R"); stopifnot(is.function(save_browser_visual_widget)); stopifnot(is.function(browser_visual_dom_summary_script)); stopifnot(is.function(capture_browser_visual_fixture)); stopifnot(is.function(write_browser_visual_index)); js <- browser_visual_dom_summary_script(); stopifnot(grepl("circle.geom-point", js, fixed=TRUE)); stopifnot(grepl("g.geom-sf.geom-sf-label", js, fixed=TRUE))'</automated>
    <automated>rg -n "selfcontained = FALSE|screenshot\\(|-dom-summary\\.json|-browser-log\\.json|index\\.html|index\\.json|htmltools::htmlEscape|Runtime\\.exceptionThrown" tests/testthat/helper-browser-visual.R</automated>
  </verify>
  <acceptance_criteria>
    - `save_browser_visual_widget()` uses `selfcontained = FALSE`.
    - `browser_visual_dom_summary_script()` is static helper-controlled JS and includes selectors for Cartesian, polygon, sf, and sf annotation marks.
    - `write_browser_visual_artifacts()` writes HTML, PNG, DOM summary JSON, and browser-log JSON under `test_output/browser-visual-smoke/`.
    - `write_browser_visual_index()` writes both `index.html` and `index.json` under `test_output/browser-visual-smoke/`.
    - No new browser or screenshot dependency appears in `DESCRIPTION` or helper code.
  </acceptance_criteria>
  <done>Shared helper can produce deterministic per-fixture artifacts and a local report without changing package sources or adding browser dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R test fixture -> filesystem | Test helper writes generated HTML, PNG, JSON, and report artifacts. |
| R helper -> Chrome DevTools Protocol | Static helper JavaScript executes in a local browser session. |
| Optional local dependency -> test result | Chrome/chromote/sf/geojsonsf availability affects execution and skip/report status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | I | `browser_visual_artifact_dir()` and generated outputs | mitigate | Write only under ignored/build-ignored `test_output/browser-visual-smoke/`; never add generated artifacts to git; verify `.gitignore` and `.Rbuildignore` already cover `test_output/`. |
| T-48-02 | T/E | DOM summary and fixture IDs | mitigate | Use static helper-owned JavaScript strings; sanitize fixture IDs with `browser_visual_fixture_id()` before filenames; do not accept fixture-supplied JavaScript. |
| T-48-03 | T/D | Browser automation dependencies | mitigate | Use existing `chromote`, `htmlwidgets`, `testthat`, and `jsonlite` stack only; do not add Playwright, Puppeteer, Selenium, webshot2, or Node tooling. |
</threat_model>

<verification>
Run the task-level automated commands. Confirm `git status --short test_output/browser-visual-smoke` does not show generated outputs because `test_output/` is ignored.
</verification>

<success_criteria>
- `tests/testthat/helper-browser-visual.R` sources successfully.
- Artifact paths, screenshots, DOM summaries, browser logs, and report helpers all target `test_output/browser-visual-smoke/`.
- Optional dependency helpers produce explicit messages for Chrome/Chromium, chromote launch, `sf`, and `geojsonsf`.
- No generated artifact path outside `test_output/` is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/48-browser-visual-smoke-coverage/48-01-SUMMARY.md`.
</output>
