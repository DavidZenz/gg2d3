---
phase: 13-interactive-legend-controls
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - inst/htmlwidgets/modules/events.js
  - inst/htmlwidgets/modules/crosstalk.js
  - inst/htmlwidgets/modules/legend.js
  - inst/htmlwidgets/modules/geoms/point.js
  - inst/htmlwidgets/modules/geoms/line.js
  - inst/htmlwidgets/modules/geoms/bar.js
  - inst/htmlwidgets/modules/geoms/rect.js
autonomous: true

must_haves:
  truths:
    - "User sees legend-driven visibility state remain synchronized with linked-view interaction state"
    - "User can hover a legend item to preview emphasis/de-emphasis without changing persistent filter state"
    - "Legend solo/hidden state remains consistent after additional interactions"
  artifacts:
    - path: "inst/htmlwidgets/modules/events.js"
      provides: "Unified state application pipeline with precedence for legend/crosstalk/brush/hover"
      contains: "applyLegendState"
    - path: "inst/htmlwidgets/modules/crosstalk.js"
      provides: "Bridge between crosstalk change events and legend state updates"
      contains: "legend"
    - path: "inst/htmlwidgets/modules/geoms/point.js"
      provides: "Legend identity attributes on rendered marks"
      contains: "data-legend-key"
  key_links:
    - from: "inst/htmlwidgets/modules/crosstalk.js"
      to: "inst/htmlwidgets/modules/events.js"
      via: "shared state bridge callback"
      pattern: "applyLegendState|getLegendState"
    - from: "inst/htmlwidgets/modules/geoms/*.js"
      to: "inst/htmlwidgets/modules/legend.js"
      via: "shared legend key identity"
      pattern: "data-legend-key"
---

<objective>
Implement the full legend-state application and synchronization layer so legend controls affect marks correctly and stay in sync with linked views.

Purpose: LEG-03 and LEG-04 are integration behaviors across legend, marks, and linked interactions; this plan turns event semantics into visible, stable outcomes.
Output: Mark identity wiring, unified style application precedence, and crosstalk synchronization contract.
</objective>

<execution_context>
@/Users/davidzenz/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-interactive-legend-controls/13-RESEARCH.md
@.planning/phases/13-interactive-legend-controls/13-01-PLAN.md
@.planning/milestones/v1.0-phases/11-advanced-interactivity/11-04-SUMMARY.md
@inst/htmlwidgets/modules/events.js
@inst/htmlwidgets/modules/crosstalk.js
@inst/htmlwidgets/modules/geoms/point.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stable legend identity attributes to rendered marks</name>
  <files>inst/htmlwidgets/modules/geoms/point.js, inst/htmlwidgets/modules/geoms/line.js, inst/htmlwidgets/modules/geoms/bar.js, inst/htmlwidgets/modules/geoms/rect.js</files>
  <action>Extend renderers so marks participating in discrete legends expose deterministic identity attributes aligned with legend items: `data-legend-key`, plus optional `data-legend-level` and `data-legend-aesthetic`. Build the key from guide-compatible dimensions (aesthetic + mapped level) and keep it stable across facets and redraws. Add attributes without changing existing geom appearance, scales, or baseline interaction behavior. For unsupported/continuous mappings, omit the attribute rather than generating ambiguous keys.</action>
  <verify>node -c inst/htmlwidgets/modules/geoms/point.js && node -c inst/htmlwidgets/modules/geoms/line.js && node -c inst/htmlwidgets/modules/geoms/bar.js && node -c inst/htmlwidgets/modules/geoms/rect.js</verify>
  <done>Legend-controlled geoms share stable key attributes that can be matched one-to-many from legend items to marks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement one-pass visual state application with precedence</name>
  <files>inst/htmlwidgets/modules/events.js, inst/htmlwidgets/modules/legend.js</files>
  <action>Implement `applyLegendState(el)` to compute final mark and legend styling in one pass from layered state inputs. Enforce precedence: hidden > solo-filter > existing brush/crosstalk persistent selection state > transient legend hover preview. Hover preview must only alter temporary emphasis/de-emphasis and must fully restore on hover-out without mutating hidden/solo state. Reuse `data-original-opacity` conventions and preserve brush-active guardrails from Phase 11. Update legend item visuals (active, hidden, solo, hover-preview classes/opacity) from the same computed state so controls and marks never diverge.</action>
  <verify>node -c inst/htmlwidgets/modules/events.js && node -c inst/htmlwidgets/modules/legend.js</verify>
  <done>Legend, mark, and interaction visuals are derived from a single deterministic state calculation with transient hover isolated from persistent filters.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize legend state with crosstalk linked-view updates</name>
  <files>inst/htmlwidgets/modules/crosstalk.js, inst/htmlwidgets/modules/events.js</files>
  <action>Integrate crosstalk selection change handling with legend state pipeline: when crosstalk changes, trigger legend state reapplication instead of independent opacity writes; when legend persistent visibility changes, update the linked-view pathway using existing SelectionHandle flow without inventing a new transport. Define and implement one explicit contract for synchronization (documented in code comments) so LEG-03 remains deterministic across widgets in the same crosstalk group. Keep backward compatibility for plots without crosstalk metadata.</action>
  <verify>node -c inst/htmlwidgets/modules/crosstalk.js && node -c inst/htmlwidgets/modules/events.js</verify>
  <done>Legend interactions and crosstalk updates stay synchronized through one shared state application path.</done>
</task>

</tasks>

<verification>
Run `node -c` on all modified JS files and execute `Rscript -e "devtools::test(filter = 'legends|interactivity|crosstalk')"`.
</verification>

<success_criteria>
- LEG-03: legend visibility and linked-view state remain synchronized (no contradictory mark/legend states)
- LEG-04: legend hover preview is transient and does not alter persistent hidden/solo filters
- Existing zoom/brush/hover interactions remain functional with legend controls enabled
</success_criteria>

<output>
After completion, create `.planning/phases/13-interactive-legend-controls/13-02-SUMMARY.md`
</output>
