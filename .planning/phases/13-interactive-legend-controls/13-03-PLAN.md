---
phase: 13-interactive-legend-controls
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - tests/testthat/test-legends.R
  - tests/testthat/test-crosstalk.R
  - inst/htmlwidgets/modules/legend.js
  - inst/htmlwidgets/modules/events.js
autonomous: false

must_haves:
  truths:
    - "User can click a discrete legend item to hide/show its corresponding series"
    - "User can double-click a discrete legend item to solo that series and can reset to restore all series"
    - "User sees legend-driven visibility state remain synchronized with linked-view interaction state"
    - "User can hover a legend item to preview emphasis/de-emphasis without changing persistent filter state"
  artifacts:
    - path: "tests/testthat/test-legends.R"
      provides: "Regression tests for legend identity/state contract and discrete-vs-colorbar behavior"
      contains: "interactive legend"
    - path: "tests/testthat/test-crosstalk.R"
      provides: "Regression tests for crosstalk compatibility with legend-controlled visibility"
      contains: "legend"
    - path: "inst/htmlwidgets/modules/events.js"
      provides: "Finalized hover/toggle/solo/reset behavior after test-driven fixes"
      contains: "legend:hoverin"
  key_links:
    - from: "tests/testthat/test-legends.R"
      to: "inst/htmlwidgets/modules/legend.js"
      via: "guide identity + discrete-only interaction contract"
      pattern: "legend|colorbar"
    - from: "tests/testthat/test-crosstalk.R"
      to: "inst/htmlwidgets/modules/crosstalk.js"
      via: "linked-view compatibility assertions"
      pattern: "crosstalk"
---

<objective>
Harden interactive legend controls with regression coverage and end-to-end verification, then close any final implementation gaps discovered during verification.

Purpose: Interactive UI behavior is prone to event-order and state-layer regressions; this plan ensures Phase 13 ships with stable, test-backed behavior for LEG-01..LEG-04.
Output: Expanded tests plus user-approved interaction verification for toggle, solo/reset, linked sync, and hover preview.
</objective>

<execution_context>
@/Users/davidzenz/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-interactive-legend-controls/13-RESEARCH.md
@.planning/phases/13-interactive-legend-controls/13-01-PLAN.md
@.planning/phases/13-interactive-legend-controls/13-02-PLAN.md
@tests/testthat/test-legends.R
@tests/testthat/test-crosstalk.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for interactive legend contracts</name>
  <files>tests/testthat/test-legends.R, tests/testthat/test-crosstalk.R</files>
  <action>Extend test-legends.R with focused tests that validate required contracts for Phase 13: discrete guides expose stable key identity metadata needed for interaction, colorbar guides do not expose discrete interaction metadata, and reset-capable discrete legend structures are present. Extend test-crosstalk.R with assertions that legend-enabled widgets retain valid crosstalk metadata/dependencies and do not break existing SharedData handling. Keep tests deterministic and aligned with current package testing style.</action>
  <verify>Rscript -e "devtools::test(filter = 'legends|crosstalk')"</verify>
  <done>New tests fail if discrete/colorbar interaction boundaries or crosstalk compatibility regress.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full interactive legend workflow (toggle, solo/reset, linked sync, hover preview) implemented and test-backed.</what-built>
  <how-to-verify>
1. Run: `Rscript -e "devtools::load_all(); source('tests/testthat/helper-legend-visual-check.R')"` after creating a helper script that writes two HTML widgets: (a) discrete legend scatter with 3 groups and crosstalk SharedData, (b) continuous colorbar plot.
2. Open generated HTML file for (a). Click one legend item once: only that series toggles hidden/visible (LEG-01).
3. Double-click one legend item: only that series remains visible (solo). Trigger reset control: all series visible again (LEG-02).
4. With two widgets in same crosstalk group, verify legend-driven visibility and linked selection stay aligned (no contradictory visible/hidden state) (LEG-03).
5. Hover legend item: non-target series de-emphasize temporarily; mouseout restores prior persistent state exactly (LEG-04).
6. Open generated HTML file for (b): confirm colorbar remains non-interactive (no toggle/solo/hover behavior).
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or list failing step numbers and observed behavior.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Fix issues found during verification and finalize phase stability</name>
  <files>inst/htmlwidgets/modules/legend.js, inst/htmlwidgets/modules/events.js</files>
  <action>If checkpoint feedback reports failures, implement targeted fixes for event sequencing, reset behavior, hover transient restoration, or state synchronization. Do not expand scope beyond LEG-01..LEG-04. Re-run relevant test filters and regenerate visual check outputs to confirm closure of reported failures.</action>
  <verify>Rscript -e "devtools::test(filter = 'legends|crosstalk|interactivity')" && node -c inst/htmlwidgets/modules/legend.js && node -c inst/htmlwidgets/modules/events.js</verify>
  <done>All checkpoint-reported failures are resolved and regression tests remain green.</done>
</task>

</tasks>

<verification>
1. `Rscript -e "devtools::test(filter = 'legends|crosstalk|interactivity')"` passes
2. Human verification checkpoint approved for LEG-01..LEG-04
3. `node -c inst/htmlwidgets/modules/legend.js && node -c inst/htmlwidgets/modules/events.js` passes
</verification>

<success_criteria>
- All four Phase 13 roadmap success criteria are demonstrated and approved
- Discrete legend interactions are stable under repeated click/dblclick/hover sequences
- Linked-view synchronization remains intact when legend state changes
- Colorbar guides remain out of scope and non-interactive
</success_criteria>

<output>
After completion, create `.planning/phases/13-interactive-legend-controls/13-03-SUMMARY.md`
</output>
