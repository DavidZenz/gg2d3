---
phase: 13-interactive-legend-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/htmlwidgets/modules/events.js
  - inst/htmlwidgets/modules/legend.js
  - inst/htmlwidgets/gg2d3.js
autonomous: true

must_haves:
  truths:
    - "User can click a discrete legend item to hide/show its corresponding series"
    - "User can double-click a discrete legend item to solo that series"
    - "User can reset from solo/hidden state back to all-visible"
    - "Continuous colorbars remain non-interactive"
  artifacts:
    - path: "inst/htmlwidgets/modules/events.js"
      provides: "Canonical legend interaction state and semantic dispatch handlers"
      contains: "legend:toggle"
    - path: "inst/htmlwidgets/modules/legend.js"
      provides: "Discrete legend item identity attributes and bound interaction listeners"
      contains: "legend-item"
    - path: "inst/htmlwidgets/gg2d3.js"
      provides: "Post-render wiring that initializes legend interaction pipeline"
      contains: "attachLegend"
  key_links:
    - from: "inst/htmlwidgets/modules/legend.js"
      to: "inst/htmlwidgets/modules/events.js"
      via: "d3.dispatch semantic legend events"
      pattern: "legend:(toggle|solo|reset)"
    - from: "inst/htmlwidgets/gg2d3.js"
      to: "inst/htmlwidgets/modules/events.js"
      via: "renderValue initialization hook"
      pattern: "gg2d3\.events\.attachLegend"
---

<objective>
Establish the core legend interaction architecture for discrete guides: canonical state, semantic events, and DOM wiring.

Purpose: LEG-01 and LEG-02 require deterministic state transitions (toggle, solo, reset) that cannot be reliably achieved by direct one-off DOM mutation.
Output: Shared legend state engine + discrete legend event bindings integrated into widget render lifecycle.
</objective>

<execution_context>
@/Users/davidzenz/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-interactive-legend-controls/13-RESEARCH.md
@.planning/milestones/v1.0-phases/07-legend-system/07-04-SUMMARY.md
@.planning/milestones/v1.0-phases/11-advanced-interactivity/11-04-SUMMARY.md
@inst/htmlwidgets/modules/legend.js
@inst/htmlwidgets/modules/events.js
@inst/htmlwidgets/gg2d3.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical legend state store and semantic event bus</name>
  <files>inst/htmlwidgets/modules/events.js</files>
  <action>Create a legend interaction controller in events.js with one per-widget state object and d3.dispatch channels: legend:toggle, legend:solo, legend:reset, legend:hoverin, legend:hoverout, legend:changed. State must separate persistent and transient concerns: hidden set + solo key for persistent state, hover key for transient preview state. Implement click-vs-dblclick arbitration (deferred single click canceled on dblclick) to avoid toggle+solo races. Provide helper methods attachLegend(el, options), getLegendState(el), and applyLegendState(el) exported via window.gg2d3.events. Do not introduce new dependencies; use D3 v7 dispatch/selection already vendored.</action>
  <verify>node -c inst/htmlwidgets/modules/events.js</verify>
  <done>events.js exposes a reusable legend controller with semantic dispatch and explicit persistent/transient state boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Emit semantic legend events from discrete legend items only</name>
  <files>inst/htmlwidgets/modules/legend.js</files>
  <action>Update discrete legend rendering so each interactive key row is wrapped in a stable `.legend-item` group with deterministic identity attributes (`data-legend-key`, `data-aesthetic`, `data-level`). Bind namespaced handlers (`click.legend`, `dblclick.legend`, `mouseover.legend`, `mouseout.legend`) that call the semantic dispatcher in events.js instead of mutating mark opacity directly. Add a discrete-legend reset control in the legend container (e.g., title-adjacent text/button) that emits legend:reset. Keep colorbar guides passive: do not add legend-item handlers to guide.type === "colorbar".</action>
  <verify>node -c inst/htmlwidgets/modules/legend.js</verify>
  <done>Discrete legends publish semantic interaction events and include reset behavior; colorbars remain non-interactive.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize legend interaction pipeline during widget render</name>
  <files>inst/htmlwidgets/gg2d3.js</files>
  <action>After draw() and existing interactivity initialization in renderValue, call the new events.attachLegend initializer with current IR context and rendered SVG root. Ensure re-render/resize replaces handlers cleanly (remove previous `.legend` namespaced listeners before rebinding). Guard initialization so it runs only when discrete guides exist and does nothing for plots without legends. Do not break existing tooltip/hover/zoom/brush/crosstalk initialization order.</action>
  <verify>node -c inst/htmlwidgets/gg2d3.js</verify>
  <done>Legend interaction setup runs automatically for discrete legends on first render and subsequent redraws without duplicate handlers.</done>
</task>

</tasks>

<verification>
Run `node -c inst/htmlwidgets/modules/events.js && node -c inst/htmlwidgets/modules/legend.js && node -c inst/htmlwidgets/gg2d3.js`.
</verification>

<success_criteria>
- LEG-01 mechanics exist: discrete legend click produces deterministic toggle event
- LEG-02 mechanics exist: discrete legend dblclick produces solo event and reset event restores all-visible state
- Colorbar interactivity is explicitly excluded for this phase
</success_criteria>

<output>
After completion, create `.planning/phases/13-interactive-legend-controls/13-01-SUMMARY.md`
</output>
