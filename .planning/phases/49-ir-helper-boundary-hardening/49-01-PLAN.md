---
phase: 49-ir-helper-boundary-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/ir_scale_helpers.R
  - R/as_d3_ir.R
  - tests/testthat/test-ir-helper-boundaries.R
autonomous: true
requirements:
  - ARCH-01
must_haves:
  truths:
    - "Scale, axis, transform, and temporal metadata extraction lives behind named internal helper functions."
    - "Representative continuous, discrete, transformed, date, datetime, and coord_flip scale IR remains structurally unchanged."
    - "Scale-boundary characterization failures identify scale or axis extraction rather than only the full as_d3_ir() path."
  artifacts:
    - path: "R/ir_scale_helpers.R"
      provides: "Internal scale/axis/temporal helper boundary"
    - path: "tests/testthat/test-ir-helper-boundaries.R"
      provides: "Scale-boundary characterization tests"
  key_links:
    - from: "R/as_d3_ir.R"
      to: "R/ir_scale_helpers.R"
      via: "gg2d3_ir_scale_info() and related helpers"
      pattern: "gg2d3_ir_scale_info"
requirements_addressed:
  - ARCH-01
---

<objective>
Extract the first high-risk helper boundary from `as_d3_ir()`: scale, axis, transform, temporal, and break metadata.

Purpose: This is the safest first slice because scale logic is heavily tested already and repeated in facet panel construction. The plan creates reusable helpers and helper-level tests while preserving current IR output.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-ir-helper-boundary-hardening/49-CONTEXT.md
@.planning/phases/49-ir-helper-boundary-hardening/49-RESEARCH.md
@.planning/phases/49-ir-helper-boundary-hardening/49-PATTERNS.md
@.planning/phases/49-ir-helper-boundary-hardening/49-VALIDATION.md
@R/as_d3_ir.R
@R/ggplot2_compat.R
@tests/testthat/test-ir.R
@tests/testthat/test-date-scales.R
@tests/testthat/test-facets.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scale helper boundary</name>
  <read_first>
    R/as_d3_ir.R
    R/ggplot2_compat.R
    tests/testthat/test-ir.R
    tests/testthat/test-date-scales.R
  </read_first>
  <files>R/ir_scale_helpers.R, R/as_d3_ir.R</files>
  <action>
    Create `R/ir_scale_helpers.R` with internal helpers named:
    - `gg2d3_ir_validate_log_domain(scale_obj, domain, axis_name)`
    - `gg2d3_ir_scale_transform(scale_obj)`
    - `gg2d3_ir_temporal_metadata(scale_obj, panel_params_axis, result)`
    - `gg2d3_ir_convert_temporal_values(values, trans_name)`
    - `gg2d3_ir_axis_breaks(panel_params_axis, trans_name)`
    - `gg2d3_ir_scale_info(scale_obj, panel_params_axis, axis_name)`

    Move the existing logic from `validate_log_domain()`, `get_scale_transform()`, `get_scale_info()`, and global break temporal conversion into those helpers without changing field names or values. Keep these helpers unexported and use only base R plus existing `gg2d3_panel_labels()` and `gg2d3_continuous_range()` compatibility helpers.

    Update `R/as_d3_ir.R` so global `scales$x`, `scales$y`, `x_breaks`, `y_breaks`, `x_minor_breaks`, and `y_minor_breaks` are produced by the new helpers. Preserve the public `as_d3_ir()` signature.
  </action>
  <verify>
    <automated>rg -n "gg2d3_ir_(validate_log_domain|scale_transform|temporal_metadata|convert_temporal_values|axis_breaks|scale_info)" R/ir_scale_helpers.R R/as_d3_ir.R</automated>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-ir.R"); testthat::test_file("tests/testthat/test-date-scales.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `R/ir_scale_helpers.R` exists.
    - `R/as_d3_ir.R` calls `gg2d3_ir_scale_info(`.
    - `R/as_d3_ir.R` calls `gg2d3_ir_axis_breaks(`.
    - `tests/testthat/test-ir.R` and `tests/testthat/test-date-scales.R` exit with status 0.
  </acceptance_criteria>
  <done>Scale extraction is isolated behind named helpers with unchanged representative scale IR.</done>
</task>

<task type="auto">
  <name>Task 2: Add scale-boundary characterization tests</name>
  <read_first>
    tests/testthat/test-ir.R
    tests/testthat/test-date-scales.R
    R/ir_scale_helpers.R
    R/as_d3_ir.R
  </read_first>
  <files>tests/testthat/test-ir-helper-boundaries.R</files>
  <action>
    Create `tests/testthat/test-ir-helper-boundaries.R` if absent. Add tests with these exact test descriptions:
    - `scale helper preserves continuous and transformed domains`
    - `scale helper preserves date and datetime metadata`
    - `scale helper preserves discrete domains and coord_flip labels`

    Each test must call `as_d3_ir()` on compact ggplot fixtures and assert stable fields. Include at least these checks:
    - `ir$scales$x$type == "continuous"` for an mtcars point plot.
    - `ir$scales$x$transform == "log10"` and `ir$scales$x$base == 10` for `scale_x_log10()`.
    - `ir$scales$x$transform == "date"` and millisecond domain/break values for a `Date` x-axis.
    - `ir$scales$x$timezone == "UTC"` or an explicitly set timezone for a `POSIXct` x-axis.
    - `ir$scales$x$type == "categorical"` and `ir$scales$x$domain == c("4", "6", "8")` for `factor(cyl)`.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-ir-helper-boundaries.R")'</automated>
    <automated>rg -n "scale helper preserves continuous and transformed domains|scale helper preserves date and datetime metadata|scale helper preserves discrete domains and coord_flip labels" tests/testthat/test-ir-helper-boundaries.R</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-ir-helper-boundaries.R` contains all three exact test descriptions.
    - The helper-boundary test file exits with status 0.
    - Test failure labels mention `scale helper`.
  </acceptance_criteria>
  <done>Scale-boundary drift has focused tests that point to the scale helper boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ggplot2 build objects -> internal helpers | Helpers consume package-version-sensitive panel and scale structures. |
| internal helpers -> JS renderer IR | Helper output must keep existing field names and scalar/list types. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | T | `R/ir_scale_helpers.R` | mitigate | Preserve existing field names and run date/scale/facet tests after extraction. |
| T-49-02 | I | temporal metadata | mitigate | Add boundary tests for ms domains, breaks, labels, and timezone values. |
| T-49-03 | D | ggplot2 version differences | mitigate | Use existing compatibility wrappers and avoid direct new `ggplot2:::` calls. |
</threat_model>

<verification>
Run all task-level commands and the Phase 49 validation scale subset.
</verification>

<success_criteria>
- `R/ir_scale_helpers.R` exists and contains named internal helpers.
- `as_d3_ir()` delegates scale extraction to the helpers.
- Existing scale/date IR tests pass.
- New scale-boundary tests pass and identify the scale helper boundary by name.
</success_criteria>

<output>
After completion, create `.planning/phases/49-ir-helper-boundary-hardening/49-01-SUMMARY.md`.
</output>

