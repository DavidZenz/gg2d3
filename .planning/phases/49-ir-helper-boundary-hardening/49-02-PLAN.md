---
phase: 49-ir-helper-boundary-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - R/ir_layer_helpers.R
  - R/as_d3_ir.R
  - tests/testthat/test-ir-helper-boundaries.R
autonomous: true
requirements:
  - ARCH-01
must_haves:
  truths:
    - "Layer rowization, geom naming, aesthetic maps, var-name maps, and non-sf layer assembly live behind named helper functions."
    - "Representative non-sf, polygon, text/label, and sf annotation integration IR remains unchanged."
    - "Layer-boundary characterization failures identify layer assembly rather than only the full as_d3_ir() path."
  artifacts:
    - path: "R/ir_layer_helpers.R"
      provides: "Internal layer assembly helper boundary"
    - path: "tests/testthat/test-ir-helper-boundaries.R"
      provides: "Layer-boundary characterization tests"
  key_links:
    - from: "R/as_d3_ir.R"
      to: "R/ir_layer_helpers.R"
      via: "gg2d3_ir_layer_aes() and gg2d3_ir_layer_rows()"
      pattern: "gg2d3_ir_layer_aes"
requirements_addressed:
  - ARCH-01
---

<objective>
Extract the layer assembly boundary from `as_d3_ir()` while preserving sf helper integration.

Purpose: Layer assembly is the largest user-visible IR surface. This plan moves rowization, geom naming, aesthetic map construction, var-name mapping, and non-sf layer list construction into named internal helpers.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-ir-helper-boundary-hardening/49-CONTEXT.md
@.planning/phases/49-ir-helper-boundary-hardening/49-RESEARCH.md
@.planning/phases/49-ir-helper-boundary-hardening/49-PATTERNS.md
@R/as_d3_ir.R
@R/sf_utils.R
@tests/testthat/test-ir.R
@tests/testthat/test-polygon-interactivity.R
@tests/testthat/test-sf-annotations-ir.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add layer helper boundary</name>
  <read_first>
    R/as_d3_ir.R
    R/sf_utils.R
    R/ir_scale_helpers.R
    tests/testthat/test-ir.R
    tests/testthat/test-sf-annotations-ir.R
  </read_first>
  <files>R/ir_layer_helpers.R, R/as_d3_ir.R</files>
  <action>
    Create `R/ir_layer_helpers.R` with internal helpers named:
    - `gg2d3_ir_map_discrete(values, scale_obj)`
    - `gg2d3_ir_layer_rows(df, keep_aes)`
    - `gg2d3_ir_geom_name(layer)`
    - `gg2d3_ir_layer_aes(cols)`
    - `gg2d3_ir_apply_temporal_layer_columns(df, x_trans_name, y_trans_name)`
    - `gg2d3_ir_var_names(plot_mapping, layer_mapping)`
    - `gg2d3_ir_non_sf_layer(gname, df, aes, params, var_names)`

    Move equivalent logic out of `as_d3_ir()` without changing the data shape. `gg2d3_ir_geom_name()` must preserve special detection for `GeomSfText`, `GeomSfLabel`, and sf-coordinate text/label stats. `gg2d3_ir_layer_rows()` must preserve PANEL integer conversion, factor to character conversion, POSIXct/Date conversion, list-column preservation, and all existing `keep_aes` names.

    Update `as_d3_ir()` to use the helpers while keeping sf payload calls to `sf_layer_ir_payload()` and `sf_annotation_layer_ir_payload()` unchanged.
  </action>
  <verify>
    <automated>rg -n "gg2d3_ir_(map_discrete|layer_rows|geom_name|layer_aes|apply_temporal_layer_columns|var_names|non_sf_layer)" R/ir_layer_helpers.R R/as_d3_ir.R</automated>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-ir.R"); testthat::test_file("tests/testthat/test-polygon-interactivity.R")'</automated>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-sf-annotations-ir.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `R/ir_layer_helpers.R` exists.
    - `R/as_d3_ir.R` calls `gg2d3_ir_geom_name(`.
    - `R/as_d3_ir.R` calls `gg2d3_ir_layer_rows(` or `gg2d3_ir_non_sf_layer(`.
    - Existing non-sf, polygon, and sf annotation IR tests exit with status 0 or skip only for missing optional dependencies.
  </acceptance_criteria>
  <done>Layer assembly is isolated while preserving existing sf helper integration.</done>
</task>

<task type="auto">
  <name>Task 2: Add layer-boundary characterization tests</name>
  <read_first>
    tests/testthat/test-ir-helper-boundaries.R
    R/ir_layer_helpers.R
    R/as_d3_ir.R
    tests/testthat/test-sf-annotations-ir.R
  </read_first>
  <files>tests/testthat/test-ir-helper-boundaries.R</files>
  <action>
    Extend `tests/testthat/test-ir-helper-boundaries.R` with tests named exactly:
    - `layer helper preserves geom names and row data`
    - `layer helper preserves aesthetic variable maps`
    - `layer helper preserves sf annotation layer contracts`

    Include compact fixtures that assert:
    - point, line, rect/bar, text, and polygon layers keep expected `geom` values.
    - `ir$layers[[1]]$data[[1]]` contains scalar x/y values and no factor class.
    - `ir$layers[[1]]$var_names` maps x/y/colour labels for a mapped plot.
    - `ir$aes_by_var` resolves original variable names back to aesthetic keys.
    - sf annotation tests are guarded by `testthat::skip_if_not_installed("sf")` and `testthat::skip_if_not_installed("geojsonsf")` before any `sf::` call.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-ir-helper-boundaries.R")'</automated>
    <automated>rg -n "layer helper preserves geom names and row data|layer helper preserves aesthetic variable maps|layer helper preserves sf annotation layer contracts|skip_if_not_installed\\(\"sf\"|skip_if_not_installed\\(\"geojsonsf\"" tests/testthat/test-ir-helper-boundaries.R</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-ir-helper-boundaries.R` contains all three exact layer test descriptions.
    - sf annotation fixture code is below explicit sf and geojsonsf skip gates.
    - The helper-boundary test file exits with status 0.
  </acceptance_criteria>
  <done>Layer-boundary drift has focused tests that point to layer helper contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ggplot layer build data -> row helper | Build data is serialized into JSON-ready scalar row lists. |
| geom dispatch -> renderer | Geom names determine downstream JS renderer selection. |
| aesthetic maps -> interactivity payloads | `var_names` and `aes_by_var` affect tooltip/handler lookup behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04 | T | rowization helper | mitigate | Preserve scalar/list/factor/PANEL conversion and run non-sf IR tests. |
| T-49-05 | T | geom name dispatch | mitigate | Add boundary tests for point/line/bar/rect/text/polygon/sf annotation names. |
| T-49-06 | I | var-name maps | mitigate | Add tests for `var_names` and `aes_by_var` so interactivity payload lookup remains stable. |
</threat_model>

<verification>
Run all task-level commands plus the Phase 49 validation layer subset.
</verification>

<success_criteria>
- `R/ir_layer_helpers.R` exists and contains named internal helpers.
- `as_d3_ir()` delegates layer row/aesthetic/geom assembly to those helpers.
- Existing non-sf, polygon, and sf annotation tests pass or skip only for missing optional dependencies.
- New layer-boundary tests pass and identify the layer helper boundary by name.
</success_criteria>

<output>
After completion, create `.planning/phases/49-ir-helper-boundary-hardening/49-02-SUMMARY.md`.
</output>

