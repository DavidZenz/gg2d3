---
phase: 33-single-panel-renderer-and-interactivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/htmlwidgets/modules/geoms/sf.js
  - inst/htmlwidgets/modules/events.js
  - inst/htmlwidgets/modules/tooltip.js
  - tests/testthat/test-sf-renderer.R
  - tests/testthat/test-sf-interactivity.R
autonomous: true
requirements:
  - SFREND-01
  - SFINTR-01
user_setup: []
must_haves:
  truths:
    - "single-panel geom_sf layers render through path.geom-sf elements"
    - "sf paths carry stable row identity and centroid DOM attributes"
    - "tooltip, hover, and custom handler selectors include path.geom-sf"
    - "tooltip rows exposed to users do not include renderer private fields such as _geom or _centroid"
  artifacts:
    - path: "inst/htmlwidgets/modules/geoms/sf.js"
      provides: "single-panel sf path renderer contract"
      contains: "geom-sf"
    - path: "inst/htmlwidgets/modules/events.js"
      provides: "tooltip/hover/handler selector integration for sf paths"
      contains: "path.geom-sf"
    - path: "inst/htmlwidgets/modules/tooltip.js"
      provides: "sanitized tooltip row exposure"
      contains: "sanitize"
    - path: "tests/testthat/test-sf-interactivity.R"
      provides: "sf interactivity contract tests"
      contains: "path.geom-sf"
  key_links:
    - from: "inst/htmlwidgets/modules/geoms/sf.js"
      to: "inst/htmlwidgets/modules/events.js"
      via: "shared SVG selector"
      pattern: "path.geom-sf"
    - from: "inst/htmlwidgets/modules/tooltip.js"
      to: "tests/testthat/test-sf-interactivity.R"
      via: "private field sanitization contract"
      pattern: "_centroid"
---

<objective>
Harden the single-panel `geom_sf` renderer contract and connect sf paths to existing tooltip, hover, and handler APIs.

Purpose: Phase 32 now produces `geom_sf` IR; Phase 33 needs that IR to become stable `path.geom-sf` SVG and participate in the established interactivity path without leaking renderer internals.
Output: Updated sf renderer/event/tooltip modules plus focused sf interactivity tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-single-panel-renderer-and-interactivity/33-CONTEXT.md
@.planning/phases/33-single-panel-renderer-and-interactivity/33-RESEARCH.md
@.planning/phases/33-single-panel-renderer-and-interactivity/33-VALIDATION.md
@.planning/phases/33-single-panel-renderer-and-interactivity/33-PATTERNS.md
@inst/htmlwidgets/modules/geoms/sf.js
@inst/htmlwidgets/modules/events.js
@inst/htmlwidgets/modules/tooltip.js
@tests/testthat/test-sf-renderer.R
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-33-01: Rendered sf paths lose row identity or centroid attributes needed by interactivity | high | Assert `data-row-id`, `data-cx`, and `data-cy` contract in renderer/source tests. |
| T-33-02: Tooltip output or formatter input exposes private renderer fields | medium | Sanitize tooltip row data before field selection and formatter calls. |
| T-33-05: New sf selector changes non-sf hover or tooltip behavior | medium | Add sf selectors through the existing selector list only and preserve all other selectors. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Lock down the sf path renderer contract</name>
  <files>inst/htmlwidgets/modules/geoms/sf.js, tests/testthat/test-sf-renderer.R</files>
  <read_first>inst/htmlwidgets/modules/geoms/sf.js, tests/testthat/test-sf-renderer.R, .planning/phases/33-single-panel-renderer-and-interactivity/33-RESEARCH.md</read_first>
  <action>Review `renderSf()` and make only scoped hardening changes needed for a stable single-panel renderer contract. Preserve the current `d3.geoIdentity().reflectY(true).fitExtent(...)` projection strategy. Ensure rendered paths keep class `geom-sf`, bind the source row datum, expose `data-row-id`, and write finite `data-cx`/`data-cy` centroid attributes when a feature can be projected. If centroid projection fails, leave centroid attrs unset rather than inventing a bbox fallback. Extend `tests/testthat/test-sf-renderer.R` or add source-contract checks in `tests/testthat/test-sf-interactivity.R` to guard these strings and behavior hooks.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-renderer.R")'</verify>
  <acceptance_criteria>
    - `inst/htmlwidgets/modules/geoms/sf.js` contains `path`
    - `inst/htmlwidgets/modules/geoms/sf.js` contains `geom-sf`
    - `inst/htmlwidgets/modules/geoms/sf.js` contains `data-row-id`
    - `inst/htmlwidgets/modules/geoms/sf.js` contains `data-cx`
    - `inst/htmlwidgets/modules/geoms/sf.js` contains `data-cy`
    - `inst/htmlwidgets/modules/geoms/sf.js` contains `reflectY(true)`
    - `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-renderer.R")'` exits 0 or skips only because optional spatial packages are unavailable
  </acceptance_criteria>
  <done>Single-panel sf renderer path, row-id, and centroid contracts are explicit and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Add sf paths to existing tooltip, hover, and handler selectors</name>
  <files>inst/htmlwidgets/modules/events.js, tests/testthat/test-sf-interactivity.R</files>
  <read_first>inst/htmlwidgets/modules/events.js, inst/htmlwidgets/modules/geoms/sf.js, .planning/phases/33-single-panel-renderer-and-interactivity/33-PATTERNS.md</read_first>
  <action>Add `path.geom-sf` to the shared `INTERACTIVE_SELECTORS` list in `inst/htmlwidgets/modules/events.js` so tooltip attachment, hover effects, custom handlers, and legend state utilities all see sf paths through the same path as existing geoms. Create `tests/testthat/test-sf-interactivity.R` if needed. Add tests that read `events.js` and assert the selector is present, while also asserting existing selectors such as `circle.geom-point`, `path.geom-line`, and `rect.geom-bar` remain present.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-interactivity.R")'</verify>
  <acceptance_criteria>
    - `inst/htmlwidgets/modules/events.js` contains `path.geom-sf`
    - `tests/testthat/test-sf-interactivity.R` contains `events.js`
    - `tests/testthat/test-sf-interactivity.R` contains `path.geom-sf`
    - `tests/testthat/test-sf-interactivity.R` contains `circle.geom-point`
    - `tests/testthat/test-sf-interactivity.R` contains `rect.geom-bar`
  </acceptance_criteria>
  <done>Existing tooltip, hover, and handler APIs target sf paths through shared selectors.</done>
</task>

<task type="auto">
  <name>Task 3: Sanitize tooltip rows for sf internals</name>
  <files>inst/htmlwidgets/modules/tooltip.js, tests/testthat/test-sf-interactivity.R</files>
  <read_first>inst/htmlwidgets/modules/tooltip.js, inst/htmlwidgets/modules/geoms/sf.js, tests/testthat/test-interactivity.R</read_first>
  <action>Add a small helper in `tooltip.js` that clones data rows and removes keys beginning with `_`. Apply it after array/wrapper unwrapping and before field discovery, enrichment, custom formatter calls, and default line rendering. Explicit `config.fields` values that start with `_` must be ignored rather than rendered. Whole-row custom formatters must receive the sanitized/enriched row. Keep existing `aes_by_var`, temporal formatting, and discrete guide label behavior unchanged. Add source-contract tests in `tests/testthat/test-sf-interactivity.R` that assert `_geom` and `_centroid` are filtered by the tooltip module.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-interactivity.R")'</verify>
  <acceptance_criteria>
    - `inst/htmlwidgets/modules/tooltip.js` contains a helper whose name includes `sanitize`
    - `inst/htmlwidgets/modules/tooltip.js` filters keys with `startsWith('_')`
    - `inst/htmlwidgets/modules/tooltip.js` applies sanitized data before whole-row formatter execution
    - `tests/testthat/test-sf-interactivity.R` contains `_geom`
    - `tests/testthat/test-sf-interactivity.R` contains `_centroid`
  </acceptance_criteria>
  <done>Tooltip output and formatter input avoid exposing sf renderer internals.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-renderer.R")'`
- [ ] `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-interactivity.R")'`
- [ ] Confirm no shared projection or facet logic was added.
</verification>

<success_criteria>
- `geom_sf` single-panel output renders via `path.geom-sf`.
- Sf paths carry row identity and projected centroid attributes for later interactivity.
- Tooltip, hover, and custom handlers attach to sf paths through existing selector infrastructure.
- Tooltip content and formatter input do not expose `_geom` or `_centroid`.
</success_criteria>

<output>
After completion, create `.planning/phases/33-single-panel-renderer-and-interactivity/33-01-SUMMARY.md`
</output>
