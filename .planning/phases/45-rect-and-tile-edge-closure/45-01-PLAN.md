---
phase: 45-rect-and-tile-edge-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-rect-tile-ir.R
  - tests/testthat/test-rect-tile-renderer.R
  - .planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md
autonomous: true
requirements: [RECT-01]
must_haves:
  truths:
    - "Maintainers can run a focused rect/tile fixture matrix that distinguishes scale-limit censoring from coordinate-limit clipping."
    - "The matrix covers continuous scale limits, coord_cartesian() limits, discrete geom_tile() grids, reversed scales, coord_flip(), and facets."
    - "Renderer/update source contracts identify whether rect.geom-rect initial render and update paths are aligned enough for closure or require a Phase 45 fix."
    - "Classification evidence records which cases are confirmed mismatches versus ggplot2-compatible non-issues."
  artifacts:
    - path: "tests/testthat/test-rect-tile-ir.R"
      provides: "RECT-01 ggplot_build() and as_d3_ir() fixture matrix for rect/tile edge behavior"
      contains: "rect_tile_cases"
    - path: "tests/testthat/test-rect-tile-renderer.R"
      provides: "RECT-01 source contract tests for rect.js and geom-registry.js rect.geom-rect behavior"
      contains: "rect.geom-rect"
    - path: ".planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md"
      provides: "Human-readable classification of every Phase 45 matrix case"
      contains: "Scale limits"
  key_links:
    - from: "tests/testthat/test-rect-tile-ir.R"
      to: "R/as_d3_ir.R"
      via: "as_d3_ir() fixture matrix"
      pattern: "as_d3_ir"
    - from: "tests/testthat/test-rect-tile-renderer.R"
      to: "inst/htmlwidgets/modules/geoms/rect.js"
      via: "rect.geom-rect initial render source assertions"
      pattern: "rect\\.geom-rect"
    - from: "tests/testthat/test-rect-tile-renderer.R"
      to: "inst/htmlwidgets/modules/geom-registry.js"
      via: "rect.geom-rect update source assertions"
      pattern: "geom-registry"
---

<objective>
Build the rect/tile reproduction matrix and classify observed behavior before changing production code.

Purpose: RECT-01 and D-01 through D-08 require evidence that separates actual gg2d3 mismatches from ggplot2-compatible scale censoring and SVG panel clipping.
Output: focused IR characterization tests, source-contract tests, and a classification artifact consumed by Plan 45-02.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-rect-and-tile-edge-closure/45-CONTEXT.md
@.planning/phases/45-rect-and-tile-edge-closure/45-RESEARCH.md
@.planning/phases/45-rect-and-tile-edge-closure/45-VALIDATION.md
@.planning/phases/45-rect-and-tile-edge-closure/45-PATTERNS.md
@AGENTS.md
@R/as_d3_ir.R
@inst/htmlwidgets/modules/geoms/rect.js
@inst/htmlwidgets/modules/geom-registry.js
@tests/testthat/test-polygon-ir.R
@tests/testthat/test-polygon-renderer.R

<interfaces>
Rect/tile IR contract from `R/as_d3_ir.R`:
```r
GeomRect = "rect"
GeomTile = "rect"
keep_aes includes "xmin", "xmax", "ymin", "ymax", "PANEL"
```

Renderer contracts to characterize:
```javascript
window.gg2d3.geomRegistry.register(['rect', 'tile'], renderRect);
container.selectAll('rect.geom-rect')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the RECT-01 IR fixture matrix</name>
  <files>tests/testthat/test-rect-tile-ir.R</files>
  <read_first>
    - tests/testthat/test-polygon-ir.R
    - tests/testthat/test-regression-core.R
    - tests/testthat/test-coord-flip.R
    - R/as_d3_ir.R
    - .planning/phases/45-rect-and-tile-edge-closure/45-CONTEXT.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-RESEARCH.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-VALIDATION.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-PATTERNS.md
  </read_first>
  <action>Create `tests/testthat/test-rect-tile-ir.R`. Load the package for direct runs with `if (!isNamespaceLoaded("gg2d3")) pkgload::load_all(quiet = TRUE)` and `library(ggplot2)`. Define local helpers `rect_bounds <- c("xmin", "xmax", "ymin", "ymax")`, `row_complete_bounds(row)`, `classify_rect_ir_case(plot)`, and a named list `rect_tile_cases`. The matrix must include these exact case names and purposes: `continuous_scale_limits` proving `scale_x_continuous(limits = c(0, 1))` and `scale_y_continuous(limits = c(0, 1))` can censor out-of-limits rect bounds to `NA` per D-02; `coord_cartesian_limits` proving `coord_cartesian(xlim = c(0, 1), ylim = c(0, 1))` preserves non-NA rect bounds for clipping; `discrete_tile_grid_limits` using `geom_tile()` over a small factor/factor grid with discrete limits and asserting kept tiles retain finite/non-NA bounds; `reversed_scale_rect` using `scale_x_reverse()` and asserting finite/non-NA IR bounds without assuming original data-space ordering; `coord_flip_rect` using `coord_flip()` and asserting `ir$coord$flip` is `TRUE` with finite bounds; and `faceted_rect_tile` using `facet_wrap(~ panel)` and asserting rows span at least two `PANEL` values. Each case must compare `ggplot2::ggplot_build(plot)$data[[1]]` with `as_d3_ir(plot)$layers[[1]]`, assert `layer$geom == "rect"`, and classify whether rows should be dropped/censored or clipped/renderable. Do not add transformed-scale fixtures per D-03 and D-08.</action>
  <acceptance_criteria>
    - `rtk rg -n "rect_tile_cases|continuous_scale_limits|coord_cartesian_limits|discrete_tile_grid_limits|reversed_scale_rect|coord_flip_rect|faceted_rect_tile" tests/testthat/test-rect-tile-ir.R` returns matches.
    - `rtk rg -n "ggplot_build|as_d3_ir|layer\\$geom|rect_bounds|row_complete_bounds|ir\\$coord\\$flip|facet_wrap|geom_tile|scale_x_reverse" tests/testthat/test-rect-tile-ir.R` returns matches.
    - `rtk rg -n "scale_(x|y)_log|trans|sqrt|perceptual|screenshot|vdiffr" tests/testthat/test-rect-tile-ir.R` returns no matches.
    - `rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-ir.R")'` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-ir.R")'</automated>
  </verify>
  <done>`tests/testthat/test-rect-tile-ir.R` proves the required RECT-01 matrix exists and classifies scale-limit, coordinate-limit, discrete, reversed, flipped, and faceted behavior without transformed-scale or screenshot scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add renderer/update source contracts and classification notes</name>
  <files>tests/testthat/test-rect-tile-renderer.R, .planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md</files>
  <read_first>
    - tests/testthat/test-polygon-renderer.R
    - inst/htmlwidgets/modules/geoms/rect.js
    - inst/htmlwidgets/modules/geom-registry.js
    - inst/htmlwidgets/modules/scales.js
    - inst/htmlwidgets/gg2d3.js
    - tests/testthat/test-rect-tile-ir.R
    - .planning/phases/45-rect-and-tile-edge-closure/45-CONTEXT.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-RESEARCH.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-VALIDATION.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-PATTERNS.md
  </read_first>
  <action>Create `tests/testthat/test-rect-tile-renderer.R` with a local `read_repo_file()` helper patterned after `test-polygon-renderer.R`. Add tests asserting `inst/htmlwidgets/modules/geoms/rect.js` registers `['rect', 'tile']`, filters invalid rows by all four bounds, writes `rect.geom-rect`, branches on `bandwidth` for band scales, branches on `options.flip`, computes continuous width/height with `Math.abs`, and uses fill/stroke/opacity accessors through existing registry helpers. Add tests asserting `inst/htmlwidgets/modules/geom-registry.js` updates `rect.geom-rect`, computes `x`, `y`, `width`, and `height`, and either contains explicit band/flip-safe logic or is flagged in the classification artifact as the only remaining update-path mismatch candidate for Plan 45-02. Create `.planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md` with a table containing one row for each required case name from Task 1 and columns `Case`, `Decision`, `Observed ggplot2 behavior`, `Observed gg2d3 IR/source behavior`, `Plan 45-02 action`. The table must label scale-limit censoring and coord-cartesian clipping separately per D-02, classify only DOM/source-measurable mismatches as `fix` per D-04, classify ggplot2-compatible or clip-path-compatible behavior as `non-issue` per D-05, and explicitly state `Transformed scales: skipped per D-03 unless later evidence requires them`.</action>
  <acceptance_criteria>
    - `rtk rg -n "rect\\.geom-rect|register\\(\\['rect', 'tile'\\]|bandwidth|options\\.flip|Math\\.abs|geom-registry|width|height" tests/testthat/test-rect-tile-renderer.R` returns matches.
    - `rtk rg -n "continuous_scale_limits|coord_cartesian_limits|discrete_tile_grid_limits|reversed_scale_rect|coord_flip_rect|faceted_rect_tile|Transformed scales: skipped per D-03|Plan 45-02 action" .planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md` returns matches.
    - `rtk rg -n "screenshot|perceptual|vdiffr|scale_.*log|transformed scale fixture" tests/testthat/test-rect-tile-renderer.R .planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md` returns no implementation-gating matches.
    - `rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-ir.R"); testthat::test_file("tests/testthat/test-rect-tile-renderer.R")'` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-ir.R"); testthat::test_file("tests/testthat/test-rect-tile-renderer.R")'</automated>
  </verify>
  <done>Plan 45-02 has concrete classification evidence and source contracts for deciding whether to patch rect/tile behavior or close it as verified non-issue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ggplot data -> ggplot2 built data | User-controlled rect/tile input is transformed by ggplot2 before gg2d3 sees it. |
| ggplot2 built data -> gg2d3 IR | Built rect/tile bounds become JSON-serializable row values consumed by JavaScript. |
| Repository source -> classification artifact | Source-contract tests and maintainer notes guide later production changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | `tests/testthat/test-rect-tile-ir.R` | mitigate | Compare `ggplot_build()` rows to `as_d3_ir()` rows so tests cannot assert renderer expectations that contradict ggplot2-built data. |
| T-45-02 | Information Disclosure | rect/tile IR rows | accept | The matrix uses small synthetic public test data only and does not add new callback payload fields. |
| T-45-03 | Denial of Service | fixture matrix | accept | Tests use tiny in-memory data frames and targeted `testthat::test_file()` commands. |
| T-45-04 | Repudiation | classification notes | mitigate | Record one classification row per required case with observed ggplot2 behavior, observed gg2d3 behavior, and explicit Plan 45-02 action. |
</threat_model>

<verification>
Run:

```bash
rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-ir.R"); testthat::test_file("tests/testthat/test-rect-tile-renderer.R")'
```
</verification>

<success_criteria>
- RECT-01 appears in this plan frontmatter.
- `tests/testthat/test-rect-tile-ir.R` exists and passes with the six required matrix cases from D-01.
- Scale limits and coordinate limits are classified separately per D-02.
- `tests/testthat/test-rect-tile-renderer.R` source-checks initial and update rect paths.
- `.planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md` records fix versus non-issue decisions for Plan 45-02.
- No transformed-scale expansion, screenshot diffs, or perceptual validation is introduced.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | Reproduce and classify deferred rect/tile out-of-bounds behavior. | Task 1 and Task 2 |
| REQ | RECT-01 focused regression fixture across relevant scale/coordinate cases or proof mismatch absent. | Task 1 and Task 2 |
| RESEARCH | Matrix must compare `ggplot_build()` rows, gg2d3 IR rows, and renderer source contracts. | Task 1 and Task 2 |
| CONTEXT D-01 | Cover continuous limits, coord_cartesian limits, discrete tiles, reversed scales, coord_flip, facets. | Task 1 |
| CONTEXT D-02 | Treat scale limits and coordinate limits as distinct behaviors. | Task 1 and classification table |
| CONTEXT D-03 | Skip transformed scales unless evidence requires them. | Task 1 and Task 2 exclusions |
| CONTEXT D-04/D-05 | Fix only proven visible/DOM mismatch; close compatible behavior as non-issue. | Task 2 classification table |
| CONTEXT D-07/D-08 | Use IR/source tests, optional browser later, no screenshot diffs. | Task 1 and Task 2 verification |
</source_audit>

<output>
After completion, create `.planning/phases/45-rect-and-tile-edge-closure/45-01-SUMMARY.md`
</output>
