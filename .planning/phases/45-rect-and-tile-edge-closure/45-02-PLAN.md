---
phase: 45-rect-and-tile-edge-closure
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - R/as_d3_ir.R
  - inst/htmlwidgets/modules/geoms/rect.js
  - inst/htmlwidgets/modules/geom-registry.js
  - tests/testthat/test-rect-tile-ir.R
  - tests/testthat/test-rect-tile-renderer.R
  - tests/testthat/helper-browser-rect-tile.R
  - tests/testthat/test-rect-tile-browser.R
  - vignettes/d3-drawing-diagnostics.md
  - .planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md
  - .planning/phases/45-rect-and-tile-edge-closure/45-VERIFICATION.md
autonomous: true
requirements: [RECT-02]
must_haves:
  truths:
    - "Every Plan 45-01 case is either fixed as a confirmed mismatch or closed as a tested non-issue with rationale."
    - "Confirmed rect/tile mismatches, if present, are corrected only at the IR or renderer/update boundary implicated by the classification."
    - "Regression coverage locks the final behavior through IR tests, source tests, and optional DOM smoke only when it adds evidence."
    - "Diagnostics and Phase 45 verification notes close the v1.10 deferred rect/tile item without broad public-doc or transformed-scale scope."
  artifacts:
    - path: "tests/testthat/test-rect-tile-ir.R"
      provides: "RECT-02 regression assertions for final rect/tile behavior"
    - path: "tests/testthat/test-rect-tile-renderer.R"
      provides: "RECT-02 source contract assertions for any rect/update fix or non-issue closure"
    - path: "vignettes/d3-drawing-diagnostics.md"
      provides: "Updated diagnostic closure for rect/tile edge behavior"
      contains: "Phase 45"
    - path: ".planning/phases/45-rect-and-tile-edge-closure/45-VERIFICATION.md"
      provides: "Phase 45 verification notes and command evidence"
      contains: "RECT-02"
  key_links:
    - from: ".planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md"
      to: "R/as_d3_ir.R"
      via: "IR-boundary fix only if classification says fix"
      pattern: "Plan 45-02 action"
    - from: ".planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md"
      to: "inst/htmlwidgets/modules/geoms/rect.js"
      via: "renderer fix only if classification says fix"
      pattern: "rect\\.geom-rect"
    - from: "vignettes/d3-drawing-diagnostics.md"
      to: ".planning/phases/45-rect-and-tile-edge-closure/45-VERIFICATION.md"
      via: "closure rationale and test evidence"
      pattern: "Phase 45"
---

<objective>
Fix confirmed rect/tile mismatches or close the verified non-issue path with durable tests and documentation.

Purpose: RECT-02 and D-04 through D-11 require Phase 45 to end the deferred rect/tile out-of-bounds item with evidence, not carry vague renderer debt forward.
Output: scoped production patch only if evidence requires it, final regression tests, optional browser DOM smoke when useful, diagnostics update, and Phase 45 verification notes.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-rect-and-tile-edge-closure/45-CONTEXT.md
@.planning/phases/45-rect-and-tile-edge-closure/45-RESEARCH.md
@.planning/phases/45-rect-and-tile-edge-closure/45-VALIDATION.md
@.planning/phases/45-rect-and-tile-edge-closure/45-PATTERNS.md
@.planning/phases/45-rect-and-tile-edge-closure/45-01-SUMMARY.md
@.planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md
@AGENTS.md
@R/as_d3_ir.R
@inst/htmlwidgets/modules/geoms/rect.js
@inst/htmlwidgets/modules/geom-registry.js
@tests/testthat/test-rect-tile-ir.R
@tests/testthat/test-rect-tile-renderer.R
@vignettes/d3-drawing-diagnostics.md

<interfaces>
Allowed production boundaries per D-06:
```text
R/as_d3_ir.R                                  # only if ggplot-built rows are serialized incorrectly
inst/htmlwidgets/modules/geoms/rect.js       # initial render x/y/width/height or filtering fix
inst/htmlwidgets/modules/geom-registry.js    # update path parity fix for rect.geom-rect
```

Validation commands from `45-VALIDATION.md`:
```bash
rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-ir.R"); testthat::test_file("tests/testthat/test-rect-tile-renderer.R")'
rtk env NOT_CRAN=true Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-browser.R")'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply only classification-proven rect/tile fixes or lock non-issues</name>
  <files>R/as_d3_ir.R, inst/htmlwidgets/modules/geoms/rect.js, inst/htmlwidgets/modules/geom-registry.js, tests/testthat/test-rect-tile-ir.R, tests/testthat/test-rect-tile-renderer.R, .planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md</files>
  <read_first>
    - .planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-01-SUMMARY.md
    - tests/testthat/test-rect-tile-ir.R
    - tests/testthat/test-rect-tile-renderer.R
    - R/as_d3_ir.R
    - inst/htmlwidgets/modules/geoms/rect.js
    - inst/htmlwidgets/modules/geom-registry.js
    - .planning/phases/45-rect-and-tile-edge-closure/45-CONTEXT.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-RESEARCH.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-VALIDATION.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-PATTERNS.md
  </read_first>
  <action>Read the `Plan 45-02 action` column for every row in `45-RECT-TILE-CLASSIFICATION.md`. For rows classified `fix`, change only the implicated boundary: update `R/as_d3_ir.R` only if ggplot2-built non-NA rect/tile bounds are serialized incorrectly; update `inst/htmlwidgets/modules/geoms/rect.js` only if initial render computes non-finite or visibly wrong `x`, `y`, `width`, or `height`; update `inst/htmlwidgets/modules/geom-registry.js` only if the update path diverges from the corrected initial renderer. Preserve `rect.geom-rect`, all four-bound filtering, band-scale `bandwidth()` handling, coord-flip handling, and SVG panel clipping. For rows classified `non-issue`, add or tighten tests in `test-rect-tile-ir.R` or `test-rect-tile-renderer.R` that prove the behavior is ggplot2-compatible or intentionally clip-path-compatible. Update `45-RECT-TILE-CLASSIFICATION.md` so each row's final action is one of `fixed in R/as_d3_ir.R`, `fixed in rect.js`, `fixed in geom-registry.js`, or `closed as non-issue with tests`. Do not add transformed-scale tests, broad scale/coordinate refactors, screenshot diffs, or new rendering libraries.</action>
  <acceptance_criteria>
    - `rtk rg -n "fixed in R/as_d3_ir\\.R|fixed in rect\\.js|fixed in geom-registry\\.js|closed as non-issue with tests" .planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md` returns one final-action match for every required matrix case.
    - `rtk rg -n "rect\\.geom-rect|bandwidth|options\\.flip|Math\\.abs|xmin|xmax|ymin|ymax" tests/testthat/test-rect-tile-renderer.R tests/testthat/test-rect-tile-ir.R` returns matches.
    - `rtk rg -n "scale_(x|y)_log|trans|sqrt|vdiffr|screenshot|perceptual" R/as_d3_ir.R inst/htmlwidgets/modules/geoms/rect.js inst/htmlwidgets/modules/geom-registry.js tests/testthat/test-rect-tile-ir.R tests/testthat/test-rect-tile-renderer.R` returns no new Phase 45 implementation scope matches.
    - `rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-ir.R"); testthat::test_file("tests/testthat/test-rect-tile-renderer.R")'` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-ir.R"); testthat::test_file("tests/testthat/test-rect-tile-renderer.R")'</automated>
  </verify>
  <done>All classified rect/tile cases are either fixed at the evidence-backed boundary or locked as tested non-issues, with no broader renderer rewrite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add optional DOM smoke only if it proves visible clipping or update behavior</name>
  <files>tests/testthat/helper-browser-rect-tile.R, tests/testthat/test-rect-tile-browser.R, tests/testthat/test-rect-tile-renderer.R</files>
  <read_first>
    - .planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md
    - tests/testthat/helper-browser-polygon.R
    - tests/testthat/test-polygon-browser.R
    - tests/testthat/test-rect-tile-ir.R
    - tests/testthat/test-rect-tile-renderer.R
    - inst/htmlwidgets/modules/geoms/rect.js
    - inst/htmlwidgets/modules/geom-registry.js
    - .planning/phases/45-rect-and-tile-edge-closure/45-CONTEXT.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-RESEARCH.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-VALIDATION.md
  </read_first>
  <action>If the classification leaves a visible clipping, facet placement, reversed-scale DOM geometry, coord_flip DOM geometry, or update-path question that IR/source tests cannot prove, create `tests/testthat/helper-browser-rect-tile.R` and `tests/testthat/test-rect-tile-browser.R` patterned after the polygon browser helper. The helper must skip on CRAN, missing `chromote`, missing Chrome/Chromium, or failed session launch with explicit testthat skip messages. Browser tests must assert live DOM, not screenshots: count `rect.geom-rect`, collect numeric `x`, `y`, `width`, and `height`, assert all rendered numeric attributes are finite, assert expected panel clip-path ancestry, assert faceted cases render in multiple panels when included, and optionally trigger the existing update path if the classification identified update divergence. If IR/source tests already prove all cases, do not create the browser files; instead add a `test_that("Phase 45 does not require browser smoke for rect/tile closure", ...)` source test in `test-rect-tile-renderer.R` that reads `45-RECT-TILE-CLASSIFICATION.md` and expects no unresolved `browser-required` marker. In both paths, do not add screenshot, pixel, perceptual, Playwright, Puppeteer, Selenium, or vdiffr dependencies.</action>
  <acceptance_criteria>
    - If browser files are created: `rtk rg -n "skip_browser_rect_tile_smoke|chromote|Chrome/Chromium|rect\\.geom-rect|clip-path|width|height|is\\.finite" tests/testthat/helper-browser-rect-tile.R tests/testthat/test-rect-tile-browser.R` returns matches.
    - If browser files are not created: `rtk rg -n "does not require browser smoke|browser-required|45-RECT-TILE-CLASSIFICATION" tests/testthat/test-rect-tile-renderer.R` returns matches and `test-rect-tile-browser.R` does not exist.
    - `rtk rg -n "screenshot|pixel|perceptual|playwright|puppeteer|selenium|vdiffr" tests/testthat/helper-browser-rect-tile.R tests/testthat/test-rect-tile-browser.R tests/testthat/test-rect-tile-renderer.R 2>/dev/null` returns no Phase 45 validation-gate matches.
    - If browser files are created: `rtk env NOT_CRAN=true Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-browser.R")'` exits 0 or skips cleanly with explicit optional browser messages.
    - In all cases: `rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-renderer.R")'` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-renderer.R")'</automated>
    <automated>rtk env NOT_CRAN=true Rscript --vanilla -e 'if (file.exists("tests/testthat/test-rect-tile-browser.R")) { pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-browser.R") }'</automated>
  </verify>
  <done>Optional browser DOM smoke is either present and cleanly skippable, or explicitly proven unnecessary by source tests and classification evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Close diagnostics and Phase 45 verification notes</name>
  <files>vignettes/d3-drawing-diagnostics.md, .planning/phases/45-rect-and-tile-edge-closure/45-VERIFICATION.md, .planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md</files>
  <read_first>
    - vignettes/d3-drawing-diagnostics.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-RECT-TILE-CLASSIFICATION.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-VALIDATION.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-CONTEXT.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-RESEARCH.md
    - .planning/phases/45-rect-and-tile-edge-closure/45-01-SUMMARY.md
  </read_first>
  <action>Update the rect/tile section in `vignettes/d3-drawing-diagnostics.md` so it no longer describes the v1.10 rect/tile out-of-bounds concern as open deferred debt. The wording must mention Phase 45, distinguish scale-limit censoring from coord_cartesian/SVG clipping, state whether any confirmed mismatch was fixed or all cases were closed as non-issues, and leave broader README/public documentation to Phase 47 unless the actual fix changed the user-facing support contract per D-11. Create `.planning/phases/45-rect-and-tile-edge-closure/45-VERIFICATION.md` with sections `Goal Achievement`, `Requirements Coverage`, `Commands Run`, `Classification Outcome`, and `Residual Risks`. Include the exact targeted commands from `45-VALIDATION.md`, their pass/skip status, a note that screenshot/perceptual diffs were not introduced per D-08, and a note that transformed scales remain skipped per D-03 unless Task 1 evidence changed that conclusion. Keep `45-RECT-TILE-CLASSIFICATION.md` as the detailed evidence table and link/reference it from the verification notes.</action>
  <acceptance_criteria>
    - `rtk rg -n "Phase 45|scale-limit|coord_cartesian|SVG clipping|rect|tile|out-of-bounds" vignettes/d3-drawing-diagnostics.md` returns matches.
    - `rtk rg -n "Goal Achievement|Requirements Coverage|Commands Run|Classification Outcome|Residual Risks|RECT-01|RECT-02|45-RECT-TILE-CLASSIFICATION|screenshot/perceptual|Transformed scales" .planning/phases/45-rect-and-tile-edge-closure/45-VERIFICATION.md` returns matches.
    - `rtk rg -n "open deferred debt|pending focused regression evidence|unresolved rect/tile" vignettes/d3-drawing-diagnostics.md` returns no stale Phase 45 rect/tile closure wording.
    - `rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-ir.R"); testthat::test_file("tests/testthat/test-rect-tile-renderer.R")'` exits 0.
    - If `tests/testthat/test-rect-tile-browser.R` exists, `rtk env NOT_CRAN=true Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-browser.R")'` exits 0 or skips cleanly with explicit optional browser messages.
  </acceptance_criteria>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-ir.R"); testthat::test_file("tests/testthat/test-rect-tile-renderer.R")'</automated>
    <automated>rtk rg -n "Phase 45|RECT-02|45-RECT-TILE-CLASSIFICATION|screenshot/perceptual|Transformed scales" vignettes/d3-drawing-diagnostics.md .planning/phases/45-rect-and-tile-edge-closure/45-VERIFICATION.md</automated>
  </verify>
  <done>The v1.10 deferred rect/tile item is closed in diagnostics and Phase 45 verification notes with reproducible evidence and no out-of-scope validation machinery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gg2d3 IR -> SVG attributes | Rect/tile bounds become SVG `x`, `y`, `width`, and `height` attributes in `rect.js` and the update path. |
| SVG DOM -> optional browser assertions | Rendered local widget DOM is inspected by optional chromote tests. |
| Classification evidence -> documentation | Test outcomes are summarized in diagnostics and verification notes that future maintainers rely on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05 | Tampering | `rect.js` / `geom-registry.js` | mitigate | Patch only cases marked `fix` in the classification and verify finite numeric SVG geometry plus source parity. |
| T-45-06 | Denial of Service | SVG rect attributes | mitigate | Tests require invalid rows to be filtered and rendered numeric geometry to be finite; no large-data paths are added. |
| T-45-07 | Information Disclosure | optional browser artifacts | accept | Browser smoke uses synthetic local fixtures and existing ignored test output patterns; no user data or callback payload expansion is introduced. |
| T-45-08 | Repudiation | diagnostics and verification notes | mitigate | Record commands, pass/skip status, classification outcome, and residual risks in `45-VERIFICATION.md`. |
| T-45-09 | Elevation of Privilege | browser validation tooling | mitigate | Reuse optional chromote/testthat pattern only; do not introduce new browser stacks, remote scripts, or screenshot tooling. |
</threat_model>

<verification>
Run:

```bash
rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-ir.R"); testthat::test_file("tests/testthat/test-rect-tile-renderer.R")'
rtk env NOT_CRAN=true Rscript --vanilla -e 'if (file.exists("tests/testthat/test-rect-tile-browser.R")) { pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-rect-tile-browser.R") }'
rtk rg -n "Phase 45|RECT-02|45-RECT-TILE-CLASSIFICATION|screenshot/perceptual|Transformed scales" vignettes/d3-drawing-diagnostics.md .planning/phases/45-rect-and-tile-edge-closure/45-VERIFICATION.md
```
</verification>

<success_criteria>
- RECT-02 appears in this plan frontmatter.
- Every Plan 45-01 case is either fixed at the implicated boundary or closed as a tested non-issue.
- Targeted IR/source tests pass.
- Optional browser DOM smoke is used only if needed, remains CRAN-compatible, and has clean skip behavior.
- `vignettes/d3-drawing-diagnostics.md` closes the rect/tile deferred note with Phase 45 evidence.
- `.planning/phases/45-rect-and-tile-edge-closure/45-VERIFICATION.md` records commands, classification outcome, and residual risks.
- No screenshot/perceptual diffs, transformed-scale expansion, public-doc sweep, or broad coordinate/scale refactor is introduced.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | Fix confirmed rect/tile mismatch or explicitly close with evidence. | Task 1, Task 2, Task 3 |
| REQ | RECT-02 confirmed mismatches fixed; non-issues locked with tests and rationale. | Task 1 and Task 3 |
| RESEARCH | Patch only `R/as_d3_ir.R`, `rect.js`, or `geom-registry.js` when evidence requires it. | Task 1 |
| RESEARCH | Optional browser DOM smoke only for visible clipping/update evidence, with clean skips. | Task 2 |
| CONTEXT D-04 | Fix only visible or DOM-measurable mismatch against ggplot2. | Task 1 and Task 2 |
| CONTEXT D-05 | Close compatible/SVG-clipped behavior as non-issue with tests and rationale. | Task 1 and Task 3 |
| CONTEXT D-06 | Keep fixes at renderer or IR boundary; avoid broad refactors. | Task 1 |
| CONTEXT D-07/D-09 | Use IR/source tests plus optional CRAN-compatible browser smoke. | Task 1 and Task 2 |
| CONTEXT D-08 | Do not introduce screenshot or perceptual diffs. | Task 2 and Task 3 exclusions |
| CONTEXT D-10 | Update diagnostics and Phase 45 verification notes. | Task 3 |
| CONTEXT D-11 | Leave broad public docs to Phase 47 unless support contract changes. | Task 3 |
</source_audit>

<output>
After completion, create `.planning/phases/45-rect-and-tile-edge-closure/45-02-SUMMARY.md`
</output>
