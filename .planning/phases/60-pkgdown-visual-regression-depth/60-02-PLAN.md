---
phase: 60-pkgdown-visual-regression-depth
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - .github/workflows/pkgdown.yaml
autonomous: true
requirements: [VIS-01, VIS-03]
user_setup: []

must_haves:
  truths:
    - "The pkgdown.yaml CI workflow installs chromote so the visual capture step can launch a headless browser (VIS-01)"
    - "The pkgdown visual capture step runs test-pkgdown-visual.R AFTER pkgdown builds docs/ and BEFORE the deploy step, so docs/articles/gg2d3.html exists when the test navigates to it (VIS-01)"
    - "GG2D3_BROWSER_VISUAL_CI=true is set only on the capture step's env block, so a skip in CI escalates to a failure for that step but does not affect other steps (VIS-03)"
    - "Captured artifacts under test_output/pkgdown-visual/ are uploaded as a named CI artifact with if: always() so evidence survives step failure (VIS-01, VIS-03)"
    - "FLAGGED ASSUMPTION (VIS-01, probe unresolved): GitHub Actions ubuntu-latest has a discoverable Chrome/Chromium binary; the Locate Chrome step is non-fatal (exit 0 when absent) so a missing browser degrades to a skip rather than a hard workflow error"
    - "FLAGGED ASSUMPTION (VIS-03, probe unresolved): adding any::chromote to the existing extra-packages line is sufficient to install chromote in the website dependency set without conflict"
  artifacts:
    - ".github/workflows/pkgdown.yaml (modified: chromote install + 3 new steps)"
  key_links:
    - "The new capture step depends on the preceding 'Build site' step producing docs/articles/gg2d3.html; it runs test-pkgdown-visual.R from Plan 01"
    - "extra-packages: any::pkgdown, any::chromote, local::. installs chromote for the capture step's chromote session"
    - "The upload-artifact step uses path test_output/pkgdown-visual/ which the Plan 01 test writes into"
  prohibitions:
    - statement: "Must NOT set GG2D3_BROWSER_VISUAL_CI=true at the job-level env block; it belongs only on the 'Run pkgdown visual capture' step's env block (RESEARCH anti-pattern)"
      status: flagged-unverified
      verification: manual
    - statement: "Must NOT create a separate workflow file; the capture runs inside the existing pkgdown.yaml (D-03)"
      status: flagged-unverified
      verification: manual
    - statement: "Must NOT make the Locate Chrome step fatal on a missing browser; it must exit 0 (non-fatal) so the test skips rather than hard-failing the workflow (RESEARCH Pattern 5)"
      status: flagged-unverified
      verification: manual
---

<objective>
Wire the Plan 01 test into the existing `pkgdown.yaml` CI workflow so representative browser visual evidence is captured on every pkgdown build. This expands the proven tracer slice from local opt-in to CI: install chromote in the website dependency set, add a non-fatal Locate-Chrome step, run `test-pkgdown-visual.R` after the build with CI-escalation enabled, and upload the artifacts.

Purpose: Make the visual evidence gate part of the release pipeline (not just a local convenience), so blank/stale widgets in the actually-published site are caught before deploy (VIS-01, VIS-03).
Output: A modified `.github/workflows/pkgdown.yaml` with chromote installed and three new steps inserted after the site build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-pkgdown-visual-regression-depth/60-CONTEXT.md
@.planning/phases/60-pkgdown-visual-regression-depth/60-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan modifies:
- **File:** `.github/workflows/pkgdown.yaml`
  - **Changed line:** `extra-packages: any::pkgdown, any::chromote, local::.` (adds `any::chromote`)
  - **New step:** `Locate Chrome for chromote (pkgdown visual)` (non-fatal Chrome discovery, sets `CHROMOTE_CHROME`)
  - **New step:** `Run pkgdown visual capture` (env: `NOT_CRAN`, `GG2D3_BROWSER_VISUAL_SMOKE`, `GG2D3_BROWSER_VISUAL_CI`; runs `test-pkgdown-visual.R`)
  - **New step:** `Upload pkgdown visual artifacts` (uploads `test_output/pkgdown-visual/` as `pkgdown-visual-${{ github.run_id }}`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Install chromote and add the three visual-capture steps to pkgdown.yaml</name>
  <files>.github/workflows/pkgdown.yaml</files>
  <read_first>
    - .github/workflows/pkgdown.yaml (the file being modified — current step order, extra-packages line, insertion point)
    - .github/workflows/browser-visual-smoke.yaml (exact analog for the Locate Chrome, Run capture, and Upload artifact steps)
    - tests/testthat/test-pkgdown-visual.R (the test the capture step invokes — created in Plan 01)
    - .planning/phases/60-pkgdown-visual-regression-depth/60-PATTERNS.md (exact YAML for the three steps and the extra-packages change)
    - .planning/phases/60-pkgdown-visual-regression-depth/60-RESEARCH.md (Pattern 5; Pitfalls 1 and 6; Open Question 1)
  </read_first>
  <action>
    Modify `.github/workflows/pkgdown.yaml`.

    1. In the `setup-r-dependencies` step, change the `extra-packages` value from `any::pkgdown, local::.` to `any::pkgdown, any::chromote, local::.` (keep `needs: website` unchanged). This installs chromote per RESEARCH Pitfall 6.

    2. Insert three new steps AFTER the existing `Validate generated pkgdown site` step and BEFORE the existing `Upload pkgdown site artifact` step (per PATTERNS insertion point). YAML steps run sequentially, so this guarantees `docs/articles/gg2d3.html` exists (built by the earlier `Build site` step) when the capture runs (RESEARCH Pitfall 1):

       a. `Locate Chrome for chromote (pkgdown visual)` — a bash step that loops over `google-chrome google-chrome-stable chromium chromium-browser`, writes `CHROMOTE_CHROME=<path>` to `GITHUB_ENV` and prints `--version` on the first hit, then `exit 0`. If none found, echo a "will be skipped" message and DO NOT `exit 1` — the step must be NON-FATAL (this differs from browser-visual-smoke.yaml which exits 1). This lets the test skip rather than hard-fail the workflow (RESEARCH Pattern 5, Open Question 1).

       b. `Run pkgdown visual capture` — with a step-level `env:` block containing `NOT_CRAN: "true"`, `GG2D3_BROWSER_VISUAL_SMOKE: "true"`, and `GG2D3_BROWSER_VISUAL_CI: "true"`. The `run:` invokes `Rscript --vanilla -e` with the analog tryCatch wrapper that calls `pkgload::load_all(quiet = TRUE)`, runs `testthat::test_file("tests/testthat/test-pkgdown-visual.R")`, converts the result to a data.frame, and `quit(status = 1)` if any `failed > 0` or any `error`; the outer `error =` handler prints `FATAL:` and quits status 1. Set the env vars ONLY on this step — do NOT add `GG2D3_BROWSER_VISUAL_CI` to the job-level `env:` block (RESEARCH anti-pattern).

       c. `Upload pkgdown visual artifacts` — `if: always()`, `uses: actions/upload-artifact@v6`, with `name: pkgdown-visual-${{ github.run_id }}`, `path: test_output/pkgdown-visual/`, `if-no-files-found: warn`, `retention-days: 14`.

    Do NOT create a new workflow file (D-03). Do NOT alter the existing Build site, Validate, Upload pkgdown site artifact, or Deploy steps. Preserve the existing job-level `env:` block (only `GITHUB_PAT`).
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/pkgdown.yaml')); steps=d['jobs']['pkgdown']['steps']; names=[s.get('name','') for s in steps]; assert 'Run pkgdown visual capture' in names, 'capture step missing'; assert 'Upload pkgdown visual artifacts' in names, 'upload step missing'; assert 'Locate Chrome for chromote (pkgdown visual)' in names, 'locate step missing'; job_env=d['jobs']['pkgdown'].get('env',{}); assert 'GG2D3_BROWSER_VISUAL_CI' not in job_env, 'CI env must not be job-level'; cap=[s for s in steps if s.get('name')=='Run pkgdown visual capture'][0]; assert cap['env'].get('GG2D3_BROWSER_VISUAL_CI')=='true', 'CI env must be on capture step'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/pkgdown.yaml` `extra-packages` line contains `any::chromote`
    - `.github/workflows/pkgdown.yaml` contains a step named `Run pkgdown visual capture` that references `tests/testthat/test-pkgdown-visual.R`
    - `.github/workflows/pkgdown.yaml` contains a step named `Upload pkgdown visual artifacts` with `path: test_output/pkgdown-visual/` and `name: pkgdown-visual-${{ github.run_id }}`
    - The capture step's `env:` block sets `GG2D3_BROWSER_VISUAL_CI: "true"`; the job-level `env:` block does NOT contain `GG2D3_BROWSER_VISUAL_CI` (verified by the automated YAML assertion)
    - The Locate Chrome step for pkgdown visual is non-fatal when no browser is found: its fallback path echoes a "will be skipped" message and ends the step with a zero exit status (no non-zero exit on the not-found branch), so the workflow proceeds to the capture step which then skips
    - The three new steps appear after `Validate generated pkgdown site` and before `Upload pkgdown site artifact` in step order
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/pkgdown.yaml'))"` parses without error (valid YAML)
    - Behavior: no separate `.github/workflows/pkgdown-visual*.yaml` file is created (`ls .github/workflows/` unchanged except pkgdown.yaml edits)
  </acceptance_criteria>
  <done>pkgdown.yaml installs chromote, runs the Plan 01 visual capture after the site build with CI escalation scoped to that step only, and uploads the artifacts; YAML validates and the assertion script prints OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> GitHub artifact store | The capture step uploads `test_output/pkgdown-visual/` (PNG + JSON) to the trusted GitHub Actions artifact store using the existing pinned `actions/upload-artifact@v6`. |
| workflow env -> step env | `GG2D3_BROWSER_VISUAL_CI` is scoped to the capture step; it must not leak to other steps that could then escalate unrelated skips. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-04 | Tampering | Supply chain (actions/upload-artifact) | low | mitigate | Reuse the already-vetted `actions/upload-artifact@v6` already present in this workflow; no new third-party action is introduced. chromote is an existing CRAN Suggests dependency (Package Legitimacy Audit: approved). |
| T-60-05 | Elevation of Privilege | Env var scope leak | low | mitigate | `GG2D3_BROWSER_VISUAL_CI=true` is set on the capture step's `env:` only, never job-level, preventing unrelated steps from treating skips as failures. Verified by the automated YAML assertion. |
| T-60-06 | Denial of Service | Non-fatal Chrome discovery | low | accept | Locate Chrome exits 0 when no browser is found so a missing runner browser degrades to a test skip, not a workflow-blocking error. Accepted: CI still proceeds to deploy. |
</threat_model>

<verification>
- YAML parses and the assertion script confirms all three steps exist, chromote is installed, and CI escalation is step-scoped (VIS-01, VIS-03).
- No separate workflow file is created (D-03 respected).
- The capture step invokes the Plan 01 test file, closing the CI capture path (VIS-01).
</verification>

<success_criteria>
- `pkgdown.yaml` installs chromote, captures visual evidence after the build, escalates CI skips only on the capture step, and uploads artifacts.
- Valid YAML; assertion script prints OK.
</success_criteria>

<output>
Create `.planning/phases/60-pkgdown-visual-regression-depth/60-02-SUMMARY.md` when done.
</output>
