---
phase: 60-pkgdown-visual-regression-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-pkgdown-visual.R
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03]
user_setup: []

must_haves:
  truths:
    - "Running the opt-in test loads docs/articles/gg2d3.html in a chromote session and asserts the page renders at least 10 gg2d3 SVG widgets (VIS-01, VIS-02)"
    - "The test detects blank/stale widget regions by asserting every rendered gg2d3 SVG has >= 3 SVG child elements (DOM-based SVG child count per D-04); blankWidgetCount must equal 0 (VIS-02)"
    - "The sf widget region passes when pkgdown_site_sf_outcome() is 'rendered' (>= 1 .geom-sf element) OR is 'classified_skip' (PKGDOWN_SF_OPTIONAL_SKIP text present); 'missing' fails (D-06, VIS-02)"
    - "The test writes a review PNG plus a DOM-summary JSON per page (D-05) and a browser-log JSON into test_output/pkgdown-visual/ which is already gitignored and Rbuildignored (VIS-01, VIS-03)"
    - "The test skips cleanly when GG2D3_BROWSER_VISUAL_SMOKE is unset or chromote/Chrome is unavailable, and skips when docs/articles/gg2d3.html is missing (VIS-03)"
    - "FLAGGED ASSUMPTION (VIS-01, probe unresolved): representative browser evidence for pages including core widgets, sf, and Crosstalk examples is captured by loading the single main article docs/articles/gg2d3.html, since it contains all three example types"
    - "FLAGGED ASSUMPTION (VIS-02, probe unresolved): a stale widget region is defined as a rendered SVG with fewer than 3 child elements; the threshold constant is documented and adjustable"
    - "FLAGGED ASSUMPTION (VIS-03, probe unresolved): determinism is achieved by asserting DOM-derived integer counts (not pixel diffs) so the same built site produces the same pass/fail verdict"
  artifacts:
    - "tests/testthat/test-pkgdown-visual.R"
    - "test_output/pkgdown-visual/pkgdown-main-article.png (created at test run)"
    - "test_output/pkgdown-visual/pkgdown-main-article-dom-summary.json (created at test run)"
    - "test_output/pkgdown-visual/pkgdown-main-article-browser-log.json (created at test run)"
  key_links:
    - "test-pkgdown-visual.R sources helper-browser-visual.R (skip_browser_visual_smoke, with_chromote_session, eval_js_value, browser_visual_file_url) and helper-pkgdown-site.R (pkgdown_site_resolve_path, pkgdown_site_skip_if_generated_docs_unavailable, pkgdown_site_sf_outcome, pkgdown_site_crosstalk_outcome)"
    - "The DOM-summary JSON's blankWidgetCount and renderedSvgCount fields drive pass/fail; the PNG is human-review only"
  prohibitions:
    - statement: "Must NOT call htmlwidgets::saveWidget(); the test navigates to the pre-built docs/articles/gg2d3.html directly via browser_visual_file_url() (D-01)"
      status: flagged-unverified
      verification: manual
    - statement: "Must NOT add pkgdown-specific helper functions to the shared helper-browser-visual.R; file-private .pkgdown_* helpers live inside test-pkgdown-visual.R"
      status: flagged-unverified
      verification: manual
    - statement: "Must NOT unconditionally assert sf SVG children; always branch on pkgdown_site_sf_outcome() and accept the skip notice when outcome is classified_skip (D-06)"
      status: flagged-unverified
      verification: manual
    - statement: "Must NOT introduce pixel-brightness blank detection; blank/stale detection is DOM-based SVG child counting only (deferred/rejected in CONTEXT.md)"
      status: flagged-unverified
      verification: manual
    - statement: "Must NOT add a new opt-in env var; reuse GG2D3_BROWSER_VISUAL_SMOKE (D-10)"
      status: flagged-unverified
      verification: manual
    - statement: "Must NOT capture the interactivity article gg2d3-interactivity.html; only docs/articles/gg2d3.html is in scope (D-07, D-08)"
      status: flagged-unverified
      verification: manual
---

<objective>
Create the tracer: a single opt-in testthat file `tests/testthat/test-pkgdown-visual.R` (the new command surface per D-09) that loads the pre-built pkgdown main article in a headless chromote session, asserts that widget regions render (SVG child count based blank/stale detection), branches on sf and Crosstalk outcomes, and writes PNG + DOM-summary + browser-log artifacts under `test_output/pkgdown-visual/`. This is the end-to-end vertical slice touching every layer this phase modifies (test infra -> chromote navigation -> DOM assertion -> artifact writing), proving the architecture before CI wiring (Plan 02) and documentation (Plan 03).

Purpose: Deepen release confidence by catching blank widgets that have correct HTML structure but no rendered SVG content — something the existing marker-based `test-pkgdown-site.R` cannot detect (VIS-01, VIS-02, VIS-03).
Output: `tests/testthat/test-pkgdown-visual.R` plus its runtime artifacts in the gitignored `test_output/pkgdown-visual/` directory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-pkgdown-visual-regression-depth/60-CONTEXT.md
@.planning/phases/60-pkgdown-visual-regression-depth/60-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- **File:** `tests/testthat/test-pkgdown-visual.R`
- **Test id / testthat block:** `test_that("BVIS-PKG-01 pkgdown article browser capture detects rendered widgets and records evidence", ...)`
- **File-private function:** `.pkgdown_visual_wait_for_widgets(session, min_svg_count = 10, timeout = 30)`
- **File-private function:** `.pkgdown_visual_dom_summary_script(threshold = .PKGDOWN_VISUAL_SVG_CHILD_THRESHOLD)`
- **File-private function:** `pkgdown_visual_artifact_dir()` (returns/creates `test_output/pkgdown-visual/`)
- **File-private constant:** `.PKGDOWN_VISUAL_SVG_CHILD_THRESHOLD` (integer, documented, default 3L)
- **Runtime artifact dir:** `test_output/pkgdown-visual/` with `pkgdown-main-article.png`, `pkgdown-main-article-dom-summary.json`, `pkgdown-main-article-browser-log.json`
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end pkgdown visual capture — one article, one path, committed</name>
  <files>tests/testthat/test-pkgdown-visual.R</files>
  <read_first>
    - tests/testthat/test-browser-visual-smoke.R (exact analog: helper sourcing header, with_chromote_session usage, eval_js_value assertion loop, artifact writing)
    - tests/testthat/helper-browser-visual.R (reuse: skip_browser_visual_smoke, with_chromote_session, eval_js_value, browser_visual_file_url, browser_visual_console_collector, browser_visual_logs, assert_no_browser_visual_errors, .browser_visual_selector_count, `%||%`, .test_output_dir sourcing chain)
    - tests/testthat/helper-pkgdown-site.R (reuse: pkgdown_site_resolve_path, pkgdown_site_skip_if_generated_docs_unavailable, pkgdown_site_sf_outcome, pkgdown_site_crosstalk_outcome)
    - .planning/phases/60-pkgdown-visual-regression-depth/60-PATTERNS.md (concrete R snippets for every helper below)
    - .planning/phases/60-pkgdown-visual-regression-depth/60-RESEARCH.md (Pitfalls 2, 4, 5; selector notes)
  </read_first>
  <behavior>
    - When GG2D3_BROWSER_VISUAL_SMOKE is unset: test SKIPS (via skip_browser_visual_smoke).
    - When docs/articles/gg2d3.html is missing: test SKIPS (via pkgdown_site_skip_if_generated_docs_unavailable).
    - When opted in and docs present + Chrome available: navigate to the article, poll until >= 10 gg2d3 SVGs are present, then assert renderedSvgCount >= 10 and blankWidgetCount == 0.
    - sf branch: outcome "rendered" -> geomSfCount >= 1; outcome "classified_skip" -> page innerText contains PKGDOWN_SF_OPTIONAL_SKIP; outcome "missing" -> fail.
    - Crosstalk branch: outcome in {rendered, rendered_unlinked_assets} -> `[data-gg2d3-crosstalk-group]` count >= 1; outcome "classified_skip" -> accept; outcome "missing" -> fail.
    - After assertions: write pkgdown-main-article.png, pkgdown-main-article-dom-summary.json, pkgdown-main-article-browser-log.json to test_output/pkgdown-visual/ and assert each file exists.
    - No browser exceptions captured (assert_no_browser_visual_errors).
  </behavior>
  <action>
    Create `tests/testthat/test-pkgdown-visual.R` following the analog `test-browser-visual-smoke.R` exactly for the helper-sourcing header (source `helper-browser-visual.R` guarded on `skip_browser_visual_smoke`, and `helper-pkgdown-site.R` guarded on `pkgdown_site_sf_outcome`, checking both `tests/testthat/<file>` and `<file>` paths).

    Define file-private members (all prefixed `.pkgdown_` except the artifact-dir helper):
    - Constant `.PKGDOWN_VISUAL_SVG_CHILD_THRESHOLD <- 3L`, with a comment documenting it is Claude's-discretion (per CONTEXT.md), the definition of a non-blank widget, and adjustable without changing the interface.
    - `pkgdown_visual_artifact_dir()` returning `file.path(.test_output_dir(), "pkgdown-visual")` and creating it with `dir.create(..., showWarnings = FALSE, recursive = TRUE)`.
    - `.pkgdown_visual_wait_for_widgets(session, min_svg_count = 10, timeout = 30)` polling `document.querySelectorAll('.gg2d3.html-widget svg').length` every 0.25s until `>= min_svg_count` or timeout, then `testthat::fail(...)` with the actual count on timeout. Use `%||%` for the null-safe count in the fail message.
    - `.pkgdown_visual_dom_summary_script(threshold = .PKGDOWN_VISUAL_SVG_CHILD_THRESHOLD)` returning a JS IIFE string that reports: `title`, `widgetCount` (count of `.gg2d3.html-widget`), `renderedSvgCount` (count of `.gg2d3.html-widget svg`), `svgChildCounts` (array of per-svg child counts over `path, circle, rect, line, g`), `blankWidgetCount` (count of svgChildCounts entries `< threshold`), `geomSfCount` (count of `.geom-sf`), `crosstalkGroupCount` (count of `[data-gg2d3-crosstalk-group]`), and `bodyTextLength`.

    Write one `test_that("BVIS-PKG-01 pkgdown article browser capture detects rendered widgets and records evidence", { ... })` block. Body:
    1. `skip_browser_visual_smoke()` then `pkgdown_site_skip_if_generated_docs_unavailable()`.
    2. Resolve the article path with `pkgdown_site_resolve_path("docs/articles/gg2d3.html")`; capture `sf_outcome <- pkgdown_site_sf_outcome()` and `ct_outcome <- pkgdown_site_crosstalk_outcome()` (these read the resolved HTML; call before or inside the session as convenient).
    3. `with_chromote_session({ ... }, width = 1280, height = 900)` block:
       - `logs <- browser_visual_console_collector(session)`.
       - `url <- browser_visual_file_url(normalizePath(article_path, mustWork = TRUE))`; `session$go_to(url, delay = 2)` (delay = 2 for the 13MB page per Pitfall 2 — larger than the smoke test's delay = 1).
       - `.pkgdown_visual_wait_for_widgets(session, min_svg_count = 10, timeout = 30)`.
       - `dom_summary <- eval_js_value(session, .pkgdown_visual_dom_summary_script())`.
       - `testthat::expect_gte(dom_summary$renderedSvgCount, 10L, label = ...)` and `testthat::expect_equal(dom_summary$blankWidgetCount, 0L, label = ...)`.
       - sf branch on `sf_outcome`: "rendered" -> `expect_gte(dom_summary$geomSfCount, 1L, ...)`; "classified_skip" -> `expect_true(eval_js_value(session, "document.body.innerText.includes('PKGDOWN_SF_OPTIONAL_SKIP')"), ...)`; else `testthat::fail(...)`.
       - Crosstalk branch on `ct_outcome`: value in `c("rendered", "rendered_unlinked_assets")` -> `expect_gte(as.integer(.browser_visual_selector_count(session, "[data-gg2d3-crosstalk-group]")), 1L, ...)`; "classified_skip" -> accept (no assertion); else `testthat::fail(...)`.
       - `assert_no_browser_visual_errors(logs)`.
       - Artifact writing: `out_dir <- pkgdown_visual_artifact_dir()`; `png_path <- file.path(out_dir, "pkgdown-main-article.png")`; `json_path <- file.path(out_dir, "pkgdown-main-article-dom-summary.json")`; `log_path <- file.path(out_dir, "pkgdown-main-article-browser-log.json")`. `session$screenshot(filename = png_path, delay = 0.5)` (viewport screenshot — omit `selector` per RESEARCH Open Question 2 to avoid a giant full-page capture). `jsonlite::write_json(dom_summary, path = json_path, auto_unbox = TRUE, pretty = TRUE, null = "null")`. Write the browser log JSON as a list of `status`, `article_path`, `screenshot_path`, `dom_summary_path`, and `logs = browser_visual_logs(logs)`. `expect_true(file.exists(...))` for all three files.

    Prohibitions to honor (these literals name the exact APIs/selectors/pages the produced test file must avoid; the acceptance criteria negative-grep the produced `.R` file, not this plan prose):
    <!-- planner-discipline-allow: saveWidget -->
    <!-- planner-discipline-allow: html-widget-container -->
    <!-- planner-discipline-allow: gg2d3-interactivity.html -->
    do NOT call `saveWidget()` (D-01, navigate the pre-built file); do NOT add any of these `.pkgdown_*` helpers to `helper-browser-visual.R` (keep them file-private in this test); do NOT unconditionally assert sf children (branch on `pkgdown_site_sf_outcome()`, D-06); do NOT add pixel-brightness detection; do NOT introduce a new env var (reuse `GG2D3_BROWSER_VISUAL_SMOKE`, D-10); do NOT load the interactivity article `gg2d3-interactivity.html` (D-07/D-08). Use the `.gg2d3.html-widget` container selector, NOT the `.html-widget-container` wrapper (RESEARCH Pitfall 4).
  </action>
  <verify>
    <automated>NOT_CRAN=true GG2D3_BROWSER_VISUAL_SMOKE=true Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); res <- testthat::test_file("tests/testthat/test-pkgdown-visual.R"); df <- as.data.frame(res); if (any(df$failed > 0) || any(df$error)) quit(status = 1)'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-pkgdown-visual.R` contains `test_that("BVIS-PKG-01`
    - `tests/testthat/test-pkgdown-visual.R` contains `.gg2d3.html-widget svg` (widget SVG selector) and does NOT contain the string `saveWidget`
    - `tests/testthat/test-pkgdown-visual.R` contains `pkgdown_site_sf_outcome()` and `PKGDOWN_SF_OPTIONAL_SKIP` (sf branch present)
    - `tests/testthat/test-pkgdown-visual.R` contains `blankWidgetCount` and `.PKGDOWN_VISUAL_SVG_CHILD_THRESHOLD` (blank/stale detection present and documented)
    - `tests/testthat/test-pkgdown-visual.R` does NOT contain `.html-widget-container` and does NOT contain `gg2d3-interactivity.html`
    - Behavior: with `GG2D3_BROWSER_VISUAL_SMOKE=true` and the built `docs/articles/gg2d3.html` present locally, the automated command exits 0 and creates `test_output/pkgdown-visual/pkgdown-main-article.png`, `-dom-summary.json`, and `-browser-log.json`
    - Behavior: with `GG2D3_BROWSER_VISUAL_SMOKE` unset, the test reports SKIP (not fail) — confirm via `Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-pkgdown-visual.R")'` showing a skipped result
    - `helper-browser-visual.R` is unchanged (`git diff --name-only` does not list it)
  </acceptance_criteria>
  <done>The single happy path works end-to-end: opting in loads the built pkgdown article, asserts non-blank widget rendering with sf/Crosstalk outcome branching, writes all three artifacts, and the test skips cleanly when not opted in. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local file:// -> chromote | The test loads a locally-built `docs/articles/gg2d3.html` via a `file://` URL. Input is a repo-controlled artifact, not untrusted external data. |
| test artifacts -> git | Artifacts write to `test_output/pkgdown-visual/`, which is gitignored and Rbuildignored; no artifact enters the package build or the repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-01 | Information Disclosure | Artifact write path | low | mitigate | Artifacts write only to `test_output/pkgdown-visual/`, already excluded by `.gitignore` (`test_output/`) and `.Rbuildignore` (`^test_output$`, `^test_output/`). No secret or credential is captured; DOM summary is integer counts + title only. |
| T-60-02 | Denial of Service | chromote session on 13MB page | low | accept | Poll timeout (30s) and settle delay bound the run; the test is opt-in (`GG2D3_BROWSER_VISUAL_SMOKE`) and never runs on CRAN or default `devtools::test()`. Accepted: resource cost is bounded and gated. |
| T-60-03 | Tampering | file:// script loading | low | accept | Chrome resolves relative `gg2d3_files/` scripts against the local absolute base URL; no network fetch. Repo-controlled input only. Accepted per RESEARCH Pitfall 3. |
</threat_model>

<verification>
- Opt-in local run exits 0 and produces the three artifacts (VIS-01, VIS-02, VIS-03).
- Not-opted-in run reports SKIP, proving the gate (VIS-03 skip behavior).
- `helper-browser-visual.R` and `helper-pkgdown-site.R` remain unmodified (shared helpers stay stable).
</verification>

<success_criteria>
- `tests/testthat/test-pkgdown-visual.R` exists, passes when opted-in with the built site present, and skips otherwise.
- Blank/stale detection: `blankWidgetCount == 0` gate present; threshold constant documented.
- sf and Crosstalk outcomes branched, never unconditionally asserted.
- Three artifacts land in `test_output/pkgdown-visual/`.
</success_criteria>

<output>
Create `.planning/phases/60-pkgdown-visual-regression-depth/60-01-SUMMARY.md` when done.
</output>
