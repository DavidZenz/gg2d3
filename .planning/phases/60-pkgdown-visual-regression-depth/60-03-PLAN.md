---
phase: 60-pkgdown-visual-regression-depth
plan: 03
type: execute
wave: 3
depends_on: [60-01, 60-02]
files_modified:
  - vignettes/d3-drawing-diagnostics.md
  - README.md
autonomous: false
requirements: [VIS-03]
user_setup: []

must_haves:
  truths:
    - "The diagnostics vignette documents how to run the pkgdown visual capture locally (opt-in env var + build-site prerequisite), where artifacts land (test_output/pkgdown-visual/), and the expected skip classifications (VIS-03)"
    - "The vignette documents the SVG-child-count blank/stale detection mechanism, the threshold, and the sf/Crosstalk rendered-or-classified_skip semantics so a maintainer can interpret a failure (VIS-03)"
    - "The README notes that pkgdown visual regression evidence exists alongside the existing browser-visual-smoke evidence and points to the vignette (VIS-03)"
    - "Recorded validation evidence confirms the opt-in test passes locally (or classifies its skip) and the pkgdown.yaml step order is correct (VIS-03)"
    - "FLAGGED ASSUMPTION (VIS-03, probe unresolved): documentation is the artifact that makes local/CI behavior and skip classification discoverable; a maintainer reading the vignette can reproduce the capture and interpret pass/skip/fail without reading the test source"
  artifacts:
    - "vignettes/d3-drawing-diagnostics.md (new '## Pkgdown visual regression' subsection)"
    - "README.md (updated v1.13/v1.15 support-and-validation section referencing pkgdown visual evidence)"
    - ".planning/phases/60-pkgdown-visual-regression-depth/60-03-SUMMARY.md (validation evidence)"
  key_links:
    - "The vignette's pkgdown-visual section references test-pkgdown-visual.R (Plan 01) and the pkgdown.yaml capture step (Plan 02)"
    - "Documentation reuses the existing GG2D3_BROWSER_VISUAL_SMOKE / GG2D3_BROWSER_VISUAL_CI env var vocabulary (no new env var, D-10)"
  prohibitions:
    - statement: "Documentation must NOT claim full perceptual-diff or pixel-threshold coverage; the shipped mechanism is DOM-based SVG child counting only (FUT-01 is out of scope)"
      status: flagged-unverified
      verification: manual
    - statement: "Documentation must NOT reference the interactivity article as a captured page; only docs/articles/gg2d3.html is captured (D-07, D-08)"
      status: flagged-unverified
      verification: manual
---

<objective>
Document the pkgdown visual regression capability and record the phase's validation evidence. Add a diagnostics-vignette subsection explaining local/CI behavior, artifact locations, the blank/stale detection mechanism, and expected skip classifications; update the README to surface the new evidence type; and capture the human-verified validation run.

Purpose: Satisfy the VIS-03 documentation requirement so maintainers can reproduce the capture, interpret pass/skip/fail, and distinguish the deterministic DOM-based gate from future perceptual-diff work.
Output: Updated `vignettes/d3-drawing-diagnostics.md`, updated `README.md`, and recorded validation evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-pkgdown-visual-regression-depth/60-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan modifies/creates:
- **Section:** `vignettes/d3-drawing-diagnostics.md` — new `## Pkgdown visual regression` subsection (placed near the existing `## Browser visual smoke artifacts` and `## Pkgdown site evidence` sections)
- **Section:** `README.md` — updated support-and-validation section with a bullet for pkgdown visual regression evidence
- **File:** `.planning/phases/60-pkgdown-visual-regression-depth/60-03-SUMMARY.md` — validation evidence for the phase
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Document pkgdown visual regression usage in the diagnostics vignette and README</name>
  <files>vignettes/d3-drawing-diagnostics.md, README.md</files>
  <read_first>
    - vignettes/d3-drawing-diagnostics.md (target file; read the existing "## Browser visual smoke artifacts" (line ~241) and "## Pkgdown site evidence" (line ~47) sections to match tone, heading depth, and the env-var / test_output vocabulary)
    - README.md (target file; read the "v1.13 support and validation contract" section lines ~40-67 to match the existing bullet style and where browser-visual/pkgdown evidence is described)
    - tests/testthat/test-pkgdown-visual.R (source of truth for the run command, env vars, artifact filenames, threshold, and sf/Crosstalk branch semantics to document — created in Plan 01)
    - .github/workflows/pkgdown.yaml (source of truth for the CI step names and artifact name pkgdown-visual-${{ github.run_id }} — modified in Plan 02)
    - .planning/phases/60-pkgdown-visual-regression-depth/60-CONTEXT.md (D-01..D-11 for accurate claims; Deferred Ideas for what NOT to claim)
  </read_first>
  <action>
    Add a `## Pkgdown visual regression` section to `vignettes/d3-drawing-diagnostics.md`, placed adjacent to the existing `## Browser visual smoke artifacts` section so the two related evidence types sit together. The section must cover, in prose consistent with the existing sections:
    - **Prerequisite:** `pkgdown::build_site()` (or `build_site_github_pages`) must have populated `docs/` so `docs/articles/gg2d3.html` exists; the test skips with "generated docs site is not available" otherwise (D-02).
    - **Local run command:** `NOT_CRAN=true GG2D3_BROWSER_VISUAL_SMOKE=true Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-pkgdown-visual.R")'` — reuses the existing opt-in env var, no new one (D-10).
    - **Detection mechanism:** loads the built main article in chromote, counts SVG child elements (`path, circle, rect, line, g`) inside each `.gg2d3.html-widget` and requires `>= 3` per widget (the documented threshold); the whole page must render `>= 10` widget SVGs. This is DOM-based, deterministic blank/stale detection — explicitly NOT pixel/perceptual diffing (FUT-01 is future work, do not imply it exists).
    - **sf / Crosstalk classification:** the sf region passes when rendered (`.geom-sf` present) OR when the page carries the `PKGDOWN_SF_OPTIONAL_SKIP` notice (`classified_skip`); Crosstalk passes when rendered or `classified_skip`. This mirrors the existing pkgdown-site validation semantics (D-06).
    - **Artifacts:** `test_output/pkgdown-visual/pkgdown-main-article.png` (human review), `-dom-summary.json` (programmatic counts), `-browser-log.json`; the directory is gitignored and excluded from package builds via `.Rbuildignore` (D-11).
    - **CI behavior:** runs inside `pkgdown.yaml` after the build; uploads a `pkgdown-visual-<run_id>` artifact; in CI (`GG2D3_BROWSER_VISUAL_CI=true`) a skip escalates to a failure; a missing runner Chrome degrades to a non-fatal skip (D-03).
    - **Scope note:** only the main article `docs/articles/gg2d3.html` is captured; the interactivity article is out of scope (D-07/D-08). Do not document it as captured.

    Update the support-and-validation section of `README.md` (the section currently titled "v1.13 support and validation contract") to add one bullet noting that pkgdown visual regression evidence — deterministic DOM-based widget-render checks with PNG + JSON artifacts under `test_output/pkgdown-visual/` — supplements the marker-based pkgdown validation, and point to `vignettes/d3-drawing-diagnostics.md` for the run instructions. Keep the wording aligned with the existing browser-visual-smoke bullet; do not claim perceptual-diff coverage.

    Prohibitions: do NOT claim pixel-threshold or perceptual-diff coverage (FUT-01 out of scope); do NOT describe the interactivity article as captured (D-08).
  </action>
  <verify>
    <automated>grep -q "Pkgdown visual regression" vignettes/d3-drawing-diagnostics.md && grep -q "test-pkgdown-visual.R" vignettes/d3-drawing-diagnostics.md && grep -q "test_output/pkgdown-visual" vignettes/d3-drawing-diagnostics.md && grep -q "GG2D3_BROWSER_VISUAL_SMOKE" vignettes/d3-drawing-diagnostics.md && grep -q "pkgdown-visual" README.md && ! grep -q "gg2d3-interactivity.html" vignettes/d3-drawing-diagnostics.md && echo DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `vignettes/d3-drawing-diagnostics.md` contains a heading `## Pkgdown visual regression`
    - `vignettes/d3-drawing-diagnostics.md` contains `test-pkgdown-visual.R`, `test_output/pkgdown-visual`, `GG2D3_BROWSER_VISUAL_SMOKE`, and `PKGDOWN_SF_OPTIONAL_SKIP` (mechanism + skip classification documented)
    - `vignettes/d3-drawing-diagnostics.md` describes the SVG-child-count threshold and states the detection is DOM-based (not pixel/perceptual)
    - `vignettes/d3-drawing-diagnostics.md` does NOT reference `gg2d3-interactivity.html` in the pkgdown-visual section (scope respected)
    - `README.md` contains a reference to `pkgdown-visual` (or `pkgdown visual regression`) pointing to the diagnostics vignette
    - Neither file claims perceptual-diff / pixel-threshold coverage (grep for `perceptual` returns no new claim of coverage)
    - The automated grep chain prints `DOC_OK`
  </acceptance_criteria>
  <done>The diagnostics vignette has a complete, accurate pkgdown visual regression section and the README surfaces the new evidence type, both consistent with the existing documentation and scoped to what Phase 60 actually ships.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Record pkgdown visual regression validation evidence</name>
  <what-built>The pkgdown visual regression capability (Plan 01 test + Plan 02 CI step) and its documentation (this plan). This checkpoint records the phase's validation evidence.</what-built>
  <how-to-verify>
    1. Ensure the pkgdown site is built locally: `Rscript --vanilla -e 'pkgdown::build_site(new_process = FALSE, preview = FALSE)'` (or confirm `docs/articles/gg2d3.html` already exists and is current).
    2. Run the opt-in visual capture:
       `NOT_CRAN=true GG2D3_BROWSER_VISUAL_SMOKE=true Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-pkgdown-visual.R")'`
       Expect: the test PASSES (or classifies its skip if Chrome/chromote is unavailable locally). Confirm `test_output/pkgdown-visual/pkgdown-main-article.png` and the two JSON files were written.
    3. Open `test_output/pkgdown-visual/pkgdown-main-article.png` and confirm the pkgdown article visibly rendered gg2d3 widgets (not a blank page). Open `pkgdown-main-article-dom-summary.json` and confirm `renderedSvgCount >= 10` and `blankWidgetCount == 0`.
    4. Confirm the full test suite is still green: `NOT_CRAN=true Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_dir("tests/testthat")'` (the new opt-in test should SKIP when the smoke env var is unset).
    5. Confirm `.github/workflows/pkgdown.yaml` is valid and the three new steps are in the correct order after the build step.
    6. Read the new `## Pkgdown visual regression` vignette section and confirm the claims match the actual test behavior and scope.
  </how-to-verify>
  <resume-signal>Type "approved" once the capture passes (or classifies its skip), artifacts and DOM counts look correct, the suite is green, and the documentation is accurate — or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> maintainer action | The vignette instructs maintainers to run an opt-in local command; no untrusted input or privileged operation is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-07 | Repudiation | Validation evidence record | low | mitigate | The checkpoint records a concrete, reproducible run (command, artifact paths, DOM counts) in the SUMMARY so the VIS-03 documentation claim is backed by evidence, not assertion. |
| T-60-08 | Information Disclosure | Documented commands | low | accept | Documented commands reference only local files and the existing opt-in env var; no secrets or credentials appear in docs. Accepted. |
</threat_model>

<verification>
- Vignette section and README bullet exist with accurate, in-scope claims (VIS-03), verified by the grep chain.
- Human checkpoint records a passing (or classified-skip) local capture with inspected artifacts and DOM counts (VIS-03 evidence).
</verification>

<success_criteria>
- Diagnostics vignette documents local/CI behavior, artifact locations, detection mechanism, and skip classifications.
- README surfaces the new evidence type without overclaiming.
- Validation evidence recorded via the human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/60-pkgdown-visual-regression-depth/60-03-SUMMARY.md` when done.
</output>
