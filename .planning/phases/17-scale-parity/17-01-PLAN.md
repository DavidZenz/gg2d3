---
phase: 17-scale-parity
plan: 01
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - R/as_d3_ir.R
  - inst/htmlwidgets/gg2d3.js
  - tests/testthat/test-ir.R
autonomous: true

must_haves:
  truths:
    - "User sees continuous/discrete scales honor configured `breaks` and `minor_breaks` semantics"
    - "User sees `expand`, `oob`, and limits behavior match ggplot2 semantics"
  artifacts:
    - path: "tests/testthat/test-ir.R"
      provides: "Regression coverage for advanced scale parity"
      contains: "minor_breaks"
---

<objective>
Implement full parity for advanced scale features, specifically focusing on per-panel minor breaks and robust OOB/expansion handling.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for per-panel minor breaks</name>
  <files>tests/testthat/test-ir.R</files>
  <action>Add tests validating that: (a) minor breaks are correctly extracted for faceted plots with free scales, (b) minor breaks are preserved in the IR for both Date and POSIXct scales.</action>
  <verify>Rscript -e "devtools::test(filter = 'ir')"</verify>
  <done>New tests pass or fail as expected before/after implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Extract per-panel minor breaks in R</name>
  <files>R/as_d3_ir.R</files>
  <action>Update `panels_ir` extraction logic for both `facet_wrap` and `facet_grid` to include `x_minor_breaks` and `y_minor_breaks`. Ensure correct temporal conversion (ms) if applicable.</action>
  <verify>Rscript -e "devtools::load_all(); # verify IR output for faceted plots"</verify>
  <done>IR `panels` array contains accurate `x_minor_breaks` and `y_minor_breaks` for all panels.</done>
</task>

<task type="auto">
  <name>Task 3: Utilize per-panel minor breaks in JS</name>
  <files>inst/htmlwidgets/gg2d3.js</files>
  <action>Update `renderPanel` helper to prioritize `panelData.x_minor_breaks` and `panelData.y_minor_breaks` for grid drawing. This ensures faceted plots with free scales show correct minor grid lines.</action>
  <verify>Visual inspection of a faceted plot with `scales="free"` and custom minor breaks.</verify>
  <done>D3 rendering uses the most specific minor breaks available.</done>
</task>

<task type="auto">
  <name>Task 4: Verify OOB squishing during interactions</name>
  <files>tests/testthat/test-zoom-brush.R</files>
  <action>Add a test case with `oob = scales::squish` and verify that zooming in/out does not revert the squished points to their original values (since we use built data).</action>
  <verify>Rscript -e "devtools::test(filter = 'zoom-brush')"</verify>
  <done>OOB behavior is verified stable across interactions.</done>
</task>

</tasks>

<success_criteria>
- Faceted plots with free scales show correct minor grid lines for each panel.
- Minor breaks for date/datetime scales are correctly converted to ms and rendered.
- `oob = squish` results are preserved after zoom reset and other interactive state changes.
</success_criteria>
