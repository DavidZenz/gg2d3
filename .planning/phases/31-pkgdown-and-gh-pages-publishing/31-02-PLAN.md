---
phase: 31
plan: 02
type: execute
wave: 1
depends_on: [31-01]
files_modified:
  - vignettes/gg2d3-intro.Rmd
  - vignettes/gg2d3.Rmd
  - _pkgdown.yml
  - .Rbuildignore
  - DESCRIPTION
autonomous: true
requirements: ["DOCS-02 (proposed)"]
tags: [pkgdown, config, vignette-rename]
must_haves:
  truths:
    - "vignettes/gg2d3.Rmd exists; vignettes/gg2d3-intro.Rmd does not."
    - "VignetteIndexEntry in vignettes/gg2d3.Rmd matches the new filename context (string 'Getting started with gg2d3')."
    - "_pkgdown.yml declares url=https://davidzenz.github.io/gg2d3/, template.bootstrap=5, development.mode=release, and a curated reference: block with the three sections from the audited NAMESPACE."
    - ".Rbuildignore contains ^\\.github$."
    - "DESCRIPTION URL field contains both the GitHub repo URL and the pkgdown site URL."
    - "No README.Rmd / NEWS.md / other vignette text still references gg2d3-intro (rename side-effects fixed)."
  artifacts:
    - path: "vignettes/gg2d3.Rmd"
      provides: "Renamed Get-started vignette for pkgdown 2.x filename auto-promotion"
      contains: "VignetteIndexEntry{Getting started with gg2d3}"
    - path: "_pkgdown.yml"
      provides: "Curated pkgdown config — url, template, development.mode, reference sections"
      contains: "url: https://davidzenz.github.io/gg2d3/"
    - path: ".Rbuildignore"
      provides: "Excludes .github/ from source tarball"
      contains: "^\\.github$"
    - path: "DESCRIPTION"
      provides: "URL field pointing to both GitHub repo and pkgdown site"
      contains: "davidzenz.github.io/gg2d3"
  key_links:
    - from: "vignettes/gg2d3.Rmd"
      to: "pkgdown navbar Get-started slot"
      via: "filename auto-promotion in pkgdown 2.x"
      pattern: "vignettes/gg2d3\\.Rmd"
    - from: "_pkgdown.yml reference:"
      to: "man/*.Rd"
      via: "pkgdown reads NAMESPACE exports and resolves each contents: entry to an .Rd"
      pattern: "contents:[^a-z]*\\n  - gg2d3"
---

<objective>
Land all configuration changes — vignette rename, curated `_pkgdown.yml`, `.Rbuildignore` patch, DESCRIPTION URL — in a single autonomous wave. No site builds yet; that is Plan 03's job. No workflow files yet; that is Plan 04's job.

Purpose: Materialize the D-03, D-05, D-06, D-08, D-09, D-11, D-13 decisions on disk so Plan 03 can run `pkgdown::build_site_github_pages()` against a fully-configured source tree.

Output: A working tree where `_pkgdown.yml` is curated, `vignettes/gg2d3.Rmd` is the Get-started article, `.Rbuildignore` accounts for `.github/`, and DESCRIPTION's URL field carries the pkgdown site.

## Deviations from CONTEXT (logged)

- **D-05 reference list:** Researcher's NAMESPACE audit found that `d3_crosstalk` is NOT exported (`R/d3_crosstalk.R` is `@name d3_crosstalk_internal`) — including it in `reference:` would break the pkgdown build with "no man/d3_crosstalk.Rd". Also, `as_d3_ir` and `validate_ir` ARE exported but D-05 omitted them. Plan 02 omits `d3_crosstalk` from Interactivity and adds an "Internals" section for `as_d3_ir()` + `validate_ir()`. Recorded in this plan's SUMMARY.
- **D-09 promotion mechanism:** CONTEXT mentions "naming/role via the `articles:` index or a `getting-started` flag". Researcher verified pkgdown 2.x's canonical idiom is filename auto-promotion (a vignette named `<pkgname>.Rmd`). Plan 02 uses the rename — no `articles:` block in `_pkgdown.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-pkgdown-and-gh-pages-publishing/31-CONTEXT.md
@.planning/phases/31-pkgdown-and-gh-pages-publishing/31-RESEARCH.md
@.planning/phases/31-pkgdown-and-gh-pages-publishing/31-01-SUMMARY.md
@_pkgdown.yml
@.Rbuildignore
@NAMESPACE
@DESCRIPTION
@vignettes/gg2d3-intro.Rmd

<interfaces>
<!-- Audited NAMESPACE (ground truth for reference: block) — extracted 2026-05-15 -->

```
export(as_d3_ir)
export(d3_brush)
export(d3_hover)
export(d3_tooltip)
export(d3_zoom)
export(gg2d3)
export(validate_ir)
```

<!-- d3_crosstalk is NOT in NAMESPACE — DO NOT include in reference: -->
<!-- as_d3_ir / validate_ir ARE in NAMESPACE — include under "Internals" -->

<!-- Current _pkgdown.yml (stub to overwrite): -->
```
url: ~
template:
  bootstrap: 5
```

<!-- Current DESCRIPTION URL line: -->
```
URL: https://github.com/DavidZenz/gg2d3
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Rename intro vignette to gg2d3.Rmd and fix cross-references</name>
  <files>vignettes/gg2d3-intro.Rmd, vignettes/gg2d3.Rmd, README.Rmd (only if matches), NEWS.md (only if matches), vignettes/gg2d3-interactivity.Rmd (only if matches)</files>
  <read_first>
    - vignettes/gg2d3-intro.Rmd (first 10 lines — VignetteIndexEntry stays "Getting started with gg2d3", which is already the value)
    - .planning/phases/31-pkgdown-and-gh-pages-publishing/31-RESEARCH.md (Pattern 2 — filename auto-promotion; Pitfall 1 — cross-reference breakage)
  </read_first>
  <action>
    Execute the rename with git so history is preserved:

    ```bash
    git mv vignettes/gg2d3-intro.Rmd vignettes/gg2d3.Rmd
    ```

    The current `VignetteIndexEntry{Getting started with gg2d3}` already matches the new filename's expected content (the human-readable title doesn't need to match the filename — only the filename triggers pkgdown auto-promotion). No edit needed inside the file.

    Audit for stale references to the old filename:

    ```bash
    grep -rn "gg2d3-intro" --include="*.Rmd" --include="*.md" --include="*.R" --include="*.yml" --include="*.yaml" . 2>/dev/null | grep -v ".planning/" | grep -v "docs/" | grep -v ".git/"
    ```

    For every hit (excluding `.planning/` history, `docs/` build output, `.git/` internals):
    - If it is a markdown link like `[See the intro](gg2d3-intro.html)` or `vignette("gg2d3-intro")`, rewrite to `gg2d3.html` / `vignette("gg2d3")`.
    - If it is a comment referring to the old filename, update for accuracy.

    If `README.Rmd` was edited, regenerate `README.md`:

    ```bash
    Rscript -e 'devtools::build_readme()'
    ```

    Do NOT hand-edit `README.md` (per CLAUDE.md — README is generated).
  </action>
  <verify>
    <automated>test -f vignettes/gg2d3.Rmd && ! test -e vignettes/gg2d3-intro.Rmd && [ "$(grep -rn 'gg2d3-intro' --include='*.Rmd' --include='*.md' --include='*.R' --include='*.yml' --include='*.yaml' . 2>/dev/null | grep -v '\.planning/' | grep -v 'docs/' | grep -v '\.git/' | wc -l)" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f vignettes/gg2d3.Rmd` passes
    - `test -e vignettes/gg2d3-intro.Rmd` fails (file is gone)
    - `grep -c "VignetteIndexEntry{Getting started with gg2d3}" vignettes/gg2d3.Rmd` >= 1
    - Cross-reference grep (excluding `.planning/`, `docs/`, `.git/`) returns zero hits for `gg2d3-intro`
    - `git log --follow vignettes/gg2d3.Rmd` shows the prior history (rename preserved)
  </acceptance_criteria>
  <done>Get-started vignette is at the auto-promotion filename; no stale cross-references remain.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2.2: Rewrite _pkgdown.yml with curated config</name>
  <files>_pkgdown.yml</files>
  <read_first>
    - _pkgdown.yml (current stub — will be fully overwritten)
    - NAMESPACE (audited export list — Internals = as_d3_ir + validate_ir; Interactivity = d3_tooltip/d3_hover/d3_zoom/d3_brush)
    - .planning/phases/31-pkgdown-and-gh-pages-publishing/31-RESEARCH.md (Pattern 3 — curated reference; Code Examples → Curated _pkgdown.yml)
  </read_first>
  <action>
    Overwrite `_pkgdown.yml` with the exact content below. The current stub is two non-load-bearing lines; a clean rewrite is simpler than a patch.

    ```yaml
    url: https://davidzenz.github.io/gg2d3/

    template:
      bootstrap: 5

    development:
      mode: release

    reference:
    - title: Main entry
      desc: Convert a ggplot to an interactive D3 widget.
      contents:
      - gg2d3
    - title: Interactivity
      desc: Composable interactivity layers (pipe with `|>`).
      contents:
      - d3_tooltip
      - d3_hover
      - d3_zoom
      - d3_brush
    - title: Internals
      desc: Lower-level functions exposed for advanced use and testing.
      contents:
      - as_d3_ir
      - validate_ir
    ```

    Notes on intentional choices:
    - `url:` is set per D-03 — site URL is canonical.
    - `template.bootstrap: 5` preserved per D-08.
    - `development.mode: release` per D-13 — no `/dev/` URL split.
    - `reference:` per D-05 with the audited NAMESPACE (NOT CONTEXT D-05's listing — `d3_crosstalk` is internal, `as_d3_ir`/`validate_ir` are exported).
    - No `navbar:` block — D-06 mandates defaults.
    - No `articles:` block — D-09 filename promotion handles Get-started.
    - No `news:` block — D-07 default behavior surfaces `NEWS.md` automatically.

    After writing, parse as YAML to verify well-formedness:

    ```bash
    Rscript -e 'cat(yaml::as.yaml(yaml::read_yaml("_pkgdown.yml")))'
    ```
  </action>
  <verify>
    <automated>Rscript -e 'y <- yaml::read_yaml("_pkgdown.yml"); stopifnot(y$url == "https://davidzenz.github.io/gg2d3/", y$template$bootstrap == 5, y$development$mode == "release", length(y$reference) == 3, identical(y$reference[[2]]$contents, c("d3_tooltip","d3_hover","d3_zoom","d3_brush")), identical(y$reference[[3]]$contents, c("as_d3_ir","validate_ir"))); cat("OK\n")'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'url: https://davidzenz.github.io/gg2d3/' _pkgdown.yml` matches
    - `grep -F 'bootstrap: 5' _pkgdown.yml` matches
    - `grep -F 'mode: release' _pkgdown.yml` matches
    - `grep -c '^- title:' _pkgdown.yml` returns 3 (Main entry, Interactivity, Internals)
    - `grep -F '  - d3_crosstalk' _pkgdown.yml` returns nothing (deviation from CONTEXT D-05 applied)
    - `grep -F '  - as_d3_ir' _pkgdown.yml` matches; `grep -F '  - validate_ir' _pkgdown.yml` matches
    - `! grep -q '^navbar:' _pkgdown.yml` (no custom navbar)
    - `! grep -q '^articles:' _pkgdown.yml` (no articles block — filename promotion only)
    - YAML parses without error
  </acceptance_criteria>
  <done>_pkgdown.yml encodes D-03/D-05/D-06/D-08/D-09/D-13 correctly and parses as YAML.</done>
</task>

<task type="auto">
  <name>Task 2.3: Patch .Rbuildignore and DESCRIPTION URL field</name>
  <files>.Rbuildignore, DESCRIPTION</files>
  <read_first>
    - .Rbuildignore (current entries — confirm `^\.github$` absent, `^docs$`/`^pkgdown$`/`^_pkgdown\.yml$` already present)
    - DESCRIPTION (current `URL:` line is `https://github.com/DavidZenz/gg2d3`)
    - .planning/phases/31-pkgdown-and-gh-pages-publishing/31-RESEARCH.md (Pitfall 2 — .Rbuildignore .github; Code Examples → .Rbuildignore patch)
  </read_first>
  <action>
    **Step A — append `^\.github$` to `.Rbuildignore`** (using R rather than echo to avoid shell escaping issues with the backslash):

    ```bash
    Rscript -e 'cat("^\\\\.github$\n", file = ".Rbuildignore", append = TRUE)'
    ```

    Verify the line landed correctly (the file should contain the literal regex `^\.github$`):

    ```bash
    grep -Fx '^\.github$' .Rbuildignore
    ```

    Existing entries to KEEP (do not touch): `^docs$`, `^pkgdown$`, `^_pkgdown\.yml$`, `^\.planning$`, `^\.claude$`, etc.

    **Step B — extend DESCRIPTION `URL:` to include both URLs** (D-03 allows this). Use `desc` package for safe field editing:

    ```bash
    Rscript -e 'd <- desc::desc(file = "DESCRIPTION"); d$set_urls(c("https://github.com/DavidZenz/gg2d3", "https://davidzenz.github.io/gg2d3/")); d$write()'
    ```

    `desc::desc()$set_urls()` produces a comma-separated `URL:` field, which is the CRAN-valid format.

    Verify:

    ```bash
    grep -F "davidzenz.github.io/gg2d3" DESCRIPTION
    grep -F "github.com/DavidZenz/gg2d3" DESCRIPTION
    ```

    Both must match. Do NOT touch `BugReports:`, `Authors@R:`, or any other field.
  </action>
  <verify>
    <automated>grep -Fx '^\.github$' .Rbuildignore && grep -F "davidzenz.github.io/gg2d3" DESCRIPTION && grep -F "github.com/DavidZenz/gg2d3" DESCRIPTION && Rscript -e 'd <- desc::desc("DESCRIPTION"); urls <- d$get_urls(); stopifnot(length(urls) == 2); cat("OK\n")'</automated>
  </verify>
  <acceptance_criteria>
    - `.Rbuildignore` contains the literal line `^\.github$`
    - `DESCRIPTION` URL field lists exactly two URLs: the GitHub repo URL and the pkgdown site URL
    - `Rscript -e 'desc::desc("DESCRIPTION")$get_urls()'` returns length-2 character vector
    - `BugReports:` line in DESCRIPTION is unchanged
    - `Authors@R:` line in DESCRIPTION is unchanged
  </acceptance_criteria>
  <done>.Rbuildignore excludes `.github/`; DESCRIPTION URL field carries both endpoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source tree → R CMD check tarball | `.Rbuildignore` is the gate controlling what enters the CRAN tarball |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | I (Information disclosure) | `.github/workflows/pkgdown.yaml` accidentally included in CRAN source tarball | mitigate | Add `^\.github$` to `.Rbuildignore` in Task 2.3 before Plan 04 creates the workflow file; grep-verified `grep -Fx '^\.github$' .Rbuildignore` |
| T-31-04 | T (Tampering) | Vignette rename breaking internal links (silent docs rot) | mitigate | Task 2.1 greps for `gg2d3-intro` after rename and rewrites every hit; CI builds in Plan 03 will surface remaining broken links as warnings |
</threat_model>

<verification>
- `test -f vignettes/gg2d3.Rmd && ! test -e vignettes/gg2d3-intro.Rmd`
- YAML parse of `_pkgdown.yml` succeeds and matches the curated structure
- `.Rbuildignore` contains `^\.github$`
- DESCRIPTION URL field has both URLs
- `grep -rn "gg2d3-intro"` (excluding `.planning/`, `docs/`, `.git/`) returns nothing
</verification>

<success_criteria>
Source tree is fully configured for a pkgdown build. Plan 03 can now run `pkgdown::build_site_github_pages()` and expect the curated output structure.
</success_criteria>

<output>
After completion, create `.planning/phases/31-pkgdown-and-gh-pages-publishing/31-02-SUMMARY.md` documenting: the rename, the new `_pkgdown.yml` structure, the `.Rbuildignore` patch, the DESCRIPTION URL extension, and the D-05/D-09 deviations from CONTEXT (with the audited NAMESPACE as evidence).
</output>
