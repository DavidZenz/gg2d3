---
phase: 31
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/31-pkgdown-and-gh-pages-publishing/31-01-SUMMARY.md
autonomous: false
requirements: ["DOCS-02 (proposed)"]
tags: [pkgdown, ghpages, preflight]
must_haves:
  truths:
    - "pkgdown (>= 2.2.0) is installed locally and loadable."
    - "usethis is installed locally and loadable."
    - "Developer has confirmed a gh-pages bootstrap strategy (manual orphan branch, per D-15 Claude's-discretion)."
    - "GITHUB_PAT presence is recorded (informational only — manual bootstrap doesn't require it)."
  artifacts:
    - path: ".planning/phases/31-pkgdown-and-gh-pages-publishing/31-01-SUMMARY.md"
      provides: "Preflight findings: installed versions, PAT status, recorded bootstrap decision"
      contains: "bootstrap_strategy: manual-orphan"
  key_links:
    - from: "preflight"
      to: "wave 1 config edits"
      via: "summary records that pkgdown is locally buildable"
      pattern: "pkgdown.*2\\.[2-9]"
---

<objective>
Confirm the local environment can build the pkgdown site and pin the gh-pages bootstrap strategy before any artifacts are touched.

Purpose: Wave 0 gap closure for the Nyquist validation table — pkgdown/usethis install state and bootstrap decision are MISSING references that downstream waves rely on. Catches "command not found" failures early, before vignette renames and YAML rewrites land in git.

Output: 31-01-SUMMARY.md recording installed versions, PAT availability, and the locked bootstrap strategy.

## Deviations from CONTEXT (logged)

- **D-01 deploy mechanism:** CONTEXT phrasing references `pkgdown::deploy_to_branch()`. The live r-lib `pkgdown.yaml` template uses `pkgdown::build_site_github_pages()` + `JamesIves/github-pages-deploy-action@v4.8.0` (SHA-pinned `d92aa235...`). Plans 02–04 follow the live template verbatim — this is the canonical artifact `usethis::use_pkgdown_github_pages()` would install, so the spirit of D-01 ("standard r-lib path") is honored. Plan 04 records the diff in its SUMMARY.
- **Bootstrap strategy (D-15 discretion item):** Chosen = **manual `git checkout --orphan gh-pages`**. Rationale: transparent, no PAT-permission surprises, no risk of `usethis` overwriting the `_pkgdown.yml` we craft in Wave 1. Recorded here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-pkgdown-and-gh-pages-publishing/31-CONTEXT.md
@.planning/phases/31-pkgdown-and-gh-pages-publishing/31-RESEARCH.md
@.planning/phases/31-pkgdown-and-gh-pages-publishing/31-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Audit local pkgdown + usethis install</name>
  <files>.planning/phases/31-pkgdown-and-gh-pages-publishing/31-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/31-pkgdown-and-gh-pages-publishing/31-RESEARCH.md (Environment Availability table; pkgdown 2.2.0 expected)
    - .planning/phases/31-pkgdown-and-gh-pages-publishing/31-VALIDATION.md (Wave 0 Requirements checklist)
  </read_first>
  <action>
    Run from the repo root:

    ```bash
    Rscript -e 'cat("pkgdown:", as.character(packageVersion("pkgdown")), "\n"); cat("usethis:", as.character(packageVersion("usethis")), "\n"); cat("R:", R.version.string, "\n")' 2>&1 | tee /tmp/gg2d3-preflight.log
    ```

    If `packageVersion("pkgdown")` errors with "there is no package called 'pkgdown'", install it:

    ```bash
    Rscript -e 'install.packages("pkgdown", repos = "https://cloud.r-project.org")'
    ```

    Same for usethis if missing. Then re-run the version check.

    Acceptance: `pkgdown` >= 2.2.0 and `usethis` >= 3.0.0 (researcher pinned versions). If pkgdown < 2.2.0, the filename-based "Get started" promotion in Plan 02 may not fire — abort and upgrade.

    Append findings to `31-01-SUMMARY.md` under a `## Preflight` section with three lines: `pkgdown: <version>`, `usethis: <version>`, `R: <version>`.
  </action>
  <verify>
    <automated>Rscript -e 'stopifnot(packageVersion("pkgdown") >= "2.2.0"); stopifnot(packageVersion("usethis") >= "3.0.0"); cat("OK\n")'</automated>
  </verify>
  <acceptance_criteria>
    - `Rscript -e 'packageVersion("pkgdown")'` returns >= 2.2.0
    - `Rscript -e 'packageVersion("usethis")'` returns >= 3.0.0
    - `31-01-SUMMARY.md` contains a `## Preflight` section with the three version lines
    - `grep -E '^pkgdown: 2\.[2-9]' .planning/phases/31-pkgdown-and-gh-pages-publishing/31-01-SUMMARY.md` matches
  </acceptance_criteria>
  <done>SUMMARY.md exists with installed-version evidence; pkgdown / usethis meet floors.</done>
</task>

<task type="auto">
  <name>Task 1.2: Record GITHUB_PAT presence and lock bootstrap strategy</name>
  <files>.planning/phases/31-pkgdown-and-gh-pages-publishing/31-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/31-pkgdown-and-gh-pages-publishing/31-CONTEXT.md (D-15 discretion item — bootstrap choice)
    - .planning/phases/31-pkgdown-and-gh-pages-publishing/31-RESEARCH.md (Pitfall 3 — gh-pages bootstrap; Open Question 1)
  </read_first>
  <action>
    Check for GITHUB_PAT (informational only — manual bootstrap does not require it):

    ```bash
    Rscript -e 'pat <- Sys.getenv("GITHUB_PAT", unset = Sys.getenv("GITHUB_TOKEN", unset = "")); cat("PAT_PRESENT:", nzchar(pat), "\n")'
    ```

    Append a `## Bootstrap Decision` section to `31-01-SUMMARY.md`:

    ```markdown
    ## Bootstrap Decision

    **Strategy:** manual-orphan (git checkout --orphan gh-pages + manual GitHub Pages settings toggle)

    **Rationale:**
    - Transparent — every git operation is visible in the commit log
    - No dependency on GITHUB_PAT being correctly scoped
    - No risk of `usethis::use_pkgdown_github_pages()` overwriting the curated `_pkgdown.yml` produced in Plan 02
    - Idempotent: orphan branch is created once, never again

    **GITHUB_PAT status (informational):** PAT_PRESENT=<true|false>

    **Concrete steps that will be executed in Plan 04 Task 4.2 (recorded here for traceability):**
    1. `git checkout --orphan gh-pages`
    2. `git rm -rf .`
    3. `echo "placeholder" > index.html && git add index.html`
    4. `git commit -m "Initial gh-pages branch"`
    5. `git push origin gh-pages`
    6. `git checkout master`
    7. **Manual UI step (cannot be automated):** github.com/DavidZenz/gg2d3/settings/pages → Source: "Deploy from a branch" → Branch: `gh-pages / (root)` → Save
    ```

    Replace `<true|false>` with the actual PAT_PRESENT value from the Rscript call.
  </action>
  <verify>
    <automated>grep -q "^**Strategy:** manual-orphan" .planning/phases/31-pkgdown-and-gh-pages-publishing/31-01-SUMMARY.md || grep -q "Strategy:.*manual-orphan" .planning/phases/31-pkgdown-and-gh-pages-publishing/31-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY contains a `## Bootstrap Decision` section
    - The section names the strategy as `manual-orphan`
    - The section enumerates the 7 concrete bootstrap steps
    - The section records the GITHUB_PAT presence boolean
    - `grep -c "git checkout --orphan gh-pages" .planning/phases/31-pkgdown-and-gh-pages-publishing/31-01-SUMMARY.md` >= 1
  </acceptance_criteria>
  <done>Bootstrap strategy locked and documented; Plan 04 has an unambiguous procedure to follow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1.3: Human sign-off on preflight + bootstrap decision</name>
  <what-built>
    `31-01-SUMMARY.md` recording installed pkgdown/usethis/R versions, GITHUB_PAT presence, and the manual-orphan bootstrap strategy.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/31-pkgdown-and-gh-pages-publishing/31-01-SUMMARY.md`.
    2. Confirm `pkgdown >= 2.2.0` and `usethis >= 3.0.0`.
    3. Confirm the bootstrap strategy is `manual-orphan` and you are comfortable with that choice (the alternative would be `usethis::use_pkgdown_github_pages()` — see Pitfall 3 in 31-RESEARCH.md).
    4. Confirm the 7 bootstrap steps are listed and the one manual UI step (GitHub Pages settings toggle) is flagged.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Plan 02 (config edits) or describe issues to revise.</resume-signal>
  <files>(human-driven checkpoint — no file modifications)</files>
  <action>This is a human checkpoint. Follow the steps in `<how-to-verify>` below. The orchestrator pauses here and resumes on the `<resume-signal>` keyword.</action>
  <verify><automated>MISSING — manual human verification per `<how-to-verify>` steps; no automated substitute (this is the D-NN human gate per CONTEXT)</automated></verify>
  <done>Human types the resume signal after completing all `<how-to-verify>` checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev shell → CRAN mirror | Installing pkgdown/usethis pulls binaries; CRAN is treated as trusted (project-wide assumption) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | T (Tampering) | CRAN install of pkgdown/usethis | accept | Same trust posture as rest of project; out of phase scope to harden |
| T-31-02 | I (Information disclosure) | Logging `GITHUB_PAT` presence | mitigate | We log only the boolean `nzchar(pat)`, never the token value; pattern `cat("PAT_PRESENT:", nzchar(pat))` is grep-verifiable |
</threat_model>

<verification>
- `Rscript -e 'packageVersion("pkgdown")'` returns >= 2.2.0
- `Rscript -e 'packageVersion("usethis")'` returns >= 3.0.0
- `31-01-SUMMARY.md` exists with `## Preflight` and `## Bootstrap Decision` sections
- Human approves preflight and bootstrap strategy at Task 1.3 checkpoint
</verification>

<success_criteria>
Wave 0 complete: environment is buildable, bootstrap path is locked, human has signed off. Plan 02 may proceed.
</success_criteria>

<output>
Create `.planning/phases/31-pkgdown-and-gh-pages-publishing/31-01-SUMMARY.md` with `## Preflight`, `## Bootstrap Decision`, and `## Deviations` sections (the latter logging the D-01 mechanism deviation noted in the objective above).
</output>
