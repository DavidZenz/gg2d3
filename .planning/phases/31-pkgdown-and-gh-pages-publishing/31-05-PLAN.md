---
phase: 31
plan: 05
type: execute
wave: 4
depends_on: [31-04]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/31-pkgdown-and-gh-pages-publishing/31-05-SUMMARY.md
autonomous: false
requirements: ["DOCS-02 (proposed)"]
tags: [pkgdown, d-14-checkpoint, requirements]
must_haves:
  truths:
    - "Human has verified the PUBLISHED Get-started page at https://davidzenz.github.io/gg2d3/articles/gg2d3.html shows an interactive widget (D-14 hard requirement)."
    - "Repo 'About' sidebar lists https://davidzenz.github.io/gg2d3/ as the site URL (recommended polish from CONTEXT code_context section)."
    - "REQUIREMENTS.md now includes a DOCS-02 entry mapping this phase's publishing capability."
    - "ROADMAP.md Phase 31 goal is filled in (no longer '[To be planned]') and points at the published site."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Canonical DOCS-02 requirement entry"
      contains: "DOCS-02"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 31 goal and success criteria filled in"
      contains: "https://davidzenz.github.io/gg2d3/"
    - path: ".planning/phases/31-pkgdown-and-gh-pages-publishing/31-05-SUMMARY.md"
      provides: "Final phase sign-off — D-14 evidence + traceability commit"
  key_links:
    - from: "https://davidzenz.github.io/gg2d3/articles/gg2d3.html"
      to: "an interactive gg2d3 widget in a real user's browser"
      via: "GitHub Pages serves gh-pages branch; htmlwidgets bundles d3.v7.min.js"
      pattern: "human-verified"
    - from: "DOCS-02 in REQUIREMENTS.md"
      to: "Phase 31 in ROADMAP.md"
      via: "Requirements field on Phase 31"
      pattern: "DOCS-02"
---

<objective>
Close the phase by performing the D-14 hard requirement (human interactivity verification on the PUBLISHED site) and recording the publishing capability as a formal requirement (DOCS-02) plus a finalized Phase 31 entry in ROADMAP.

Purpose: D-14 is the binding success criterion for this phase — "build success alone is not sufficient." This plan exists solely to execute the live-site human checkpoint and to land the housekeeping commits that retroactively make Phase 31 traceable in REQUIREMENTS.md / ROADMAP.md (per CONTEXT Deferred Idea about adding DOCS-02).

Output: A green D-14 sign-off, an updated `REQUIREMENTS.md` and `ROADMAP.md`, and a final SUMMARY proving the phase goal is met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-pkgdown-and-gh-pages-publishing/31-CONTEXT.md
@.planning/phases/31-pkgdown-and-gh-pages-publishing/31-RESEARCH.md
@.planning/phases/31-pkgdown-and-gh-pages-publishing/31-VALIDATION.md
@.planning/phases/31-pkgdown-and-gh-pages-publishing/31-04-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5.1: D-14 — HUMAN verifies the PUBLISHED site widget is interactive</name>
  <what-built>
    Plan 04's first CI run pushed `docs/` to `gh-pages` and GitHub Pages is now serving the site at `https://davidzenz.github.io/gg2d3/`. The Get-started article ought to embed a `gg2d3()` widget that uses the vendored `d3.v7.min.js`. This checkpoint confirms it ACTUALLY works in a real user's browser on the live URL — not localhost, not a screenshot, not "it built green."
  </what-built>
  <how-to-verify>
    1. Open `https://davidzenz.github.io/gg2d3/` in Chrome (or Firefox).

       Verify (visual sanity): the navbar shows `Home / Get started / Reference / Articles / Changelog` and a GitHub icon (D-06).

    2. Click **Get started** (or navigate directly to `https://davidzenz.github.io/gg2d3/articles/gg2d3.html`).

    3. Run all FIVE D-14 checks on the live page (mirror of Plan 03 Task 3.2's preflight, but against the public URL):

       (a) **Widget renders** — a `gg2d3()` plot is visible (not a static image, not an empty div).

       (b) **Tooltip hover** — hovering shows aesthetic values.

       (c) **At least one of:** scroll-wheel zoom rescales axes, click-drag pan moves the plot, or brush selection works. (Whichever the Get-started article demonstrates.)

       (d) **DevTools Console clean** — F12 → Console → no `Uncaught` errors, no `d3 is not defined`.

       (e) **Network tab** — reload page → filter `d3.v7.min.js` → status `200`. Asset path should be something like `https://davidzenz.github.io/gg2d3/articles/gg2d3_files/d3-7/d3.v7.min.js` (exact subpath may vary by htmlwidgets version).

    4. Also spot-check (D-07, D-11, D-13):
       - `https://davidzenz.github.io/gg2d3/news/index.html` — Changelog renders from `NEWS.md`.
       - `https://davidzenz.github.io/gg2d3/` — homepage matches `README.md` content.
       - `https://davidzenz.github.io/gg2d3/dev/` — should 404 (no dev split).
       - `https://davidzenz.github.io/gg2d3/articles/gg2d3-interactivity.html` — second vignette renders.

    5. If ALL pass: type "approved — D-14 green". This unblocks Task 5.2.

       If ANY fail: per D-15, fix in-phase. Most common failure at this stage is a misconfigured Pages settings (Task 4.2 step 7) — re-verify the Pages source is set to `gh-pages / (root)`. If genuine widget breakage, return to Plan 03 to reproduce and fix locally, then re-push.
  </how-to-verify>
  <resume-signal>Type "approved — D-14 green" if all five checks pass on the LIVE URL. Otherwise describe which check failed and what was observed.</resume-signal>
  <files>(human-driven checkpoint — no file modifications)</files>
  <action>This is a human checkpoint. Follow the steps in `<how-to-verify>` below. The orchestrator pauses here and resumes on the `<resume-signal>` keyword.</action>
  <verify><automated>MISSING — manual human verification per `<how-to-verify>` steps; no automated substitute (this is the D-NN human gate per CONTEXT)</automated></verify>
  <done>Human types the resume signal after completing all `<how-to-verify>` checks.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5.2: HUMAN — add the pkgdown URL to the GitHub repo "About" sidebar</name>
  <what-built>
    This is the one optional UI polish flagged in CONTEXT code_context. The repo's "About" widget (top-right of github.com/DavidZenz/gg2d3) shows a link slot that GitHub does not expose to git or the gh CLI directly (technically `gh repo edit --homepage` works — see below; presenting as a `human-action` because the user typically does this in the UI in one click).
  </what-built>
  <how-to-verify>
    Prefer the CLI path (single command, no browser):

    ```bash
    gh repo edit DavidZenz/gg2d3 --homepage "https://davidzenz.github.io/gg2d3/"
    ```

    Verify:

    ```bash
    gh repo view DavidZenz/gg2d3 --json homepageUrl --jq .homepageUrl
    ```

    Expected output: `https://davidzenz.github.io/gg2d3/`.

    If `gh repo edit` fails (auth issues, etc.), the UI fallback is: github.com/DavidZenz/gg2d3 → click the ⚙️ gear next to "About" in the right sidebar → set "Website" to `https://davidzenz.github.io/gg2d3/` → Save.

    Type "done" to resume.
  </how-to-verify>
  <resume-signal>Type "done" once the About sidebar carries the pkgdown URL (CLI or UI). This task is the only one in Plan 05 that is non-blocking for D-14 itself — but it's the documented closing polish.</resume-signal>
  <files>(human-driven checkpoint — no file modifications)</files>
  <action>This is a human checkpoint. Follow the steps in `<how-to-verify>` below. The orchestrator pauses here and resumes on the `<resume-signal>` keyword.</action>
  <verify><automated>MISSING — manual human verification per `<how-to-verify>` steps; no automated substitute (this is the D-NN human gate per CONTEXT)</automated></verify>
  <done>Human types the resume signal after completing all `<how-to-verify>` checks.</done>
</task>

<task type="auto">
  <name>Task 5.3: Propose DOCS-02 in REQUIREMENTS.md and finalize Phase 31 in ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — to find the v1.1 section and locate where DOCS-02 should land)
    - .planning/ROADMAP.md lines 170-178 (the Phase 31 stub block to fill in)
    - .planning/phases/31-pkgdown-and-gh-pages-publishing/31-04-SUMMARY.md (commit SHA, CI run URL for traceability)
  </read_first>
  <action>
    **Step A — add DOCS-02 to REQUIREMENTS.md.**

    Read the full file to find the existing DOCS-01 row (Phase 18). DOCS-02 is the publishing analog and should sit next to it in the same table/section. Use the same row format the file uses.

    Add a row equivalent to (adjust columns to match the file's actual schema):

    ```
    | DOCS-02 | Public package site published to GitHub Pages reflecting v1.1 docs | Phase 31 | Met — site live at https://davidzenz.github.io/gg2d3/ as of <YYYY-MM-DD> |
    ```

    Use the Edit tool to add this row in the appropriate section. Do NOT touch other rows. If REQUIREMENTS.md uses a markdown-list format instead of a table, mirror that format.

    **Step B — finalize Phase 31 in ROADMAP.md.**

    Replace the stub block at `.planning/ROADMAP.md` (lines roughly 170–178):

    ```markdown
    ### Phase 31: pkgdown and GH Pages publishing

    **Goal:** [To be planned]
    **Requirements**: TBD
    **Depends on:** Phase 18
    **Plans:** 0 plans

    Plans:
    - [ ] TBD (run /gsd-plan-phase 31 to break down)
    ```

    With (use Edit, preserve surrounding markdown):

    ```markdown
    ### Phase 31: pkgdown and GH Pages publishing

    **Goal:** A pkgdown site at https://davidzenz.github.io/gg2d3/ is rebuilt and redeployed on every push to master (and on releases), and at least one `gg2d3()` widget in the published "Get started" article is verifiably interactive in a user's browser.
    **Requirements**: DOCS-02
    **Depends on:** Phase 18
    **Plans:** 5 plans

    Plans:
    - [x] 31-01-PLAN.md — Wave 0 preflight (pkgdown/usethis install, GITHUB_PAT, manual-orphan bootstrap decision)
    - [x] 31-02-PLAN.md — Wave 1 config edits (rename vignette to gg2d3.Rmd, rewrite _pkgdown.yml, patch .Rbuildignore, extend DESCRIPTION URL)
    - [x] 31-03-PLAN.md — Wave 2 local build + D-15 preflight human interactivity check
    - [x] 31-04-PLAN.md — Wave 3 workflow file + gh-pages branch bootstrap + first CI deploy
    - [x] 31-05-PLAN.md — Wave 4 D-14 published-site human checkpoint + DOCS-02 + ROADMAP finalize
    ```

    Also update the "Progress" table at the bottom of ROADMAP.md if it has a Phase 31 row (it currently does not — line 167 ends at Phase 18). Append a new row consistent with the existing format:

    ```
    | 19. pkgdown and GH Pages publishing | v1.1 | 5/5 | Complete | <today> |
    ```

    (Use the actual completion date when running this task.)
  </action>
  <verify>
    <automated>grep -q "DOCS-02" .planning/REQUIREMENTS.md && grep -q "davidzenz.github.io/gg2d3" .planning/ROADMAP.md && ! grep -A2 "### Phase 31" .planning/ROADMAP.md | grep -q "\[To be planned\]"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DOCS-02" .planning/REQUIREMENTS.md` matches
    - Phase 31 entry in ROADMAP.md no longer reads `**Goal:** [To be planned]`
    - Phase 31 entry in ROADMAP.md references the pkgdown URL and DOCS-02
    - Phase 31 plan list in ROADMAP.md lists all 5 plans (31-01 through 31-05)
  </acceptance_criteria>
  <done>REQUIREMENTS.md formally captures the publishing capability; ROADMAP.md reflects Phase 31 done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public GitHub Pages URL → end users | Phase deliverable; ongoing surface, not net-new threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-11 | T (Tampering) | Stale ROADMAP / REQUIREMENTS.md leading future planners to re-do Phase 31 work | mitigate | Task 5.3 lands the traceability commit; grep verifies DOCS-02 + URL are recorded |
| T-19-12 | D (Denial of service) | First-load latency / asset 404 on the live site | accept | Static assets on GitHub Pages CDN; D-14 human check covers the load-success case. If a future asset issue arises, it's a Phase-N hotfix, not a Phase 31 mitigation. |
</threat_model>

<verification>
- Human approval of D-14 on live URL (Task 5.1)
- `gh repo view DavidZenz/gg2d3 --json homepageUrl` returns the pkgdown URL
- REQUIREMENTS.md and ROADMAP.md updated
</verification>

<success_criteria>
Phase 31 goal achieved: live pkgdown site, verified interactive widget in the published Get-started article, automatic rebuild on push, traceability landed in REQUIREMENTS.md and ROADMAP.md.
</success_criteria>

<output>
Finalize `.planning/phases/31-pkgdown-and-gh-pages-publishing/31-05-SUMMARY.md` with: D-14 human-check evidence (timestamp, browser, what was tested), repo "About" URL confirmation, REQUIREMENTS.md / ROADMAP.md diff references, and a closing checklist mapping each D-NN decision to the plan that satisfied it.
</output>
