---
phase: 31
plan: 03
type: execute
wave: 2
depends_on: [31-02]
files_modified:
  - docs/  # build output — gitignored, not committed
  - .planning/phases/31-pkgdown-and-gh-pages-publishing/31-03-SUMMARY.md
autonomous: false
requirements: ["DOCS-02 (proposed)"]
tags: [pkgdown, build, d-15-preflight]
must_haves:
  truths:
    - "pkgdown::build_site_github_pages() exits 0 from a fresh R session."
    - "docs/.nojekyll exists (proves build_site_github_pages was used, not build_site)."
    - "docs/articles/gg2d3.html exists with class=html-widget div and d3.v7.min.js script reference (Get-started auto-promotion + htmlwidget plumbing both worked)."
    - "docs/articles/gg2d3_files/ directory exists (htmlwidget dependency propagation worked)."
    - "docs/news/index.html exists (NEWS.md surfaced as Changelog per D-07)."
    - "Human verified locally-served Get-started article is interactively functional (D-15 preflight for D-14)."
    - "No d3-drawing-diagnostics content leaked into docs/ (D-10 honored)."
    - "No dev/ subdirectory in docs/ (D-13 honored)."
  artifacts:
    - path: "docs/.nojekyll"
      provides: "Disables Jekyll on GH Pages so _files/ asset dirs survive"
    - path: "docs/articles/gg2d3.html"
      provides: "Built Get-started article with embedded htmlwidget"
      contains: "html-widget"
    - path: "docs/articles/gg2d3_files"
      provides: "Bundled htmlwidget asset directory including vendored d3.v7.min.js"
    - path: "docs/news/index.html"
      provides: "NEWS.md surfaced as Changelog page"
  key_links:
    - from: "vignettes/gg2d3.Rmd"
      to: "docs/articles/gg2d3.html"
      via: "pkgdown::build_articles() during build_site_github_pages()"
      pattern: "html-widget"
    - from: "inst/htmlwidgets/lib/d3/d3.v7.min.js"
      to: "docs/articles/gg2d3_files/.../d3.v7.min.js"
      via: "htmlwidgets dependency system propagated by pkgdown"
      pattern: "d3\\.v7\\.min\\.js"
---

<objective>
Run the local pkgdown build, prove the htmlwidget plumbing survives, and have the human inspect the live local page before any workflow goes near CI.

Purpose: D-15 explicitly mandates a verification wave AFTER local build and BEFORE committing the workflow. This plan IS that wave. If the widget doesn't work locally, fixing it here costs minutes; fixing it after a public failed deploy costs reputation.

Output: Verified `docs/` tree, five automated grep proofs of widget plumbing, and a human sign-off on local interactivity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-pkgdown-and-gh-pages-publishing/31-CONTEXT.md
@.planning/phases/31-pkgdown-and-gh-pages-publishing/31-RESEARCH.md
@.planning/phases/31-pkgdown-and-gh-pages-publishing/31-VALIDATION.md
@.planning/phases/31-pkgdown-and-gh-pages-publishing/31-02-SUMMARY.md
@_pkgdown.yml
@vignettes/gg2d3.Rmd
@inst/htmlwidgets/gg2d3.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Run pkgdown::build_site_github_pages() and run five automated proxy assertions</name>
  <files>docs/ (build output, gitignored), .planning/phases/31-pkgdown-and-gh-pages-publishing/31-03-SUMMARY.md</files>
  <read_first>
    - _pkgdown.yml (must reflect Plan 02 edits)
    - .planning/phases/31-pkgdown-and-gh-pages-publishing/31-RESEARCH.md (Pitfall 4 — htmlwidget failure modes; Code Examples → Local build)
    - .planning/phases/31-pkgdown-and-gh-pages-publishing/31-VALIDATION.md (the five D-14 automated proxy checks)
  </read_first>
  <action>
    From the repo root, run the build in a fresh R subprocess to avoid stale state:

    ```bash
    rm -rf docs
    Rscript -e 'pkgdown::build_site_github_pages(new_process = FALSE, install = FALSE)' 2>&1 | tee /tmp/gg2d3-pkgdown-build.log
    ```

    `new_process = FALSE` runs in the current Rscript session (faster, easier to debug); `install = FALSE` skips reinstalling the package (we use `pkgload`/source tree directly).

    Expected build outputs to verify (D-07, D-09, D-11, D-13, D-14-proxy):

    ```bash
    set -e
    # D-14 automated proxy 1: .nojekyll
    test -f docs/.nojekyll
    # D-14 automated proxy 2: d3.v7.min.js script reference in Get-started
    grep -Eq 'src="[^"]*d3\.v7\.min\.js"' docs/articles/gg2d3.html
    # D-14 automated proxy 3: html-widget div present
    grep -Eq 'class="[^"]*html-widget' docs/articles/gg2d3.html
    # D-14 automated proxy 4: _files asset directory exists
    test -d docs/articles/gg2d3_files
    # D-14 automated proxy 5: build exit code (implicit — `set -e` above catches non-zero)
    echo "All 5 automated proxies passed"

    # D-07: NEWS surfaced
    test -f docs/news/index.html
    # D-09: Get started navbar text
    grep -q "Get started" docs/index.html
    # D-10: diagnostics doc NOT in published output
    ! find docs -name "*diagnostic*" 2>/dev/null | grep -q .
    # D-11: README → homepage
    test -f docs/index.html
    # D-13: no /dev/ split
    ! test -d docs/dev
    ```

    If ANY assertion fails, do NOT proceed to Task 3.2. Diagnose first:
    - If the html-widget div is missing → check chunk output; CONTEXT D-15 anticipates adding `htmltools::tagList()` wrapping.
    - If `gg2d3_files/` is missing → check `inst/htmlwidgets/gg2d3.yaml` declares `d3.v7.min.js` as a dependency.
    - If `.nojekyll` is missing → confirm `build_site_github_pages()` (not `build_site()`) was the called function.
    - If broken-link warnings appear in the build log → revisit cross-references missed by Task 2.1's grep.

    Capture results in `31-03-SUMMARY.md`:

    ```markdown
    ## Local Build

    - Command: `Rscript -e 'pkgdown::build_site_github_pages(new_process = FALSE, install = FALSE)'`
    - Exit code: 0
    - Build log: /tmp/gg2d3-pkgdown-build.log
    - Warnings (count + summary): <fill in>

    ## D-14 Automated Proxies

    | # | Check | Status |
    |---|-------|--------|
    | 1 | `test -f docs/.nojekyll` | PASS |
    | 2 | `grep d3.v7.min.js docs/articles/gg2d3.html` | PASS |
    | 3 | `grep html-widget docs/articles/gg2d3.html` | PASS |
    | 4 | `test -d docs/articles/gg2d3_files` | PASS |
    | 5 | build exit 0 | PASS |
    ```
  </action>
  <verify>
    <automated>test -f docs/.nojekyll && grep -Eq 'src="[^"]*d3\.v7\.min\.js"' docs/articles/gg2d3.html && grep -Eq 'class="[^"]*html-widget' docs/articles/gg2d3.html && test -d docs/articles/gg2d3_files && test -f docs/news/index.html && test -f docs/index.html && ! test -d docs/dev && ! find docs -name "*diagnostic*" 2>/dev/null | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - `pkgdown::build_site_github_pages()` exits 0
    - All 5 D-14 automated proxy checks pass
    - `docs/news/index.html` exists (D-07)
    - `docs/index.html` exists (D-11)
    - `docs/dev/` does not exist (D-13)
    - No file under `docs/` matches `*diagnostic*` (D-10)
    - `31-03-SUMMARY.md` contains a `## Local Build` section and a `## D-14 Automated Proxies` table with all rows PASS
  </acceptance_criteria>
  <done>Local build green, all automated D-14 proxies pass, side-decisions (D-07/D-10/D-11/D-13) verified on disk.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3.2: HUMAN — local browser interactivity preflight (D-15 mandate)</name>
  <what-built>
    Locally-built `docs/` tree with the Get-started article at `docs/articles/gg2d3.html` containing an embedded `gg2d3()` widget. Automated proxies confirm the plumbing is present; this checkpoint confirms the widget actually behaves interactively in a real browser. This is the D-15 preflight that gates Plan 04 (workflow commit) — failure here means fix locally, not after a public deploy.
  </what-built>
  <how-to-verify>
    1. Serve the docs directory over HTTP (file:// breaks some asset paths):

       ```bash
       python3 -m http.server -d docs 4321 &
       # OR if servr is installed:
       # Rscript -e 'servr::httd("docs", port = 4321)'
       ```

    2. Open `http://localhost:4321/articles/gg2d3.html` in Chrome (or Firefox).

    3. Run all FIVE checks (these are the D-14 human checks, run locally first per D-15):

       (a) **Widget renders** — confirm a `gg2d3()` plot is visible (not a static image, not an empty `<div>`, not a "broken widget" placeholder).

       (b) **Tooltip hover** — hover over a data point/bar; a tooltip with aesthetic values appears.

       (c) **Zoom or pan or crosstalk brush** — depending on which widget is featured in the Get-started article, exercise at least ONE interaction (scroll-wheel zoom, click-drag pan, brush selection). The plot must visibly respond.

       (d) **DevTools Console clean** — open DevTools (F12) → Console tab. No `Uncaught` errors, no `d3 is not defined`, no `htmlwidgets` errors.

       (e) **Network tab confirms d3.v7.min.js loaded 200 OK** — DevTools → Network → reload page → filter `d3.v7.min.js` → status column shows `200`.

    4. Stop the HTTP server (`fg` then Ctrl-C; or `kill` the python3 process).

    5. If ALL five pass: type "approved". The Wave 3 workflow commit can proceed.

       If ANY fail: describe what failed; we will diagnose in-phase per D-15 (do NOT defer). Common in-phase fixes:
       - Wrap chunk output in `htmltools::tagList(gg2d3(...))`
       - Set `screenshot.force = FALSE` in the relevant chunk or in `_pkgdown.yml`
       - Confirm `inst/htmlwidgets/gg2d3.yaml` declares `d3.v7.min.js` under `dependencies:`
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass, or describe the failing check (a/b/c/d/e) plus what you observed for in-phase triage.</resume-signal>
  <files>(human-driven checkpoint — no file modifications)</files>
  <action>This is a human checkpoint. Follow the steps in `<how-to-verify>` below. The orchestrator pauses here and resumes on the `<resume-signal>` keyword.</action>
  <verify><automated>MISSING — manual human verification per `<how-to-verify>` steps; no automated substitute (this is the D-NN human gate per CONTEXT)</automated></verify>
  <done>Human types the resume signal after completing all `<how-to-verify>` checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build output `docs/` → local browser | Local-only at this stage; no public surface yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | D (Denial of service / availability) | Broken widget reaching public site without local pre-flight catching it | mitigate | This plan IS the mitigation. D-15 mandates local human check BEFORE workflow commit. Task 3.2 is the gate. |
| T-31-06 | T (Tampering) | `docs/` accidentally committed to master (overwrites users' clones with build artifacts) | mitigate | `^docs$` already in `.Rbuildignore`; verify `docs/` is in `.gitignore` — if not, add it. `git status` after build must NOT show `docs/` as untracked-to-commit |
</threat_model>

<verification>
- All 5 D-14 automated proxies pass
- Human confirms widget is interactive in local browser
- DevTools console is clean for the Get-started page
</verification>

<success_criteria>
Local pkgdown site builds cleanly, the htmlwidget plumbing is intact, and a human has verified that the published artifact will not be a static-blob embarrassment. Plan 04 (workflow commit + gh-pages bootstrap) is unblocked.
</success_criteria>

<output>
After completion, finalize `.planning/phases/31-pkgdown-and-gh-pages-publishing/31-03-SUMMARY.md` with the `## Local Build` log, `## D-14 Automated Proxies` table, and a `## Human Preflight` section recording the five-check outcome plus any in-phase fixes that were applied.
</output>
