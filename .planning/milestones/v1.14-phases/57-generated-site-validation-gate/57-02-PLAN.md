---
phase: 57-generated-site-validation-gate
plan: "02"
type: execute
wave: 2
depends_on:
  - "57-01"
files_modified:
  - tools/validate-pkgdown-site.R
  - tests/testthat/helper-pkgdown-site.R
  - .github/workflows/pkgdown.yaml
autonomous: true
requirements:
  - SITE-01
user_setup: []
must_haves:
  truths:
    - "Maintainers can run one local command to validate committed generated docs quickly."
    - "Maintainers can run one release command to rebuild README/help/pkgdown output before validating generated docs."
    - "GitHub Actions validates generated pkgdown output after build and before deploy."
    - "Rendered sf is required only when spatial packages are loadable or the command explicitly requires it."
  artifacts:
    - path: "tools/validate-pkgdown-site.R"
      provides: "Maintainer and CI validation command"
      contains: "--mode"
    - path: ".github/workflows/pkgdown.yaml"
      provides: "CI generated-site validation step before deploy"
      contains: "Validate generated pkgdown site"
  key_links:
    - from: "tools/validate-pkgdown-site.R"
      to: "tests/testthat/helper-pkgdown-site.R"
      via: "shared validation helper sourcing"
      pattern: "pkgdown_site_validate_quick"
    - from: ".github/workflows/pkgdown.yaml"
      to: "tools/validate-pkgdown-site.R"
      via: "post-build validation step"
      pattern: "--mode ci"
---

<objective>
Add the maintainer-facing generated-site validation command and wire it into the pkgdown workflow before deploy.

Purpose: This turns the reusable Phase 57 helper into the repeatable local/CI gate required by SITE-01.
Output: `tools/validate-pkgdown-site.R`, helper refinements if needed, and `.github/workflows/pkgdown.yaml` validation step.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57-generated-site-validation-gate/57-CONTEXT.md
@.planning/phases/57-generated-site-validation-gate/57-RESEARCH.md
@.planning/phases/57-generated-site-validation-gate/57-VALIDATION.md
@.planning/phases/57-generated-site-validation-gate/57-PATTERNS.md
@.planning/phases/57-generated-site-validation-gate/57-01-SUMMARY.md
@AGENTS.md
@tests/testthat/helper-pkgdown-site.R
@tests/testthat/test-pkgdown-site.R
@.github/workflows/pkgdown.yaml

<interfaces>
Required commands:
```bash
rtk Rscript --vanilla tools/validate-pkgdown-site.R --mode quick
rtk Rscript --vanilla tools/validate-pkgdown-site.R --mode release
```

CI workflow placement:
```yaml
- name: Build site
  run: pkgdown::build_site_github_pages(new_process = FALSE, install = FALSE)
  shell: Rscript {0}

- name: Validate generated pkgdown site
  run: Rscript tools/validate-pkgdown-site.R --mode ci
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the quick/release/ci validation script</name>
  <files>tools/validate-pkgdown-site.R, tests/testthat/helper-pkgdown-site.R</files>
  <read_first>
    <file>.planning/phases/57-generated-site-validation-gate/57-CONTEXT.md</file>
    <file>.planning/phases/57-generated-site-validation-gate/57-RESEARCH.md</file>
    <file>tests/testthat/helper-pkgdown-site.R</file>
    <file>tests/testthat/test-pkgdown-site.R</file>
    <file>DESCRIPTION</file>
    <file>AGENTS.md</file>
  </read_first>
  <action>Create `tools/validate-pkgdown-site.R` with an executable Rscript entrypoint. Parse `--mode quick`, `--mode release`, and `--mode ci`; default to `quick`. Source `tests/testthat/helper-pkgdown-site.R` from the repository root. In `quick` mode, call `pkgdown_site_validate_source_contract()` and `pkgdown_site_validate_quick(require_rendered_sf = FALSE)`. In `release` mode, run `devtools::document()`, `devtools::build_readme()`, and `pkgdown::build_site_github_pages(new_process = FALSE, install = FALSE)` before validation; then require rendered sf only when `pkgdown_site_spatial_loadable()` returns TRUE. In `ci` mode, inspect the just-built site without rebuilding and require rendered sf only when `pkgdown_site_spatial_loadable()` returns TRUE. Print a concise final line `Generated pkgdown site validation passed (mode: <mode>, sf outcome: <outcome>)`. Stop with `call. = FALSE` on unknown mode or validation failure. If the helper needs one additional pure function to return the sf outcome without making expectations, add it there rather than duplicating parsing in the script.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'parse("tools/validate-pkgdown-site.R"); cat("script parses\\n")'</automated>
    <automated>rtk Rscript --vanilla tools/validate-pkgdown-site.R --mode quick</automated>
    <automated>rtk rg -n "mode:|quick|release|ci|pkgdown_site_validate_quick|pkgdown_site_spatial_loadable" tools/validate-pkgdown-site.R</automated>
  </verify>
  <acceptance_criteria>
    - `tools/validate-pkgdown-site.R` exists.
    - `rtk rg -n "quick|release|ci|devtools::document\\(\\)|devtools::build_readme\\(\\)|pkgdown::build_site_github_pages" tools/validate-pkgdown-site.R` finds all modes and rebuild commands.
    - `rtk rg -n "pkgdown_site_validate_source_contract|pkgdown_site_validate_quick|pkgdown_site_spatial_loadable" tools/validate-pkgdown-site.R` proves the script uses the shared helper.
    - `rtk Rscript --vanilla tools/validate-pkgdown-site.R --mode quick` exits 0 in the current generated-site state.
  </acceptance_criteria>
  <done>The validation command can be run locally in quick mode and has release/CI mode behavior wired to shared helper logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add the CI validation step before pkgdown deploy</name>
  <files>.github/workflows/pkgdown.yaml</files>
  <read_first>
    <file>.github/workflows/pkgdown.yaml</file>
    <file>tools/validate-pkgdown-site.R</file>
    <file>.planning/phases/57-generated-site-validation-gate/57-CONTEXT.md</file>
    <file>.planning/phases/57-generated-site-validation-gate/57-RESEARCH.md</file>
  </read_first>
  <action>Update `.github/workflows/pkgdown.yaml` so the existing `Verify website dependencies` and `Build site` steps remain unchanged, then add a new step named `Validate generated pkgdown site` immediately after `Build site` and before `Deploy to GitHub pages`. The step must run `Rscript tools/validate-pkgdown-site.R --mode ci`. Do not use `shell: Rscript {0}` for this step because the workflow is executing a script file with arguments, not inline R code. Do not add a second pkgdown build inside the workflow; CI mode validates the just-built `docs/` output.</action>
  <verify>
    <automated>rtk rg -n "Verify website dependencies|Build site|Validate generated pkgdown site|Rscript tools/validate-pkgdown-site.R --mode ci|Deploy to GitHub pages" .github/workflows/pkgdown.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "Validate generated pkgdown site" .github/workflows/pkgdown.yaml` finds the new step.
    - `rtk rg -n "Rscript tools/validate-pkgdown-site.R --mode ci" .github/workflows/pkgdown.yaml` finds the CI validation command.
    - The new validation step appears after `Build site` and before `Deploy to GitHub pages` when reading `.github/workflows/pkgdown.yaml`.
    - `.github/workflows/pkgdown.yaml` still contains `Verify website dependencies`, `needs: website`, and `pkgdown::build_site_github_pages(new_process = FALSE, install = FALSE)`.
  </acceptance_criteria>
  <done>CI validates the generated pkgdown site before deploying it.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the command/test integration</name>
  <files>tools/validate-pkgdown-site.R, tests/testthat/helper-pkgdown-site.R, tests/testthat/test-pkgdown-site.R, .github/workflows/pkgdown.yaml</files>
  <read_first>
    <file>tools/validate-pkgdown-site.R</file>
    <file>tests/testthat/helper-pkgdown-site.R</file>
    <file>tests/testthat/test-pkgdown-site.R</file>
    <file>.github/workflows/pkgdown.yaml</file>
  </read_first>
  <action>Run and record focused validation for the integrated gate. Use quick mode as the normal local check. Do not run release mode unless the executor chooses to pay the rebuild cost in this plan; release mode is required in final Phase 57 verification if not run here. If local quick validation fails because generated `docs/` is stale, rebuild using release mode, inspect the diff, and commit only source-derived generated files required by Phase 57.</action>
  <verify>
    <automated>rtk Rscript --vanilla tools/validate-pkgdown-site.R --mode quick</automated>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-pkgdown-site.R")'</automated>
    <automated>rtk Rscript --vanilla -e 'devtools::test(filter = "pkgdown-site")'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk Rscript --vanilla tools/validate-pkgdown-site.R --mode quick` exits 0.
    - `rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-pkgdown-site.R")'` exits 0.
    - `rtk Rscript --vanilla -e 'devtools::test(filter = "pkgdown-site")'` exits 0.
  </acceptance_criteria>
  <done>The command, helper, focused test, and CI workflow all point to one generated-site validation gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer shell -> generated docs | Local command validates or rebuilds generated site artifacts. |
| GitHub Actions build -> deploy | CI must not deploy generated output before validation. |
| optional spatial packages -> strict sf requirement | The gate must avoid confusing missing optional deps with missing generated output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Repudiation | helper/test canonical logic | inherited | Plan 57-01 owns shared helper extraction. |
| T-57-02 | Repudiation | local validation command | mitigate | Add `tools/validate-pkgdown-site.R` with quick/release/ci modes and explicit pass/fail output. |
| T-57-03 | Repudiation | optional sf classification | mitigate | Require rendered sf in release/CI only when `sf` and `geojsonsf` are loadable; otherwise accept classified skip. |
| T-57-04 | Tampering/Repudiation | CI deploy route | mitigate | Add validation step after `Build site` and before deploy. |
| T-57-05 | Denial of Service | local release validation cost | mitigate | Keep quick mode as default and reserve release rebuild mode for release checks. |
</threat_model>

<verification>
Run:
```bash
rtk Rscript --vanilla tools/validate-pkgdown-site.R --mode quick
rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-pkgdown-site.R")'
rtk rg -n "Validate generated pkgdown site|Rscript tools/validate-pkgdown-site.R --mode ci" .github/workflows/pkgdown.yaml
```
</verification>

<success_criteria>
- `tools/validate-pkgdown-site.R` exists and supports quick, release, and ci modes.
- Quick mode passes locally against committed generated `docs/`.
- The pkgdown workflow validates after build and before deploy.
- Strict rendered-sf behavior depends on actual spatial package loadability.
</success_criteria>

<output>
After completion, create `.planning/phases/57-generated-site-validation-gate/57-02-SUMMARY.md`.
</output>
