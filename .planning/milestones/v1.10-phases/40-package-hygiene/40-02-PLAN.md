---
phase: 40-package-hygiene
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - tests/testthat/helper-browser-sf.R
  - tests/testthat/helper-sf-fixtures.R
  - tests/testthat/test-sf-browser.R
  - .planning/phases/40-package-hygiene/40-SKIP-AUDIT.md
autonomous: true
requirements: [HYG-02]
user_setup: []
must_haves:
  truths:
    - "Optional browser and spatial validation skips name the missing package, Chrome/Chromium availability, or session launch issue."
    - "CRAN-like runs keep browser smoke tests skipped instead of requiring Chrome/chromote."
    - "Non-browser source and IR tests remain runnable when browser tests skip."
  artifacts:
    - path: "tests/testthat/helper-browser-sf.R"
      provides: "central browser skip behavior and messages"
    - path: "tests/testthat/helper-sf-fixtures.R"
      provides: "shared optional spatial fixture helpers"
    - path: ".planning/phases/40-package-hygiene/40-SKIP-AUDIT.md"
      provides: "record of optional dependency skip paths and any changes"
  key_links:
    - from: "tests/testthat/test-sf-browser.R"
      to: "tests/testthat/helper-browser-sf.R"
      via: "skip_browser_sf_smoke()"
      pattern: "skip_browser_sf_smoke"
---

<objective>
Harden optional browser and spatial validation skips so missing local tools produce clear skips, not failures or hidden dependency assumptions.

Purpose: HYG-02 preserves CRAN-friendly behavior while making local browser/spatial validation easier to diagnose.
Output: skip behavior audit, centralized skip-message refinements where needed, and targeted tests/source checks proving no heavyweight browser stack was added.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-package-hygiene/40-CONTEXT.md
@.planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md
@tests/testthat/helper-browser-sf.R
@tests/testthat/helper-sf-fixtures.R
@tests/testthat/test-sf-browser.R
@tests/testthat/test-sf-ir.R
@tests/testthat/test-sf-renderer.R
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Browser smoke tests fail on CRAN or clean CI because Chrome/chromote is unavailable | high | Keep `testthat::skip_on_cran()`, package skip checks, Chrome detection, and chromote launch probes centralized in `skip_browser_sf_smoke()`. |
| Missing optional sf/geojsonsf dependencies produce opaque errors before testthat can skip | high | Ensure fixture/test entry points call `skip_if_not_installed()` before constructing sf objects or serializing GeoJSON. |
| Skip hardening weakens non-browser regression coverage | medium | Limit browser skips to browser smoke tests; keep source-level and IR tests runnable independently. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Audit optional skip paths</name>
  <files>.planning/phases/40-package-hygiene/40-SKIP-AUDIT.md</files>
  <read_first>
    - tests/testthat/helper-browser-sf.R
    - tests/testthat/helper-sf-fixtures.R
    - tests/testthat/test-sf-browser.R
    - tests/testthat/test-sf-ir.R
    - tests/testthat/test-sf-renderer.R
    - tests/testthat/test-sf-utils.R
    - .planning/phases/40-package-hygiene/40-CONTEXT.md
  </read_first>
  <action>Create `.planning/phases/40-package-hygiene/40-SKIP-AUDIT.md` with sections exactly named `## Browser Skip Gate`, `## Spatial Skip Gate`, `## Non-Browser Coverage`, and `## Required Changes`. Record the current skip chain: `testthat::skip_on_cran()`, `testthat::skip_if_not_installed("chromote", "0.5.1")`, `testthat::skip_if_not_installed("sf")`, `testthat::skip_if_not_installed("geojsonsf")`, `chromote::find_chrome()`, and `chromote::ChromoteSession$new()`. Record whether any tests construct `sf::` objects before `skip_if_not_installed("sf")`. Record that Node, Playwright, Puppeteer, Selenium, and screenshot-diff tooling remain out of scope.</action>
  <verify>
    <automated>rtk rg -n "## Browser Skip Gate|skip_on_cran|chromote|find_chrome|ChromoteSession|## Spatial Skip Gate|## Non-Browser Coverage|## Required Changes" .planning/phases/40-package-hygiene/40-SKIP-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `rtk Rscript --vanilla -e 'stopifnot(file.exists(".planning/phases/40-package-hygiene/40-SKIP-AUDIT.md"))'` exits 0.
    - `rtk rg -n "## Browser Skip Gate|## Spatial Skip Gate|## Non-Browser Coverage|## Required Changes" .planning/phases/40-package-hygiene/40-SKIP-AUDIT.md` returns all four headings.
    - `rtk rg -n "Playwright|Puppeteer|Selenium|screenshot-diff|Node" .planning/phases/40-package-hygiene/40-SKIP-AUDIT.md` returns matches in an out-of-scope/no-new-tooling context.
  </acceptance_criteria>
  <done>Optional dependency skip behavior is documented before code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Harden browser skip helper messages</name>
  <files>tests/testthat/helper-browser-sf.R, .planning/phases/40-package-hygiene/40-SKIP-AUDIT.md</files>
  <read_first>
    - tests/testthat/helper-browser-sf.R
    - tests/testthat/test-sf-browser.R
    - .planning/phases/40-package-hygiene/40-SKIP-AUDIT.md
  </read_first>
  <action>Update `tests/testthat/helper-browser-sf.R` only if the audit shows unclear or duplicated skip behavior. Preserve the function name `skip_browser_sf_smoke()`. Keep these checks in order: `testthat::skip_on_cran()`, `testthat::skip_if_not_installed("chromote", "0.5.1")`, `testthat::skip_if_not_installed("sf")`, `testthat::skip_if_not_installed("geojsonsf")`, `chromote::find_chrome()`, and a guarded `chromote::ChromoteSession$new(width = 10, height = 10)` launch probe. Ensure the Chrome absence message contains `Chrome/Chromium not available for chromote sf smoke tests` and the launch failure message starts with `chromote session launch unavailable:`. Add comments only where they explain why launch probing is needed before live tests. Update `40-SKIP-AUDIT.md` `## Required Changes` to say either `No helper changes required` or list exact helper changes made.</action>
  <verify>
    <automated>rtk rg -n "skip_browser_sf_smoke|skip_on_cran|skip_if_not_installed\\(\"chromote\", \"0\\.5\\.1\"\\)|Chrome/Chromium not available for chromote sf smoke tests|chromote session launch unavailable" tests/testthat/helper-browser-sf.R</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "skip_browser_sf_smoke|skip_on_cran|skip_if_not_installed\\(\"chromote\", \"0\\.5\\.1\"\\)|skip_if_not_installed\\(\"sf\"\\)|skip_if_not_installed\\(\"geojsonsf\"\\)|find_chrome|ChromoteSession" tests/testthat/helper-browser-sf.R` returns matches.
    - `rtk rg -n "Chrome/Chromium not available for chromote sf smoke tests|chromote session launch unavailable:" tests/testthat/helper-browser-sf.R` returns both messages.
    - `rtk rg -n "No helper changes required|helper changes made" .planning/phases/40-package-hygiene/40-SKIP-AUDIT.md` returns a match.
  </acceptance_criteria>
  <done>Browser skip behavior remains centralized and diagnostic.</done>
</task>

<task type="auto">
  <name>Task 3: Verify optional skip and source coverage boundaries</name>
  <files>tests/testthat/helper-browser-sf.R, tests/testthat/helper-sf-fixtures.R, tests/testthat/test-sf-browser.R, .planning/phases/40-package-hygiene/40-SKIP-AUDIT.md</files>
  <read_first>
    - tests/testthat/helper-browser-sf.R
    - tests/testthat/helper-sf-fixtures.R
    - tests/testthat/test-sf-browser.R
    - tests/testthat/test-sf-ir.R
    - tests/testthat/test-sf-renderer.R
    - .planning/phases/40-package-hygiene/40-SKIP-AUDIT.md
  </read_first>
  <action>Run source-level checks that prove Phase 40 did not broaden browser tooling or hide non-browser coverage. Confirm `tests/testthat/test-sf-browser.R` calls `skip_browser_sf_smoke()` for live browser tests. Confirm non-browser sf IR/source tests still use `skip_if_not_installed("sf")` and `skip_if_not_installed("geojsonsf")` directly rather than depending on Chrome. If a test constructs sf objects before skip guards, move the skip guard earlier in that test file. Do not add live browser execution requirements to non-browser tests.</action>
  <verify>
    <automated>rtk rg -n "skip_browser_sf_smoke|skip_if_not_installed\\(\"sf\"\\)|skip_if_not_installed\\(\"geojsonsf\"\\)" tests/testthat/test-sf-browser.R tests/testthat/test-sf-ir.R tests/testthat/test-sf-renderer.R tests/testthat/test-sf-utils.R</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "skip_browser_sf_smoke" tests/testthat/test-sf-browser.R` returns matches.
    - `rtk rg -n "skip_if_not_installed\\(\"sf\"\\)|skip_if_not_installed\\(\"geojsonsf\"\\)" tests/testthat/test-sf-ir.R tests/testthat/test-sf-renderer.R tests/testthat/test-sf-utils.R` returns matches.
    - `rtk rg -n "playwright|puppeteer|selenium|pixel diff|visual diff" tests/testthat/helper-browser-sf.R tests/testthat/test-sf-browser.R DESCRIPTION` returns no matches.
  </acceptance_criteria>
  <done>Optional browser/spatial validation skips are clear and do not weaken non-browser coverage.</done>
</task>

</tasks>

<verification>
Run:

```bash
rtk rg -n "skip_browser_sf_smoke|skip_on_cran|chromote session launch unavailable|Chrome/Chromium not available" tests/testthat/helper-browser-sf.R tests/testthat/test-sf-browser.R
rtk rg -n "skip_if_not_installed\\(\"sf\"\\)|skip_if_not_installed\\(\"geojsonsf\"\\)" tests/testthat/test-sf-ir.R tests/testthat/test-sf-renderer.R tests/testthat/test-sf-utils.R
rtk rg -n "playwright|puppeteer|selenium|pixel diff|visual diff" tests/testthat/helper-browser-sf.R tests/testthat/test-sf-browser.R DESCRIPTION
```
</verification>

<success_criteria>
- HYG-02 is covered by an optional skip audit and helper/source checks.
- Browser smoke tests still skip on CRAN-like and missing-tool environments.
- Missing Chrome/chromote and missing spatial packages produce clear testthat skips.
- No Node browser tooling or screenshot-diff infrastructure is introduced.
</success_criteria>
