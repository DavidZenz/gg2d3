---
phase: 40-package-hygiene
plan: 03
type: execute
wave: 3
depends_on: [40-02]
files_modified:
  - .gitignore
  - .Rbuildignore
  - tests/testthat/helper-sf-fixtures.R
  - tests/testthat/helper-browser-sf.R
  - tests/testthat/test-sf-browser.R
  - tests/testthat/test-date-scales.R
  - .planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md
autonomous: true
requirements: [HYG-03]
user_setup: []
must_haves:
  truths:
    - "Generated browser fixtures, browser logs, visual HTML outputs, and local check outputs land under predictable ignored paths."
    - "Running fixture-generating or artifact-checking tests does not leave tracked source-tree noise beyond intentional files."
    - "Failure artifacts remain available for debugging and are not deleted automatically."
  artifacts:
    - path: ".planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md"
      provides: "artifact path and ignore-rule audit"
    - path: ".gitignore"
      provides: "local generated artifact ignore rules"
    - path: ".Rbuildignore"
      provides: "package build artifact exclusions where needed"
  key_links:
    - from: "tests/testthat/helper-browser-sf.R"
      to: ".gitignore"
      via: "test_output/browser-sf"
      pattern: "browser_sf_artifact_dir"
---

<objective>
Normalize generated artifact paths and ignore rules so local browser fixtures, logs, visual checks, and check outputs do not pollute package sources.

Purpose: HYG-03 keeps release-hardening validation repeatable without hiding useful local debugging artifacts.
Output: artifact audit, ignore/build-ignore updates if needed, and source checks proving generated outputs are predictable and ignored.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-package-hygiene/40-CONTEXT.md
@.planning/phases/40-package-hygiene/40-SKIP-AUDIT.md
@.gitignore
@.Rbuildignore
@tests/testthat/helper-sf-fixtures.R
@tests/testthat/helper-browser-sf.R
@tests/testthat/test-sf-browser.R
@tests/testthat/test-date-scales.R
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Local smoke runs leave generated HTML/log/check files as untracked source noise | medium | Keep all generated local artifacts under `test_output/`, `test_*_files/`, or `*.Rcheck/` and ensure ignore rules cover them. |
| Package build accidentally includes local browser fixture output | medium | Add or verify `.Rbuildignore` coverage for generated output roots that can exist during local checks. |
| Debugging evidence is deleted before failures can be inspected | low | Preserve failure artifacts under ignored paths and document where to find them. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Audit generated artifact paths</name>
  <files>.planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md</files>
  <read_first>
    - .gitignore
    - .Rbuildignore
    - tests/testthat/helper-sf-fixtures.R
    - tests/testthat/helper-browser-sf.R
    - tests/testthat/test-sf-browser.R
    - tests/testthat/test-date-scales.R
    - tests/testthat/visual-advanced-stats-check.R
    - tests/testthat/visual-coord-flip-check.R
    - tests/testthat/visual-legend-check.R
    - tests/testthat/visual-polar-coords-check.R
    - tests/testthat/visual-reference-geoms-check.R
    - tests/testthat/visual-zoom-line-check.R
  </read_first>
  <action>Create `.planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md` with sections exactly named `## Generated Paths`, `## Ignore Coverage`, `## Build Ignore Coverage`, and `## Required Changes`. Record every generated local output pattern found by `rtk rg -n "test_output|saveWidget|save_html|writeLines|Rcheck|browser_sf_artifact_dir|\\.test_output_dir" tests R vignettes README.Rmd .gitignore .Rbuildignore`. The expected safe roots are `test_output/`, `test_output/browser-sf/`, `test_*_files/`, root-level `*.html`, root-level `*.png`, root-level `*.pdf`, and `*.Rcheck/`. Note any path that writes outside those roots as a required change.</action>
  <verify>
    <automated>rtk rg -n "## Generated Paths|## Ignore Coverage|## Build Ignore Coverage|## Required Changes|test_output|browser-sf|Rcheck" .planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `rtk Rscript --vanilla -e 'stopifnot(file.exists(".planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md"))'` exits 0.
    - `rtk rg -n "## Generated Paths|## Ignore Coverage|## Build Ignore Coverage|## Required Changes" .planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md` returns all four headings.
    - `rtk rg -n "test_output/|test_output/browser-sf|\\*\\.Rcheck|root-level" .planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md` returns matches.
  </acceptance_criteria>
  <done>Generated artifact paths and existing ignore coverage are documented.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize artifact roots and ignore rules</name>
  <files>.gitignore, .Rbuildignore, tests/testthat/helper-sf-fixtures.R, tests/testthat/helper-browser-sf.R, tests/testthat/test-date-scales.R, .planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md</files>
  <read_first>
    - .gitignore
    - .Rbuildignore
    - tests/testthat/helper-sf-fixtures.R
    - tests/testthat/helper-browser-sf.R
    - tests/testthat/test-date-scales.R
    - .planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md
  </read_first>
  <action>Apply only the required changes identified in `40-ARTIFACT-AUDIT.md`. Ensure generated browser and visual outputs use the project-root `test_output/` convention rather than ad hoc relative roots such as `"../../test_output"` when a shared helper is available. If `test-date-scales.R` still writes to `"../../test_output"`, replace that with a helper that resolves the package root or reuses `.test_output_dir()` when sourced. Ensure `.gitignore` includes `test_output/`, `test_*_files/`, root-level `/*.html`, root-level `/*.png`, root-level `/*.pdf`, and `*.Rcheck/`. Ensure `.Rbuildignore` excludes local generated outputs if they can exist during package build, including `^test_output$`, `^test_output/`, `^.*\\.Rcheck$`, and root-level generated HTML/PNG/PDF patterns if not already covered by package policy. Update `40-ARTIFACT-AUDIT.md` `## Required Changes` with exact files changed or `No changes required`.</action>
  <verify>
    <automated>rtk rg -n "test_output/|test_\\*_files|\\*\\.Rcheck|/\\*\\.html|/\\*\\.png|/\\*\\.pdf" .gitignore && rtk rg -n "\\^test_output|Rcheck|html|png|pdf" .Rbuildignore</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "test_output/" .gitignore` returns a match.
    - `rtk rg -n "\\*\\.Rcheck/" .gitignore` returns a match.
    - `rtk rg -n "\\^test_output|Rcheck" .Rbuildignore` returns matches.
    - `rtk rg -n "\\.\\./\\.\\./test_output" tests/testthat/test-date-scales.R` returns no matches unless `40-ARTIFACT-AUDIT.md` documents a no-change rationale.
    - `rtk rg -n "No changes required|Changed files:" .planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md` returns a match.
  </acceptance_criteria>
  <done>Generated artifacts use predictable ignored paths and package builds exclude local outputs.</done>
</task>

<task type="auto">
  <name>Task 3: Prove artifact hygiene after fixture generation</name>
  <files>.gitignore, .Rbuildignore, tests/testthat/helper-sf-fixtures.R, tests/testthat/helper-browser-sf.R, .planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md</files>
  <read_first>
    - .gitignore
    - .Rbuildignore
    - tests/testthat/helper-sf-fixtures.R
    - tests/testthat/helper-browser-sf.R
    - .planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md
  </read_first>
  <action>Run source and ignore checks that prove generated artifacts are predictable and ignored. Do not delete existing user artifacts. Use `git check-ignore test_output/browser-sf/phase40-artifact-smoke.html` and `git check-ignore test_output/browser-sf/phase40-artifact-smoke-page-errors.log` to prove browser fixture and log paths are ignored. If the ignored-path checks fail, update `.gitignore` or `.Rbuildignore` as appropriate. Append a `## Verification Evidence` section to `40-ARTIFACT-AUDIT.md` with the exact commands run and their outcomes.</action>
  <verify>
    <automated>rtk git check-ignore test_output/browser-sf/phase40-artifact-smoke.html && rtk git check-ignore test_output/browser-sf/phase40-artifact-smoke-page-errors.log</automated>
  </verify>
  <acceptance_criteria>
    - `rtk git check-ignore test_output/browser-sf/phase40-artifact-smoke.html` exits 0.
    - `rtk git check-ignore test_output/browser-sf/phase40-artifact-smoke-page-errors.log` exits 0.
    - `rtk rg -n "## Verification Evidence|phase40-artifact-smoke.html|phase40-artifact-smoke-page-errors.log" .planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md` returns matches.
  </acceptance_criteria>
  <done>Artifact hygiene is verified without deleting local debugging evidence.</done>
</task>

</tasks>

<verification>
Run:

```bash
rtk rg -n "test_output/" .gitignore
rtk rg -n "\\^test_output|Rcheck" .Rbuildignore
rtk git check-ignore test_output/browser-sf/phase40-artifact-smoke.html
rtk git check-ignore test_output/browser-sf/phase40-artifact-smoke-page-errors.log
rtk rg -n "## Verification Evidence" .planning/phases/40-package-hygiene/40-ARTIFACT-AUDIT.md
```
</verification>

<success_criteria>
- HYG-03 is covered by an artifact path audit and ignore/build-ignore updates where needed.
- Browser fixtures and logs are under ignored `test_output/browser-sf/` paths.
- Local visual/check outputs do not pollute package source or package builds.
- Debugging artifacts are preserved in ignored paths instead of being deleted automatically.
</success_criteria>
