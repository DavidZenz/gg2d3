---
phase: 40-package-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DESCRIPTION
  - .planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md
autonomous: true
requirements: [HYG-01]
user_setup: []
must_haves:
  truths:
    - "Every direct non-base `pkg::fun()` or `library(pkg)` use in R/, tests/, vignettes/, and README.Rmd is represented in DESCRIPTION Imports or Suggests."
    - "Runtime package dependencies stay in Imports only when package code requires them outside optional paths."
    - "Test, vignette, browser, visual-check, and documentation helper dependencies are declared in Suggests."
  artifacts:
    - path: "DESCRIPTION"
      provides: "release-hygiene dependency declarations"
    - path: ".planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md"
      provides: "auditable record of direct package usage and DESCRIPTION classification"
  key_links:
    - from: "tests/testthat/helper-sf-fixtures.R"
      to: "DESCRIPTION"
      via: "pkgload and rprojroot direct Suggests declarations"
      pattern: "pkgload::load_all|rprojroot::find_package_root_file"
---

<objective>
Audit direct package usage and update DESCRIPTION so package tests, helper code, examples, visual checks, vignettes, and documentation generation do not rely on undeclared direct dependencies.

Purpose: HYG-01 makes the release surface reproducible in clean development and check environments.
Output: DESCRIPTION dependency declarations plus a dependency audit artifact explaining the classification.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-package-hygiene/40-CONTEXT.md
@DESCRIPTION
@R/as_d3_ir.R
@R/gg2d3.R
@R/sf_utils.R
@tests/testthat/helper-sf-fixtures.R
@tests/testthat/helper-browser-sf.R
@tests/testthat/test-sf-browser.R
@vignettes/gg2d3.Rmd
@vignettes/gg2d3-interactivity.Rmd
@README.Rmd
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| A clean test environment fails because tests use a package that is only installed transitively | high | Audit direct namespace and library calls and declare every non-base direct package in DESCRIPTION. |
| Heavy optional browser/spatial tooling is moved to Imports and becomes a hard runtime dependency | medium | Classify browser, visual, test, and vignette-only packages as Suggests unless R/ package code requires them unconditionally. |
| Runtime sf support silently becomes optional even though package code requires geojsonsf for conversion | medium | Keep runtime sf conversion behavior explicit and document `geojsonsf` classification in the audit. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create direct dependency audit</name>
  <files>.planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md</files>
  <read_first>
    - DESCRIPTION
    - .planning/phases/40-package-hygiene/40-CONTEXT.md
    - R/as_d3_ir.R
    - R/gg2d3.R
    - R/sf_utils.R
    - tests/testthat/helper-sf-fixtures.R
    - tests/testthat/helper-browser-sf.R
    - tests/testthat/test-sf-browser.R
    - vignettes/gg2d3.Rmd
    - vignettes/gg2d3-interactivity.Rmd
    - README.Rmd
  </read_first>
  <action>Create `.planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md`. Include sections named exactly `## Scan Commands`, `## Direct Namespace Usage`, `## DESCRIPTION Classification`, and `## Follow-up Decisions`. Run or reproduce these scans in the artifact: `rtk rg --no-filename -o "[A-Za-z][A-Za-z0-9.]*::" R tests vignettes README.Rmd -g "*.R" -g "*.Rmd" | sed 's/::$//' | sort -u`, `rtk rg -n "library\\(|requireNamespace\\(|skip_if_not_installed\\(" R tests vignettes README.Rmd -g "*.R" -g "*.Rmd"`, and `rtk Rscript --vanilla -e 'd <- read.dcf("DESCRIPTION"); cat(d[1, "Imports"], "\\n---SUGGESTS---\\n", d[1, "Suggests"], "\\n")'`. In `## DESCRIPTION Classification`, record at minimum these candidate packages and their expected class: Imports candidates `ggplot2`, `htmlwidgets`, `jsonlite`, `grid`, `rlang`; Suggests candidates `crosstalk`, `testthat`, `chromote`, `pkgload`, `rprojroot`, `sf`, `geojsonsf`, `rnaturalearth`, `htmltools`, `V8`, `knitr`, `rmarkdown`, `scales`, `mgcv`, `maps`, `devtools`. Mark base/recommended packages such as `tools` and `utils` as no DESCRIPTION action unless R package policy requires otherwise.</action>
  <verify>
    <automated>rtk rg -n "## Scan Commands|## Direct Namespace Usage|## DESCRIPTION Classification|ggplot2|htmltools|V8|knitr|rmarkdown|scales|mgcv|maps" .planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `rtk Rscript --vanilla -e 'stopifnot(file.exists(".planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md"))'` exits 0.
    - `rtk rg -n "## Scan Commands|## Direct Namespace Usage|## DESCRIPTION Classification|## Follow-up Decisions" .planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md` returns all four headings.
    - `rtk rg -n "htmltools|V8|knitr|rmarkdown|scales|mgcv|maps|devtools" .planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md` returns matches.
  </acceptance_criteria>
  <done>Direct dependency usage is captured in an auditable artifact before DESCRIPTION changes.</done>
</task>

<task type="auto">
  <name>Task 2: Update DESCRIPTION dependency declarations</name>
  <files>DESCRIPTION, .planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md</files>
  <read_first>
    - DESCRIPTION
    - .planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md
    - R/gg2d3.R
    - R/sf_utils.R
    - tests/testthat/helper-sf-fixtures.R
    - tests/testthat/helper-browser-sf.R
    - tests/testthat/test-layout-facet-free.R
    - tests/testthat/test-geoms-phase5.R
    - tests/testthat/test-ir.R
    - vignettes/gg2d3.Rmd
    - vignettes/gg2d3-interactivity.Rmd
    - README.Rmd
  </read_first>
  <action>Update `DESCRIPTION` according to the audit. Keep `Imports:` limited to packages required by runtime R package code, including the already-present `ggplot2`, `htmlwidgets`, `jsonlite`, `grid`, and `rlang`; keep or add `crosstalk` in Imports if `R/gg2d3.R` and `R/d3_crosstalk.R` use `crosstalk::` for runtime widget behavior. Keep optional development, test, browser, vignette, and visual-check packages in `Suggests:`. Ensure `Suggests:` includes direct optional usages found by the audit, including `testthat (>= 3.0.0)`, `chromote (>= 0.5.1)`, `pkgload`, `rprojroot`, `sf (>= 1.0.0)`, `geojsonsf (>= 2.0.0)`, `rnaturalearth`, `htmltools`, `V8`, `knitr`, `rmarkdown`, `scales`, `mgcv`, `maps`, and `devtools` unless the audit proves a package is only mentioned as install documentation and does not need DESCRIPTION. Keep alphabetical or locally consistent ordering within each field and preserve existing metadata such as `Config/testthat/edition` and `Config/roxygen2/version`.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'd <- read.dcf("DESCRIPTION"); imports <- d[1, "Imports"]; suggests <- d[1, "Suggests"]; required <- c("testthat", "chromote", "pkgload", "rprojroot", "sf", "geojsonsf", "rnaturalearth", "htmltools", "V8", "knitr", "rmarkdown", "scales", "mgcv", "maps"); missing <- required[!grepl(paste0("\\\\b", required, "\\\\b"), suggests)]; if (length(missing)) stop(paste("Missing Suggests:", paste(missing, collapse = ", "))); stopifnot(grepl("\\\\bggplot2\\\\b", imports), grepl("\\\\bhtmlwidgets\\\\b", imports)); cat("DESCRIPTION dependencies ok\\n")'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "^Imports:|^Suggests:|chromote|pkgload|rprojroot|htmltools|V8|knitr|rmarkdown|scales|mgcv|maps" DESCRIPTION` returns matches.
    - `rtk Rscript --vanilla -e 'read.dcf("DESCRIPTION"); cat("DESCRIPTION parse ok\\n")'` exits 0.
    - `rtk Rscript --vanilla -e 'd <- read.dcf("DESCRIPTION"); suggests <- d[1, "Suggests"]; required <- c("testthat", "chromote", "pkgload", "rprojroot", "sf", "geojsonsf", "rnaturalearth", "htmltools", "V8", "knitr", "rmarkdown", "scales", "mgcv", "maps"); missing <- required[!grepl(paste0("\\\\b", required, "\\\\b"), suggests)]; if (length(missing)) stop(paste("Missing Suggests:", paste(missing, collapse = ", ")))'` exits 0, unless the audit file documents a specific no-action rationale for a package.
  </acceptance_criteria>
  <done>DESCRIPTION declares the direct package usage needed by package tests, helpers, vignettes, examples, browser tooling, and spatial validation.</done>
</task>

<task type="auto">
  <name>Task 3: Verify metadata hygiene</name>
  <files>DESCRIPTION, .planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md</files>
  <read_first>
    - DESCRIPTION
    - .planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md
    - tests/testthat/helper-browser-sf.R
    - tests/testthat/helper-sf-fixtures.R
  </read_first>
  <action>Run a bounded metadata hygiene check after DESCRIPTION changes. Confirm `read.dcf("DESCRIPTION")` parses, confirm the audit artifact lists every package added or intentionally not added, and run the source checks for direct `pkg::` usage. If any package remains directly referenced but absent from both Imports and Suggests with no documented no-action rationale, update DESCRIPTION or the audit before completing this task. Do not run full `R CMD check` in this plan; Phase 42 owns the full release gate.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'read.dcf("DESCRIPTION"); stopifnot(file.exists(".planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md")); cat("metadata hygiene spot-check ok\\n")'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk Rscript --vanilla -e 'read.dcf("DESCRIPTION"); cat("DESCRIPTION parse ok\\n")'` exits 0.
    - `rtk rg -n "Added to Imports|Added to Suggests|No action|Deferred" .planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md` returns at least one match.
    - `rtk rg -n "Playwright|Puppeteer|Selenium|node browser stack" DESCRIPTION` returns no matches.
  </acceptance_criteria>
  <done>Package dependency metadata is parseable, justified, and still avoids new heavy browser stacks.</done>
</task>

</tasks>

<verification>
Run:

```bash
rtk Rscript --vanilla -e 'read.dcf("DESCRIPTION"); cat("DESCRIPTION parse ok\n")'
rtk rg -n "chromote|pkgload|rprojroot|htmltools|V8|knitr|rmarkdown|scales|mgcv|maps" DESCRIPTION
rtk rg -n "## DESCRIPTION Classification|Added to Imports|Added to Suggests|No action|Deferred" .planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md
```
</verification>

<success_criteria>
- HYG-01 is covered by an audit artifact and DESCRIPTION updates.
- DESCRIPTION parses with `read.dcf()`.
- Direct optional test/browser/vignette/helper dependencies are declared or explicitly justified as no-action in the audit.
- No Node-based browser tooling is added to DESCRIPTION.
</success_criteria>
