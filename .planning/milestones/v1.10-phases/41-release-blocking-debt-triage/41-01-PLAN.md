---
phase: 41-release-blocking-debt-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md
  - DESCRIPTION
  - tests/testthat/test-sf-browser.R
autonomous: true
requirements: [DEBT-01]
user_setup: []
must_haves:
  truths:
    - "Direct `pkgload` and `rprojroot` usages are declared in DESCRIPTION or explicitly classified as non-blocking with evidence."
    - "Facet browser panel identity assertions preserve panel order and do not validate only a sorted distribution."
    - "Every advisory item from DEBT-01 has a status, evidence, release-blocking judgment, rationale, and next step in `41-DEBT-AUDIT.md`."
  artifacts:
    - path: ".planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md"
      provides: "release-facing advisory follow-up classification"
    - path: "DESCRIPTION"
      provides: "dependency declaration evidence"
    - path: "tests/testthat/test-sf-browser.R"
      provides: "facet panel identity assertion evidence"
  key_links:
    - from: ".planning/milestones/v1.9-phases/36-browser-sf-smoke-harness/36-REVIEW.md"
      to: "tests/testthat/test-sf-browser.R"
      via: "facet panel identity advisory"
      pattern: "faceted sf fixtures keep panel-local path counts"
---

<objective>
Resolve or classify recent advisory follow-ups for Phase 41 DEBT-01.

Purpose: Maintainers need release-reviewable evidence that direct dependency declarations and facet browser panel identity assertions are no longer open blockers.
Output: `41-DEBT-AUDIT.md` entries for `pkgload`, `rprojroot`, and facet panel identity, plus narrow code/test updates only if verification finds a gap.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-release-blocking-debt-triage/41-CONTEXT.md
@.planning/phases/41-release-blocking-debt-triage/41-RESEARCH.md
@.planning/phases/41-release-blocking-debt-triage/41-PATTERNS.md
@.planning/milestones/v1.9-phases/36-browser-sf-smoke-harness/36-REVIEW.md
@DESCRIPTION
@tests/testthat/test-sf-browser.R
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Release notes claim advisory follow-ups are resolved without source evidence | high | Create `41-DEBT-AUDIT.md` entries with exact verification commands and outcomes. |
| Facet tests regress to sorted counts and stop proving panel identity | high | Verify direct ordered vector comparison and update the test if `sort(panel_counts)` appears in the BRSF-02 path. |
| Dependency declarations are accidentally moved out of Suggests before release validation | medium | Verify `DESCRIPTION` parses and `Suggests` contains `pkgload` and `rprojroot`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create advisory debt audit entries</name>
  <files>.planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md</files>
  <read_first>
    - .planning/phases/41-release-blocking-debt-triage/41-CONTEXT.md
    - .planning/phases/41-release-blocking-debt-triage/41-RESEARCH.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - DESCRIPTION
    - tests/testthat/test-sf-browser.R
  </read_first>
  <action>Create `.planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md` if it does not exist. Add sections named exactly `## Audit Schema`, `## DEBT-01 Advisory Follow-ups`, `## DEBT-02 Renderer and Documentation Debt`, and `## Verification Evidence`. Under `## Audit Schema`, define the columns exactly as `Item`, `Requirement`, `Evidence`, `Status`, `Release-blocking judgment`, `Action`, `Rationale`, and `Next step`. Under `## DEBT-01 Advisory Follow-ups`, add three rows/items named exactly `pkgload direct declaration`, `rprojroot direct declaration`, and `facet panel identity assertions`. Initially set each status to `Pending verification`; later tasks must replace those statuses with `Resolved`, `Fixed`, or `Deferred non-blocker`.</action>
  <verify>
    <automated>rtk rg -n "## Audit Schema|## DEBT-01 Advisory Follow-ups|pkgload direct declaration|rprojroot direct declaration|facet panel identity assertions|Release-blocking judgment|Next step" .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `rtk Rscript --vanilla -e 'stopifnot(file.exists(".planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md"))'` exits 0.
    - `rtk rg -n "## Audit Schema|## DEBT-01 Advisory Follow-ups|## DEBT-02 Renderer and Documentation Debt|## Verification Evidence" .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md` returns all four headings.
    - `rtk rg -n "pkgload direct declaration|rprojroot direct declaration|facet panel identity assertions" .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md` returns all three item names.
  </acceptance_criteria>
  <done>The shared debt audit artifact exists and has placeholders for all DEBT-01 advisory items.</done>
</task>

<task type="auto">
  <name>Task 2: Verify direct dependency declarations</name>
  <files>DESCRIPTION, .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md</files>
  <read_first>
    - DESCRIPTION
    - .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md
    - .planning/phases/40-package-hygiene/40-DEPENDENCY-AUDIT.md
  </read_first>
  <action>Run the DESCRIPTION verification for `pkgload` and `rprojroot`. If both packages are present in `Suggests`, update the corresponding `41-DEBT-AUDIT.md` rows to `Status: Resolved`, `Release-blocking judgment: Not blocking`, `Action: No code change required`, `Rationale: DESCRIPTION Suggests declares the direct helper dependencies`, and `Next step: Keep covered by Phase 42 package checks`. If either package is missing, add it to `DESCRIPTION` under `Suggests`, preserving the existing DCF formatting, then mark that item `Status: Fixed` with `Action: Added missing Suggests declaration`.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'd <- read.dcf("DESCRIPTION"); suggests <- d[1, "Suggests"]; stopifnot(grepl("pkgload", suggests, fixed = TRUE), grepl("rprojroot", suggests, fixed = TRUE)); cat("dependency advisory resolved\n")'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk Rscript --vanilla -e 'read.dcf("DESCRIPTION"); cat("DESCRIPTION parse ok\\n")'` exits 0.
    - `rtk Rscript --vanilla -e 'd <- read.dcf("DESCRIPTION"); suggests <- d[1, "Suggests"]; stopifnot(grepl("pkgload", suggests, fixed = TRUE), grepl("rprojroot", suggests, fixed = TRUE))'` exits 0.
    - `rtk rg -n "pkgload direct declaration.*(Resolved|Fixed)|rprojroot direct declaration.*(Resolved|Fixed)" .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md` returns matches for both items.
  </acceptance_criteria>
  <done>Direct dependency declaration advisory items are resolved or fixed with evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Verify facet panel identity assertions</name>
  <files>tests/testthat/test-sf-browser.R, .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md</files>
  <read_first>
    - tests/testthat/test-sf-browser.R
    - .planning/milestones/v1.9-phases/36-browser-sf-smoke-harness/36-REVIEW.md
    - .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md
  </read_first>
  <action>Inspect the test named `BRSF-02 DOM: faceted sf fixtures keep panel-local path counts` in `tests/testthat/test-sf-browser.R`. Ensure `expected_panel_counts` contains exactly `"phase35-sf-facet-wrap.html" = c(1L, 1L)` and `"phase35-sf-facet-grid.html" = c(1L, 0L, 0L, 1L)`. Ensure the assertion compares `panel_counts` directly with `expected_panel_counts[[fixture_name]]` and does not call `sort(panel_counts)` or `sort(expected_panel_counts[[fixture_name]])` inside this test. If the test already satisfies these conditions, do not change the test and mark `facet panel identity assertions` as `Status: Resolved`, `Release-blocking judgment: Not blocking`, `Action: No code change required`, `Rationale: BRSF-02 preserves panel-order identity`, and `Next step: Keep covered by browser smoke source checks`. If the test fails any condition, update only this test block to the exact expected vectors and direct comparison described above, then mark the audit item `Status: Fixed`.</action>
  <verify>
    <automated>rtk rg -n "phase35-sf-facet-wrap.html\" = c\\(1L, 1L\\)|phase35-sf-facet-grid.html\" = c\\(1L, 0L, 0L, 1L\\)|expect_equal\\(panel_counts, expected_panel_counts\\[\\[fixture_name\\]\\]\\)" tests/testthat/test-sf-browser.R</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "phase35-sf-facet-wrap.html\" = c\\(1L, 1L\\)" tests/testthat/test-sf-browser.R` returns a match.
    - `rtk rg -n "phase35-sf-facet-grid.html\" = c\\(1L, 0L, 0L, 1L\\)" tests/testthat/test-sf-browser.R` returns a match.
    - `rtk rg -n "expect_equal\\(panel_counts, expected_panel_counts\\[\\[fixture_name\\]\\]\\)" tests/testthat/test-sf-browser.R` returns a match.
    - `rtk Rscript --vanilla -e 'txt <- readLines("tests/testthat/test-sf-browser.R"); start <- grep("BRSF-02 DOM: faceted sf fixtures keep panel-local path counts", txt, fixed = TRUE); stopifnot(length(start) == 1L); block <- txt[start:min(length(txt), start + 60L)]; stopifnot(!any(grepl("sort\\\\(panel_counts|sort\\\\(expected_panel_counts", block))); cat("facet identity assertion ok\\n")'` exits 0.
    - `rtk rg -n "facet panel identity assertions.*(Resolved|Fixed)" .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md` returns a match.
  </acceptance_criteria>
  <done>Facet browser panel identity is proven by ordered panel-local assertions or fixed to do so.</done>
</task>

<task type="auto">
  <name>Task 4: Record advisory verification evidence</name>
  <files>.planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md</files>
  <read_first>
    - .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md
    - DESCRIPTION
    - tests/testthat/test-sf-browser.R
  </read_first>
  <action>Append a `### DEBT-01 evidence` subsection under `## Verification Evidence` in `41-DEBT-AUDIT.md`. Include the exact commands run for DESCRIPTION parsing, `pkgload`/`rprojroot` Suggests verification, ordered facet count scans, direct comparison scan, and no-sort block check. Record the outcome of each command as `passed` or `fixed then passed`. Ensure no DEBT-01 row remains `Pending verification`.</action>
  <verify>
    <automated>rtk rg -n "### DEBT-01 evidence|dependency advisory resolved|facet identity assertion ok|pkgload direct declaration.*(Resolved|Fixed)|rprojroot direct declaration.*(Resolved|Fixed)|facet panel identity assertions.*(Resolved|Fixed)" .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "### DEBT-01 evidence|dependency advisory resolved|facet identity assertion ok" .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md` returns matches.
    - `rtk Rscript --vanilla -e 'txt <- readLines(".planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md"); debt01 <- txt[grep("## DEBT-01 Advisory Follow-ups", txt):grep("## DEBT-02 Renderer and Documentation Debt", txt)]; stopifnot(!any(grepl("Pending verification", debt01))); cat("DEBT-01 audit statuses complete\\n")'` exits 0.
  </acceptance_criteria>
  <done>DEBT-01 advisory follow-ups have release-reviewable evidence and no pending statuses.</done>
</task>

</tasks>

<verification>
Run:

```bash
rtk Rscript --vanilla -e 'read.dcf("DESCRIPTION"); cat("DESCRIPTION parse ok\n")'
rtk Rscript --vanilla -e 'd <- read.dcf("DESCRIPTION"); suggests <- d[1, "Suggests"]; stopifnot(grepl("pkgload", suggests, fixed = TRUE), grepl("rprojroot", suggests, fixed = TRUE)); cat("dependency advisory resolved\n")'
rtk rg -n "phase35-sf-facet-wrap.html\" = c\\(1L, 1L\\)|phase35-sf-facet-grid.html\" = c\\(1L, 0L, 0L, 1L\\)|expect_equal\\(panel_counts, expected_panel_counts\\[\\[fixture_name\\]\\]\\)" tests/testthat/test-sf-browser.R
rtk rg -n "pkgload direct declaration.*(Resolved|Fixed)|rprojroot direct declaration.*(Resolved|Fixed)|facet panel identity assertions.*(Resolved|Fixed)|### DEBT-01 evidence" .planning/phases/41-release-blocking-debt-triage/41-DEBT-AUDIT.md
```
</verification>

<success_criteria>
- DEBT-01 is covered by `41-DEBT-AUDIT.md`.
- `pkgload` and `rprojroot` are present in DESCRIPTION Suggests or fixed there.
- Facet browser panel identity assertions compare ordered panel counts directly.
- No DEBT-01 item remains `Pending verification`.
</success_criteria>
