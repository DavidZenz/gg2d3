---
phase: 39-package-internals-hardening
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - R/as_d3_ir.R
  - R/ggplot2_compat.R
  - tests/testthat/test-ggplot2-compat.R
  - tests/testthat/test-layout.R
  - tests/testthat/test-legends.R
autonomous: true
requirements: [HARD-02]
user_setup: []
must_haves:
  truths:
    - "Private ggplot2 theme access is centralized behind internal compatibility helpers."
    - "Version-sensitive panel axis/range/label extraction is characterized by tests before future refactors rely on it."
    - "Theme, legend, date-scale, and coord_flip behavior remains unchanged."
  artifacts:
    - path: "R/ggplot2_compat.R"
      provides: "internal wrappers around ggplot2 private or version-sensitive access"
    - path: "tests/testthat/test-ggplot2-compat.R"
      provides: "characterization tests for wrapper behavior and fallbacks"
    - path: "R/as_d3_ir.R"
      provides: "call sites that use compatibility wrappers instead of raw private access"
  key_links:
    - from: "R/as_d3_ir.R"
      to: "R/ggplot2_compat.R"
      via: "gg2d3_plot_theme() and gg2d3_calc_element()"
      pattern: "gg2d3_calc_element"
---

<objective>
Centralize ggplot2 private and version-sensitive access behind compatibility helpers and characterize the current behavior that `as_d3_ir()` depends on.

Purpose: HARD-02 makes future ggplot2 upgrades and internals refactors safer without changing the current IR output.
Output: `R/ggplot2_compat.R`, targeted compatibility tests, and updated call sites in `R/as_d3_ir.R`.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-package-internals-hardening/39-RESEARCH.md
@.planning/phases/39-package-internals-hardening/39-PATTERNS.md
@R/as_d3_ir.R
@tests/testthat/test-layout.R
@tests/testthat/test-legends.R
@tests/testthat/test-date-scales.R
@tests/testthat/test-coord-flip.R
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Wrappers hide ggplot2 extraction failures and silently degrade IR | medium | Wrapper tests must assert explicit fallback values for missing/invalid inputs. |
| Replacing private calls changes theme or legend output | high | Run existing layout and legend tests after every wrapper call-site change. |
| Panel axis helper mishandles coord_flip/date axes | high | Run coord_flip and date-scale tests after panel-axis wrapper adoption. |
| New helper names are confused for public API | low | Keep helpers unexported and avoid roxygen export tags. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add ggplot2 compatibility characterization tests</name>
  <files>tests/testthat/test-ggplot2-compat.R</files>
  <read_first>
    - R/as_d3_ir.R
    - tests/testthat/test-layout.R
    - tests/testthat/test-legends.R
    - tests/testthat/test-date-scales.R
    - tests/testthat/test-coord-flip.R
  </read_first>
  <action>Create `tests/testthat/test-ggplot2-compat.R`. Add tests named `HARD-02 gg2d3_plot_theme resolves plot theme`, `HARD-02 gg2d3_calc_element returns fallback on missing element`, `HARD-02 gg2d3_panel_axis extracts displayed axes`, `HARD-02 gg2d3_continuous_range reads expanded range`, and `HARD-02 gg2d3_panel_labels returns character labels`. Use small deterministic ggplot2 plots: `ggplot(mtcars, aes(wt, mpg)) + geom_point() + theme(legend.position = "bottom")`, a discrete x-axis plot, a Date x-axis plot, and a `coord_flip()` plot. If the helpers do not exist yet, write the tests against the planned names and commit them with the implementation in Task 2 if needed.</action>
  <verify>
    <automated>rtk rg -n "HARD-02 gg2d3_plot_theme|HARD-02 gg2d3_calc_element|HARD-02 gg2d3_panel_axis|HARD-02 gg2d3_continuous_range|HARD-02 gg2d3_panel_labels" tests/testthat/test-ggplot2-compat.R</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "gg2d3_plot_theme|gg2d3_calc_element|gg2d3_panel_axis|gg2d3_continuous_range|gg2d3_panel_labels" tests/testthat/test-ggplot2-compat.R` returns matches.
    - `rtk rg -n "legend.position|coord_flip|Date|continuous_range|get_labels" tests/testthat/test-ggplot2-compat.R` returns matches.
  </acceptance_criteria>
  <done>Current ggplot2 internals assumptions are captured in a focused test file.</done>
</task>

<task type="auto">
  <name>Task 2: Implement compatibility helpers and replace private theme access</name>
  <files>R/ggplot2_compat.R, R/as_d3_ir.R, tests/testthat/test-ggplot2-compat.R</files>
  <read_first>
    - R/as_d3_ir.R
    - tests/testthat/test-ggplot2-compat.R
    - .planning/phases/39-package-internals-hardening/39-RESEARCH.md
  </read_first>
  <action>Create `R/ggplot2_compat.R` with unexported helpers: `gg2d3_plot_theme(plot)` wraps `ggplot2:::plot_theme(plot)` and returns `NULL` on error; `gg2d3_calc_element(element_name, theme, default = NULL)` wraps `ggplot2:::calc_element(element_name, theme)` and returns `default` on error or `NULL`; `gg2d3_panel_axis(panel_params, axis)` returns `panel_params[[axis]]` or `NULL`; `gg2d3_continuous_range(panel_params_axis)` returns `panel_params_axis$continuous_range`, else `panel_params_axis$range`, else `NULL`; `gg2d3_panel_labels(panel_params_axis)` calls `panel_params_axis$get_labels()`, drops `NA`, coerces to character, and returns `character(0)` on error. Replace direct `ggplot2:::plot_theme` and `ggplot2:::calc_element` calls in `R/as_d3_ir.R` with these helpers. Replace repeated panel-axis label extraction with `gg2d3_panel_labels()`. Keep behavior-equivalent defaults: legend position fallback `"right"`, panel spacing fallback `7.3`, legend key size fallback `23`.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-ggplot2-compat.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "ggplot2:::" R/as_d3_ir.R` returns no matches.
    - `rtk rg -n "gg2d3_plot_theme|gg2d3_calc_element|gg2d3_panel_axis|gg2d3_continuous_range|gg2d3_panel_labels" R/ggplot2_compat.R R/as_d3_ir.R` returns matches.
    - `rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-ggplot2-compat.R")'` exits 0.
  </acceptance_criteria>
  <done>Private ggplot2 access is centralized and covered by characterization tests.</done>
</task>

<task type="auto">
  <name>Task 3: Protect layout, legend, date, and coord_flip behavior after wrapper adoption</name>
  <files>R/as_d3_ir.R, tests/testthat/test-layout.R, tests/testthat/test-legends.R, tests/testthat/test-date-scales.R, tests/testthat/test-coord-flip.R</files>
  <read_first>
    - R/as_d3_ir.R
    - R/ggplot2_compat.R
    - tests/testthat/test-layout.R
    - tests/testthat/test-legends.R
    - tests/testthat/test-date-scales.R
    - tests/testthat/test-coord-flip.R
  </read_first>
  <action>Run the existing behavior suites that depend on theme, guide, panel params, date scale, and coord flip extraction. If any fail, fix the compatibility helper or call site to restore the previous IR behavior. Do not change JavaScript layout or legend modules in this plan unless an R-side wrapper bug reveals a direct mismatch in generated IR.</action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-layout.R"); testthat::test_file("tests/testthat/test-legends.R"); testthat::test_file("tests/testthat/test-date-scales.R"); testthat::test_file("tests/testthat/test-coord-flip.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-layout.R"); testthat::test_file("tests/testthat/test-legends.R"); testthat::test_file("tests/testthat/test-date-scales.R"); testthat::test_file("tests/testthat/test-coord-flip.R")'` exits 0.
    - `rtk rg -n "ggplot2:::" R` returns no matches unless accompanied by an inline comment explaining an unavoidable exported-alternative gap.
  </acceptance_criteria>
  <done>Compatibility wrapper adoption preserves current layout, legend, date, and coord_flip contracts.</done>
</task>

</tasks>

<verification>
Run:

```bash
rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-ggplot2-compat.R")'
rtk Rscript --vanilla -e 'testthat::test_file("tests/testthat/test-layout.R"); testthat::test_file("tests/testthat/test-legends.R"); testthat::test_file("tests/testthat/test-date-scales.R"); testthat::test_file("tests/testthat/test-coord-flip.R")'
```
</verification>

<success_criteria>
- HARD-02 is covered by compatibility helper tests and existing behavior suites.
- `R/as_d3_ir.R` no longer calls `ggplot2:::` directly.
- Theme, legend, date-scale, and coord_flip tests pass.
</success_criteria>
