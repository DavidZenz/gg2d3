---
phase: 33-single-panel-renderer-and-interactivity
plan: 02
type: execute
wave: 2
depends_on:
  - "33-01"
files_modified:
  - inst/htmlwidgets/modules/brush.js
  - tests/testthat/test-sf-interactivity.R
autonomous: true
requirements:
  - SFINTR-02
user_setup: []
must_haves:
  truths:
    - "brush selection includes path.geom-sf elements"
    - "sf brush hit testing uses projected centroid attrs data-cx/data-cy only"
    - "sf brush hit testing does not fall back to polygon bbox centers"
    - "brush callback data omits private sf renderer fields"
  artifacts:
    - path: "inst/htmlwidgets/modules/brush.js"
      provides: "centroid-only sf brush selection"
      contains: "path.geom-sf"
    - path: "tests/testthat/test-sf-interactivity.R"
      provides: "sf brush contract tests"
      contains: "data-cx"
  key_links:
    - from: "inst/htmlwidgets/modules/geoms/sf.js"
      to: "inst/htmlwidgets/modules/brush.js"
      via: "projected centroid DOM attrs"
      pattern: "data-cx/data-cy"
---

<objective>
Implement centroid-only brushing for sf polygons.

Purpose: Users should be able to brush `geom_sf` polygons through the existing brush API, but selection must follow the Phase 33 decision: centroid point-in-rect only, no polygon overlap and no bbox fallback.
Output: Updated brush selector/hit testing and sf brush contract tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-single-panel-renderer-and-interactivity/33-CONTEXT.md
@.planning/phases/33-single-panel-renderer-and-interactivity/33-RESEARCH.md
@.planning/phases/33-single-panel-renderer-and-interactivity/33-VALIDATION.md
@.planning/phases/33-single-panel-renderer-and-interactivity/33-PATTERNS.md
@inst/htmlwidgets/modules/brush.js
@inst/htmlwidgets/modules/geoms/sf.js
@tests/testthat/test-sf-interactivity.R
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-33-03: Sf brush selection uses polygon bbox center instead of centroid | high | Add an explicit `path.geom-sf` branch before generic path handling and read only `data-cx`/`data-cy`. |
| T-33-02: Brush callbacks leak `_geom` and `_centroid` | medium | Sanitize selected data rows before callback/shiny payload construction. |
| T-33-06: Non-sf path brushing regresses | medium | Preserve existing generic path bbox logic for non-sf paths and add tests that source still contains non-sf path handling. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Include sf paths in brush targets</name>
  <files>inst/htmlwidgets/modules/brush.js, tests/testthat/test-sf-interactivity.R</files>
  <read_first>inst/htmlwidgets/modules/brush.js, inst/htmlwidgets/modules/events.js, .planning/phases/33-single-panel-renderer-and-interactivity/33-RESEARCH.md</read_first>
  <action>Add `path.geom-sf` to the brush module's `INTERACTIVE_SELECTORS` list. Keep existing selectors unchanged. Extend `tests/testthat/test-sf-interactivity.R` to assert `brush.js` contains `path.geom-sf` and still contains representative existing selectors such as `circle.geom-point`, `rect.geom-bar`, and `path.geom-line`.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-interactivity.R")'</verify>
  <acceptance_criteria>
    - `inst/htmlwidgets/modules/brush.js` contains `path.geom-sf`
    - `tests/testthat/test-sf-interactivity.R` contains `brush.js`
    - `tests/testthat/test-sf-interactivity.R` contains `path.geom-sf`
  </acceptance_criteria>
  <done>Brush attachment targets sf path elements.</done>
</task>

<task type="auto">
  <name>Task 2: Use centroid attrs for sf brush hit testing</name>
  <files>inst/htmlwidgets/modules/brush.js, tests/testthat/test-sf-interactivity.R</files>
  <read_first>inst/htmlwidgets/modules/brush.js, inst/htmlwidgets/modules/geoms/sf.js</read_first>
  <action>Update `isElementInPixelRect(el, pixelRect)` so `path.geom-sf` is handled before the generic path branch. Parse `data-cx` and `data-cy` with `parseFloat`. If either value is not finite, return `false`. Otherwise return whether that centroid point lies inside `pixelRect`. Do not use `getBBox()` for sf paths and do not add polygon-overlap logic. Add tests that assert `brush.js` reads `data-cx` and `data-cy`, uses a `geom-sf` branch, and keeps `getBBox()` outside the sf branch for other path geoms.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-interactivity.R")'</verify>
  <acceptance_criteria>
    - `inst/htmlwidgets/modules/brush.js` contains `data-cx`
    - `inst/htmlwidgets/modules/brush.js` contains `data-cy`
    - `inst/htmlwidgets/modules/brush.js` contains `Number.isFinite` or an equivalent finite check
    - `inst/htmlwidgets/modules/brush.js` keeps `getBBox` for non-sf path handling
    - `tests/testthat/test-sf-interactivity.R` contains `data-cx`
    - `tests/testthat/test-sf-interactivity.R` contains `data-cy`
  </acceptance_criteria>
  <done>Sf brush hit testing uses projected centroid attributes only.</done>
</task>

<task type="auto">
  <name>Task 3: Sanitize sf rows in brush callback payloads</name>
  <files>inst/htmlwidgets/modules/brush.js, tests/testthat/test-sf-interactivity.R</files>
  <read_first>inst/htmlwidgets/modules/brush.js, inst/htmlwidgets/modules/tooltip.js</read_first>
  <action>Add a small selected-data sanitization helper in `brush.js` that clones row objects and removes keys beginning with `_`. Use it in `collectSelectedData()` before returning data to `on_brush` callbacks or Shiny payloads. Preserve non-object datum behavior. Add source-contract tests proving `_geom` and `_centroid` are filtered by brush code.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-interactivity.R")'</verify>
  <acceptance_criteria>
    - `inst/htmlwidgets/modules/brush.js` contains a helper whose name includes `sanitize`
    - `inst/htmlwidgets/modules/brush.js` filters keys with `startsWith('_')`
    - `inst/htmlwidgets/modules/brush.js` applies sanitization in `collectSelectedData`
    - `tests/testthat/test-sf-interactivity.R` contains `_geom`
    - `tests/testthat/test-sf-interactivity.R` contains `_centroid`
  </acceptance_criteria>
  <done>Brush callbacks return user-facing row data without sf renderer private fields.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-interactivity.R")'`
- [ ] Confirm `path.geom-sf` brush hit testing reads `data-cx`/`data-cy` before any generic path bbox logic.
- [ ] Confirm no polygon-overlap or bbox fallback was added for sf.
</verification>

<success_criteria>
- Brush targets include `path.geom-sf`.
- Sf polygons are selected only by projected centroids.
- Missing/non-finite centroids are not selected.
- Brush callback rows do not expose `_geom` or `_centroid`.
- Existing non-sf brush selectors and path bbox behavior remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/33-single-panel-renderer-and-interactivity/33-02-SUMMARY.md`
</output>
