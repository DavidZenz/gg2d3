---
phase: 33-single-panel-renderer-and-interactivity
plan: 03
type: execute
wave: 3
depends_on:
  - "33-01"
  - "33-02"
files_modified:
  - R/d3_zoom.R
  - tests/testthat/test-zoom-brush.R
  - tests/testthat/test-sf-interactivity.R
autonomous: true
requirements:
  - SFINTR-03
user_setup: []
must_haves:
  truths:
    - "d3_zoom() detects widgets containing geom_sf layers"
    - "d3_zoom() warns and suppresses zoom config for sf widgets"
    - "d3_zoom() preserves existing non-sf behavior"
    - "brush, tooltip, and hover remain composable on sf widgets when zoom is suppressed"
  artifacts:
    - path: "R/d3_zoom.R"
      provides: "sf zoom suppression guard"
      contains: "geom_sf"
    - path: "tests/testthat/test-zoom-brush.R"
      provides: "R API regression tests for zoom suppression"
      contains: "geom_sf"
    - path: "tests/testthat/test-sf-interactivity.R"
      provides: "sf interactivity composition contract"
      contains: "d3_zoom"
  key_links:
    - from: "R/d3_zoom.R"
      to: "inst/htmlwidgets/modules/zoom.js"
      via: "guard prevents Cartesian zoom JS from attaching to sf widgets"
      pattern: "geom == \"sf\""
---

<objective>
Suppress Cartesian zoom for widgets containing `geom_sf` layers.

Purpose: Existing zoom code is Cartesian and would misrepresent projected sf paths. Phase 33 should make this behavior explicit: warn users and leave zoom disabled for sf widgets.
Output: Updated `d3_zoom()` guard and regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-single-panel-renderer-and-interactivity/33-CONTEXT.md
@.planning/phases/33-single-panel-renderer-and-interactivity/33-RESEARCH.md
@.planning/phases/33-single-panel-renderer-and-interactivity/33-VALIDATION.md
@.planning/phases/33-single-panel-renderer-and-interactivity/33-PATTERNS.md
@R/d3_zoom.R
@inst/htmlwidgets/modules/zoom.js
@tests/testthat/test-zoom-brush.R
@tests/testthat/test-sf-interactivity.R
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-33-04: Cartesian zoom transforms sf widgets into misleading maps | high | Detect sf layers in `d3_zoom()` and return before storing zoom config or attaching JS. |
| T-33-07: Suppression breaks normal point/line/bar zoom | high | Preserve validation and config path for widgets without sf layers; keep existing tests green. |
| T-33-08: Users do not understand why zoom is absent | medium | Emit a warning mentioning `geom_sf` and `zoom`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add sf detection helper to d3_zoom()</name>
  <files>R/d3_zoom.R</files>
  <read_first>R/d3_zoom.R, R/gg2d3.R, .planning/phases/33-single-panel-renderer-and-interactivity/33-RESEARCH.md</read_first>
  <action>In `d3_zoom()`, after existing input and parameter validation but before mutating `widget$x$interactivity`, detect whether `widget$x$ir$layers` contains any layer with `geom` identical to `"sf"`. Use a small local helper or internal function if that keeps the code readable. The guard must be robust to missing `x`, `ir`, or `layers` fields after the initial class validation. Keep non-sf behavior exactly as-is.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-zoom-brush.R")'</verify>
  <acceptance_criteria>
    - `R/d3_zoom.R` contains `geom`
    - `R/d3_zoom.R` contains `sf`
    - `R/d3_zoom.R` detects sf before `widget$x$interactivity$zoom <-`
  </acceptance_criteria>
  <done>`d3_zoom()` can reliably detect sf widgets before attaching zoom behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Warn and suppress zoom config for sf widgets</name>
  <files>R/d3_zoom.R, tests/testthat/test-zoom-brush.R</files>
  <read_first>R/d3_zoom.R, tests/testthat/test-zoom-brush.R, tests/testthat/test-sf-ir.R</read_first>
  <action>When the sf guard detects any sf layer, call `warning(..., call. = FALSE)` with a message containing both `geom_sf` and `zoom`, then return the original widget without setting `widget$x$interactivity$zoom` and without adding the zoom `onRender` callback. Add a test using an available sf fixture or a small `sf` object that calls `gg2d3(p) |> d3_zoom()`, expects the warning, checks the object remains a `gg2d3`/`htmlwidget`, and checks zoom config is absent. Use `skip_if_not_installed("sf")` for sf-dependent tests.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-zoom-brush.R")'</verify>
  <acceptance_criteria>
    - `R/d3_zoom.R` contains `warning(`
    - `R/d3_zoom.R` warning message contains `geom_sf`
    - `R/d3_zoom.R` warning message contains `zoom`
    - `tests/testthat/test-zoom-brush.R` contains `geom_sf`
    - `tests/testthat/test-zoom-brush.R` contains `expect_warning`
    - `tests/testthat/test-zoom-brush.R` asserts zoom config is absent for sf widgets
  </acceptance_criteria>
  <done>Sf widgets warn and return without zoom enabled.</done>
</task>

<task type="auto">
  <name>Task 3: Protect non-sf zoom and sf interactivity composition</name>
  <files>tests/testthat/test-zoom-brush.R, tests/testthat/test-sf-interactivity.R</files>
  <read_first>tests/testthat/test-zoom-brush.R, tests/testthat/test-sf-interactivity.R, R/d3_zoom.R</read_first>
  <action>Ensure existing point-plot zoom tests still assert enabled config, custom scale extents, and direction handling. Add an sf composition test showing `gg2d3(sf_plot) |> d3_brush() |> d3_tooltip() |> d3_hover()` remains configured, and adding `d3_zoom()` to an sf widget warns without removing other interactivity configs. Keep this test in a targeted sf file if needed to avoid unrelated existing hover-default expectations.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-zoom-brush.R"); testthat::test_file("tests/testthat/test-sf-interactivity.R")'</verify>
  <acceptance_criteria>
    - `tests/testthat/test-zoom-brush.R` still contains non-sf `d3_zoom()` enabled assertions
    - `tests/testthat/test-sf-interactivity.R` contains `d3_brush`
    - `tests/testthat/test-sf-interactivity.R` contains `d3_tooltip`
    - `tests/testthat/test-sf-interactivity.R` contains `d3_hover`
    - `tests/testthat/test-sf-interactivity.R` contains `d3_zoom`
  </acceptance_criteria>
  <done>Zoom suppression does not regress normal zoom or other sf interactivity APIs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-zoom-brush.R")'`
- [ ] `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-interactivity.R")'`
- [ ] Confirm `R/d3_zoom.R` returns before attaching onRender for sf widgets.
</verification>

<success_criteria>
- `d3_zoom()` warns on widgets containing `geom_sf`.
- Sf widgets do not receive zoom config or zoom JS callbacks.
- Existing non-sf zoom tests still pass.
- Brush, tooltip, and hover remain configurable on sf widgets.
</success_criteria>

<output>
After completion, create `.planning/phases/33-single-panel-renderer-and-interactivity/33-03-SUMMARY.md`
</output>
