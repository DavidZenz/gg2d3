---
phase: 34-stacked-and-faceted-projection-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/as_d3_ir.R
  - R/sf_utils.R
  - R/validate_ir.R
  - tests/testthat/test-sf-ir.R
autonomous: true
requirements:
  - SFREND-02
  - SFREND-03
user_setup: []
must_haves:
  truths:
    - "accepted sf geometries are grouped by PANEL before projection metadata is computed"
    - "each sf panel gets an sf_bbox numeric vector c(xmin, ymin, xmax, ymax) or NULL when no accepted sf features exist"
    - "stacked sf layers in one panel contribute to the same panel sf_bbox"
    - "coord$bbox remains the all-panel/all-layer union for backward compatibility"
  artifacts:
    - path: "R/as_d3_ir.R"
      provides: "panel-keyed sf bbox metadata in IR"
      contains: "sf_bbox"
    - path: "R/sf_utils.R"
      provides: "small bbox helper for accepted sf geometries"
      contains: "st_bbox"
    - path: "R/validate_ir.R"
      provides: "sf_bbox shape validation"
      contains: "sf_bbox"
    - path: "tests/testthat/test-sf-ir.R"
      provides: "stacked sf bbox contract tests"
      contains: "sf_bbox"
  key_links:
    - from: "R/sf_utils.R"
      to: "R/as_d3_ir.R"
      via: "accepted sf geometry bbox helper"
      pattern: "sf_bbox"
    - from: "R/as_d3_ir.R"
      to: "inst/htmlwidgets/gg2d3.js"
      via: "ir.panels[[i]]$sf_bbox"
      pattern: "PANEL"
---

<objective>
Add R-side panel-level sf projection metadata.

Purpose: Phase 34 needs a shared projection source before JavaScript can render stacked and faceted sf layers correctly. This plan makes the IR explicit: each panel owns `sf_bbox`, computed only from accepted sf polygon-family geometries in that panel.
Output: Updated sf helpers, IR assembly, validation, and R tests for stacked single-panel metadata.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-stacked-and-faceted-projection-alignment/34-CONTEXT.md
@.planning/phases/34-stacked-and-faceted-projection-alignment/34-RESEARCH.md
@.planning/phases/34-stacked-and-faceted-projection-alignment/34-VALIDATION.md
@.planning/phases/34-stacked-and-faceted-projection-alignment/34-PATTERNS.md
@R/as_d3_ir.R
@R/sf_utils.R
@R/validate_ir.R
@tests/testthat/test-sf-ir.R
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-34-01: Stacked sf layers still compute layer-local bboxes | high | Group accepted sf geometries across all sf layers by `PANEL` and write one `sf_bbox` per panel. |
| T-34-02: Empty facet panels silently use a global bbox | high | Store `sf_bbox = NULL` when a panel has no accepted sf geometries and test missing-combination behavior in later plans. |
| T-34-07: sf validation reintroduces Cartesian range requirements | medium | Keep existing coord sf exemption for `x_range`/`y_range`; validate only `sf_bbox` shape when present. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add an sf bbox helper for accepted geometries</name>
  <files>R/sf_utils.R, tests/testthat/test-sf-ir.R</files>
  <read_first>R/sf_utils.R, tests/testthat/test-sf-ir.R, .planning/phases/34-stacked-and-faceted-projection-alignment/34-RESEARCH.md</read_first>
  <action>Add an internal helper in `R/sf_utils.R` named `sf_bbox_values` or similarly clear. It must accept an `sfc` object or `NULL`, return `NULL` when the geometry input is `NULL` or length 0, and otherwise return `unname(as.numeric(sf::st_bbox(geom)))`. Do not warn for empty inputs. Add/extend tests in `tests/testthat/test-sf-ir.R` using `skip_if_not_installed("sf")` and `skip_if_not_installed("geojsonsf")` that exercise the helper indirectly through `as_d3_ir()` rather than exporting it.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-ir.R")'</verify>
  <acceptance_criteria>
    - `R/sf_utils.R` contains `sf_bbox`
    - `R/sf_utils.R` contains `st_bbox`
    - `R/sf_utils.R` returns `NULL` for zero-length geometry input
    - `tests/testthat/test-sf-ir.R` contains `sf_bbox`
    - `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-ir.R")'` exits 0 or skips only because optional spatial packages are unavailable
  </acceptance_criteria>
  <done>Reusable sf bbox calculation exists and is covered through IR tests.</done>
</task>

<task type="auto">
  <name>Task 2: Build panel-keyed sf bbox metadata in as_d3_ir()</name>
  <files>R/as_d3_ir.R, tests/testthat/test-sf-ir.R</files>
  <read_first>R/as_d3_ir.R, R/sf_utils.R, tests/testthat/test-sf-ir.R, .planning/phases/34-stacked-and-faceted-projection-alignment/34-CONTEXT.md</read_first>
  <action>In `as_d3_ir()`, replace the layer-only `sf_coord_geometries` collection with two related collections: one all-accepted-geometry list for preserving `coord$bbox`, and one panel-keyed collection for per-panel bboxes. When `gname == "sf"` and `sf_prepared$geometry` has accepted rows, read the corresponding `PANEL` values from `sf_prepared$data$PANEL` (default to `1L` if absent), append each accepted geometry to that panel's bucket, and keep all accepted geometries in the global bucket. After `panels_ir` is constructed, if `is_sf_coord` is true, add `sf_bbox = <numeric bbox>` to each panel entry when that panel has accepted sf geometries, or `sf_bbox = NULL` when it does not. Keep `coord$bbox` as the union across all accepted sf geometries. Do not include non-sf layer rows in any sf bbox bucket.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-ir.R")'</verify>
  <acceptance_criteria>
    - `R/as_d3_ir.R` contains `sf_bbox`
    - `R/as_d3_ir.R` groups sf geometry by `PANEL`
    - `R/as_d3_ir.R` still writes `coord` with `bbox`
    - `tests/testthat/test-sf-ir.R` asserts `ir$panels[[1]]$sf_bbox`
    - `tests/testthat/test-sf-ir.R` includes a stacked two-layer sf fixture whose panel `sf_bbox` spans both layers
  </acceptance_criteria>
  <done>IR panels expose shared sf bbox metadata computed from accepted sf layers.</done>
</task>

<task type="auto">
  <name>Task 3: Validate the sf_bbox IR contract</name>
  <files>R/validate_ir.R, tests/testthat/test-sf-ir.R</files>
  <read_first>R/validate_ir.R, R/as_d3_ir.R, tests/testthat/test-sf-ir.R</read_first>
  <action>Extend `validate_ir()` panel validation so when `coord$type == "sf"` and a panel has non-NULL `sf_bbox`, it must be numeric with length exactly 4 and all finite values. Emit a warning for malformed `sf_bbox`; do not stop unless existing validation already stops. Preserve the existing behavior where sf panels may have `x_range = NULL` and `y_range = NULL`. Add tests that mutate a valid sf IR to set malformed `sf_bbox` values and expect a warning containing `sf_bbox`.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-ir.R")'</verify>
  <acceptance_criteria>
    - `R/validate_ir.R` contains `sf_bbox`
    - `R/validate_ir.R` warns when `sf_bbox` length is not 4
    - `R/validate_ir.R` warns when `sf_bbox` contains non-finite values
    - `tests/testthat/test-sf-ir.R` contains `expect_warning`
    - `tests/testthat/test-sf-ir.R` contains `sf_bbox`
  </acceptance_criteria>
  <done>Malformed sf panel bbox metadata is detectable without breaking valid sf panels.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-ir.R")'`
- [ ] Confirm `ir$panels[[i]]$sf_bbox` is present for sf panels with accepted features.
- [ ] Confirm `coord$bbox` remains present for sf plots with accepted features.
- [ ] Confirm non-sf panels and non-sf facet tests are not changed by the new field.
</verification>

<success_criteria>
- Multiple sf layers in one panel contribute to one shared panel `sf_bbox`.
- Faceted sf IR has panel-keyed `sf_bbox` metadata.
- Empty sf panels represent absence with `sf_bbox = NULL`, not a global fallback.
- Valid sf IR passes `validate_ir()` without warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/34-stacked-and-faceted-projection-alignment/34-01-SUMMARY.md`
</output>
