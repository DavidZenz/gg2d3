---
phase: 34-stacked-and-faceted-projection-alignment
plan: 03
type: execute
wave: 3
depends_on:
  - "34-01"
  - "34-02"
files_modified:
  - tests/testthat/test-facets.R
  - tests/testthat/test-facet-grid.R
  - tests/testthat/test-sf-ir.R
  - tests/testthat/test-sf-visual.R
autonomous: true
requirements:
  - SFREND-02
  - SFREND-03
user_setup: []
must_haves:
  truths:
    - "facet_wrap sf panels expose per-panel sf_bbox values from only their own PANEL rows"
    - "facet_grid sf panels preserve layout while using per-panel sf_bbox metadata"
    - "empty sf facet panels use NULL sf_bbox instead of global fallback"
    - "stacked and faceted sf targeted tests pass together"
  artifacts:
    - path: "tests/testthat/test-facets.R"
      provides: "facet_wrap sf bbox isolation tests"
      contains: "geom_sf"
    - path: "tests/testthat/test-facet-grid.R"
      provides: "facet_grid sf bbox/layout tests"
      contains: "sf_bbox"
    - path: "tests/testthat/test-sf-ir.R"
      provides: "shared sf fixture helpers or metadata assertions"
      contains: "sf_bbox"
    - path: "tests/testthat/test-sf-visual.R"
      provides: "optional visual fixture notes or generated examples"
      contains: "geom_sf"
  key_links:
    - from: "tests/testthat/test-facets.R"
      to: "R/as_d3_ir.R"
      via: "facet_wrap PANEL-specific sf_bbox"
      pattern: "facet_wrap"
    - from: "tests/testthat/test-facet-grid.R"
      to: "R/as_d3_ir.R"
      via: "facet_grid PANEL-specific sf_bbox"
      pattern: "facet_grid"
---

<objective>
Lock Phase 34 behavior with stacked and faceted sf regression fixtures.

Purpose: The implementation needs tests that fail for independent layer fitting, cross-panel bbox leakage, and empty-panel global fallback. This plan adds those fixtures and runs the targeted Phase 34 suite together.
Output: Focused sf facet/stacked tests and optional visual fixture coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-stacked-and-faceted-projection-alignment/34-CONTEXT.md
@.planning/phases/34-stacked-and-faceted-projection-alignment/34-RESEARCH.md
@.planning/phases/34-stacked-and-faceted-projection-alignment/34-VALIDATION.md
@.planning/phases/34-stacked-and-faceted-projection-alignment/34-PATTERNS.md
@tests/testthat/test-facets.R
@tests/testthat/test-facet-grid.R
@tests/testthat/test-sf-ir.R
@tests/testthat/test-sf-visual.R
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-34-05: `facet_wrap()` sf panels use global bbox metadata | high | Add far-apart panel fixture and assert each panel `sf_bbox` matches only its own polygon extent. |
| T-34-06: `facet_grid()` loses layout or leaks bbox into missing panels | high | Add grid fixture with a missing combination and assert panel count/layout plus NULL `sf_bbox` for the empty panel. |
| T-34-09: Tests pass in source contract but fail in combined targeted suite | medium | Run all Phase 34 targeted test files in one command before summary. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add facet_wrap sf panel isolation tests</name>
  <files>tests/testthat/test-facets.R</files>
  <read_first>tests/testthat/test-facets.R, tests/testthat/test-sf-ir.R, R/as_d3_ir.R</read_first>
  <action>Add sf-dependent tests to `tests/testthat/test-facets.R` guarded by `skip_if_not_installed("sf")` and `skip_if_not_installed("geojsonsf")`. Build a small `sf::st_sf()` fixture with two facet values and far-apart polygon squares, for example panel A spanning `0..1` and panel B spanning `100..101`. Render `ggplot2::ggplot(sf_data) + ggplot2::geom_sf() + ggplot2::facet_wrap(~facet)`. Assert `ir$facets$type == "wrap"`, `length(ir$panels) == 2`, each panel has non-NULL `sf_bbox`, panel 1 bbox max x is less than 10, panel 2 bbox min x is greater than 90, and the two panel bboxes are not identical. Keep existing non-sf facet tests unchanged.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-facets.R")'</verify>
  <acceptance_criteria>
    - `tests/testthat/test-facets.R` contains `geom_sf`
    - `tests/testthat/test-facets.R` contains `facet_wrap`
    - `tests/testthat/test-facets.R` contains `sf_bbox`
    - `tests/testthat/test-facets.R` contains `skip_if_not_installed("sf")`
    - `tests/testthat/test-facets.R` asserts far-apart panel bbox values
  </acceptance_criteria>
  <done>`facet_wrap()` sf panels are covered for per-panel bbox isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Add facet_grid sf layout and empty-panel tests</name>
  <files>tests/testthat/test-facet-grid.R</files>
  <read_first>tests/testthat/test-facet-grid.R, tests/testthat/test-facets.R, R/as_d3_ir.R</read_first>
  <action>Add sf-dependent tests to `tests/testthat/test-facet-grid.R` guarded by `skip_if_not_installed("sf")` and `skip_if_not_installed("geojsonsf")`. Build a small sf fixture with row and column facet variables where one row/column combination is missing. Render `ggplot2::ggplot(sf_data) + ggplot2::geom_sf() + ggplot2::facet_grid(row_var ~ col_var)`. Assert `ir$facets$type == "grid"`, `length(ir$panels)` equals the full grid size, non-empty panels have panel-specific `sf_bbox` values from their own far-apart geometries, and the missing panel has `is.null(panel$sf_bbox)`. Also assert `ROW`, `COL`, and `PANEL` remain integer in `ir$facets$layout`.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-facet-grid.R")'</verify>
  <acceptance_criteria>
    - `tests/testthat/test-facet-grid.R` contains `geom_sf`
    - `tests/testthat/test-facet-grid.R` contains `facet_grid`
    - `tests/testthat/test-facet-grid.R` contains `sf_bbox`
    - `tests/testthat/test-facet-grid.R` contains `is.null`
    - `tests/testthat/test-facet-grid.R` asserts full grid panel count despite a missing combination
  </acceptance_criteria>
  <done>`facet_grid()` sf panels are covered for layout preservation, bbox isolation, and empty panels.</done>
</task>

<task type="auto">
  <name>Task 3: Run and tighten the targeted Phase 34 suite</name>
  <files>tests/testthat/test-sf-ir.R, tests/testthat/test-sf-renderer.R, tests/testthat/test-facets.R, tests/testthat/test-facet-grid.R, tests/testthat/test-sf-visual.R</files>
  <read_first>tests/testthat/test-sf-ir.R, tests/testthat/test-sf-renderer.R, tests/testthat/test-facets.R, tests/testthat/test-facet-grid.R, tests/testthat/test-sf-visual.R, .planning/phases/34-stacked-and-faceted-projection-alignment/34-VALIDATION.md</read_first>
  <action>Run the full Phase 34 targeted suite. If failures show brittle helper duplication, extract a tiny local fixture helper inside the relevant test file rather than adding a package-level test helper. If `tests/testthat/test-sf-visual.R` can cheaply include or reference stacked/faceted fixtures without browser automation, add optional fixture coverage guarded by spatial package skips; otherwise leave visual browser hardening for Phase 35 and document that in the summary. Do not loosen any assertions that prove per-panel bbox isolation, data/geometry pair filtering, or empty-panel NULL behavior.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-ir.R"); testthat::test_file("tests/testthat/test-sf-renderer.R"); testthat::test_file("tests/testthat/test-facets.R"); testthat::test_file("tests/testthat/test-facet-grid.R")'</verify>
  <acceptance_criteria>
    - `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-ir.R"); testthat::test_file("tests/testthat/test-sf-renderer.R"); testthat::test_file("tests/testthat/test-facets.R"); testthat::test_file("tests/testthat/test-facet-grid.R")'` exits 0 or skips only because optional spatial packages are unavailable
    - `tests/testthat/test-sf-ir.R` contains `sf_bbox`
    - `tests/testthat/test-sf-renderer.R` contains `sfBBox`
    - `tests/testthat/test-facets.R` contains `sf_bbox`
    - `tests/testthat/test-facet-grid.R` contains `sf_bbox`
  </acceptance_criteria>
  <done>Phase 34 targeted tests pass together and cover the planned projection alignment behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-ir.R"); testthat::test_file("tests/testthat/test-sf-renderer.R"); testthat::test_file("tests/testthat/test-facets.R"); testthat::test_file("tests/testthat/test-facet-grid.R")'`
- [ ] Confirm `facet_wrap()` sf tests distinguish per-panel bboxes numerically.
- [ ] Confirm `facet_grid()` sf tests cover missing panel `sf_bbox = NULL`.
- [ ] Confirm no global-comparison projection mode or browser reprojection was added.
</verification>

<success_criteria>
- `facet_wrap()` sf maps filter rows by `PANEL` and fit each panel using that panel's sf features.
- `facet_grid()` sf maps preserve layout and use per-panel bbox metadata without cross-panel leakage.
- Empty sf panels do not receive a global bbox fallback.
- The targeted Phase 34 suite passes together.
</success_criteria>

<output>
After completion, create `.planning/phases/34-stacked-and-faceted-projection-alignment/34-03-SUMMARY.md`
</output>
