---
phase: 32-geom-sf-ir-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "32-01"
files_modified:
  - R/as_d3_ir.R
  - R/validate_ir.R
  - tests/testthat/test-sf-ir.R
autonomous: true
requirements:
  - SFIR-01
  - SFIR-02
  - SFIR-03
user_setup: []
must_haves:
  truths:
    - "as_d3_ir() uses the hardened sf helper for geom_sf layers"
    - "geom_sf IR contains only accepted polygon-family rows and matching GeoJSON geometries"
    - "geom_sf IR exposes sf diagnostics for accepted/skipped rows and CRS handling"
    - "coord sf bbox remains present for accepted sf geometry and no JavaScript reprojection metadata is introduced"
  artifacts:
    - path: "R/as_d3_ir.R"
      provides: "production geom_sf IR integration"
      contains: "sf_diagnostics"
    - path: "R/validate_ir.R"
      provides: "sf IR structural validation"
      contains: "geometries"
    - path: "tests/testthat/test-sf-ir.R"
      provides: "IR integration tests for filtered sf layers and diagnostics"
      contains: "sf_diagnostics"
  key_links:
    - from: "R/as_d3_ir.R"
      to: "R/sf_utils.R"
      via: "prepare_sf_geometry_ir helper call"
      pattern: "prepare_sf_geometry_ir"
    - from: "R/as_d3_ir.R"
      to: "tests/testthat/test-sf-ir.R"
      via: "as_d3_ir geom_sf tests"
      pattern: "as_d3_ir\\(.*geom_sf"
---

<objective>
Integrate the hardened sf helper into `as_d3_ir()` and validate the production IR shape for Phase 32.

Purpose: The package needs stable `geom_sf` IR that filters unsupported geometry predictably, preserves row/geometry alignment, exposes diagnostics, and keeps bbox/CRS metadata in the R layer.
Output: Updated `GeomSf` branch, sf validation checks, and IR integration tests for SFIR-01 through SFIR-03.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-geom-sf-ir-foundation/32-CONTEXT.md
@.planning/phases/32-geom-sf-ir-foundation/32-RESEARCH.md
@.planning/phases/32-geom-sf-ir-foundation/32-VALIDATION.md
@.planning/phases/32-geom-sf-ir-foundation/32-PATTERNS.md
@R/as_d3_ir.R
@R/validate_ir.R
@R/sf_utils.R
@tests/testthat/test-sf-ir.R
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| IR data rows and GeoJSON geometries diverge after unsupported rows are filtered | high | `as_d3_ir()` must use helper-returned filtered data and geometries from the same result object, and tests must assert equal lengths and source `row_id`. |
| Layer diagnostics are omitted, making skipped data invisible to later phases and users | medium | The sf layer object must include `sf_diagnostics`, and validation/tests must check the field exists. |
| Phase 32 accidentally implements Phase 34 projection semantics or JavaScript reprojection | medium | Keep metadata limited to bbox/CRS/diagnostics and avoid D3 or panel-projection renderer changes. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wire prepared sf output into as_d3_ir</name>
  <files>R/as_d3_ir.R</files>
  <read_first>R/as_d3_ir.R, R/sf_utils.R, .planning/phases/32-geom-sf-ir-foundation/32-CONTEXT.md, .planning/phases/32-geom-sf-ir-foundation/32-RESEARCH.md</read_first>
  <action>Update the `if (gname == "sf")` branch in `R/as_d3_ir.R` to call `sf_prepared <- prepare_sf_geometry_ir(df)` from Plan 01. Use the returned filtered `data` and `geometries` from the same helper result. The layer object must include `geom = "sf"`, `geom_type = sf_prepared$geom_type`, `geometries = sf_prepared$geometries`, `data = to_rows(sf_prepared$data)`, `aes = aes`, `params = g_params`, `crs = sf_prepared$crs`, `sf_diagnostics = sf_prepared$sf_diagnostics`, and `var_names = var_names`. Do not set `row_id <- seq_along(...)` in this branch anymore; row ids must come from the helper and preserve original source row numbers. Preserve existing non-sf branch behavior.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-ir.R")'</verify>
  <acceptance_criteria>
    - `R/as_d3_ir.R` contains `prepare_sf_geometry_ir`
    - `R/as_d3_ir.R` contains `sf_diagnostics`
    - `R/as_d3_ir.R` does not contain `row_id"]] <- seq_along(sf_geom_strings)` in the sf branch
    - `R/as_d3_ir.R` contains `sf_prepared <- prepare_sf_geometry_ir(df)`
    - `R/as_d3_ir.R` contains `geometries = sf_prepared$geometries`
    - `R/as_d3_ir.R` contains `data       = to_rows(sf_prepared$data)`
  </acceptance_criteria>
  <done>`as_d3_ir()` consumes one prepared sf helper result for filtered data, geometries, CRS, geom type, and diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Validate sf IR structure</name>
  <files>R/validate_ir.R</files>
  <read_first>R/validate_ir.R, R/as_d3_ir.R, .planning/phases/32-geom-sf-ir-foundation/32-CONTEXT.md</read_first>
  <action>Extend `validate_ir()` so sf layers receive structural checks without changing non-sf validation behavior. For layers where `layer$geom == "sf"`, check that `layer$geometries` is a character vector, `length(layer$geometries) == length(layer$data)`, and `layer$sf_diagnostics` is present with at least `accepted_rows` and `skipped_rows`. If these required pieces are missing or inconsistent, stop with messages containing `"sf layer"` and the failing field name. Keep the existing behavior that sf panels may have `NULL` `x_range`/`y_range`.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-ir.R")'</verify>
  <acceptance_criteria>
    - `R/validate_ir.R` contains `layer$geom == "sf"` or an equivalent sf-layer condition
    - `R/validate_ir.R` contains `sf_diagnostics`
    - `R/validate_ir.R` contains `accepted_rows`
    - `R/validate_ir.R` contains `skipped_rows`
    - `R/validate_ir.R` contains `length(layer$geometries) == length(layer$data)` or an equivalent length check
  </acceptance_criteria>
  <done>`validate_ir()` catches malformed sf layer structure and still accepts valid sf IR.</done>
</task>

<task type="auto">
  <name>Task 3: Add IR integration tests for filtered sf behavior</name>
  <files>tests/testthat/test-sf-ir.R</files>
  <read_first>tests/testthat/test-sf-ir.R, tests/testthat/test-sf-utils.R, R/as_d3_ir.R, R/validate_ir.R</read_first>
  <action>Extend `tests/testthat/test-sf-ir.R` with integration tests proving SFIR-01, SFIR-02, and SFIR-03. Add tests that build a small `sf` object with mixed polygon and unsupported geometry rows, pass it through `ggplot2::ggplot(...)+ggplot2::geom_sf()` and `as_d3_ir()`, and assert: (1) `ir$layers[[1]]$geom == "sf"`; (2) `length(layer$data) == length(layer$geometries)`; (3) retained `row_id` values preserve original source row numbers after unsupported rows are skipped; (4) `layer$sf_diagnostics$skipped_rows` records unsupported rows; (5) `layer$sf_diagnostics$unsupported_geometry_types` includes the unsupported type; (6) missing CRS triggers a warning containing `"missing CRS"`; and (7) `validate_ir(ir)` does not warn/error for valid filtered sf IR. Preserve existing NC happy-path tests.</action>
  <verify>Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-ir.R")'</verify>
  <acceptance_criteria>
    - `tests/testthat/test-sf-ir.R` contains `sf_diagnostics`
    - `tests/testthat/test-sf-ir.R` contains `skipped_rows`
    - `tests/testthat/test-sf-ir.R` contains `unsupported_geometry_types`
    - `tests/testthat/test-sf-ir.R` contains `missing CRS`
    - `tests/testthat/test-sf-ir.R` contains `validate_ir`
    - `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-ir.R")'` exits 0 or skips only because optional spatial packages are unavailable
  </acceptance_criteria>
  <done>IR integration tests prove filtered sf rows, diagnostics, CRS behavior, and validation semantics.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-utils.R")'`
- [ ] `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); testthat::test_file("tests/testthat/test-sf-ir.R")'`
- [ ] `Rscript --vanilla -e 'pkgload::load_all(quiet = TRUE); ir <- as_d3_ir(ggplot2::ggplot(sf::st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)) + ggplot2::geom_sf()); validate_ir(ir); stopifnot(ir$layers[[1]]$geom == "sf")'`
</verification>

<success_criteria>
- `as_d3_ir()` extracts polygon-family `geom_sf` layers through the hardened helper path.
- Filtered sf layer `data` and `geometries` arrays remain parallel.
- `row_id` preserves source row identity for accepted rows.
- Layer diagnostics expose accepted/skipped rows and CRS handling.
- Known CRS inputs still normalize to WGS84 and bbox metadata remains available.
</success_criteria>

<output>
After completion, create `.planning/phases/32-geom-sf-ir-foundation/32-02-SUMMARY.md`
</output>
