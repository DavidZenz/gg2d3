---
phase: 53-renderer-and-ir-contract-consolidation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/htmlwidgets/modules/geom-contracts.js
  - tests/testthat/test-renderer-wiring-contracts.R
autonomous: true
requirements:
  - ARCH-01
  - ARCH-03
must_haves:
  truths:
    - "Every registered renderer alias is governed by geom-contracts.js per D-01 and D-04."
    - "Module path, yaml load order, render selector, update selector, interaction selector, and public payload drift fail source tests with actionable messages per D-05 and D-07."
    - "Renderer-private fields remain declared in the contract and guarded from public payload surfaces per T-53-01."
  artifacts:
    - path: "inst/htmlwidgets/modules/geom-contracts.js"
      provides: "Internal renderer contract manifest with explicit exception reasons"
      contains: "window.gg2d3.geomContracts"
    - path: "tests/testthat/test-renderer-wiring-contracts.R"
      provides: "Strict source-level renderer contract tests"
      contains: "geom contract module paths and htmlwidgets load order are complete"
  key_links:
    - from: "tests/testthat/test-renderer-wiring-contracts.R"
      to: "inst/htmlwidgets/modules/geom-contracts.js"
      via: "contract parser helpers"
      pattern: "extract_contract_entries"
    - from: "tests/testthat/test-renderer-wiring-contracts.R"
      to: "inst/htmlwidgets/gg2d3.yaml"
      via: "yaml script load-order assertions"
      pattern: "gg2d3.yaml"
    - from: "inst/htmlwidgets/modules/geom-contracts.js"
      to: "inst/htmlwidgets/modules/public-data.js"
      via: "publicPayload and privateFields expectations"
      pattern: "publicPayload"
---

<objective>
Harden the existing internal renderer contract source of truth so renderer alias, module loading, render selector, update selector, interaction selector, and public payload drift is detected by deterministic source tests.

Purpose: Satisfies ARCH-01 and ARCH-03 without introducing generated contracts, public extension semantics, browser-only gates, or new dependencies.
Output: Stronger `geom-contracts.js` exception metadata and stricter `test-renderer-wiring-contracts.R` coverage.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-renderer-and-ir-contract-consolidation/53-CONTEXT.md
@.planning/phases/53-renderer-and-ir-contract-consolidation/53-RESEARCH.md
@.planning/phases/53-renderer-and-ir-contract-consolidation/53-PATTERNS.md
@.planning/phases/53-renderer-and-ir-contract-consolidation/53-VALIDATION.md
@.planning/milestones/v1.12-phases/50-renderer-wiring-and-interaction-contracts/50-01-SUMMARY.md
@.planning/milestones/v1.12-phases/50-renderer-wiring-and-interaction-contracts/50-02-SUMMARY.md
@.planning/milestones/v1.12-phases/50-renderer-wiring-and-interaction-contracts/50-03-SUMMARY.md
@inst/htmlwidgets/modules/geom-contracts.js
@inst/htmlwidgets/modules/geom-registry.js
@inst/htmlwidgets/gg2d3.yaml
@inst/htmlwidgets/modules/public-data.js
@inst/htmlwidgets/modules/events.js
@inst/htmlwidgets/modules/brush.js
@inst/htmlwidgets/modules/crosstalk.js
@inst/htmlwidgets/modules/tooltip.js
@tests/testthat/test-renderer-wiring-contracts.R

<interfaces>
Existing contract API:
```javascript
window.gg2d3.geomContracts = {
  all: all,
  names: names,
  find: find,
  selectorsFor: selectorsFor,
  privateFields: privateFields
};
```

Existing test helpers to extend:
```r
read_module(path)
extract_js_strings(text)
extract_contract_entries(contract_js)
contract_aliases(contract_js)
registered_aliases()
contract_update_selectors(contract_js)
contract_interaction_selectors(contract_js, surface)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict module, render selector, and load-order contract tests</name>
  <files>tests/testthat/test-renderer-wiring-contracts.R</files>
  <read_first>
    - tests/testthat/test-renderer-wiring-contracts.R
    - inst/htmlwidgets/modules/geom-contracts.js
    - inst/htmlwidgets/gg2d3.yaml
    - inst/htmlwidgets/modules/geom-registry.js
    - inst/htmlwidgets/modules/geoms/point.js
    - inst/htmlwidgets/modules/geoms/interval.js
    - .planning/phases/53-renderer-and-ir-contract-consolidation/53-CONTEXT.md
    - .planning/phases/53-renderer-and-ir-contract-consolidation/53-PATTERNS.md
  </read_first>
  <behavior>
    - Test that every contract `module` path exists under `inst/htmlwidgets/modules/` and appears in `inst/htmlwidgets/gg2d3.yaml`.
    - Test that `geom-contracts.js` loads before `public-data.js`, `tooltip.js`, `events.js`, `brush.js`, `crosstalk.js`, `geom-registry.js`, and every `geoms/*.js` module.
    - Test that each `renderSelectors` selector is backed by the declared renderer module source. For dynamic interval selectors, accept `geom-interval-` plus `interval-item`; otherwise require all class tokens from the selector to appear in the module source.
    - Use `info = paste(...)` so failures name the geom, alias, module path, selector, or load-order pair per D-07.
  </behavior>
  <action>
    Extend `tests/testthat/test-renderer-wiring-contracts.R` with parser helpers named `contract_modules()`, `contract_render_selectors()`, `yaml_script_entries()`, `selector_class_tokens()`, and `selector_supported_by_module()`. Add tests named exactly `geom contract module paths and htmlwidgets load order are complete` and `geom contract render selectors are produced by declared renderer modules`. Keep the source-level R parser approach from Phase 50; do not add V8, browser execution, generated contracts, or broad snapshots per D-02, D-14, and D-15.
  </action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-renderer-wiring-contracts.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "contract_modules|contract_render_selectors|yaml_script_entries|selector_class_tokens|selector_supported_by_module" tests/testthat/test-renderer-wiring-contracts.R` prints all five helper names.
    - `rtk rg -n "geom contract module paths and htmlwidgets load order are complete|geom contract render selectors are produced by declared renderer modules" tests/testthat/test-renderer-wiring-contracts.R` prints both exact test names.
    - `rtk rg -n "Missing contract module|Missing yaml script|Missing renderer selector|Load order drift" tests/testthat/test-renderer-wiring-contracts.R` prints actionable failure strings.
    - The automated test command exits 0.
  </acceptance_criteria>
  <done>Renderer module paths, yaml loading order, and render selector drift are covered by source tests with geom/module/selector-specific messages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make explicit exceptions and public payload expectations contract-driven</name>
  <files>inst/htmlwidgets/modules/geom-contracts.js, tests/testthat/test-renderer-wiring-contracts.R</files>
  <read_first>
    - inst/htmlwidgets/modules/geom-contracts.js
    - inst/htmlwidgets/modules/public-data.js
    - inst/htmlwidgets/modules/events.js
    - inst/htmlwidgets/modules/brush.js
    - inst/htmlwidgets/modules/tooltip.js
    - inst/htmlwidgets/modules/crosstalk.js
    - tests/testthat/test-renderer-wiring-contracts.R
    - tests/testthat/test-polygon-interactivity.R
    - tests/testthat/test-sf-interactivity.R
    - tests/testthat/test-sf-annotations-interactivity.R
    - .planning/phases/53-renderer-and-ir-contract-consolidation/53-CONTEXT.md
    - .planning/phases/53-renderer-and-ir-contract-consolidation/53-RESEARCH.md
  </read_first>
  <behavior>
    - Empty `update.selectors` is allowed only with `update.type = "explicit-none"` and a non-empty `reason` per D-06.
    - Empty interaction surfaces are allowed only as `{ selectors: [], reason: "..." }`; reason text must name the surface or module, for example crosstalk.
    - Every contract entry declares `publicPayload: true` or `publicPayload: false`.
    - Every declared `privateFields` value is covered by public sanitizer tests so T-53-01 cannot regress silently.
  </behavior>
  <action>
    Convert any remaining empty interaction arrays in `geom-contracts.js` for `hline`, `vline`, and `abline` into reason-bearing objects for `events`, `brush`, and `crosstalk`; preserve the current selector behavior by keeping `selectors: []`. Extend `tests/testthat/test-renderer-wiring-contracts.R` with helpers named `contract_update_exceptions()`, `contract_interaction_exceptions()`, `contract_public_payloads()`, and `contract_private_fields()`. Add tests named exactly `geom contract exceptions are explicit and reason-bearing` and `geom contract public payload expectations are sanitizer-covered`. Keep `publicData.sanitizeDatum` as the only sanitizer source; do not add per-module sanitizer logic per D-01 and T-53-01.
  </action>
  <verify>
    <automated>rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-renderer-wiring-contracts.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "contract_update_exceptions|contract_interaction_exceptions|contract_public_payloads|contract_private_fields" tests/testthat/test-renderer-wiring-contracts.R` prints all four helper names.
    - `rtk rg -n "geom contract exceptions are explicit and reason-bearing|geom contract public payload expectations are sanitizer-covered" tests/testthat/test-renderer-wiring-contracts.R` prints both exact test names.
    - `rtk rg -n "reason: '.*currently|reason: \".*currently|publicPayload: true|publicPayload: false" inst/htmlwidgets/modules/geom-contracts.js` prints reason-bearing exceptions and explicit public payload fields.
    - `rtk rg -n "publicData\\.sanitizeDatum|_polygonPoints|_sourceIndex|_geom|_centroid|_sfFamily|_sfAnchor|_pointCoord|_pointIndex" tests/testthat/test-renderer-wiring-contracts.R` prints sanitizer and private-field coverage.
    - The automated test command exits 0.
  </acceptance_criteria>
  <done>Explicit renderer exceptions and public payload sanitizer expectations are encoded in the manifest and enforced by source tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer-private datum -> public tooltip/event/brush payload | Underscore-prefixed renderer fields must not leak to user callbacks, Shiny-style inputs, tooltips, or brush selected data. |
| internal contract manifest -> renderer/interactivity modules | Source drift can disable rendering or interactions without network/auth/database involvement. |

No network, authentication, authorization, database, cryptography, or secret-management surface is added by this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Information Disclosure | `public-data.js`, `events.js`, `brush.js`, `tooltip.js`, `geom-contracts.js` | mitigate | Keep `publicData.sanitizeDatum()` as the shared public payload sanitizer and enforce every contract `privateFields` entry in `test-renderer-wiring-contracts.R`. |
| T-53-02 | Tampering / Integrity | `geom-contracts.js`, `gg2d3.yaml`, `geom-registry.js`, `geoms/*.js` | mitigate | Add strict source tests for aliases, modules, load order, render selectors, update selectors, interaction selectors, and explicit exceptions. |
| T-53-03 | Tampering / Integrity | R IR helper extraction | accept | This plan does not modify R IR helpers; Plan 53-02 mitigates IR behavior drift. |
</threat_model>

<verification>
Primary gate:
`rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-renderer-wiring-contracts.R")'`

Optional adjacent gate when sanitizer or interaction selector assertions change:
`rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-polygon-interactivity.R"); testthat::test_file("tests/testthat/test-sf-interactivity.R"); testthat::test_file("tests/testthat/test-sf-annotations-interactivity.R")'`
</verification>

<success_criteria>
- `geom-contracts.js` remains the internal source of truth per D-01.
- Contract tests fail with messages naming missing aliases, modules, selectors, exceptions, private fields, or sanitizer surfaces per D-07.
- No generated contract tooling, public extension API, committed pixel golden, or browser-only primary gate is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/53-renderer-and-ir-contract-consolidation/53-01-SUMMARY.md`.
</output>
