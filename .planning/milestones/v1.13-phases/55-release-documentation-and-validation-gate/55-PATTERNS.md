# Phase 55: release-documentation-and-validation-gate - Pattern Map

**Mapped:** 2026-05-28
**Files analyzed:** 11 new/modified targets plus 6 supporting analogs
**Analogs found:** 11 / 11

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `README.Rmd` | documentation source | transform | `README.Rmd` | exact |
| `README.md` | generated documentation | transform | `README.md` generated from `README.Rmd` | exact |
| `vignettes/gg2d3.Rmd` | documentation source | request-response example/docs transform | `vignettes/gg2d3.Rmd` | exact |
| `vignettes/d3-drawing-diagnostics.md` | diagnostics documentation | batch/evidence | `vignettes/d3-drawing-diagnostics.md` | exact |
| `R/gg2d3.R` | roxygen source | transform | `R/gg2d3.R` | exact |
| `man/gg2d3.Rd` | generated help | transform | `man/gg2d3.Rd` generated from `R/gg2d3.R` | exact |
| `NAMESPACE` | generated config | transform | `NAMESPACE` generated by roxygen2 | exact |
| `NEWS.md` | release notes | batch/evidence summary | `NEWS.md` current heading plus Phase 43 release-note pattern | role-match |
| `.planning/phases/55-release-documentation-and-validation-gate/55-VALIDATION.md` | validation strategy | batch/evidence | `.planning/phases/55-release-documentation-and-validation-gate/55-VALIDATION.md`, Phase 53 validation | exact |
| `.planning/phases/55-release-documentation-and-validation-gate/55-GATE-RUN.md` | release gate evidence | batch/file-I/O evidence | `.planning/milestones/v1.10-phases/42-release-validation-gate/42-GATE-RUN.md` | exact |
| `.planning/phases/55-release-documentation-and-validation-gate/55-VERIFICATION.md` | final verification evidence | batch/traceability | `.planning/phases/54-geometry-polish-closure/54-VERIFICATION.md` plus Phase 52/53 evidence | exact |

## Pattern Assignments

### `README.Rmd` (documentation source, transform)

**Analog:** `README.Rmd`

**Generated-source marker** (lines 1-13):
````markdown
---
output: github_document
---

<!-- README.md is generated from README.Rmd. Please edit that file -->

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  fig.path = "man/figures/README-",
  out.width = "100%"
)
```
````

**Conservative support table and scoped claims** (lines 52-97):
````markdown
| Basic | `geom_point`, `geom_line`, `geom_path`, `geom_bar`, `geom_col`, `geom_rect`, `geom_tile`, `geom_text`, `geom_label`, `geom_polygon` |

Ordinary `geom_polygon()` renders as grouped closed SVG paths with ggplot2 built
row order preserved inside each group.

`geom_rect()` and `geom_tile()` support distinguishes scale-limit censoring from
`coord_cartesian()` panel clipping...

Ordinary `geom_label()` renders bounded SVG label groups with a backing rect and
text...

These support claims are intentionally scoped. gg2d3 does not claim complete
ggplot2 parity...
````

**Regeneration instruction** (lines 174-180):
````markdown
devtools::load_all()
devtools::test()
```

---

*Note:* `README.md` is generated from `README.Rmd`. Use `devtools::build_readme()` to re-render.
````

**Apply to Phase 55:** edit this source before `README.md`; add v1.13 validation, architecture, gate, and `FUT-01` through `FUT-06` language here first.

---

### `README.md` (generated documentation, transform)

**Analog:** `README.md`

**Generated marker** (lines 1-4):
```markdown
<!-- README.md is generated from README.Rmd. Please edit that file -->

# gg2d3
```

**Generated support wording mirrors source** (lines 46-93):
```markdown
| Basic | `geom_point`, `geom_line`, `geom_path`, `geom_bar`, `geom_col`, `geom_rect`, `geom_tile`, `geom_text`, `geom_label`, `geom_polygon` |

Ordinary `geom_polygon()` renders as grouped closed SVG paths...
`geom_rect()` and `geom_tile()` support distinguishes scale-limit censoring...
Ordinary `geom_label()` renders bounded SVG label groups...

These support claims are intentionally scoped. gg2d3 does not claim
complete ggplot2 parity...
```

**Generated footer** (lines 188-189):
```markdown
*Note:* `README.md` is generated from `README.Rmd`. Use
`devtools::build_readme()` to re-render.
```

**Apply to Phase 55:** do not hand-edit except as generated diff inspection. Regenerate with `rtk Rscript --vanilla -e 'devtools::build_readme()'` after source changes.

---

### `vignettes/gg2d3.Rmd` (documentation source, request-response examples)

**Analog:** `vignettes/gg2d3.Rmd`

**Support summary pattern** (lines 42-49):
```markdown
## Supported geoms

gg2d3 supports the core Cartesian geoms below, ordinary `geom_polygon()`,
polygon-family, point-family, and line-family `geom_sf()`, plus projected-anchor
`geom_sf_text()` and `geom_sf_label()` annotations.
All aesthetics that ggplot2 maps (color, fill, size, shape, alpha, linewidth)
are carried through to D3.
Detailed geometry caveats live in `vignettes/d3-drawing-diagnostics.md`.
```

**Concrete example plus boundary** (lines 101-123):
```markdown
### Ordinary polygons

Ordinary `geom_polygon()` renders each group as a grouped closed SVG path while
preserving ggplot2's built row order.

...

This is a grouped-path contract, not a GIS topology engine: topology/hole repair
outside clean ggplot2 built groups is deferred.
```

**Optional validation and residual risk bullets** (lines 213-222):
```markdown
- Optional browser validation is R/testthat/chromote based and may skip cleanly;
  when available, it covers sf family interactivity, stacked overlays, faceted
  and empty panels, projected anchor placement, sanitized interactivity
  payloads, and zoom suppression.
- gg2d3 does not provide tile basemaps, slippy map controls, JavaScript-side
  CRS reprojection, true geometry-overlap brushing, or large-map performance
  guarantees.
```

**Apply to Phase 55:** keep examples user-facing and concise; point to diagnostics for caveats rather than duplicating the full release gate.

---

### `vignettes/d3-drawing-diagnostics.md` (diagnostics documentation, batch/evidence)

**Analog:** `vignettes/d3-drawing-diagnostics.md`

**Support and non-goal pattern** (lines 9-31):
```markdown
gg2d3 supports core Cartesian geoms ... plus polygon-family, point-family, and line-family `geom_sf`
and projected-anchor `geom_sf_text()` / `geom_sf_label()` annotations.

Residual risks remain explicit: topology and hole behavior beyond clean grouped
closed paths is not shipped by Phase 54.
```

**Browser visual smoke command and artifact pattern** (lines 61-108):
````markdown
## Browser visual smoke artifacts

Maintainers can generate local browser-rendered visual smoke artifacts with the
opt-in test runner. The skip-friendly command is:

```bash
Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-browser-visual-smoke.R")'
```

The full artifact command is:

```bash
NOT_CRAN=true GG2D3_BROWSER_VISUAL_SMOKE=true Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-browser-visual-smoke.R")'
```

The CI-equivalent local command is:

```bash
NOT_CRAN=true GG2D3_BROWSER_VISUAL_SMOKE=true GG2D3_BROWSER_VISUAL_CI=true Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); res <- testthat::test_file("tests/testthat/test-browser-visual-smoke.R"); df <- as.data.frame(res); if (any(df$failed > 0 | df$error)) quit(status = 1)'
```

Generated files live under `test_output/browser-visual-smoke/`, which is ignored
by git and excluded from package builds.
````

**Architecture/future-work pattern** (lines 161-171):
```markdown
On the R side, `as_d3_ir()` remains the orchestrator...
full `as_d3_ir()` modularization remains deferred to
FUT-03. The generated renderer documentation remains deferred to FUT-04...

These diagnostics do not add committed baseline images or image-difference
gates.
```

**v1.13 residual risk list** (lines 190-199):
```markdown
## Phase 54 residual-risk list

The v1.13 geometry polish contract is deliberately bounded. The following items
are deferred and not shipped by Phase 54:

- Ordinary polygon topology and hole repair beyond grouped closed paths.
- Ordinary polygon `subgroup` / `rule` compound-path rendering.
- Tile basemaps and slippy map controls.
- JavaScript-side CRS reprojection.
- ggrepel-compatible collision avoidance.
```

**Apply to Phase 55:** diagnostics should hold detailed commands, artifact paths, skip semantics, and deferred items; README/vignette should link here instead of carrying long gate detail.

---

### `R/gg2d3.R` (roxygen source, transform)

**Analog:** `R/gg2d3.R`

**Roxygen support/caveat pattern** (lines 1-24):
```r
#' Render a ggplot as a D3 widget
#'
#' gg2d3 supports ordinary `geom_polygon()` as grouped closed SVG paths,
#' `geom_rect()` and `geom_tile()` edge behavior for the shipped Cartesian
#' scale and panel-clipping contract, and `geom_sf()` polygon-family
#' (`POLYGON`, `MULTIPOLYGON`), point-family (`POINT`, `MULTIPOINT`), and
#' line-family (`LINESTRING`, `MULTILINESTRING`) layers.
#'
#' Detailed caveats for polygon topology, rect/tile transformed-scale behavior,
#' sf annotation placement, ggrepel-style collision avoidance, path-following
#' text, and map anti-features are tracked in
#' `vignettes/d3-drawing-diagnostics.md`.
#'
#' @export
gg2d3 <- function(x, width = NULL, height = NULL, elementId = NULL) {
```

**Apply to Phase 55:** keep roxygen concise and source-backed. Run `rtk Rscript --vanilla -e 'devtools::document()'` after edits and inspect `man/gg2d3.Rd`.

---

### `man/gg2d3.Rd` (generated help, transform)

**Analog:** `man/gg2d3.Rd`

**Generated-file guard** (lines 1-2):
```r
% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg2d3.R
```

**Generated description/details** (lines 18-34):
```r
\description{
gg2d3 supports ordinary \code{geom_polygon()} as grouped closed SVG paths,
\code{geom_rect()} and \code{geom_tile()} edge behavior for the shipped Cartesian
scale and panel-clipping contract...
}
\details{
Detailed caveats for polygon topology, rect/tile transformed-scale behavior...
\code{vignettes/d3-drawing-diagnostics.md}. Optional browser validation for sf
behavior is R/testthat/chromote based and may skip cleanly when optional
local tooling is unavailable.
}
```

**Apply to Phase 55:** generated only; verify alignment after `devtools::document()`.

---

### `NAMESPACE` (generated config, transform)

**Analog:** `NAMESPACE`

**Generated-file guard and export style** (lines 1-15):
```r
# Generated by roxygen2: do not edit by hand

export(as_d3_ir)
export(d3_brush)
export(d3_handlers)
export(d3_hover)
export(d3_tooltip)
export(d3_transitions)
export(d3_zoom)
export(detect_dominant_geom_type)
export(extract_sf_geometries)
export(get_layer_crs)
export(gg2d3)
export(normalize_to_wgs84)
export(validate_ir)
```

**Apply to Phase 55:** inspect generated diffs only; retain changes only if backed by roxygen source.

---

### `NEWS.md` (release notes, batch/evidence summary)

**Analog:** `NEWS.md` current structure; Phase 43 release-note evidence pattern from `.planning/milestones/v1.10-phases/43-documentation-and-release-notes/43-PATTERNS.md`

**Current insertion point and bullet style** (lines 1-6):
```markdown
# gg2d3 (development version)

## gg2d3 0.6.0 (2026-03-31)
* **Specialized Geoms**: Added support for `geom_dotplot`, `geom_rug`, `geom_errorbar`, `geom_linerange`, and `geom_pointrange`.
* **API Polish**: Standardized interactivity attachment and optimized rendering performance for state updates.
```

**Phase 43 release evidence guidance** (`43-PATTERNS.md` lines 303-304):
```markdown
- Use 42-GATE-RUN.md as the source for checks-run release notes.
- Use 42-VALIDATION-GATE.md as the source for maintainer-facing validation commands.
```

**Apply to Phase 55:** insert a v1.13 section immediately under the development heading. Summarize shipped CI/browser artifacts, renderer/IR contracts, geometry polish, release-gate commands, artifact paths, residual risks, and `FUT-01` through `FUT-06`. Do not paste raw `00check.log`, browser logs, or local console output.

---

### `.planning/phases/55-release-documentation-and-validation-gate/55-VALIDATION.md` (validation strategy, batch/evidence)

**Analog:** existing `55-VALIDATION.md`; Phase 53 validation structure

**Frontmatter and purpose** (lines 1-12):
```markdown
---
phase: 55
slug: release-documentation-and-validation-gate
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-28
---

# Phase 55 - Validation Strategy

> Per-phase validation contract for release documentation and validation-gate closure.
```

**Gate command matrix** (lines 18-24):
```markdown
| **Quick run command** | `rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-renderer-wiring-contracts.R"); testthat::test_file("tests/testthat/test-ir-helper-boundaries.R"); testthat::test_file("tests/testthat/test-text-label-polish.R")'` |
| **Full suite command** | `rtk Rscript --vanilla -e 'devtools::document(); devtools::build_readme(); devtools::test()'` |
| **Release check command** | From `/private/tmp`: `rtk R CMD build --no-manual /Users/davidzenz/R/gg2d3` then `rtk R CMD check --as-cran gg2d3_0.0.0.9000.tar.gz` |
```

**Per-task evidence map** (lines 42-49):
```markdown
| 55-01-01 | 01 | 1 | REL-01 | T-55-01 | Public docs do not overclaim unsupported parity or leak local artifacts. | docs/source | `rtk rg -n "v1.13|browser visual|geom_label|geom_polygon|rect|tile|geom-contracts|FUT-" README.Rmd README.md vignettes/gg2d3.Rmd vignettes/d3-drawing-diagnostics.md R/gg2d3.R man/gg2d3.Rd NEWS.md` | yes | pending |
| 55-02-03 | 02 | 2 | REL-02 | T-55-03 | Browser visual smoke pass/skip/CI artifact evidence is explicit. | browser/artifact | `rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-browser-visual-smoke.R")'` plus CI/artifact evidence when feasible | yes | pending |
| 55-04-01 | 04 | 4 | REL-01, REL-02, REL-03 | T-55-05 | Final evidence maps every v1.13 requirement to source, tests, docs, and gate results. | planning/evidence | `rtk rg -n "CI-01|ARCH-01|GEOM-01|REL-01|REL-02|REL-03" .planning/phases/55-release-documentation-and-validation-gate/55-VERIFICATION.md .planning/phases/55-release-documentation-and-validation-gate/55-GATE-RUN.md` | no W0 | pending |
```

**Apply to Phase 55:** update status/outcomes as execution happens; keep one row per release gate behavior and evidence artifact.

---

### `.planning/phases/55-release-documentation-and-validation-gate/55-GATE-RUN.md` (release gate evidence, batch/file-I/O evidence)

**Analog:** `.planning/milestones/v1.10-phases/42-release-validation-gate/42-GATE-RUN.md`

**Frontmatter and environment snapshot** (lines 1-18):
```markdown
---
phase: 42
slug: release-validation-gate
status: passed-with-expected-skips
created: 2026-05-23
---

# Phase 42 - Gate Run Evidence

## Environment Snapshot

Commands run from repository root (`/Users/davidzenz/R/gg2d3`) on 2026-05-23:

| Command | Outcome |
|---|---|
| `rtk Rscript --version` | passed; `Rscript (R) version 4.6.0 (2026-04-24)` |
| `rtk Rscript --vanilla -e 'pkgs <- c("devtools", "testthat", "roxygen2", "chromote", "sf", "geojsonsf", "knitr", "rmarkdown"); for (pkg in pkgs) cat(pkg, if (requireNamespace(pkg, quietly = TRUE)) as.character(utils::packageVersion(pkg)) else "NOT_INSTALLED", "\n")'` | passed; package versions recorded below |
| `rtk Rscript --vanilla -e 'cat(read.dcf("DESCRIPTION")[1, "Version"])'` | passed; `0.0.0.9000` |
```

**Full gate table pattern** (lines 56-72):
```markdown
| Command | Working Directory | Outcome | Expected Skip Or Failure | Artifact Path |
|---|---|---|---|---|
| `rtk Rscript --vanilla -e 'devtools::document(); devtools::build_readme(); devtools::test()'` | `/Users/davidzenz/R/gg2d3` | final run passed: 817 passed, 40 skipped, 6 warnings. | Expected skips: missing `sf`, empty crosstalk test, disabled visual-test context. | Generated docs reviewed in working tree. |
| `rtk R CMD build --no-manual /Users/davidzenz/R/gg2d3` | `/private/tmp` | final build passed; rebuilt `gg2d3_0.0.0.9000.tar.gz`. | None. | `/private/tmp/gg2d3_0.0.0.9000.tar.gz` |
| `rtk R CMD check --as-cran gg2d3_0.0.0.9000.tar.gz` | `/private/tmp` | final check passed with 4 NOTEs and no ERROR/WARNING. | NOTEs retained as release evidence... | `/private/tmp/gg2d3.Rcheck/00check.log`; exact check directory `/private/tmp/gg2d3.Rcheck`. |
```

**Expected skip classification** (lines 74-84):
```markdown
## Expected Optional Skips

| Skip | Source | Classification |
|---|---|---|
| `{sf} cannot be loaded` | `tests/testthat/test-regression-core.R` | Expected optional spatial skip because `sf` is `NOT_INSTALLED`... |
| `On CRAN` | `tests/testthat/test-sf-browser.R` | Expected optional browser smoke skip... |
```

**Apply to Phase 55:** create the same shape with current v1.13 commands: focused source gates, docs generation, full tests, browser smoke local/CI evidence, build/check from `/private/tmp`, and explicit artifact paths. ERROR/WARNING blocks; optional browser/spatial skips are acceptable only when documented.

---

### `.planning/phases/55-release-documentation-and-validation-gate/55-VERIFICATION.md` (final verification evidence, batch/traceability)

**Analog:** `.planning/phases/54-geometry-polish-closure/54-VERIFICATION.md` plus Phase 52 browser evidence

**Verifier frontmatter pattern** (lines 1-11):
```markdown
---
phase: 54-geometry-polish-closure
verified: 2026-05-28T18:56:26Z
status: passed
score: 12/12 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Visual label-box polish"
    expected: "Ordinary geom_label boxes render with acceptable padding..."
    result: "Passed via localhost browser inspection..."
---
```

**Observable truths table** (lines 28-41):
```markdown
| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Ordinary `geom_label()` box/padding/fill/stroke behavior is implemented... | VERIFIED | `GeomLabel` maps to `label`... docs describe bounded support... |
| 11 | Phase 54 validation records quick source tests, renderer contract tests, diagnostics grep, optional browser smoke behavior, and full suite evidence. | VERIFIED | `.planning/phases/54-geometry-polish-closure/54-VALIDATION.md` has `status: executed`... |
| 12 | README source/generated text is aligned... | VERIFIED | `README.Rmd:54-96` and generated `README.md:46-92`... |
```

**Required artifacts table** (lines 45-63):
```markdown
| Artifact | Expected | Status | Details |
|---|---|---|---|
| `tests/testthat/test-renderer-wiring-contracts.R` | Renderer contract wiring tests | VERIFIED | Label brush/update/public contract assertions pass. |
| `vignettes/d3-drawing-diagnostics.md` | Geometry diagnostics | VERIFIED | Public boundary distinguishes shipped support from future work. |
| `README.Rmd`, `README.md` | README source/generated alignment | VERIFIED | Both contain Phase 54 support and limitation language. |
```

**Requirements coverage table** (lines 99-108):
```markdown
| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| GEOM-01 | 54-01, 54-04 | Ordinary `geom_label()` box, padding, fill, stroke, and text behavior implemented or explicitly deferred | SATISFIED | Implemented bounded label path in IR/renderer/contracts and documented in diagnostics/README. |
...
No orphaned Phase 54 requirements found in `.planning/REQUIREMENTS.md`; GEOM-01 through GEOM-04 are all mapped to Phase 54.
```

**Phase 52 browser artifact evidence** (`52-VERIFICATION.md` lines 20-23, 45-47):
```markdown
| CI-01: Dedicated CI/local command opts into browser visual smoke and fails browser-level skips in CI mode | VERIFIED | `.github/workflows/browser-visual-smoke.yaml` sets `GG2D3_BROWSER_VISUAL_SMOKE=true` and `GG2D3_BROWSER_VISUAL_CI=true`... |
| CI-02: Outputs deterministic browser visual artifacts and report metadata | VERIFIED | `write_browser_visual_index()` validates rows and writes `index.html` plus `index.json` metadata... |
...
- GitHub Actions workflow run `26575140296` ... passed.
- Downloaded artifact `test_output/github-run-26575140296/browser-visual-smoke-26575140296/index.json` - all 9 fixture rows passed.
```

**Apply to Phase 55:** map every v1.13 requirement (`CI-01` through `REL-03`) to source, tests, docs, gate result, and artifact evidence. Include Phase 52-54 verified outcomes as upstream evidence, then add Phase 55 REL outcomes.

## Shared Patterns

### Source-First Documentation

**Source:** `README.Rmd`, `R/gg2d3.R`, `man/gg2d3.Rd`
**Apply to:** `README.Rmd`, `README.md`, `R/gg2d3.R`, `man/gg2d3.Rd`, `NAMESPACE`

```bash
rtk Rscript --vanilla -e 'devtools::document(); devtools::build_readme()'
rtk git diff -- README.Rmd README.md R/gg2d3.R man/gg2d3.Rd NAMESPACE
```

Do not manually edit generated `README.md`, `man/*.Rd`, or `NAMESPACE` except to inspect generated diff behavior.

### Conservative Public Claims

**Source:** `README.Rmd` lines 92-97; `vignettes/d3-drawing-diagnostics.md` lines 190-199
**Apply to:** all public docs and `NEWS.md`

```markdown
These support claims are intentionally scoped. gg2d3 does not claim complete
ggplot2 parity...
```

Keep v1.13 support bounded: browser artifacts, renderer/IR contracts, ordinary labels, ordinary polygons as grouped paths, rect/tile transformed-bound filtering, and explicit `FUT-01` through `FUT-06` deferrals.

### Browser Visual Artifact Evidence

**Source:** `tests/testthat/helper-browser-visual.R`, `tests/testthat/test-browser-visual-smoke.R`, `.github/workflows/browser-visual-smoke.yaml`
**Apply to:** diagnostics, `55-GATE-RUN.md`, `55-VERIFICATION.md`, `NEWS.md`

**Skip/fail behavior** (`helper-browser-visual.R` lines 60-69):
```r
browser_visual_ci_mode <- function() {
  identical(Sys.getenv("GG2D3_BROWSER_VISUAL_CI"), "true")
}

browser_visual_skip_or_fail <- function(message) {
  if (browser_visual_ci_mode()) {
    stop(message, call. = FALSE)
  }
  testthat::skip(message)
}
```

**Artifact validation** (`helper-browser-visual.R` lines 474-523):
```r
validate_browser_visual_rows <- function(rows, allow_spatial_skips = TRUE) {
  ...
  if (identical(status, "passed")) {
    missing <- names(artifact_paths)[!nzchar(artifact_paths) | !file.exists(artifact_paths)]
    if (length(missing) > 0) {
      testthat::fail(...)
    }
  } else if (identical(status, "skipped")) {
    skip_reason <- row$skip_reason %||% ""
    if (browser_visual_ci_mode()) {
      spatial_skip <- isTRUE(allow_spatial_skips) &&
        grepl("^sf-", id) &&
        grepl("Missing optional dependencies:", skip_reason, fixed = TRUE)
```

**Index/report writing** (`helper-browser-visual.R` lines 535-600):
```r
index_json <- file.path(out_dir, "index.json")
index_html <- file.path(out_dir, "index.html")
...
"<p>Generated local artifacts for maintainer inspection. These files live under <code>test_output/browser-visual-smoke/</code>.</p>",
```

**Workflow upload** (`browser-visual-smoke.yaml` lines 12-15, 41-52):
```yaml
env:
  NOT_CRAN: "true"
  GG2D3_BROWSER_VISUAL_SMOKE: "true"
  GG2D3_BROWSER_VISUAL_CI: "true"

- name: Run browser visual smoke
  run: |
    Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); res <- testthat::test_file("tests/testthat/test-browser-visual-smoke.R"); df <- as.data.frame(res); if (any(df$failed > 0 | df$error)) quit(status = 1)'

- name: Upload browser visual smoke artifacts
  uses: actions/upload-artifact@v4
  with:
    name: browser-visual-smoke-${{ github.run_id }}
    path: test_output/browser-visual-smoke/
```

### Release Gate Evidence Without Artifact Leakage

**Source:** `.planning/milestones/v1.10-phases/42-release-validation-gate/42-VALIDATION-GATE.md` lines 66-75; `.gitignore`, `.Rbuildignore`
**Apply to:** `55-GATE-RUN.md`, `NEWS.md`, `55-VERIFICATION.md`

```markdown
- `/private/tmp/gg2d3_*.Rcheck/00check.log` for package check summaries.
- `README.md`, `man/*.Rd`, and `NAMESPACE` diffs after documentation generation.

Browser smoke artifacts are local debugging evidence under ignored paths. Inspect them locally and redact logs before sharing outside the release-validation context.
```

Ignored/build-excluded paths:
```text
.gitignore:10:test_output/
.gitignore:40:*.Rcheck/
.Rbuildignore:17:^test_output$
.Rbuildignore:18:^test_output/
.Rbuildignore:22:^.*\.Rcheck$
```

### Focused Gates Before Full Gate

**Source:** `55-VALIDATION.md` lines 42-47; Phase 53/54 validation files
**Apply to:** execution plans and `55-GATE-RUN.md`

```bash
rtk Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-renderer-wiring-contracts.R"); testthat::test_file("tests/testthat/test-ir-helper-boundaries.R"); testthat::test_file("tests/testthat/test-text-label-polish.R"); testthat::test_file("tests/testthat/test-polygon-ir.R"); testthat::test_file("tests/testthat/test-polygon-renderer.R"); testthat::test_file("tests/testthat/test-rect-tile-ir.R"); testthat::test_file("tests/testthat/test-rect-tile-renderer.R")'
rtk Rscript --vanilla -e 'devtools::document(); devtools::build_readme(); devtools::test()'
```

### Full v1.13 Requirement Traceability

**Source:** `.planning/REQUIREMENTS.md` lines 8-31, 66-80; `54-VERIFICATION.md` lines 99-108
**Apply to:** `55-VERIFICATION.md`

```markdown
| Requirement | Phase | Status |
|-------------|-------|--------|
| CI-01 | Phase 52 | Complete |
| ARCH-01 | Phase 53 | Complete |
| GEOM-01 | Phase 54 | Complete |
| REL-01 | Phase 55 | Pending |
| REL-02 | Phase 55 | Pending |
| REL-03 | Phase 55 | Pending |
```

For Phase 55 final verification, convert pending REL rows to satisfied/verified only after source docs, generated docs, release notes, gate execution, and package check evidence are recorded.

## No Analog Found

All classified files have close analogs in the repository. No file needs planner-only invention from research patterns.

## Metadata

**Analog search scope:** repository root docs, `R/`, `man/`, `vignettes/`, `tests/testthat/`, `.github/workflows/`, `.planning/phases/52-54`, `.planning/milestones/v1.10-phases/42-43`
**Files scanned:** 90+ repository and planning files via `rg --files`, phase artifact lists, and targeted `rg`
**Pattern extraction date:** 2026-05-28
