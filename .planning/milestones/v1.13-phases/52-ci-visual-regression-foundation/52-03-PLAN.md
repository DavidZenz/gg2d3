---
phase: 52-ci-visual-regression-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 52-01
  - 52-02
files_modified:
  - vignettes/d3-drawing-diagnostics.md
  - tests/testthat/helper-browser-visual.R
  - tests/testthat/test-browser-visual-smoke.R
  - .github/workflows/browser-visual-smoke.yaml
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-03
must_haves:
  truths:
    - "Maintainer docs explain local quick runs, full local artifact runs, CI-equivalent runs, artifact inspection, and CHROMOTE_CHROME troubleshooting."
    - "Docs explicitly state that screenshots are inspection evidence and pixel thresholds/goldens are deferred."
    - "Final source checks prove Phase 52 covers CI-01, CI-02, and CI-03 without committing generated artifacts."
  artifacts:
    - path: "vignettes/d3-drawing-diagnostics.md"
      provides: "CI/local browser visual smoke documentation and troubleshooting"
    - path: ".github/workflows/browser-visual-smoke.yaml"
      provides: "Source-checked dedicated CI workflow"
    - path: "tests/testthat/helper-browser-visual.R"
      provides: "Source-checked CI report contract"
    - path: "tests/testthat/test-browser-visual-smoke.R"
      provides: "Source-checked opt-in runner"
  key_links:
    - from: "vignettes/d3-drawing-diagnostics.md"
      to: ".github/workflows/browser-visual-smoke.yaml"
      via: "CI workflow mention"
      pattern: "browser-visual-smoke.yaml"
    - from: "vignettes/d3-drawing-diagnostics.md"
      to: "test_output/browser-visual-smoke/"
      via: "artifact inspection guidance"
      pattern: "test_output/browser-visual-smoke"
---

<objective>
Document the Phase 52 CI/local browser visual smoke workflow and run final validation checks.

Purpose: after helper/test hardening and CI workflow creation, maintainers need exact commands, artifact inspection guidance, troubleshooting notes, and source checks that prove the phase delivered CI-safe non-pixel regression confidence.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-ci-visual-regression-foundation/52-CONTEXT.md
@.planning/phases/52-ci-visual-regression-foundation/52-RESEARCH.md
@.planning/phases/52-ci-visual-regression-foundation/52-PATTERNS.md
@.planning/phases/52-ci-visual-regression-foundation/52-VALIDATION.md
@vignettes/d3-drawing-diagnostics.md
@tests/testthat/helper-browser-visual.R
@tests/testthat/test-browser-visual-smoke.R
@.github/workflows/browser-visual-smoke.yaml
@.gitignore
@.Rbuildignore
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document CI, local commands, artifacts, and troubleshooting</name>
  <read_first>
    vignettes/d3-drawing-diagnostics.md
    .github/workflows/browser-visual-smoke.yaml
    tests/testthat/helper-browser-visual.R
    tests/testthat/test-browser-visual-smoke.R
    .planning/phases/52-ci-visual-regression-foundation/52-CONTEXT.md
  </read_first>
  <files>vignettes/d3-drawing-diagnostics.md</files>
  <action>
    Update the existing `## Browser visual smoke artifacts` section in `vignettes/d3-drawing-diagnostics.md`.

    Preserve the existing quick skip-friendly command:
    `Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-browser-visual-smoke.R")'`

    Preserve the existing full local artifact command:
    `NOT_CRAN=true GG2D3_BROWSER_VISUAL_SMOKE=true Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-browser-visual-smoke.R")'`

    Add the CI-equivalent local command:
    `NOT_CRAN=true GG2D3_BROWSER_VISUAL_SMOKE=true GG2D3_BROWSER_VISUAL_CI=true Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-browser-visual-smoke.R")'`

    Add documentation stating:
    - GitHub Actions workflow file is `.github/workflows/browser-visual-smoke.yaml`.
    - CI runs on pull requests and `workflow_dispatch`.
    - CI uploads the full `test_output/browser-visual-smoke/` directory as an artifact on every run.
    - `index.html` is the human-readable report and `index.json` is the structured report with metadata.
    - Per-fixture artifacts are `<fixture>.html`, `<fixture>.png`, `<fixture>-dom-summary.json`, and `<fixture>-browser-log.json`.
    - `CHROMOTE_CHROME=/path/to/chrome` can be set as a troubleshooting override when `chromote::find_chrome()` does not locate the intended browser.
    - In the dedicated CI workflow, browser-level skips are failures; optional `sf`/`geojsonsf` row skips may pass only when explicit in the report.
    - Screenshots are inspection evidence only; committed golden screenshots and pixel thresholds are deferred.

    Do not paste local logs, absolute artifact paths, screenshots, or GitHub run URLs into the docs.
  </action>
  <verify>
    <automated>rg -n "Browser visual smoke artifacts|GG2D3_BROWSER_VISUAL_CI|browser-visual-smoke.yaml|workflow_dispatch|CHROMOTE_CHROME|index\\.html|index\\.json|pixel thresholds|golden screenshots|test_output/browser-visual-smoke" vignettes/d3-drawing-diagnostics.md</automated>
  </verify>
  <acceptance_criteria>
    - Docs contain quick, full local, and CI-equivalent commands.
    - Docs mention `.github/workflows/browser-visual-smoke.yaml`.
    - Docs mention `CHROMOTE_CHROME` as troubleshooting override.
    - Docs state screenshots are evidence and pixel thresholds/goldens are deferred.
    - Docs do not include absolute local artifact paths or pasted log contents.
  </acceptance_criteria>
  <done>Maintainer docs describe how to run, inspect, and troubleshoot Phase 52 visual smoke locally and in CI.</done>
</task>

<task type="auto">
  <name>Task 2: Run final source, workflow, and artifact-boundary checks</name>
  <read_first>
    tests/testthat/helper-browser-visual.R
    tests/testthat/test-browser-visual-smoke.R
    vignettes/d3-drawing-diagnostics.md
    .github/workflows/browser-visual-smoke.yaml
    .gitignore
    .Rbuildignore
    DESCRIPTION
    .planning/phases/52-ci-visual-regression-foundation/52-VALIDATION.md
  </read_first>
  <files>tests/testthat/helper-browser-visual.R, tests/testthat/test-browser-visual-smoke.R, vignettes/d3-drawing-diagnostics.md, .github/workflows/browser-visual-smoke.yaml</files>
  <action>
    Run final verification commands. If a check fails, fix the implicated Phase 52 file while preserving all Phase 52 decisions.

    Required final checks:
    - Quick command without visual opt-in passes by explicit skip.
    - Helper/test source contains `GG2D3_BROWSER_VISUAL_CI`, `validate_browser_visual_rows`, `browser_visual_report_metadata`, and `jsonlite::read_json`.
    - Workflow source contains pull request/manual triggers, CI env flags, the exact Rscript command, and `actions/upload-artifact@v4`.
    - Docs contain CI/local commands, `CHROMOTE_CHROME`, artifact paths, and pixel/golden deferral.
    - `.gitignore` and `.Rbuildignore` still exclude `test_output/`.
    - Source files do not introduce Playwright, Puppeteer, Selenium, webshot2, Node browser tooling, pixel diff code, or committed golden screenshot support.
    - `git status --short test_output .github/workflows/browser-visual-smoke.yaml tests/testthat/helper-browser-visual.R tests/testthat/test-browser-visual-smoke.R vignettes/d3-drawing-diagnostics.md` does not show generated `test_output/` files.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-browser-visual-smoke.R")'</automated>
    <automated>rg -n "GG2D3_BROWSER_VISUAL_CI|validate_browser_visual_rows|browser_visual_report_metadata|jsonlite::read_json|browser_visual_ci_mode" tests/testthat/helper-browser-visual.R tests/testthat/test-browser-visual-smoke.R</automated>
    <automated>rg -n "pull_request|workflow_dispatch|GG2D3_BROWSER_VISUAL_SMOKE|GG2D3_BROWSER_VISUAL_CI|actions/upload-artifact@v4|test_output/browser-visual-smoke|test-browser-visual-smoke\\.R" .github/workflows/browser-visual-smoke.yaml</automated>
    <automated>rg -n "GG2D3_BROWSER_VISUAL_CI|browser-visual-smoke.yaml|CHROMOTE_CHROME|test_output/browser-visual-smoke|pixel thresholds|golden screenshots" vignettes/d3-drawing-diagnostics.md</automated>
    <automated>Rscript --vanilla -e 'files <- c("DESCRIPTION", "tests/testthat/helper-browser-visual.R", "tests/testthat/test-browser-visual-smoke.R", "vignettes/d3-drawing-diagnostics.md", ".github/workflows/browser-visual-smoke.yaml"); pattern <- "Playwright|Puppeteer|Selenium|webshot2|pixel diff|pixel-diff|golden image|committed golden|npm|node_modules"; hits <- unlist(lapply(files[file.exists(files)], function(f) grep(pattern, readLines(f, warn=FALSE), value=TRUE))); allowed <- grep("deferred|do not introduce|not add|no pixel|golden screenshots are deferred", hits, value=TRUE, ignore.case=TRUE, invert=TRUE); if (length(allowed)) stop(paste(allowed, collapse="\n"))'</automated>
    <automated>rg -n "^test_output/|^test_output$|test_output" .gitignore .Rbuildignore</automated>
  </verify>
  <acceptance_criteria>
    - Quick command skips cleanly without browser dependency requirements.
    - Workflow, helper, test, and docs all reference the Phase 52 CI contract.
    - Forbidden browser stacks and pixel/golden implementations are absent.
    - Generated browser visual files remain ignored and uncommitted.
    - CI-01, CI-02, and CI-03 are covered by source, workflow, docs, and verification commands.
  </acceptance_criteria>
  <done>Phase 52 passes final source/workflow/docs checks and is ready for verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local/CI commands -> maintainer docs | Docs tell maintainers which env vars change test behavior. |
| Generated artifacts -> committed source | Final checks prevent generated browser artifacts from entering git/package sources. |
| Non-pixel evidence -> user expectations | Docs must not overclaim pixel regression support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-07 | I | Documentation | mitigate | Document artifact classes and commands only; do not paste local logs, absolute paths, screenshots, or run URLs. |
| T-52-08 | R/I | Pixel/golden deferral | mitigate | State that screenshots are inspection evidence and pixel thresholds/goldens are deferred. |
| T-52-09 | I | Generated files | mitigate | Verify `.gitignore`/`.Rbuildignore` exclude `test_output/` and confirm `git status` does not show generated artifact files. |
</threat_model>

<verification>
Run every task-level automated command. If the CI-equivalent full browser command cannot run locally because Chrome/chromote is unavailable, record that as an environment limitation in the summary and rely on the dedicated GitHub Actions workflow/manual run for final browser-available evidence.
</verification>

<success_criteria>
- Maintainer docs cover local and CI visual smoke operation.
- Workflow, helper, test, and docs all agree on artifact paths and env vars.
- Screenshots remain non-gated inspection evidence.
- All Phase 52 requirements are covered by plans and verifiable checks.
</success_criteria>

<output>
After completion, create `.planning/phases/52-ci-visual-regression-foundation/52-03-SUMMARY.md`.
</output>
