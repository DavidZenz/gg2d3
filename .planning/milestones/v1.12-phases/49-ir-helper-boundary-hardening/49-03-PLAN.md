---
phase: 49-ir-helper-boundary-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 49-02
files_modified:
  - R/ir_facet_helpers.R
  - R/as_d3_ir.R
  - tests/testthat/test-ir-helper-boundaries.R
  - .planning/phases/49-ir-helper-boundary-hardening/49-VALIDATION.md
autonomous: true
requirements:
  - ARCH-01
must_haves:
  truths:
    - "Facet and panel metadata construction lives behind named internal helper functions."
    - "Representative facet_wrap, facet_grid, free-scale, sf panel bbox, and no-facet IR remains unchanged."
    - "Phase 49 validation proves scale, layer, facet, sf, annotation, and browser-smoke checks pass or skip explicitly."
  artifacts:
    - path: "R/ir_facet_helpers.R"
      provides: "Internal facet/panel helper boundary"
    - path: "tests/testthat/test-ir-helper-boundaries.R"
      provides: "Facet-boundary characterization tests"
    - path: ".planning/phases/49-ir-helper-boundary-hardening/49-VALIDATION.md"
      provides: "Final validation commands and pass conditions"
  key_links:
    - from: "R/as_d3_ir.R"
      to: "R/ir_facet_helpers.R"
      via: "gg2d3_ir_facets()"
      pattern: "gg2d3_ir_facets"
requirements_addressed:
  - ARCH-01
---

<objective>
Extract the facet/panel metadata boundary and run final Phase 49 plan validation.

Purpose: Facet metadata is lengthy, repeated, and sensitive to free scales and sf panel bboxes. This final plan isolates that boundary and proves the whole Phase 49 helper extraction contract.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-ir-helper-boundary-hardening/49-CONTEXT.md
@.planning/phases/49-ir-helper-boundary-hardening/49-RESEARCH.md
@.planning/phases/49-ir-helper-boundary-hardening/49-PATTERNS.md
@.planning/phases/49-ir-helper-boundary-hardening/49-VALIDATION.md
@R/as_d3_ir.R
@R/ir_scale_helpers.R
@R/ir_layer_helpers.R
@R/sf_utils.R
@tests/testthat/test-facets.R
@tests/testthat/test-facet-grid.R
@tests/testthat/test-sf-ir.R
@tests/testthat/test-browser-visual-smoke.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add facet helper boundary</name>
  <read_first>
    R/as_d3_ir.R
    R/ir_scale_helpers.R
    R/sf_utils.R
    tests/testthat/test-facets.R
    tests/testthat/test-facet-grid.R
    tests/testthat/test-sf-ir.R
  </read_first>
  <files>R/ir_facet_helpers.R, R/as_d3_ir.R</files>
  <action>
    Create `R/ir_facet_helpers.R` with internal helpers named:
    - `gg2d3_ir_panel_ranges(pp, xscale_obj, yscale_obj, is_flip, x_trans_name, y_trans_name)`
    - `gg2d3_ir_panel_spacing(theme)`
    - `gg2d3_ir_facet_wrap(layout_obj, build, xscale_obj, yscale_obj, is_flip, x_trans_name, y_trans_name, theme)`
    - `gg2d3_ir_facet_grid(layout_obj, build, xscale_obj, yscale_obj, is_flip, x_trans_name, y_trans_name, theme)`
    - `gg2d3_ir_facets(build, scales, xscale_obj, yscale_obj, is_flip, is_sf_coord, x_trans_name, y_trans_name, theme, sf_panel_geometries)`

    Move the existing facet/panel construction logic from `as_d3_ir()` into these helpers. Reuse `gg2d3_ir_axis_breaks()` or `gg2d3_ir_convert_temporal_values()` from Plan 01 instead of duplicating temporal conversion. Preserve no-facet output shape, facet wrap/grid layout fields, strip fields, `panels` fields, free scale behavior, and `attach_sf_panel_bboxes()` behavior for sf coord panels.

    Update `as_d3_ir()` so it calls `gg2d3_ir_facets(...)` and receives both `facets` and `panels` values.
  </action>
  <verify>
    <automated>rg -n "gg2d3_ir_(panel_ranges|panel_spacing|facet_wrap|facet_grid|facets)" R/ir_facet_helpers.R R/as_d3_ir.R</automated>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-facets.R"); testthat::test_file("tests/testthat/test-facet-grid.R")'</automated>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-sf-ir.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `R/ir_facet_helpers.R` exists.
    - `R/as_d3_ir.R` calls `gg2d3_ir_facets(`.
    - Facet wrap and facet grid tests exit with status 0.
    - sf IR tests exit with status 0 or skip only for missing optional dependencies.
  </acceptance_criteria>
  <done>Facet and panel metadata construction is isolated behind named helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Add facet-boundary characterization and final validation</name>
  <read_first>
    tests/testthat/test-ir-helper-boundaries.R
    R/ir_facet_helpers.R
    R/as_d3_ir.R
    .planning/phases/49-ir-helper-boundary-hardening/49-VALIDATION.md
  </read_first>
  <files>tests/testthat/test-ir-helper-boundaries.R, .planning/phases/49-ir-helper-boundary-hardening/49-VALIDATION.md</files>
  <action>
    Extend `tests/testthat/test-ir-helper-boundaries.R` with tests named exactly:
    - `facet helper preserves wrap and grid layout metadata`
    - `facet helper preserves free scale panel ranges`
    - `facet helper preserves sf panel bbox contracts`

    The first two tests must use non-sf fixtures and assert stable `facets$type`, `facets$nrow`, `facets$ncol`, `panels` length, `SCALE_X`/`SCALE_Y`, and per-panel range fields. The sf panel bbox test must skip before any `sf::` call unless `sf` and `geojsonsf` are installed, then assert an empty sf facet panel keeps `sf_bbox` as `NULL`.

    Update `49-VALIDATION.md` if actual final verification commands differ from the planned commands, but keep the four required command families: helper-boundary tests, core IR/facet/date tests, sf/sf-annotation tests, and browser visual smoke skip path.
  </action>
  <verify>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-ir-helper-boundaries.R")'</automated>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-ir.R"); testthat::test_file("tests/testthat/test-facets.R"); testthat::test_file("tests/testthat/test-facet-grid.R"); testthat::test_file("tests/testthat/test-date-scales.R")'</automated>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-sf-ir.R"); testthat::test_file("tests/testthat/test-sf-annotations-ir.R")'</automated>
    <automated>Rscript --vanilla -e 'pkgload::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test-browser-visual-smoke.R")'</automated>
    <automated>rg -n "facet helper preserves wrap and grid layout metadata|facet helper preserves free scale panel ranges|facet helper preserves sf panel bbox contracts" tests/testthat/test-ir-helper-boundaries.R</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-ir-helper-boundaries.R` contains all three exact facet test descriptions.
    - Full Phase 49 validation commands exit with status 0 or explicit optional dependency skips.
    - `49-VALIDATION.md` still lists helper-boundary, core IR/facet/date, sf/sf-annotation, and browser visual smoke checks.
  </acceptance_criteria>
  <done>Facet-boundary drift has focused tests and Phase 49 validation is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ggplot facet layout -> IR metadata | Layout data drives rendered panel placement and scale domains. |
| sf panel geometries -> panel bboxes | sf geometry accumulation controls map panel projection extents. |
| optional browser smoke -> validation evidence | Browser smoke is local/optional and must skip explicitly when unavailable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-07 | T | facet layout helper | mitigate | Run wrap/grid/free-scale tests and add facet-boundary characterization. |
| T-49-08 | I | sf panel bboxes | mitigate | Preserve `attach_sf_panel_bboxes()` integration and run sf bbox tests. |
| T-49-09 | D | optional browser/sf checks | mitigate | Treat dependency-limited skips as acceptable only when explicit; keep normal tests independent of browser opt-in. |
</threat_model>

<verification>
Run every task-level command. Confirm no generated files under `test_output/` are staged.
</verification>

<success_criteria>
- `R/ir_facet_helpers.R` exists and contains named internal helpers.
- `as_d3_ir()` delegates facet/panel metadata construction to those helpers.
- Existing facet, sf, annotation, and browser smoke tests pass or skip explicitly.
- New helper-boundary tests cover scale, layer, and facet responsibilities.
- ARCH-01 is fully covered by the three Phase 49 plans.
</success_criteria>

<output>
After completion, create `.planning/phases/49-ir-helper-boundary-hardening/49-03-SUMMARY.md`.
</output>
