---
phase: 25-api-polish
plan: 01
type: execute
wave: 1
depends_on: ["24-01"]
files_modified:
  - R/d3_tooltip.R
  - R/d3_zoom.R
  - R/d3_brush.R
  - R/d3_hover.R
  - R/d3_handlers.R
  - R/d3_transitions.R
  - inst/htmlwidgets/gg2d3.js
  - README.md
autonomous: true

must_haves:
  truths:
    - "Developer API is consistent and uses a standardized onRender pattern"
    - "Rendering performance is optimized for structural state updates"
  artifacts:
    - path: "README.md"
      provides: "Final documentation of all new features"
      contains: "coord_polar"
---

<objective>
Polish the developer API, standardize interactivity attachment, and optimize rendering performance for Milestone v1.6 finalization.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Standardize interactivity functions</name>
  <files>R/d3_*.R</files>
  <action>Refactor all `d3_*` functions to use a common validation pattern and ensure they use the `setTimeout(..., 0)` onRender template for reliable event attachment.</action>
  <verify>Run existing interactivity tests and confirm they still pass.</verify>
  <done>All interactivity functions follow a consistent implementation pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Implement update-only mode in JS</name>
  <files>inst/htmlwidgets/gg2d3.js</files>
  <action>Update `renderValue` to detect if the structural IR (layout, scales) is unchanged. if so, skip clearing the SVG and only update geom attributes using the existing `updateGeoms` helper.</action>
  <verify>Toggle a legend item and confirm no full SVG redraw occurs (no flicker).</verify>
  <done>Redraw performance is optimized for common interactive state changes.</done>
</task>

<task type="auto">
  <name>Task 3: Final documentation audit</name>
  <files>README.md, NEWS.md</files>
  <action>Update README.md with comprehensive examples for all v1.1-v1.6 features (Polar, Transitions, Custom Handlers, etc.). Update NEWS.md with the latest milestone accomplishments.</action>
  <verify>Read through README and confirm all examples are runnable and accurate.</verify>
  <done>Documentation is complete and reflects the full state of the package.</done>
</task>

<task type="auto">
  <name>Task 4: Performance stress test</name>
  <files>tests/testthat/test-performance.R</files>
  <action>Create a test case with a large dataset (5000+ points) and verify that `gg2d3()` still completes within acceptable time limits.</action>
  <verify>Rscript -e "devtools::test(filter = 'performance')"</verify>
  <done>Performance baseline is established and verified.</done>
</task>

</tasks>

<success_criteria>
- All `d3_*` functions are consistent in their internal implementation.
- Redrawing after a legend toggle is instant and flicker-free.
- README.md provides a clear, high-fidelity overview of the package capabilities.
</success_criteria>
