---
phase: 24-specialized-geoms
plan: 01
type: execute
wave: 1
depends_on: ["23-01"]
files_modified:
  - R/as_d3_ir.R
  - inst/htmlwidgets/gg2d3.yaml
  - inst/htmlwidgets/modules/geom-registry.js
  - tests/testthat/test-geoms-phase4.R
files_created:
  - inst/htmlwidgets/modules/geoms/dotplot.js
  - inst/htmlwidgets/modules/geoms/rug.js
  - inst/htmlwidgets/modules/geoms/interval.js
autonomous: true

must_haves:
  truths:
    - "User can render geom_dotplot with correct dot stacking"
    - "User can render geom_rug for data density markers"
    - "User can render interval geoms (errorbar, linerange, pointrange)"
  artifacts:
    - path: "inst/htmlwidgets/modules/geoms/dotplot.js"
      provides: "D3 renderer for dotplots"
      contains: "renderDotplot"
---

<objective>
Implement specialized geoms (dotplot, rug, errorbar, linerange, pointrange) to achieve full standard ggplot2 geom coverage.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update R extraction for specialized geoms</name>
  <files>R/as_d3_ir.R</files>
  <action>Update `as_d3_ir.R` to include `stackpos`, `binwidth`, `countidx` in `keep_aes`. Ensure `GeomDotplot` and `GeomRug` are correctly mapped to IR names. Extract `sides` parameter for rug.</action>
  <verify>Rscript -e "devtools::load_all(); p <- ggplot(mtcars, aes(mpg)) + geom_dotplot(); ir <- as_d3_ir(p); stopifnot(ir$layers[[1]]$geom == 'dotplot')"</verify>
  <done>R extraction supports all new specialized geoms.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dotplot renderer</name>
  <files>inst/htmlwidgets/modules/geoms/dotplot.js</files>
  <action>Implement `renderDotplot` using SVG circles. Use `stackpos` and `binwidth` to position dots accurately.</action>
  <verify>Visual check of a dotplot.</verify>
  <done>Dotplots render correctly in D3.</done>
</task>

<task type="auto">
  <name>Task 3: Implement rug renderer</name>
  <files>inst/htmlwidgets/modules/geoms/rug.js</files>
  <action>Implement `renderRug` using SVG segments. Position segments along panel edges based on the `sides` parameter.</action>
  <verify>Visual check of a plot with rugs on various sides.</verify>
  <done>Rugs render correctly in D3.</done>
</task>

<task type="auto">
  <name>Task 4: Implement interval geoms renderer</name>
  <files>inst/htmlwidgets/modules/geoms/interval.js</files>
  <action>Implement unified renderer for `errorbar`, `linerange`, and `pointrange`. Use `ymin`, `ymax` for vertical bounds and `xmin`, `xmax` for horizontal widths.</action>
  <verify>Visual check of errorbars and pointranges.</verify>
  <done>Interval geoms render correctly in D3.</done>
</task>

<task type="auto">
  <name>Task 5: Register new geoms and verify transitions</name>
  <files>inst/htmlwidgets/gg2d3.yaml, inst/htmlwidgets/modules/geom-registry.js</files>
  <action>Register new JS files in YAML. Update `updateGeoms` in `geomRegistry.js` to support animating these new geom types.</action>
  <verify>Zoom a dotplot and confirm dots move smoothly.</verify>
  <done>New geoms are fully integrated into the widget lifecycle.</done>
</task>

<task type="auto">
  <name>Task 6: Add regression tests</name>
  <files>tests/testthat/test-geoms-phase4.R</files>
  <action>Add IR structure assertions for all new geom types.</action>
  <verify>Rscript -e "devtools::test(filter = 'geoms-phase4')"</verify>
  <done>New geoms are test-backed.</done>
</task>

</tasks>

<success_criteria>
- `geom_dotplot()` renders circles stacked at correct data positions.
- `geom_rug(sides = "tr")` renders density markers on the top and right edges.
- `geom_errorbar()` shows vertical lines with horizontal caps at ymin/ymax.
- All new geoms support smooth transitions and tooltips.
</success_criteria>
