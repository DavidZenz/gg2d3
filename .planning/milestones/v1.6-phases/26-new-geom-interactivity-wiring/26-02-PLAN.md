---
phase: 26-new-geom-interactivity-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [API-02]

must_haves:
  truths:
    - "README.md reflects all v1.6 features including dotplot, rug, errorbar, interval geoms, and full interactivity API"
  artifacts:
    - path: "README.md"
      provides: "Up-to-date rendered documentation matching README.Rmd"
      contains: "geom_dotplot"
  key_links:
    - from: "README.Rmd"
      to: "README.md"
      via: "devtools::build_readme() pandoc rendering"
      pattern: "geom_errorbar"
---

<objective>
Regenerate README.md from the already-updated README.Rmd so shipped documentation reflects all v1.6 features.

Purpose: README.Rmd was updated with comprehensive v1.6 content but `devtools::build_readme()` was never run, leaving README.md stale at the pre-v1.6 state.

Output: Regenerated README.md matching README.Rmd content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate README.md from README.Rmd</name>
  <files>README.md</files>
  <read_first>
    README.Rmd
    README.md
  </read_first>
  <action>
    Run `devtools::build_readme()` in R to regenerate README.md from README.Rmd. This is a single command:

    ```r
    Rscript -e 'devtools::build_readme()'
    ```

    If devtools is not available, use:
    ```r
    Rscript -e 'rmarkdown::render("README.Rmd", output_format = "github_document", output_file = "README.md")'
    ```

    After regeneration, verify the output contains v1.6 features by checking for key strings:
    - `geom_dotplot` (GEOM-20)
    - `geom_rug` (GEOM-21)
    - `geom_errorbar` (GEOM-22)
    - `d3_transitions` or `d3_handlers` (interactivity API docs)

    Do NOT manually edit README.md — it must be machine-generated from README.Rmd to maintain consistency.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/gg2d3 && grep -c 'geom_dotplot' README.md && grep -c 'geom_rug' README.md && grep -c 'geom_errorbar' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the string `geom_dotplot`
    - README.md contains the string `geom_rug`
    - README.md contains the string `geom_errorbar`
    - README.md contains either `d3_transitions` or `d3_handlers`
    - All grep counts from verify command show >= 1
  </acceptance_criteria>
  <done>README.md is regenerated from README.Rmd and contains all v1.6 feature documentation including new geoms and interactivity API.</done>
</task>

</tasks>

<verification>
1. README.md mentions all three new geom types (dotplot, rug, errorbar)
2. README.md mentions interactivity API functions
3. README.md was generated by pandoc (not manually edited)
</verification>

<success_criteria>
- grep confirms geom_dotplot, geom_rug, geom_errorbar present in README.md
- README.md file modification timestamp is newer than plan execution start
</success_criteria>

<output>
After completion, create `.planning/phases/26-new-geom-interactivity-wiring/26-02-SUMMARY.md`
</output>
